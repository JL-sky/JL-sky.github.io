<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Latex</title>
    <url>/2022/04/05/Latex/</url>
    <content><![CDATA[<!-- more -->

<h1 id="公式详解"><a href="#公式详解" class="headerlink" title="公式详解"></a>公式详解</h1><ul>
<li><h2 id="普通公式"><a href="#普通公式" class="headerlink" title="普通公式"></a>普通公式</h2></li>
</ul>
<pre><code class="latex">\section&#123;公式详解&#125;
% 普通公式
$ y=x+1 $\newline
</code></pre>
<ul>
<li><h2 id="公式居中"><a href="#公式居中" class="headerlink" title="公式居中"></a>公式居中</h2></li>
</ul>
<pre><code class="latex">% 公式居中
$$ y=x-1 $$
</code></pre>
<ul>
<li><h2 id="带标号公式"><a href="#带标号公式" class="headerlink" title="带标号公式"></a>带标号公式</h2></li>
</ul>
<pre><code class="latex">% 带标号公式
\begin&#123;equation&#125;    
    y=x^2
\end&#123;equation&#125;

\begin&#123;equation&#125;
    y=x+2
\end&#123;equation&#125;
</code></pre>
<ul>
<li><h2 id="上标"><a href="#上标" class="headerlink" title="上标"></a>上标</h2></li>
</ul>
<pre><code class="latex">% 上标
$$ y=x^2+1 $$
</code></pre>
<ul>
<li><h2 id="下标"><a href="#下标" class="headerlink" title="下标"></a>下标</h2></li>
</ul>
<pre><code class="latex">% 下标
$$ x_1,x_&#123;12&#125;,x_3 $$
% 特殊字母
$\alpha$,$\beta$
</code></pre>
<ul>
<li><h2 id="根号"><a href="#根号" class="headerlink" title="根号"></a>根号</h2></li>
</ul>
<pre><code class="latex">% 根号
$$\sqrt&#123;2&#125;$$
$$\sqrt[3]&#123;2&#125;$$
$$\sqrt&#123;x^2+y^2&#125;$$
$$\sqrt&#123;1+\sqrt&#123;1+x^2&#125;&#125;$$
</code></pre>
<ul>
<li><h2 id="分式"><a href="#分式" class="headerlink" title="分式"></a>分式</h2></li>
</ul>
<pre><code class="latex">% 分式
$$\frac&#123;x&#125;&#123;x+1&#125;$$
$$\frac&#123;x&#125;&#123;x^2+x+1&#125;$$
$$\frac&#123;\sqrt&#123;x+1&#125;&#125;&#123;\sqrt[3]&#123;x-1&#125;&#125;$$
$$\frac&#123;1&#125;&#123;1+\sqrt&#123;x+1&#125;&#125;$$
</code></pre>
<ul>
<li><h2 id="括号"><a href="#括号" class="headerlink" title="括号"></a>括号</h2></li>
</ul>
<pre><code class="latex">% 括号
$$(x+1)$$
$$x[2+(x+2)]$$
$$&#123;x+1&#125;$$
$$3(\frac&#123;2&#125;&#123;5&#125;)$$
$$3\left(\frac&#123;2&#125;&#123;5&#125;\right)$$
$$3\left[\frac&#123;2&#125;&#123;5&#125;\right]$$
$$\left|\frac&#123;x&#125;&#123;x+1&#125;\right|$$
% \left与\right必须成对出现
$$\left\&#123;x^2\right.$$
</code></pre>
<p><a href="https://imgtu.com/i/LB5FVH"><img src="https://s1.ax1x.com/2022/04/19/LB5FVH.png" alt="LB5FVH.png"></a><a href="https://imgtu.com/i/LB5Pqe"><img src="https://s1.ax1x.com/2022/04/19/LB5Pqe.png" alt="LB5Pqe.png"></a></p>
<h2 id="运算符"><a href="#运算符" class="headerlink" title="运算符"></a>运算符</h2><ul>
<li>正负号</li>
</ul>
<pre><code class="latex">$$cos\alpha=\pm 1$$
$$sin\beta=\pm \frac&#123;\sqrt&#123;3&#125;&#125;&#123;2&#125;$$
</code></pre>
<p><a href="https://imgtu.com/i/LB5Do9"><img src="https://s1.ax1x.com/2022/04/19/LB5Do9.png" alt="LB5Do9.png"></a></p>
<ul>
<li>不等号</li>
</ul>
<pre><code class="latex">$$a^x+y \neq a^\left.x+y\right.$$
</code></pre>
<p><a href="https://imgtu.com/i/LB5siR"><img src="https://s1.ax1x.com/2022/04/19/LB5siR.png" alt="LB5siR.png"></a></p>
<ul>
<li><p>积分号</p>
<ul>
<li>不定积分</li>
</ul>
<pre><code class="latex">$$\int x^2dx=\frac&#123;x^3&#125;&#123;3&#125;$$
$$\int x^3 \mathrm&#123;d&#125;x=\frac&#123;1&#125;&#123;4&#125;x^4$$%将dx中的d变为正体
</code></pre>
<ul>
<li>定积分</li>
</ul>
<pre><code class="latex">$$\int_0^1x^2\mathrm&#123;d&#125;x=\frac&#123;1&#125;&#123;3&#125;$$
</code></pre>
<ul>
<li>多重积分</li>
</ul>
<pre><code class="latex">$$\iint \left(x^2+y^2\right)dxdy$$
$$\iiint \left(x^2+y^2+z^2\right)dxdydz$$
</code></pre>
<p><a href="https://imgtu.com/i/LB56Rx"><img src="https://s1.ax1x.com/2022/04/19/LB56Rx.png" alt="LB56Rx.png"></a></p>
</li>
<li><p>导数与微分</p>
<ul>
<li>常微分</li>
</ul>
<pre><code class="latex">$$y&#39;+p(z)y+q(x)=0$$
$$y&#39;&#39;+py&#39;+qy=0$$
$$\frac&#123;dy&#125;&#123;dx&#125;=2x$$
$$\frac&#123;d^2y&#125;&#123;dx^2&#125;=x\frac&#123;dy&#125;&#123;dx&#125;+\alpha=0$$
</code></pre>
<ul>
<li>偏微分</li>
</ul>
<pre><code class="latex">$$\frac&#123;\partial y&#125;&#123;\partial x&#125;+t\frac&#123;\partial y&#125;&#123;\partial z&#125;=0$$
</code></pre>
<p><a href="https://imgtu.com/i/LB5cz6"><img src="https://s1.ax1x.com/2022/04/19/LB5cz6.png" alt="LB5cz6.png"></a></p>
</li>
<li><p>求和</p>
</li>
</ul>
<pre><code class="latex">$$\sum_&#123;k=1&#125;^n k^2=\frac&#123;1&#125;&#123;5&#125;n^3+\frac&#123;1&#125;&#123;3&#125;n^2+n$$
</code></pre>
<p><a href="https://imgtu.com/i/LB52QK"><img src="https://s1.ax1x.com/2022/04/19/LB52QK.png" alt="LB52QK.png"></a></p>
<ul>
<li>极限</li>
</ul>
<pre><code class="latex">$$\lim_&#123;n \to \infty&#125;f(x)$$
</code></pre>
<p><a href="https://imgtu.com/i/LB5RsO"><img src="https://s1.ax1x.com/2022/04/19/LB5RsO.png" alt="LB5RsO.png"></a></p>
<ul>
<li>矩阵</li>
</ul>
<pre><code class="latex">\begin&#123;displaymath&#125;
    \left[
    \begin&#123;array&#125;&#123;ccc&#125;
         1&amp;2&amp;3  \\
         4&amp;5&amp;6  \\
         4&amp;5&amp;6  \\
    \end&#123;array&#125;
    \right]
\end&#123;displaymath&#125;
</code></pre>
<p><a href="https://imgtu.com/i/LBjkCD"><img src="https://s1.ax1x.com/2022/04/19/LBjkCD.png" alt="LBjkCD.png"></a></p>
<pre><code class="latex">\begin&#123;displaymath&#125;
    \left[
    \begin&#123;array&#125;&#123;cccc&#125;
         1&amp;2&amp; \cdots &amp;9  \\
         4&amp;5&amp; \cdots &amp;10  \\
         \vdots&amp;\vdots&amp; \ddots &amp;\vdots  \\
         4&amp;5&amp; \cdots &amp;20  \\
    \end&#123;array&#125;latex
    \right]
\end&#123;displaymath&#125;
</code></pre>
<p><a href="https://imgtu.com/i/LBjA8e"><img src="https://s1.ax1x.com/2022/04/19/LBjA8e.png" alt="LBjA8e.png"></a></p>
<pre><code class="latex">\begin&#123;displaymath&#125;
f(x)=\frac&#123;1&#125;&#123;3&#125;\times
    \left[
    \begin&#123;array&#125;&#123;ccc&#125;
         1&amp;2&amp;3  \\
         4&amp;5&amp;6  \\
         4&amp;5&amp;6  \\
    \end&#123;array&#125;
    \right]
\end&#123;displaymath&#125;
</code></pre>
<p><a href="https://imgtu.com/i/LBji4O"><img src="https://s1.ax1x.com/2022/04/19/LBji4O.png" alt="LBji4O.png"></a></p>
<ul>
<li>分段函数</li>
</ul>
<pre><code class="latex">\begin&#123;displaymath&#125;
g(x)=
\left\&#123;
\begin&#123;array&#125;&#123;cc&#125;
     x+1&amp; x&gt;1 \\
     x&amp; x\leq1\\
     
\end&#123;array&#125;
\right.latex
\end&#123;displaymath&#125;
</code></pre>
<p><a href="https://imgtu.com/i/LBjPUK"><img src="https://s1.ax1x.com/2022/04/19/LBjPUK.png" alt="LBjPUK.png"></a></p>
<h1 id="列表"><a href="#列表" class="headerlink" title="列表"></a>列表</h1><h2 id="无序列表"><a href="#无序列表" class="headerlink" title="无序列表"></a>无序列表</h2><pre><code class="latex">\begin&#123;document&#125;
\maketitle
% 无序列表
\begin&#123;itemize&#125;
    \item subject
    \begin&#123;itemize&#125;
        \item chinese
        \item math
        \item english
    \end&#123;itemize&#125;
    

    \item animal
    \begin&#123;itemize&#125;
        \item dog
        \item cat
    \end&#123;itemize&#125;
    \item student

\end&#123;itemize&#125;
</code></pre>
<p><a href="https://imgtu.com/i/LB5CrD"><img src="https://s1.ax1x.com/2022/04/19/LB5CrD.png" alt="LB5CrD.png"></a></p>
<h2 id="有序列表"><a href="#有序列表" class="headerlink" title="有序列表"></a>有序列表</h2><pre><code class="latex">\begin&#123;enumerate&#125;
    \item subject
    \begin&#123;enumerate&#125;
        \item chinese
        \item math
        \item english
    \end&#123;enumerate&#125;
    

    \item animal
    \begin&#123;enumerate&#125;
        \item dog
        \item cat
    \end&#123;enumerate&#125;
    \item student

\end&#123;enumerate&#125;
</code></pre>
<p><a href="https://imgtu.com/i/LB5AIA"><img src="https://s1.ax1x.com/2022/04/19/LB5AIA.png" alt="LB5AIA.png"></a></p>
<h1 id="插入图片"><a href="#插入图片" class="headerlink" title="插入图片"></a>插入图片</h1><ul>
<li><h2 id="单图"><a href="#单图" class="headerlink" title="单图"></a>单图</h2></li>
</ul>
<pre><code class="latex">\documentclass[12pt]&#123;article&#125;
\usepackage[utf8]&#123;ctex&#125;
% 插入图片的宏包
\usepackage&#123;graphicx&#125;
\date&#123;&#125;
\begin&#123;document&#125;
% \maketitle
\begin&#123;figure&#125;[htp]%h:here t:top p:pciture,设置图片到顶部
    %设置图片居中
    \centering
    %导入图片
    \includegraphics[width=4cm]&#123;dog.jpg&#125;
    % 设置图片标题
    \caption&#123;柯基&#125;
    \label&#123;fig:my_label&#125;
\end&#123;figure&#125;

\end&#123;document&#125;
</code></pre>
<p><a href="https://imgtu.com/i/LB5KsS"><img src="https://s1.ax1x.com/2022/04/19/LB5KsS.png" alt="LB5KsS.png"></a></p>
<ul>
<li><h2 id="多图排版"><a href="#多图排版" class="headerlink" title="多图排版"></a>多图排版</h2></li>
</ul>
<p>导入宏包</p>
<pre><code class="latex">\usepackage&#123;graphicx&#125;
\usepackage&#123;subfigure&#125;
</code></pre>
<p>嵌入代码</p>
<pre><code class="latex">\begin&#123;figure&#125;[htp]
    \centering
    \subfigure[dog]&#123;  % 第一个子图，xx 为第一个字体的名称
    \includegraphics[width=3.4cm]&#123;dog.jpg&#125;  % 图片路径和大小
    &#125;
\quad
    \subfigure[code]&#123;  % 第二个子图
    \includegraphics[width=3.4cm]&#123;code.png&#125;
    &#125;
    \caption&#123;figure&#125;    % 整个大图的标题
    \label&#123;fig&#125;       % 标签
\end&#123;figure&#125;
</code></pre>
<p><a href="https://imgtu.com/i/LB50Z4"><img src="https://s1.ax1x.com/2022/04/19/LB50Z4.png" alt="LB50Z4.png"></a></p>
<h1 id="表格"><a href="#表格" class="headerlink" title="表格"></a>表格</h1><pre><code class="latex">\begin&#123;table&#125;[htb]
    \centering
    % 设置列数，c表示单元格数据居中，|表示表格竖线
    \begin&#123;tabular&#125;&#123;|c|c|c|c|&#125;
        \hline%表格横线
         姓名&amp;性别&amp;年龄&amp;成绩  \\
        \hline
         张三&amp;男&amp;18&amp;98\\
         \hline
         李四&amp;女&amp;21&amp;65\\
         \hline
         王五&amp;男&amp;22&amp;66\\
         \hline
    \end&#123;tabular&#125;
    \caption&#123;成绩表&#125;
    \label&#123;tab:my_label&#125;
\end&#123;table&#125;
</code></pre>
<p><a href="https://imgtu.com/i/LB5uM8"><img src="https://s1.ax1x.com/2022/04/19/LB5uM8.png" alt="LB5uM8.png"></a></p>
<h1 id="脚注与定理"><a href="#脚注与定理" class="headerlink" title="脚注与定理"></a>脚注与定理</h1><pre><code class="latex">\documentclass[12pt]&#123;article&#125;
\usepackage[utf8]&#123;ctex&#125;

\title&#123;我的论文\footnote&#123;发布于2020年&#125;&#125;
\author&#123;姜磊\footnote&#123;十佳青年学者&#125;&#125;
\date&#123;2020/4/13&#125;

\begin&#123;document&#125;
\maketitle

\section&#123;介绍&#125;
这是介绍\footnote&#123;引言&#125;
\section&#123;定理&#125;
这是定理
% 定理,[section]表示加小标
\newtheorem&#123;theorem&#125;&#123;定理&#125;[section]
\begin&#123;theorem&#125;
菲涅尔
\end&#123;theorem&#125;

\begin&#123;theorem&#125;
数列定理
\end&#123;theorem&#125;

\end&#123;document&#125;
</code></pre>
<p><a href="https://imgtu.com/i/LB5lZQ"><img src="https://s1.ax1x.com/2022/04/19/LB5lZQ.png" alt="LB5lZQ.png"></a></p>
<h1 id="子标题"><a href="#子标题" class="headerlink" title="子标题"></a>子标题</h1><pre><code class="latex">\section&#123;绪论&#125;
\subsection&#123;背景&#125;
\subsection&#123;引言&#125;
</code></pre>
<p><a href="https://imgtu.com/i/LB51aj"><img src="https://s1.ax1x.com/2022/04/19/LB51aj.png" alt="LB51aj.png"></a></p>
<h1 id="目录与附录"><a href="#目录与附录" class="headerlink" title="目录与附录"></a>目录与附录</h1><ul>
<li><h2 id="目录"><a href="#目录" class="headerlink" title="目录"></a>目录</h2></li>
</ul>
<p>在**\begin{document}**后插入</p>
<pre><code class="latex">\tableofcontents
</code></pre>
<p><a href="https://imgtu.com/i/LB5Gin"><img src="https://s1.ax1x.com/2022/04/19/LB5Gin.png" alt="LB5Gin.png"></a></p>
<ul>
<li><h2 id="附录"><a href="#附录" class="headerlink" title="附录"></a>附录</h2></li>
</ul>
<pre><code class="latex">%首先引入附录宏包
\usepackage&#123;appendix&#125;

%在主体中嵌入以下代码
%指定下一页
\newpage
% 附录
\appendix
% \appendixpage

%在目录中插入Appendices
\addappheadtotoc
\section&#123;数据&#125;
\section&#123;代码&#125;
</code></pre>
<h1 id="参考文献"><a href="#参考文献" class="headerlink" title="参考文献"></a>参考文献</h1><pre><code class="latex">\begin&#123;document&#125;
\maketitle
this is a book\cite&#123;文献1&#125;\newline
这里引用文献\cite&#123;文献2&#125;

% 参考文献
\begin&#123;thebibliography&#125;&#123;9&#125;%9表示参考文献条数为1~9，99表示条数为1~99，以此类推
\bibitem&#123;文献1&#125;胡欣韵.数据挖掘算法的改进研究[D].安徽大学，2019
\bibitem&#123;文献2&#125; Macqueen J.Some methods for classification and analysis of multivariate observations[C].Proceedings of the Fifth BerKeley Symposium on Mathematical and Probability,BerKeley,1967,1(14):281-297
\end&#123;thebibliography&#125;


\end&#123;document&#125;
</code></pre>
<p><a href="https://imgtu.com/i/LB5UMT"><img src="https://s1.ax1x.com/2022/04/19/LB5UMT.png" alt="LB5UMT.png"></a></p>
<h1 id="字体设置"><a href="#字体设置" class="headerlink" title="字体设置"></a>字体设置</h1><h2 id="字体样式"><a href="#字体样式" class="headerlink" title="字体样式"></a>字体样式</h2><pre><code class="latex">This is a new book\\
</code></pre>
<ul>
<li><h3 id="加粗"><a href="#加粗" class="headerlink" title="加粗"></a>加粗</h3></li>
</ul>
<pre><code class="latex">This is a \textbf&#123;new&#125; book\\
</code></pre>
<ul>
<li><h3 id="斜体"><a href="#斜体" class="headerlink" title="斜体"></a>斜体</h3></li>
</ul>
<pre><code class="latex">This is a \textit&#123;new&#125; book\\
</code></pre>
<ul>
<li><h3 id="字母大写"><a href="#字母大写" class="headerlink" title="字母大写"></a>字母大写</h3></li>
</ul>
<pre><code class="latex">This is a \textsc&#123;new&#125; book\\
</code></pre>
<ul>
<li><h3 id="等线"><a href="#等线" class="headerlink" title="等线"></a>等线</h3></li>
</ul>
<pre><code class="latex">This is a \texttt&#123;new&#125; book\\
</code></pre>
<p><a href="https://imgtu.com/i/LB5tzV"><img src="https://s1.ax1x.com/2022/04/19/LB5tzV.png" alt="LB5tzV.png"></a></p>
<ul>
<li><h3 id="自定义字体"><a href="#自定义字体" class="headerlink" title="自定义字体"></a>自定义字体</h3><pre><code class="latex">\newCJKfontfamily\huawenxinwei&#123;STXINWEI.TTF&#125;[AutoFakeSlant,AutoFakeBold]%华文新魏
</code></pre>
<p>如：</p>
<pre><code class="latex">\kai \fontsize&#123;42pt&#125;&#123;\baselineskip&#125;\selectfont
你好世界
\huawenxinwei \zihao&#123;0&#125;
你好世界
</code></pre>
<p><a href="https://imgtu.com/i/LB5BdJ"><img src="https://s1.ax1x.com/2022/04/19/LB5BdJ.png" alt="LB5BdJ.png"></a></p>
</li>
</ul>
<h2 id="字体大小"><a href="#字体大小" class="headerlink" title="字体大小"></a>字体大小</h2><ul>
<li><h4 id="第一种设置"><a href="#第一种设置" class="headerlink" title="第一种设置"></a>第一种设置</h4></li>
</ul>
<pre><code class="latex">This is a &#123;\tiny new&#125; book\\
This is a &#123;\scriptsize new&#125; book\\
This is a &#123;\footnotesize new&#125; book\\
This is a &#123;\normalsize new&#125; book\\
This is a &#123;\large new&#125; book\\
This is a &#123;\Large new&#125; book\\
</code></pre>
<p><a href="https://imgtu.com/i/LB5asU"><img src="https://s1.ax1x.com/2022/04/19/LB5asU.png" alt="LB5asU.png"></a></p>
<ul>
<li><h4 id="第二种设置"><a href="#第二种设置" class="headerlink" title="第二种设置"></a>第二种设置</h4></li>
</ul>
<pre><code class="latex">\newcommand&#123;\chuhao&#125;&#123;\fontsize&#123;42pt&#125;&#123;44.9pt&#125;\selectfont&#125;  % 初号, 1.5倍行距
\newcommand&#123;\xiaochu&#125;&#123;\fontsize&#123;30pt&#125;&#123;40pt&#125;\selectfont&#125;  % 小初, 1.5倍行距
\newcommand&#123;\yihao&#125;&#123;\fontsize&#123;26pt&#125;&#123;36pt&#125;\selectfont&#125;  % 一号, 1.4倍行距
\newcommand&#123;\erhao&#125;&#123;\fontsize&#123;22pt&#125;&#123;28pt&#125;\selectfont&#125;  % 二号, 1.25倍行距
\newcommand&#123;\xiaoer&#125;&#123;\fontsize&#123;18pt&#125;&#123;18pt&#125;\selectfont&#125;  % 小二, 单倍行距
\newcommand&#123;\sanhao&#125;&#123;\fontsize&#123;16pt&#125;&#123;24pt&#125;\selectfont&#125;  % 三号, 1.5倍行距
\newcommand&#123;\xiaosan&#125;&#123;\fontsize&#123;15pt&#125;&#123;22pt&#125;\selectfont&#125;  % 小三, 1.5倍行距
\newcommand&#123;\sihao&#125;&#123;\fontsize&#123;14pt&#125;&#123;21pt&#125;\selectfont&#125;  % 四号, 1.5倍行距
\newcommand&#123;\banxiaosi&#125;&#123;\fontsize&#123;13pt&#125;&#123;19.5pt&#125;\selectfont&#125;  % 半小四, 1.5倍行距
\newcommand&#123;\xiaosi&#125;&#123;\fontsize&#123;12pt&#125;&#123;18pt&#125;\selectfont&#125;  % 小四, 1.5倍行距
\newcommand&#123;\dawuhao&#125;&#123;\fontsize&#123;11pt&#125;&#123;11pt&#125;\selectfont&#125;  % 大五号, 单倍行距
\newcommand&#123;\wuhao&#125;&#123;\fontsize&#123;10.5pt&#125;&#123;10.5pt&#125;\selectfont&#125;  % 五号, 单倍行距
\newcommand&#123;\xiaowu&#125;&#123;\fontsize&#123;9pt&#125;&#123;9pt&#125;\selectfont&#125;  % 五号, 单倍行距
</code></pre>
<ul>
<li><h4 id="第三种设置"><a href="#第三种设置" class="headerlink" title="第三种设置"></a>第三种设置</h4></li>
</ul>
<p>加载<strong>ctex</strong>宏包</p>
<pre><code class="latex">\zihao&#123;1&#125;%一号字体
\zihao&#123;-3&#125;%小三号字体
</code></pre>
<h1 id="页面设置"><a href="#页面设置" class="headerlink" title="页面设置"></a>页面设置</h1><h2 id="页边距"><a href="#页边距" class="headerlink" title="页边距"></a>页边距</h2><pre><code class="latex">% 设置页边距，左右1.5，上下均为1
\usepackage[left=1.5in,right=1.5in,top=1in,botton=1in]&#123;geometry&#125;
</code></pre>
<h2 id="行间距"><a href="#行间距" class="headerlink" title="行间距"></a>行间距</h2><ul>
<li>全局行间距</li>
</ul>
<pre><code class="latex">% 设置全局行间距1.5倍，默认1.2倍行间距
\linespread&#123;1.5&#125;
</code></pre>
<ul>
<li>段落行间距</li>
</ul>
<pre><code class="latex">%包裹段落使用\linespread&#123;2&#125;\selectfont
&#123;\linespread&#123;2&#125;\selectfont
“物以类聚，人以群分”，生活中离不开聚类。聚类分析仅根据在数据中发现
的描述对象及其关系的信息，将数据对象分组。其目标是，使不同类之间的数据
对象彼此相异，同一类内的数据对象彼此相似。组内的相似性(同质性)越大，
组间差别越大，聚类就越好。\par&#125;
</code></pre>
<p>如：</p>
<pre><code class="latex">\documentclass[12pt]&#123;article&#125;
\usepackage[utf8]&#123;ctex&#125;
% 设置页边距，左右1.5，上下均为1
\usepackage[left=1.5in,right=1.5in,top=1in,botton=1in]&#123;geometry&#125;
% 设置全局行间距1.5倍，默认1.2倍行间距
\linespread&#123;1.5&#125;

\title&#123;我的论文\footnote&#123;发布于2020年&#125;&#125;
\author&#123;姜磊\footnote&#123;十佳青年学者&#125;&#125;
\date&#123;2020/4/13&#125;

\begin&#123;document&#125;
\maketitle
\section&#123;聚类分析&#125;
% 设置段落行间距
&#123;\linespread&#123;2&#125;\selectfont
“物以类聚，人以群分”，生活中离不开聚类。聚类分析仅根据在数据中发现
的描述对象及其关系的信息，将数据对象分组。其目标是，使不同类之间的数据
对象彼此相异，同一类内的数据对象彼此相似。组内的相似性(同质性)越大，
组间差别越大，聚类就越好。\par&#125;
聚类是典型的交叉学科，在很多领域有广泛的应用，其研究已有 60 多年的
历史。聚类分析是一个不断探索的分析过程，分类时根据数据本身进行分类而无
需预先确定分类标准。采用不同的方法进行聚类分析得到的结果往往不尽相同。
从现实应用角度分析，聚类无疑为数据挖掘的重要任务之一。它不仅可以作为一
个单独的工具，有效获取数据的分布状况，观察每个类中数据的特征，从而进一
步分析所研究的特定的聚类集合，也能够作为分类和相关性分析等其他算法的预
处理步骤。聚类能够发现数据之间的潜在关系，从而进一步得到数据的分布模式，
因此在数据分析、图像处理、模式识别、市场分析等领域中它已被广泛应用

\end&#123;document&#125;
</code></pre>
<p><a href="https://imgtu.com/i/LB5yJ1"><img src="https://s1.ax1x.com/2022/04/19/LB5yJ1.png" alt="LB5yJ1.png"></a></p>
<h2 id="页眉页脚"><a href="#页眉页脚" class="headerlink" title="页眉页脚"></a>页眉页脚</h2><ul>
<li>标准 LaTeX 提供下列四种页版式，可用 <strong>\pagestyle{页版式}</strong> 命令来设置页面版式：</li>
</ul>
<table>
<thead>
<tr>
<th>LaTeX标准页版式</th>
<th>样式说明</th>
</tr>
</thead>
<tbody><tr>
<td>empty</td>
<td>无页眉页脚</td>
</tr>
<tr>
<td>plain（默认）</td>
<td>无页眉，页脚为居中页码</td>
</tr>
<tr>
<td>headings</td>
<td>页眉为章节标题，无页脚</td>
</tr>
<tr>
<td>myheadings</td>
<td>页眉内容可自定义，无页脚</td>
</tr>
</tbody></table>
<ul>
<li>自定义页眉页脚</li>
</ul>
<pre><code class="latex">\pagestyle&#123;fancy&#125;
% 自定义页眉
\lhead&#123;team&#125;
\chead&#123;聚类分析&#125;
\rhead&#123;lof1&#125;
% 自定义页脚
\lfoot&#123;&#125;
\cfoot&#123;第\thepage 页&#125;%\thepage显示当前页码
\rfoot&#123;&#125;
</code></pre>
<h1 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h1><h2 id="overleaf中文显示"><a href="#overleaf中文显示" class="headerlink" title="overleaf中文显示"></a>overleaf中文显示</h2><p>引入</p>
<pre><code class="latex">\usepackage[utf8]&#123;ctex&#125;
</code></pre>
<p>修改<strong>菜单</strong>中<strong>编辑器</strong>选项为<strong>xeLaTex</strong></p>
<p><a href="https://imgtu.com/i/LB5JGq"><img src="https://s1.ax1x.com/2022/04/19/LB5JGq.png" alt="LB5JGq.png"></a></p>
<h2 id="控制符"><a href="#控制符" class="headerlink" title="控制符"></a>控制符</h2><ul>
<li>换行</li>
</ul>
<pre><code class="latex">\\
或
\newline
</code></pre>
<ul>
<li>换段</li>
</ul>
<pre><code class="latex">\par
</code></pre>
<p>如：</p>
<pre><code class="latex">你好世界\\
我是你\par
我是谁
</code></pre>
<p><a href="https://imgtu.com/i/LB5dLF"><img src="https://s1.ax1x.com/2022/04/19/LB5dLF.png" alt="LB5dLF.png"></a></p>
<ul>
<li><p>显示当前页码</p>
<pre><code>\thepage
</code></pre>
</li>
<li><p>空格</p>
<pre><code class="latex">~
\quad%一个空格
\qquad%两个空格
</code></pre>
</li>
</ul>
<h2 id="Latex符号大全"><a href="#Latex符号大全" class="headerlink" title="Latex符号大全"></a>Latex符号大全</h2><p><a href="https://blog.csdn.net/YEN_CSDN/article/details/79966985">(14条消息) LaTex符号大全（LaTeX_Symbols）_YEN_csdn的博客-CSDN博客_latex符号</a></p>
]]></content>
      <categories>
        <category>Latex</category>
      </categories>
      <tags>
        <tag>Latex</tag>
      </tags>
  </entry>
  <entry>
    <title>c++系统编程</title>
    <url>/2022/04/20/c++%E7%B3%BB%E7%BB%9F%E7%BC%96%E7%A8%8B/</url>
    <content><![CDATA[<!-- more -->

<h1 id="多进程linux命令"><a href="#多进程linux命令" class="headerlink" title="多进程linux命令"></a>多进程linux命令</h1><table>
<thead>
<tr>
<th>命令</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td>ps -aux</td>
<td>查看所有进程信息</td>
</tr>
<tr>
<td>ps -ajx</td>
<td>查看所有进程信息（显示父进程id等更多信息）</td>
</tr>
<tr>
<td>kill</td>
<td>杀死进程</td>
</tr>
</tbody></table>
<h1 id="多进程编程"><a href="#多进程编程" class="headerlink" title="多进程编程"></a>多进程编程</h1><h2 id="创建子进程"><a href="#创建子进程" class="headerlink" title="创建子进程"></a>创建子进程</h2><pre><code class="c++">#include&lt;stdlib.h&gt;
#include&lt;stdio.h&gt;
#include&lt;string.h&gt;
#include&lt;sys/types.h&gt;
#include&lt;unistd.h&gt;
int main()
&#123;
    int pid=fork();//创建子进程
    if(pid&lt;0)//子进程创建失败
        perror(&quot;fork&quot;);//打印失败信息
    if(pid&gt;0)//子进程创建成功，且当前进入父进程执行，此时pid表示子进程id
    &#123;
        printf( &quot;father-&gt; pid : %d, fpid : %d\n&quot; , getpid() , getppid( ) );//getpid()表示获取当前进程id，getppid()表示获取当前进行的父进程id
        sleep ( 1);//让父进程后死亡
    &#125;
    if(pid==0)//此时进行子进程执行
        printf( &quot;child-&gt; pid : %d , fpid:%d\n&quot; , getpid() , getppid( ));
    return 0;
&#125;
</code></pre>
<ul>
<li>父子进程的执行是随机的</li>
<li>子进程完全复制父进程用户区的数据，但与父进程的内核区的数据不完全相同，如进程id</li>
</ul>
<h2 id="多进程的GDB调试"><a href="#多进程的GDB调试" class="headerlink" title="多进程的GDB调试"></a>多进程的GDB调试</h2><pre><code class="c++">#include&lt;sys/types.h&gt;
#include&lt;unistd.h&gt;
#include&lt;stdio.h&gt;
#include&lt;stdlib.h&gt;
int main(int args, char* argc[])
&#123;
    int pid = fork();
    if (pid &gt; 0)
    &#123;
        printf(&quot;I am parentProcess,my id is %d\n&quot;, getpid());
        for (int i = 0; i &lt; 10; i++)
        &#123;
            printf(&quot;%d	-&gt;	i=%d\n&quot;,getpid(), i);
            sleep(1);
        &#125;
    &#125;
    if (pid == 0)
    &#123;
        printf(&quot;I am childProcess,my id is %d and my parentProcess id is %d\n&quot;,
            getpid(), getppid());
        for (int j = 0; j &lt; 10; j++)
        &#123;
            printf(&quot;%d	-&gt;	j=%d\n&quot;,getpid() ,j);
            sleep(1);
        &#125;
    &#125;
    else
        perror(&quot;fork&quot;);
    return 1;
&#125;
</code></pre>
<p><img src="C:\Users\ASUS\AppData\Roaming\Typora\typora-user-images\image-20230120233214495.png" alt="image-20230120233214495"></p>
<h2 id="创建三个子进程"><a href="#创建三个子进程" class="headerlink" title="创建三个子进程"></a>创建三个子进程</h2><pre><code class="c++">#include&lt;stdlib.h&gt;
#include&lt;stdio.h&gt;
#include&lt;string.h&gt;
#include&lt;sys/types.h&gt;
#include&lt;unistd.h&gt;
int main()
&#123;
        for(int i=0;i&lt;3;i++)
        &#123;
                int pid=fork();
                if(pid&lt;0)
                &#123;
                        printf(&quot;error!\n&quot;);
                        return -1;
                &#125;
                if(pid&gt;0)
                &#123;
                        printf(&quot;father-&gt; pid:%d,fpid:%d\n&quot;,getpid(),getppid());
                        // sleep(1);//让父进程后死亡
                &#125;
                if(pid==0)
                        printf(&quot;child-&gt; pid:%d,fpid:%d\n&quot;,getpid(),getppid());
        &#125;
        sleep(10);
        return 0;
&#125;
</code></pre>
<p><img src="https://s3.bmp.ovh/imgs/2022/04/19/3a75007c0e06e4cd.png"><img src="https://s3.bmp.ovh/imgs/2022/04/19/8429dd51eae9c26b.png"></p>
<pre><code class="c++">#include&lt;stdlib.h&gt;
#include&lt;stdio.h&gt;
#include&lt;string.h&gt;
#include&lt;sys/types.h&gt;
#include&lt;unistd.h&gt;
int main()
&#123;
        for(int i=0;i&lt;3;i++)
        &#123;
                int pid=fork();
                if(pid&lt;0)
                &#123;
                        printf(&quot;error!\n&quot;);
                        return -1;
                &#125;
                if(pid&gt;0)
                &#123;
                        printf(&quot;father-&gt; pid:%d,fpid:%d\n&quot;,getpid(),getppid());
                        // sleep(1);//让父进程后死亡
                &#125;
                if(pid==0)
                &#123;
                        printf(&quot;child-&gt; pid:%d,fpid:%d\n&quot;,getpid(),getppid());
                        break;//当子进程进入循环体时就直接退出循环，让子进程不再创建子进程
                &#125;
        &#125;
        sleep(10);
        return 0;
&#125;
</code></pre>
<p><img src="https://s3.bmp.ovh/imgs/2022/04/19/753811118e8f49b0.png"></p>
<ul>
<li>在写操作下父子进程之间不共享全局变量，但在读操作下他们共享一份变量</li>
</ul>
<pre><code class="c++">#include&lt;stdlib.h&gt;
#include&lt;stdio.h&gt;
#include&lt;string.h&gt;
#include&lt;sys/types.h&gt;
#include&lt;unistd.h&gt;
int glo=99;//定义全局变量，在父进程中修改它，在子进程中打印它，看他是否出现变化
int main()
&#123;
        int pid=fork();
        if(pid&lt;0)
        &#123;
                printf(&quot;error!\n&quot;);
                return -1;
        &#125;
        if(pid&gt;0)
        &#123;
                printf(&quot;father-&gt; pid:%d,fpid:%d\n&quot;,getpid(),getppid());
                printf(&quot;%p&quot;,&amp;glo);//输出地址
                glo++;
        &#125;
        if(pid==0)
        &#123;
                sleep(1);//让父进程先执行
                printf(&quot;child-&gt; pid:%d,fpid:%d\n&quot;,getpid(),getppid());
                printf(&quot;%d\n&quot;,glo);
                printf(&quot;%p&quot;,&amp;glo);//输出地址
        &#125;
        sleep(10);
        return 0;
&#125;
</code></pre>
<p><img src="https://s3.bmp.ovh/imgs/2022/04/19/a300a273b08723b0.png"></p>
<p><img src="https://s3.bmp.ovh/imgs/2022/04/19/1faf7cbb8a23a478.png"></p>
<h2 id="execl函数族"><a href="#execl函数族" class="headerlink" title="execl函数族"></a>execl函数族</h2><p>exec函数族的作用是根据指定的文件名找到可执行文件，并用它来取代调用进程的内容，换句话说，就是在调用进程内部执行一个可执行文件。这里的可执行文件既可以是二进制文件，也可以是任何Linux下可执行的脚本文件。</p>
<pre><code class="c++">int execl(const char *path, const char *arg, ...);
</code></pre>
<p>execl()函数用于执行参数path字符串所代表的文件路径（必须指定路径），接下来是一系列可变参数，它们代表执行该文件时传递过去的 argv[0]、argv[1]… argv[n] ，最后一个参数必须用空指针NULL作为结束的标志。</p>
<pre><code class="c++">#include&lt;iostream&gt;
using namespace std;
int main(int argc,char* argv[])//第一个参数为命令行的个数，第二个参数为命令行字符串，如ls -l，则argc=2，其中argv[0]=&quot;ls&quot;,argv[1]=&quot;-l&quot;
&#123;
     cout&lt;&lt;&quot;hello world&quot;&lt;&lt;endl;
 // for(int i=0;i&lt;argc;i++)
 // &#123;
 //     cout&lt;&lt;&quot;argc[&quot;&lt;&lt;i&lt;&lt;&quot;]:&quot;&lt;&lt;argv[i]&lt;&lt;endl;
 // &#125;
 return 1;
&#125;
</code></pre>
<pre><code class="c++">#include&lt;sys/types.h&gt;
#include&lt;stdio.h&gt;
#include&lt;stdlib.h&gt;
#include&lt;unistd.h&gt;
int main()
&#123;
    int pid = fork();
    if (pid &gt; 0)
    &#123;
        printf(&quot;farent:%d\n&quot;, getpid());
    &#125;
    if (pid == 0)
    &#123;
        sleep(1);
        //第一个参数为可执行文件名称，
        //其余参数为该可执行文件传入的可变参数，最后一个参数必须为NULL
        //当执行成功时不返回，否则返回-1
        //作用：在进程中执行一个程序，替换原来的进程用户区数据
        execl(&quot;/usr/bin/ls&quot;, &quot;ls&quot;, &quot;-n&quot;, NULL);//在子进程中执行系统程序ls -a
        //如果execl函数执行成功，则子进程将不会继续向下执行
        printf(&quot;child:%d\n&quot;, getpid());
    &#125;
    for (int i = 0; i &lt; 3; i++)
    &#123;
        printf(&quot;%d\n&quot;, getpid());
    &#125;
    return 0;
&#125;
</code></pre>
<p><strong>execlp函数</strong>：在环境变量中查找可执行文件，故在第一个参数中可不必写可执行文件的绝对路径</p>
<pre><code class="c++">        int ret=execlp(&quot;ls&quot;, &quot;ls&quot;, &quot;-n&quot;, NULL);//在子进程中执行系统程序ls -a
</code></pre>
<h2 id="孤儿进程与僵尸进程"><a href="#孤儿进程与僵尸进程" class="headerlink" title="孤儿进程与僵尸进程"></a>孤儿进程与僵尸进程</h2><p><strong>孤儿进程：</strong>父进程先死亡，子进程仍在运行，此时的子进程成为孤儿进程，且该子进程将由系统的初始进程init“领养”，故孤儿进程执行结束时可以被回收</p>
<pre><code class="c++">#include&lt;sys/types.h&gt;
#include&lt;unistd.h&gt;
#include&lt;stdio.h&gt;
#include&lt;stdlib.h&gt;
int main(int args, char* argc[])
&#123;
    int pid = fork();
    if (pid &gt; 0)
    &#123;
        printf(&quot;farent-&gt;%d\n&quot;, getpid());
        for (int i = 0; i &lt; 0; i++)
            printf(&quot;i=%d\n&quot;, i);
    &#125;
    if (pid == 0)
    &#123;
        sleep(1);//让父进程执行完后死亡，留下子进程
        printf(&quot;child-&gt;%d and my parent-&gt;%d\n&quot;, getpid(), getppid());
        for (int j = 0; j &lt; 3; j++)
        &#123;
            printf(&quot;j=%d\n&quot;, j);
        &#125;
    &#125;
    return 1;
&#125;
</code></pre>
<p><strong>僵尸进程：</strong>当进程结束时，其用户区数据部分将自动释放，但内核区的pcb必须由父进程释放，因此如果子进程执行结束时，父进程迟迟不回收（比如当父进程进入死循环时）子进程的pcb，则子进程此时成为僵尸进程，僵尸进程不能被kill命令杀死，且占用进程id号，然而系统的id号是有限的，因此如果存在大量的僵尸进程，将会给系统带来麻烦。处理僵尸进程的方法就是要求父进程必须回收子进程</p>
<pre><code class="c++">#include&lt;sys/types.h&gt;
#include&lt;unistd.h&gt;
#include&lt;stdio.h&gt;
#include&lt;stdlib.h&gt;
int main(int args, char* argc[])
&#123;
    int pid = fork();
    if (pid &gt; 0)
    &#123;
        while (1)//让父进程进入死循环，当子进程执行完时将无法回收子进程，则此时子进程将成功僵尸进程
        &#123;
            printf(&quot;farent-&gt;%d\n&quot;, getpid());
            sleep(1);
        &#125;
        //for (int i = 0; i &lt; 0; i++)
            //printf(&quot;i=%d\n&quot;, i);
    &#125;
    if (pid == 0)
    &#123;
        //sleep(1);
        printf(&quot;child-&gt;%d and my parent-&gt;%d\n&quot;, getpid(), getppid());
        for (int j = 0; j &lt; 3; j++)
        &#123;
            printf(&quot;j=%d\n&quot;, j);
        &#125;
    &#125;
    return 1;
&#125;
</code></pre>
<h2 id="进程的回收"><a href="#进程的回收" class="headerlink" title="进程的回收"></a>进程的回收</h2><p><em><em>int wait(int</em> wstatus)</em>*:阻塞父进程，直到有子进程退出时继续执行，以便让父进程回收子进程的资源</p>
<pre><code class="c++">#include&lt;stdio.h&gt;
#include&lt;stdlib.h&gt;
#include&lt;unistd.h&gt;
#include&lt;sys/types.h&gt;
#include&lt;sys/wait.h&gt;
int main()
&#123;
    int pid;
    for (int i = 0; i &lt; 5; i++)//创建五个进程
    &#123;
        pid = fork();
        
        if (pid == 0)
        &#123;
            break;
        &#125;
    &#125;

    if (pid &gt; 0)
    &#123;
        while (1)
        &#123;
            printf(&quot;farent	-&gt;	%d\n&quot;, getpid());
            //阻塞父进程的执行，
            //直到一个子进程退出时就回收该子进程的资源
            //如果回收成功就返回该子进程的id，否则（或子进程已全部回收完毕）返回-1
            //该函数每次只能回收一个子进程
            int ret = wait(NULL);
            printf(&quot;child has died,id=%d\n&quot;, ret);
            sleep(1);
        &#125;
    &#125;
    if (pid == 0)
    &#123;
        while (1)
        &#123;
            printf(&quot;child	-&gt;	%d\n&quot;, getpid());
            sleep(1);
        &#125;
    &#125;

    return 0;
&#125;
</code></pre>
<p><strong>int waitpid()</strong></p>
<h2 id="进程间通信"><a href="#进程间通信" class="headerlink" title="进程间通信"></a>进程间通信</h2><ul>
<li>管道（简单，本质为内核缓冲区）</li>
<li>信号（系统开销小）</li>
<li>共享映射区（有无血缘关系的进程之间都可进行通信）</li>
<li>本地socket套接字（稳定）</li>
</ul>
<h2 id="匿名管道"><a href="#匿名管道" class="headerlink" title="匿名管道"></a>匿名管道</h2><h3 id="父子进程管道通信"><a href="#父子进程管道通信" class="headerlink" title="父子进程管道通信"></a>父子进程管道通信</h3><p><img src="C:\Users\ASUS\AppData\Roaming\Typora\typora-user-images\image-20230122180235783.png" alt="image-20230122180235783"></p>
<pre><code class="c++">#include&lt;sys/types.h&gt;
#include&lt;stdio.h&gt;
#include&lt;stdlib.h&gt;
#include&lt;unistd.h&gt;
#include&lt;string.h&gt;
int main()
&#123;
    int fd[2];//作为管道的传出参数，表示文件描述符，其中fd[0]表示读端，fd[1]表示写端
    int ret = pipe(fd);//创建管道
    int pid = fork();//创建子进程，此时父子进程共享管道两端的读写文件描述符
    if (pid &gt; 0)
    &#123;
        //父进程关闭读端，写数据
        //close(fd[0]);
        char* str = &quot;hello,I am farent!&quot;;
        write(fd[1], str, strlen(str));//向管道的写端进行写数据
        printf(&quot;farent	-&gt;	%d,发送成功！\n&quot;, getpid());

        char buf[64];
        memset(&amp;buf, 0, sizeof(buf));
        int n0 = read(fd[0], buf, sizeof(buf));
        printf(&quot;farent	-&gt;	%d,共接受%d个字符：%s\n&quot;, getpid(), n0, buf);

    &#125;
    if (pid == 0)
    &#123;
        //close(fd[1]);
        char buf[64];
        memset(&amp;buf, 0, sizeof(buf));
        int n=read(fd[0], buf, sizeof(buf));
        printf(&quot;child	-&gt;	%d共接受到%d个字符：%s\n&quot;,getpid(), n, buf);

        char* s = &quot;I am child!&quot;;
        write(fd[1], s, strlen(s));
        printf(&quot;child	-&gt;	%d发送成功!\n&quot;, getpid());
    &#125;

    return  0;
&#125;
</code></pre>
<p><img src="https://s3.bmp.ovh/imgs/2022/04/19/8ce8115939fb3482.png"></p>
<h3 id="兄弟进程管道通信"><a href="#兄弟进程管道通信" class="headerlink" title="兄弟进程管道通信"></a>兄弟进程管道通信</h3><pre><code class="c++">#include&lt;iostream&gt;
#include&lt;string.h&gt;
#include&lt;stdlib.h&gt;
#include&lt;sys/types.h&gt;
#include&lt;sys/wait.h&gt;
#include&lt;unistd.h&gt;
using namespace std;
int main()
&#123;
        int fd[2];
        int ret=pipe(fd);
        if(ret&lt;0)
        &#123;
                cout&lt;&lt;&quot;pipe error!&quot;&lt;&lt;endl;
                return -1;
        &#125;
        int i=-1;
        for(i=0;i&lt;2;i++)
        &#123;
                int pid=fork();
                if(pid&lt;0)
                &#123;
                        cout&lt;&lt;&quot;fork error!&quot;&lt;&lt;endl;
                        return -1;
                &#125;
                if(pid&gt;0)
                &#123;
                        // cout&lt;&lt;&quot;fater:&quot;&lt;&lt;getpid()&lt;&lt;endl;
                        wait(NULL);
                &#125;
                else
                &#123;
                        cout&lt;&lt;&quot;i==&quot;&lt;&lt;i&lt;&lt;&quot;\tchild:&quot;&lt;&lt;getpid()&lt;&lt;&quot;---&gt;&quot;&lt;&lt;getppid()&lt;&lt;endl;
                        break;
                &#125;
        &#125;
        if(i==0)//子进程1
        &#123;
                close(fd[0]);//关闭读端，写入数据
                char message[64]=&quot;hello world!&quot;;
                write(fd[1],message,strlen(message));
        &#125;
        if(i==1)//子进程2
        &#123;
                close(fd[1]);//关闭写端，接受数据
                char buff[64];
                memset(&amp;buff,0,sizeof(buff));
                int n=read(fd[0],buff,sizeof(buff));
                cout&lt;&lt;&quot;共受到&quot;&lt;&lt;n&lt;&lt;&quot;个字符\t---&gt;\t&quot;&lt;&lt;buff&lt;&lt;endl;
        &#125;
     return 1;
&#125;
</code></pre>
<p><img src="https://s3.bmp.ovh/imgs/2022/04/19/2dc733fbe43ce171.png"></p>
<h3 id="用管道通信模拟系统命令ps-aux-grep-bash"><a href="#用管道通信模拟系统命令ps-aux-grep-bash" class="headerlink" title="用管道通信模拟系统命令ps aux | grep bash"></a>用管道通信模拟系统命令ps aux | grep bash</h3><pre><code class="c++">#include&lt;sys/types.h&gt;
#include&lt;stdio.h&gt;
#include&lt;stdlib.h&gt;
#include&lt;unistd.h&gt;
#include&lt;string.h&gt;
#include&lt;sys/wait.h&gt;
int main()
&#123;
    int fd[2];
    int ret = pipe(fd);//创建管道
    if (ret &lt; 0)
    &#123;
        perror(&quot;pipe&quot;);
        return 0;
    &#125;
    int pid = fork();//创建子进程
    if (pid &gt; 0)//父进程执行ps -aux，并将数据通过管道发送给子进程
    &#123;
        close(fd[0]);//关闭读端
        dup2(fd[1], STDOUT_FILENO);//由于execlp函数的输出为std屏幕，因此需要将输出重定向到管道的写端
        execlp(&quot;ps&quot;, &quot;ps&quot;, &quot;-aux&quot;,NULL);//执行ps -aux
        perror(&quot;execlp&quot;);//如果excelp函数执行成功，则该句不执行，否则可通过该语句返回错误信息
        wait(NULL);//等待子进程结束回收子进程
        return 0;
    &#125;
    if (pid == 0)//子进程接受ps -aux信息
    &#123;
        close(fd[1]);//关闭写端
        char buf[1024];//定义缓冲区接受数据
        memset(&amp;buf, 0, sizeof(buf));
        while (read(fd[0], buf, sizeof(buf)) &gt; 0)//由于缓冲区大小有限，因此一次读取的内容不完整，需要进行循环读取，直到将管道数据读取完毕
        &#123;
            printf(&quot;%s&quot;, buf);
            memset(&amp;buf, 0, 1024);
        &#125;
    &#125;
    else &#123;
        perror(&quot;fork&quot;);
        return 0;
    &#125;
    return 0;
&#125;
</code></pre>
<h2 id="有名管道"><a href="#有名管道" class="headerlink" title="有名管道"></a>有名管道</h2><h3 id="有名管道的使用"><a href="#有名管道的使用" class="headerlink" title="有名管道的使用"></a>有名管道的使用</h3><p>可实现非血缘关系进程之间的通信</p>
<p><strong>write.cpp</strong></p>
<pre><code class="c++">#include&lt;sys/types.h&gt;
#include&lt;unistd.h&gt;
#include&lt;sys/wait.h&gt;
#include&lt;stdio.h&gt;
#include&lt;stdlib.h&gt;
#include&lt;sys/stat.h&gt;
#include&lt;fcntl.h&gt;
#include&lt;string.h&gt;
int main()
&#123;
    int ret = access(&quot;fifo&quot;, F_OK);//判断文件是否存在
    if (ret &lt; 0)
    &#123;
        printf(&quot;管道不存在，创建管道\n&quot;);
        ret = mkfifo(&quot;fifo&quot;, 0777);//创建有名管道文件
        if (ret == -1)
        &#123;
            perror(&quot;mkfifo&quot;);
            return 0;
        &#125;
    &#125;
    //打开管道文件
    int fd = open(&quot;fifo&quot;, O_WRONLY);//以只写方式打开文件
    if (fd == -1)
    &#123;
        perror(&quot;open&quot;);
        return 0;
    &#125;
    //向管道文件中写数据
    for (int i = 0; i &lt; 100; i++)
    &#123;
        char buf[1024];
        memset(&amp;buf, 0, sizeof(buf));
        sprintf(buf, &quot;hello,%d\n&quot;, i);
        printf(&quot;发送数据：%s\n&quot;, buf);
        write(fd, buf, strlen(buf));
        sleep(1);
    &#125;
    close(fd);
    return 0;
&#125;
</code></pre>
<p><strong>read.cpp</strong></p>
<pre><code class="c++">#include&lt;sys/types.h&gt;
#include&lt;sys/stat.h&gt;
#include&lt;stdio.h&gt;
#include&lt;stdlib.h&gt;
#include&lt;unistd.h&gt;
#include&lt;string.h&gt;
#include&lt;fcntl.h&gt;
int main()
&#123;
    int fd = open(&quot;fifo&quot;, O_RDONLY);//打开管道文件
    if (fd &lt; 0)
    &#123;
        perror(&quot;open&quot;);
        exit(0);
    &#125;
    while (1)
    &#123;
        char buf[1024];
        memset(&amp;buf, 0, sizeof(buf));
        int len = read(fd, buf,sizeof(buf));//读取管道文件数据
        if (len == 0)
        &#123;
            printf(&quot;写端关闭了\n&quot;);
            break;
        &#125;
        printf(&quot;接受数据:%s\n&quot;, buf);

    &#125;
    close(fd);
    return 0;
&#125;
</code></pre>
<h3 id="利用有名管道实现聊天功能"><a href="#利用有名管道实现聊天功能" class="headerlink" title="利用有名管道实现聊天功能"></a>利用有名管道实现聊天功能</h3><p><img src="C:\Users\ASUS\AppData\Roaming\Typora\typora-user-images\image-20230123225111855.png" alt="image-20230123225111855"></p>
<p>​	</p>
<h1 id="线程"><a href="#线程" class="headerlink" title="线程"></a>线程</h1><h2 id="线程创建"><a href="#线程创建" class="headerlink" title="线程创建"></a>线程创建</h2><pre><code class="c++">//int pthread_create(pthread_t* thread, const pthread_attr_t* attr,
//    void* (*start_routine) (void*), void* arg);
//参数
//    -线程号
//    -线程属性
//    -函数指针，其内容即是子线程的具体函数代码
//				子线程的返回类型为void*,传入参数类型也为void*
//    -给第三个参数（子线程函数）的传入参数
//返回值
//	创建成功则返回0，否则返回错误号，通过streero返回错误信息

#include&lt;stdio.h&gt;
#include&lt;pthread.h&gt;
#include&lt;string.h&gt;
#include&lt;sys/types.h&gt;
#include&lt;unistd.h&gt;
#include&lt;stdlib.h&gt;

void* callback(void* arg)
&#123;
    printf(&quot;child thread....\npid=%d,tid2=%d\nnum=%d\n&quot;,
        getpid(),pthread_self(),*(int*)arg);//将空指针参数arg转换为int*，再解引用取值
    return NULL;
&#125;

int main()
&#123;
    pthread_t tid;
    //创建子线程，子线程具体代码由callback函数执行，并通过tid传出子线程的id
    //int ret = pthread_create(&amp;tid, NULL, callback, NULL);
    
    //设置子线程传入参数
    int num = 10;
    int ret = pthread_create(&amp;tid, NULL, callback, (void*)&amp;num);//将传入参数地址并转换类型为void*

    if (ret != 0)
    &#123;
        char* str = strerror(ret);
        printf(&quot;error:%s\n&quot;, str);
        return 0;
    &#125;
    //main()为主线程
    sleep(1);
    printf(&quot;tid1=%d&quot;, tid);
    for (int i = 0; i &lt; 3; i++)
    &#123;
        printf(&quot;%d\n&quot;, i);
    &#125;
    return 0;
&#125;
</code></pre>
<h2 id="线程传参"><a href="#线程传参" class="headerlink" title="线程传参"></a>线程传参</h2><pre><code class="c++">#include&lt;pthread.h&gt;
#include&lt;stdio.h&gt;
#include&lt;stdlib.h&gt;
#include&lt;string&gt;
#include&lt;sys/types.h&gt;
#include&lt;unistd.h&gt;
#include&lt;iostream&gt;
using namespace std;
//线程执行函数
struct test
&#123;
 int data;
 // char name[64];
 string name;
&#125;;
void *mythread(void* arg)
&#123;
 //获取子线程所在的进程空间id与其自身进程号
 printf(&quot;child thread,pid==[%d],id==[%ld]\n&quot;,getpid(),pthread_self());
 // int n=*(int *)arg;
 // printf(&quot;n=%d\n&quot;,n);
 struct test *t;
 t=(struct test*)arg;
 cout&lt;&lt;t-&gt;data&lt;&lt;&quot;\t&quot;&lt;&lt;t-&gt;name&lt;&lt;endl;
&#125;

int main()
&#123;
 int n=99;//传入整型
 struct test t;//传入结构体
 t.data=10;
 t.name=&quot;LiHua&quot;;
 pthread_t thread;
 // int ret=pthread_create(&amp;thread,NULL,mythread,NULL);
 int ret =pthread_create(&amp;thread,NULL,mythread,&amp;t);
 if(ret!=0)
 &#123;
        // printf(&quot;pthread_create error,[%s]\n&quot;,strerror(ret));
        cout&lt;&lt;&quot;pthread_create error&quot;&lt;&lt;ret&lt;&lt;endl;
        return -1;
 &#125;
 printf(&quot;main thread,pid==[%d],id==[%ld]\n&quot;,getpid(),pthread_self());
 sleep(1);//由于子线程与主线程共享地址空间，故当主线程空间撤销时，子线程也将不存在，该语句是为了防止子线程还未启动但主线程却撤销
 return 0;
&#125;
</code></pre>
<h2 id="多线程创建"><a href="#多线程创建" class="headerlink" title="多线程创建"></a>多线程创建</h2><pre><code class="c++">#include&lt;iostream&gt;
#include&lt;string&gt;
#include&lt;sys/types.h&gt;
#include&lt;unistd.h&gt;
#include&lt;pthread.h&gt;
using namespace std;
typedef struct person
&#123;
        string name;
        int age;
&#125;person;
void* mythread(void* arg)
&#123;
        // person *p;
        // p=(person* )arg;
        // cout&lt;&lt;&quot;name:&quot;&lt;&lt;p-&gt;name&lt;&lt;endl;
        // cout&lt;&lt;&quot;age:&quot;&lt;&lt;p-&gt;age&lt;&lt;endl;
        int n=*(int*)arg;
        cout&lt;&lt;&quot;\t&quot;&lt;&lt;n&lt;&lt;&quot;-&gt;&quot;&lt;&lt;getpid()&lt;&lt;&quot; &quot;&lt;&lt;pthread_self()&lt;&lt;endl;
&#125;
int main()
&#123;
        // person per;
        // per.name=&quot;李华&quot;;
        // per.age=18;
        pthread_t thread[5];
        int arr[5];
        for(int i=0;i&lt;5;i++)
        &#123;
                arr[i]=i;
                int ret =pthread_create(&amp;thread[i],NULL,mythread,&amp;arr[i]);
                if(ret!=0)
                &#123;
                        cout&lt;&lt;&quot;pthread_create error!&quot;&lt;&lt;endl;
                        return -1;
                &#125;
        &#125;
        cout&lt;&lt;getpid()&lt;&lt;&quot;\t--&gt;\t&quot;&lt;&lt;pthread_self()&lt;&lt;endl;
        sleep(1);
        return 1;
&#125;
</code></pre>
<p><img src="https://s3.bmp.ovh/imgs/2022/04/19/ac0485d9b4260bfd.png"></p>
<h2 id="线程分离"><a href="#线程分离" class="headerlink" title="线程分离"></a>线程分离</h2><pre><code class="c++">int pthread_detach(pthread_t thread);
/*
功能   使调用线程与当前进程分离，分离后不代表此线程不依赖当前进程，线程分离的目的是将线程资源回收
       的工作交由系统自动完成，也就是说当被分离的线程结束之后，系统会自动回收它的资源，不需要使用pthread_join进行回收，所以，此
       函数不会阻塞

参数    thread 线程号
返回    成功 0     失败 非0
*/
</code></pre>
<pre><code class="c++">#include&lt;stdio.h&gt;
#include&lt;pthread.h&gt;
#include&lt;stdlib.h&gt;
#include&lt;unistd.h&gt;
#include&lt;string.h&gt;
void* mythread(void* arg)
&#123;
    int n = *(int*)arg;
    printf(&quot;child%d:-&gt;	pid=%d,tid=%d\n&quot;, n, getpid(), pthread_self());
    //sleep(3);
    return NULL;
&#125;

int main()
&#123;
    pthread_t tid;
    int num = 10;
    int ret = pthread_create(&amp;tid, NULL, mythread, &amp;num);
    if (ret != 0)
    &#123;
        char* error = strerror(ret);
        printf(&quot;error:%s\n&quot;, error);
        return -1;
    &#125;
    //sleep(1);

    //设置子线程为分离状态，运行结束后可有系统自动回收其资源
    pthread_detach(tid);

    pthread_exit(NULL);//退出当前线程 return 0;
&#125;
</code></pre>
<h2 id="线程回收"><a href="#线程回收" class="headerlink" title="线程回收"></a>线程回收</h2><pre><code>   1. 用于等待其他线程结束：当调用 pthread_join() 时，当前线程会处于阻塞状态，直到被调用的线程结束后，当前线程才会重新开始执行。
   2. 对线程的资源进行回收：如果一个线程是非分离的（默认情况下创建的线程都是非分离）并且没有对该线程使用 pthread_join() 的话，该线程结束后并不会释放其内存空间，这会导致该线程变成了“僵尸线程”
</code></pre>
<pre><code class="c++">#include&lt;stdio.h&gt;
#include&lt;pthread.h&gt;
#include&lt;stdlib.h&gt;
#include&lt;unistd.h&gt;
#include&lt;string.h&gt;
int tt = 10;
void* mythread(void* arg)
&#123;
    int n = *(int*)arg;
    printf(&quot;child%d:-&gt;	pid=%d,tid=%d\n&quot;, n, getpid(), pthread_self());
    //sleep(3);
    //return NULL;
    pthread_exit ((void*)&amp;tt);//return (void*)&amp;tt
&#125;

//int pthread_join(pthread_t thread, void** retval);
//作用：连接已经终止的线程，回收子线程资源。阻塞当前线程执行，直到要回收的线程运行结束用当前线程回收子线程
//注：子进程只能通过父进程回收，而线程的资源可通过其他线程回收，一般情况下我们指定用主线程进行回收
//参数：
//	-要回收的线程id
//	-传出参数，将回收线程的返回值传出来，其类型为二级指针，即指针变量的地址

//不能连接已经分离的线程

int main()
&#123;
    pthread_t tid;
    int num = 10;
    int ret = pthread_create(&amp;tid, NULL, mythread, &amp;num);
    if (ret != 0)
    &#123;
        char* error = strerror(ret);
        printf(&quot;error:%s\n&quot;, error);
        return -1;
    &#125;
    //sleep(1);

    //pthread_join(tid, NULL);//回收子线程
    int* thread_reval;//用指针变量接收传出参数值
    pthread_join(tid, (void**)&amp;thread_reval);//回收子线程，并传出子线程返回值
    printf(&quot;线程资源回收完毕！\n&quot;);
    printf(&quot;tt=%d\n&quot;, *thread_reval);

    pthread_exit(NULL);//退出当前线程 return 0;
&#125;
</code></pre>
<h2 id="线程属性"><a href="#线程属性" class="headerlink" title="线程属性"></a>线程属性</h2><pre><code class="c++">初始化线程属性变量	pthread_attr_init()
设置/获取线程属性
撤销线程属性变量	pthread_attr_destory()
</code></pre>
<pre><code class="c++">#include&lt;pthread.h&gt;
#include&lt;stdio.h&gt;
#include&lt;stdlib.h&gt;
#include&lt;string.h&gt;
#include&lt;unistd.h&gt;

void* mythread(void* arg)
&#123;
    printf(&quot;childThreadId:%d\n&quot;, pthread_self());
    return NULL;
&#125;
int main()
&#123;
    pthread_attr_t attr;
    pthread_attr_init(&amp;attr);//初始化线程变量
    pthread_attr_setdetachstate(&amp;attr, PTHREAD_CREATE_DETACHED);//设置属性变量为分离状态
    
    
    pthread_t tid;
    int ret= pthread_create(&amp;tid,&amp;attr,mythread,NULL);
    if (ret != 0)
    &#123;
        char* error = strerror(ret);
        printf(&quot;error:%s\n&quot;, error);
        return -1;
    &#125;
    size_t size;
    pthread_attr_getstacksize(&amp;attr, &amp;size);//获取线程默认分配栈的大小
    printf(&quot;stackSize:%ld\n&quot;, size);
    sleep(1);
    pthread_attr_destroy(&amp;attr);//销毁线程属性变量

    return 0;
&#125;
</code></pre>
<h2 id="线程同步"><a href="#线程同步" class="headerlink" title="线程同步"></a>线程同步</h2><h3 id="同步概念与测试"><a href="#同步概念与测试" class="headerlink" title="同步概念与测试"></a>同步概念与测试</h3><pre><code class="c++">测试：设置三个线程进行售票，其中总票数不变，设置为全局变量
</code></pre>
<pre><code class="c++">#include&lt;stdio.h&gt;
#include&lt;stdlib.h&gt;
#include&lt;string.h&gt;
#include&lt;unistd.h&gt;
#include&lt;pthread.h&gt;


int stick = 100;//总票数

void* mythread(void* arg)
&#123;

    //临界区，各线程并发执行
    while (stick &gt; 0)
    &#123;
        usleep(3000);
        printf(&quot;%d:正在售出第%d张票\n&quot;, pthread_self(), stick);
        stick--;
    &#125;
    return NULL;
&#125;

int main()
&#123;
    pthread_t tid[3];
    //创建三个子线程进行售票
    for (int i = 0; i &lt; 3; i++)
    &#123;
        int ret = pthread_create(&amp;tid[i], NULL, mythread, NULL);
        if (ret != 0)
        &#123;
            char* error = strerror(ret);
            printf(&quot;error:%s\n&quot;, error);
            return -1;
        &#125;
    &#125;
    //回收子线程资源
    for (int i = 0; i &lt; 3; i++)
    &#123;
        pthread_join(tid[i],NULL);
    &#125;
    return 0;
&#125;
</code></pre>
<p><img src="https://s3.bmp.ovh/imgs/2022/04/19/a6f011a68de1e3c4.png"></p>
<h3 id="互斥锁"><a href="#互斥锁" class="headerlink" title="互斥锁"></a>互斥锁</h3><pre><code class="c++">#include&lt;stdio.h&gt;
#include&lt;stdlib.h&gt;
#include&lt;string.h&gt;
#include&lt;unistd.h&gt;
#include&lt;pthread.h&gt;


int stick = 100;//总票数
//定义互斥锁，该锁一般定义为全局变量，否则如果定义在主线程中，当主线程结束之后回收栈资源，则该锁将不能被再次使用
pthread_mutex_t mutex;

void* mythread(void* arg)
&#123;
    //临界区，各线程并发执行
    while (1)
    &#123;
        pthread_mutex_lock(&amp;mutex);//加锁
        if (stick &gt; 0)
        &#123;
            usleep(3000);
            printf(&quot;%d:正在售出第%d张票\n&quot;, pthread_self(), stick);
            stick--;
        &#125;
        else &#123;
            pthread_mutex_unlock(&amp;mutex);//解锁
            break;
        &#125;
        pthread_mutex_unlock(&amp;mutex);
    &#125;
    return NULL;
&#125;

int main()
&#123;
    pthread_t tid[3];
    pthread_mutex_init(&amp;mutex,NULL);//初始化互斥锁

    //创建三个子线程进行售票
    for (int i = 0; i &lt; 3; i++)
    &#123;
        int ret = pthread_create(&amp;tid[i], NULL, mythread, NULL);
        if (ret != 0)
        &#123;
            char* error = strerror(ret);
            printf(&quot;error:%s\n&quot;, error);
            return -1;
        &#125;
    &#125;
    //回收子线程资源
    for (int i = 0; i &lt; 3; i++)
    &#123;
        pthread_join(tid[i],NULL);
    &#125;

    pthread_mutex_destroy(&amp;mutex);//释放互斥锁资源
    return 0;
&#125;
</code></pre>
<pre><code class="c++">#include&lt;iostream&gt;
#include&lt;string&gt;
#include&lt;sys/types.h&gt;
#include&lt;unistd.h&gt;
#include&lt;pthread.h&gt;
#include&lt;time.h&gt;
using namespace std;
pthread_mutex_t mutex;//定义锁
void* mythread(void* arg)
&#123;
        int n;
        int c=*(int*)arg;
        for(int i=0;i&lt;5;i++)
        &#123;
                if(c==0)
                &#123;
                        pthread_mutex_lock(&amp;mutex);//第一个线程加锁
                        cout&lt;&lt;&quot;hello &quot;;
                        sleep(rand()%3);
                        cout&lt;&lt;&quot;world!&quot;&lt;&lt;endl;
                        pthread_mutex_unlock(&amp;mutex);//第一个线程解锁
                &#125;
                else
                &#123;
                        pthread_mutex_lock(&amp;mutex);//第二个线程加锁
                        cout&lt;&lt;&quot;HELLO &quot;;
                        sleep(rand()%3);
                        cout&lt;&lt;&quot;WORLD!&quot;&lt;&lt;endl;
                        pthread_mutex_unlock(&amp;mutex);//第二个线程解锁
                &#125;
        &#125;
&#125;
int main()
&#123;
        srand((unsigned)time(0));
        pthread_mutex_init(&amp;mutex,NULL);//初始化锁
        pthread_t thread[2];
        int arr[2];
        for(int i=0;i&lt;2;i++)
        &#123;
                arr[i]=i;
                int ret=pthread_create(&amp;thread[i],NULL,mythread,&amp;arr[i]);
                if(ret!=0)
                &#123;
                        cout&lt;&lt;&quot;第&quot;&lt;&lt;i&lt;&lt;&quot;个线程error!&quot;&lt;&lt;endl;
                        return -1;
                &#125;
        &#125;
        pthread_join(thread[0],NULL);
        pthread_join(thread[1],NULL);
        pthread_mutex_destroy(&amp;mutex);//释放锁
        return 1;
&#125;
</code></pre>
<p><strong>加锁前：</strong></p>
<p><img src="https://s3.bmp.ovh/imgs/2022/04/19/9da0a4a5b52a3667.png"></p>
<p><strong>加锁后：</strong></p>
<p><img src="https://s3.bmp.ovh/imgs/2022/04/19/5dfd534c3f74d17c.png"></p>
<h3 id="死锁"><a href="#死锁" class="headerlink" title="死锁"></a>死锁</h3><h3 id="读写锁"><a href="#读写锁" class="headerlink" title="读写锁"></a>读写锁</h3><p><strong>写独占，读共享</strong></p>
<pre><code class="c++">#include&lt;iostream&gt;
#include&lt;string&gt;
#include&lt;sys/types.h&gt;
#include&lt;unistd.h&gt;
#include&lt;pthread.h&gt;
using namespace std;
int number=0;
pthread_rwlock_t rwlock;//定义读写锁
void* thread_read(void* arg)
&#123;
        int c=*(int*)arg;
        int n;
        while(1)
        &#123;
                pthread_rwlock_rdlock(&amp;rwlock);//加读锁
                n=number;
                cout&lt;&lt;&quot;r:[&quot;&lt;&lt;c&lt;&lt;&quot;]&quot;&lt;&lt;n&lt;&lt;endl;
                pthread_rwlock_unlock(&amp;rwlock);//解锁
                sleep(1);
        &#125;
&#125;
void* thread_write(void* arg)
&#123;
        int c=*(int*)arg;
        int n;
        while(1)
        &#123;
                pthread_rwlock_wrlock(&amp;rwlock);//加写锁
                n=number;
                n++;
                number=n;
                cout&lt;&lt;&quot;w:[&quot;&lt;&lt;c&lt;&lt;&quot;]&quot;&lt;&lt;number&lt;&lt;endl;
                pthread_rwlock_unlock(&amp;rwlock);//解锁
                sleep(1);
        &#125;
&#125;
int main()
&#123;
        pthread_rwlock_init(&amp;rwlock,NULL);//读写锁初始化
        int arr[8];
        pthread_t thread[8];
        int i;
        for(int i=0;i&lt;3;i++)//定义三个写进程
        &#123;
                arr[i]=i;
                int ret=pthread_create(&amp;thread[i],NULL,thread_write,&amp;arr[i]);
                if(ret!=0)
                &#123;
                        cout&lt;&lt;&quot;thread_read error!&quot;&lt;&lt;endl;
                        return -1;
                &#125;
        &#125;
        for(int i=3;i&lt;8;i++)//定义五个读进程
        &#123;
                arr[i]=i;
                int ret=pthread_create(&amp;thread[i],NULL,thread_read,&amp;arr[i]);
                if(ret!=0)
                &#123;
                        cout&lt;&lt;&quot;thread_write error!&quot;&lt;&lt;endl;
                        return -1;
                &#125;
        &#125;
        for(int j=0;j&lt;8;j++)
        &#123;
                pthread_join(thread[j],NULL);
        &#125;
        pthread_rwlock_destroy(&amp;rwlock);//释放读写锁
        return 1;
&#125;
</code></pre>
<p><strong>加锁前：</strong></p>
<p><img src="https://s2.loli.net/2022/04/19/2g61hQAtdsXeBEZ.png" alt="image-20220411172728414.png"></p>
<p><strong>加锁后：</strong></p>
<p><img src="https://s2.loli.net/2022/04/19/1RFXThacAfGJwUo.png" alt="image-20220411172523719.png"></p>
<h3 id="条件变量"><a href="#条件变量" class="headerlink" title="条件变量"></a>条件变量</h3><h4 id="单生产者消费者线程"><a href="#单生产者消费者线程" class="headerlink" title="单生产者消费者线程"></a>单生产者消费者线程</h4><pre><code class="c++">#include&lt;iostream&gt;
#include&lt;string&gt;
#include&lt;sys/types.h&gt;
#include&lt;unistd.h&gt;
#include&lt;time.h&gt;
#include&lt;pthread.h&gt;
using namespace std;
//定义链表结构
typedef struct node
&#123;
        int data;
        struct node* next;
&#125;node,*list;

//维护一个共享链表
list l=NULL;
#if 0
l=new node;
l-&gt;next=NULL;
#endif

//定义互斥变量与条件变量
pthread_mutex_t mutex;
pthread_cond_t cond;

//生产者进程
void* producer(void* arg)
&#123;
        // list l=(node*)arg;
        srand((unsigned)time(0));
        while(1)
        &#123;
            //生产产品
            node* s=new node;

            s-&gt;data=rand()%100+1;
            cout&lt;&lt;&quot;生产中：&quot;&lt;&lt;s-&gt;data&lt;&lt;endl;
            //将产品放入共享数据区域
            pthread_mutex_lock(&amp;mutex);//加锁

            #if 0
            s-&gt;next=l-&gt;next;
            l-&gt;next=s;
            #endif
            //头插
            #if 1
            s-&gt;next=l;
            l=s;
            #endif
            pthread_mutex_unlock(&amp;mutex);//解锁
            //发信号取数据，若有多个等待被唤醒的线程，则该函数可一次性唤醒所有等待唤醒的线程，这些之前等待的线程将自行抢占CPU
            pthread_cond_signal(&amp;cond);
            sleep(rand()%3);
        &#125;
&#125;
//消费者进程
void* consumer(void* arg)
&#123;
        srand((unsigned)time(0));
        // list l=(node*)arg;
        while(1)
        &#123;
            //从共享数据区域中取数据
            pthread_mutex_lock(&amp;mutex);//加锁
            if(l==NULL)
            &#123;
                //等待生产者进程唤醒该进程
                pthread_cond_wait(&amp;cond,&amp;mutex);
            &#125;
            
            //取产品
            
            // node* p=l-&gt;next;
            node* p=l;
            cout&lt;&lt;&quot;已消费：&quot;&lt;&lt;p-&gt;data&lt;&lt;endl;
            // l-&gt;next=p-&gt;next;
            l=l-&gt;next;
            delete p;
            pthread_mutex_unlock(&amp;mutex);//解锁
            sleep(rand()%3);
        &#125;
&#125;

int main()
&#123;
        pthread_mutex_init(&amp;mutex,NULL);
        pthread_cond_init(&amp;cond,NULL);

        pthread_t threadProducer;
        pthread_t threadConsumer;
        //维护一个共享链表
        // list l=(node*)malloc(sizeof(node));
        // list l=new node;
        // l-&gt;next=NULL;

        pthread_create(&amp;threadProducer,NULL,producer,&amp;l);
        pthread_create(&amp;threadConsumer,NULL,consumer,&amp;l);

        pthread_join(threadProducer,NULL);
        pthread_join(threadConsumer,NULL);

        pthread_mutex_destroy(&amp;mutex);
        pthread_cond_destroy(&amp;cond);

        return 1;
&#125;
</code></pre>
<p><strong>注：</strong>pthread_cond_wait()函数必须与mutex互斥变量配对使用，因为其内部实现的主要步骤为：</p>
<pre><code class="c++">1.线程放在等待队列上，解锁

2.等待 pthread_cond_signal或者pthread_cond_broadcast信号之后去竞争锁

3.若竞争到互斥索则加锁。
</code></pre>
<p><img src="https://s2.loli.net/2022/04/19/zwARMPNqJ4LaoHi.png" alt="image-20220412142530152.png"></p>
<h4 id="多生产者消费者线程"><a href="#多生产者消费者线程" class="headerlink" title="多生产者消费者线程"></a>多生产者消费者线程</h4><pre><code class="c++">#include&lt;iostream&gt;
#include&lt;string&gt;
#include&lt;sys/types.h&gt;
#include&lt;unistd.h&gt;
#include&lt;time.h&gt;
#include&lt;pthread.h&gt;
using namespace std;
//定义链表节点结构
typedef struct node
&#123;
    int data;
    struct node* next;
&#125;node,*list;

//定义共享数据区域
list l=NULL;

#if 0
l=new node;
l-&gt;next=NULL;
#endif

//定义互斥变量与条件变量
pthread_mutex_t mutex;
pthread_cond_t cond;

//生产者线程
void* producer(void* arg)
&#123;
    int n=*(int*)arg;
    srand((unsigned)time(0));
    while(1)
    &#123;
        //生产数据
        node* s=new node;
        s-&gt;data=rand()%100+1;
        cout&lt;&lt;&quot;[&quot;&lt;&lt;n&lt;&lt;&quot;]生产：&quot;&lt;&lt;s-&gt;data&lt;&lt;endl;
        //互斥将生产的数据放入共享缓冲区
        pthread_mutex_lock(&amp;mutex);//加锁

        #if 0
        s-&gt;next=l-&gt;next;
        l-&gt;next=s;
        #endif
        //头插法将数据插入到缓冲区中
        #if 1
        s-&gt;next=l;
        l=s;
        #endif
        pthread_mutex_unlock(&amp;mutex);//解锁
        pthread_cond_signal(&amp;cond);//唤醒消费者进程
        sleep(rand()%3);
    &#125;
&#125;


//消费者线程
void* consumer(void* arg)
&#123;
    int n=*(int*)arg;
    srand((unsigned)time(0));
    // list l=(node*)arg;
    while(1)
    &#123;
        pthread_mutex_lock(&amp;mutex);
        if(l==NULL)
        &#123;
            pthread_cond_wait(&amp;cond,&amp;mutex);//等待唤醒
        &#125;

        //由于pthread_cond_singal()唤醒的所有的消费者进程，使得各消费者进行抢占CPU，因此会出现一个消费者消费完所有数据后此后还有消费者取数据，而此时l==NULL,故p-&gt;data中已无数据
        if(l==NULL)
        &#123;
            pthread_mutex_unlock(&amp;mutex);//当出现缓冲区中数据无数据进行判断时，首先需要对缓冲区进行解锁以防止出现死锁现象
            continue;//跳出该消费者取数据循环中
        &#125;

        // node* p=l-&gt;next;
        node* p=l;
        cout&lt;&lt;&quot;[&quot;&lt;&lt;n&lt;&lt;&quot;]消费：&quot;&lt;&lt;p-&gt;data&lt;&lt;endl;
        // l-&gt;next=p-&gt;next;
        l=l-&gt;next;
        delete p;
        pthread_mutex_unlock(&amp;mutex);
        sleep(rand()%3);
    &#125;
&#125;

int main()
&#123;
    pthread_mutex_init(&amp;mutex,NULL);
    pthread_cond_init(&amp;cond,NULL);

    //定义五个生产者与消费者进程组
    pthread_t threadProducer[5];
    pthread_t threadConsumer[5];
    int arr[5];

    for(int i=0;i&lt;5;i++)
    &#123;
        arr[i]=i;
        pthread_create(&amp;threadProducer[i],NULL,producer,&amp;arr[i]);
        pthread_create(&amp;threadConsumer[i],NULL,consumer,&amp;arr[i]);
    &#125;


    for(int j=0;j&lt;5;j++)
    &#123;
        pthread_join(threadProducer[j],NULL);
        pthread_join(threadConsumer[j],NULL);
    &#125;

    pthread_mutex_destroy(&amp;mutex);
    pthread_cond_destroy(&amp;cond);

    return 1;
&#125;
</code></pre>
<p><img src="https://s2.loli.net/2022/04/19/eH78TNj5b1Xwof9.png" alt="image-20220412124128152.png"></p>
<h4 id="信号量"><a href="#信号量" class="headerlink" title="信号量"></a>信号量</h4><pre><code class="c++">#include&lt;iostream&gt;
#include&lt;string&gt;
#include&lt;sys/types.h&gt;
#include&lt;unistd.h&gt;
#include&lt;pthread.h&gt;
#include&lt;semaphore.h&gt;
#include&lt;time.h&gt;
using namespace std;
//定义链表节点结构
typedef struct node
&#123;
    int data;
    struct node* next;
&#125;node,*list;
//定义共享区域
list l=NULL;
//定义信号量
sem_t pro;
sem_t con;
//生产者线程
void* producer(void* arg)
&#123;
    srand((unsigned)time(0));
    while(1)
    &#123;
        sem_wait(&amp;pro);
        //生产数据
        node* s=new node;
        s-&gt;data=rand()%100+1;
        //将数据存放共享区域
        s-&gt;next=l;
        l=s;
        cout&lt;&lt;&quot;producer:&quot;&lt;&lt;s-&gt;data&lt;&lt;endl;
        sem_post(&amp;con);//给消费者发信号数据已经存放完毕
        sleep(rand()%3);
    &#125;
&#125;
//消费者线程
void* consumer(void* arg)
&#123;
    srand((unsigned)time(0));
    while(1)
    &#123;
        sem_wait(&amp;con);
        //从共享区域中读出数据
        node* p=l;
        cout&lt;&lt;&quot;consumer:&quot;&lt;&lt;p-&gt;data&lt;&lt;endl;
        //将读出的数据从共享区域中删除
        l=p-&gt;next;
        delete p;
        sem_post(&amp;pro);//给生产者发信号数据已从共享区域中读出
        sleep(rand()%3);
    &#125;
&#125;

int main()
&#123;
    //初始化信号量
    sem_init(&amp;pro,0,5);
    sem_init(&amp;con,0,0);
    //设置线程id
    pthread_t thread_producer;
    pthread_t thread_consumer;
    //创建子线程
    pthread_create(&amp;thread_producer,NULL,producer,NULL);
    pthread_create(&amp;thread_consumer,NULL,consumer,NULL);
    //等待子线程退出，并回收子线程区域
    pthread_join(thread_producer,NULL);
    pthread_join(thread_consumer,NULL);
    //销毁子线程信号量
    sem_destroy(&amp;pro);
    sem_destroy(&amp;con);
    return 1;
&#125;
</code></pre>
<p><img src="https://s2.loli.net/2022/04/19/8KuOnj2Bx3r7gJm.png" alt="image-20220413102851778.png"></p>
<p>​	</p>
]]></content>
      <categories>
        <category>系统编程</category>
      </categories>
      <tags>
        <tag>系统编程</tag>
        <tag>c++</tag>
      </tags>
  </entry>
  <entry>
    <title>C++网络编程</title>
    <url>/2022/04/30/c++%E7%BD%91%E7%BB%9C%E7%BC%96%E7%A8%8B/</url>
    <content><![CDATA[<!-- more -->



<h1 id="socket编程"><a href="#socket编程" class="headerlink" title="socket编程"></a>socket编程</h1><p>[Socket原理讲解](https:	&#x2F;&#x2F;blog.csdn.net&#x2F;pashanhu6402&#x2F;article&#x2F;details&#x2F;96428887)</p>
<h2 id="服务端开发"><a href="#服务端开发" class="headerlink" title="服务端开发"></a>服务端开发</h2><p><strong>server.cpp</strong></p>
<pre><code class="c++">#include&lt;iostream&gt;
#include&lt;string.h&gt;
#include&lt;sys/types.h&gt;
#include&lt;unistd.h&gt;
#include&lt;arpa/inet.h&gt;
#include&lt;netinet/in.h&gt;
using namespace std;
int main()
&#123;
        // #include &lt;sys/types.h&gt;          /* See NOTES */
    // #include &lt;sys/socket.h&gt;
    //返回文件描述符
    //domain:协议族；type：socket类型：protocol：协议，为0表示自动创建于type对应的协议
    // int socket(int domain, int type, int protocol);

    //创建socket
        int lfd=socket(AF_INET,SOCK_STREAM,0);//AF_INET：ipV4协议；SOCK_STREAM：tcp协议
    
        if(lfd&lt;0)
        &#123;
                perror(&quot;socket error&quot;);
                return -1;
        &#125;
    
        // #include &lt;sys/types.h&gt;          /* See NOTES */

 //    #include &lt;sys/socket.h&gt;
 //    int bind(int sockfd, const struct sockaddr *addr,
 //            socklen_t addrlen)

        //绑定，将端口号与ip绑定到socket上
        struct sockaddr_in serv;
        bzero(&amp;serv,sizeof(serv));//初始化内存
        serv.sin_family=AF_INET;//协议
        serv.sin_port=htons(8888);//端口号
        serv.sin_addr.s_addr=htonl(INADDR_ANY);//使用本地任意ip
        int ret=bind(lfd,(struct sockaddr*)&amp;serv,sizeof(serv));
        if(ret&lt;0)
        &#123;
                perror(&quot;bind error&quot;);
                return -1;
        &#125;
    
        // #include &lt;sys/types.h&gt;          /* See NOTES */

 //    #include &lt;sys/socket.h&gt;
 //    int listen(int sockfd, int backlog);

        //监听客户端，并设置监听队列数最大为128到监听队列
        int lis=listen(lfd,128);
        if(lis&lt;0)
        &#123;
                perror(&quot;error&quot;);
                return -1;
        &#125;
    
        // #include &lt;sys/types.h&gt;          /* See NOTES */

 //    #include &lt;sys/socket.h&gt;
 //    int accept(int sockfd, struct sockaddr *addr, socklen_t *addrlen);

        //从链接队列中取出一个链接	
        int cfd=accept(lfd,NULL,NULL);
        cout&lt;&lt;&quot;lfd:&quot;&lt;&lt;lfd&lt;&lt;&quot;\tcfd:&quot;&lt;&lt;cfd&lt;&lt;endl;
    
        //接受数据
        int i=0,n=0;
        char buf[1024];
        while(1)
        &#123;
                memset(buf,0,sizeof(buf));//内存初始化
                n=read(cfd,buf,sizeof(buf));//读取数据
                  if(n&lt;=0)
                &#123;
                    cout&lt;&lt;&quot;read error or client connect closed!&quot;&lt;&lt;endl;
                    break;
                &#125;
                cout&lt;&lt;&quot;共接受&quot;&lt;&lt;n&lt;&lt;&quot;个字符：&quot;&lt;&lt;buf&lt;&lt;endl;
                write(cfd,&quot;OK!&quot;,n);
                write(cfd,buf,n);//发送数据
        &#125;
    
        close(lfd);
        close(cfd);
        return 1;

&#125;
</code></pre>
<p>linux编译</p>
<pre><code class="c++">g++ server.cpp -o server
</code></pre>
<p>服务端启动</p>
<pre><code class="c++">./server
</code></pre>
<p>客户端测试</p>
<pre><code class="c++">nc 127.0.0.1 8888
</code></pre>
<p><a href="https://imgtu.com/i/Lv5Y4I"><img src="https://s1.ax1x.com/2022/04/29/Lv5Y4I.png" alt="Lv5Y4I.png"></a></p>
<h2 id="客户端开发"><a href="#客户端开发" class="headerlink" title="客户端开发"></a>客户端开发</h2><pre><code class="c++">#include&lt;iostream&gt;
#include&lt;string.h&gt;
#include&lt;sys/types.h&gt;
#include&lt;unistd.h&gt;
#include&lt;arpa/inet.h&gt;
#include&lt;netinet/in.h&gt;
using namespace std;
int main()
&#123;
        // #include &lt;sys/types.h&gt;          /* See NOTES */
    // #include &lt;sys/socket.h&gt;
    //返回文件描述符
    //domain:协议族；type：socket类型：protocol：协议，为0表示自动创建于type对应的协议
    // int socket(int domain, int type, int protocol);

    //创建socket
        int cfd=socket(AF_INET,SOCK_STREAM,0);//AF_INET：ipV4协议；SOCK_STREAM：tcp协议
    
        if(cfd&lt;0)
        &#123;
                perror(&quot;socket error&quot;);
                return -1;
        &#125;
    //链接服务端
        struct sockaddr_in serv;
        serv.sin_family=AF_INET;
        serv.sin_port=htons(8888);
        inet_pton(AF_INET,&quot;127.0.0.1&quot;,&amp;serv.sin_addr.s_addr);
        int ret=connect(cfd,(struct sockaddr *)&amp;serv,sizeof(serv));
        if(ret&lt;0)
        &#123;
                perror(&quot;connect error&quot;);
                return -1;
        &#125;
    //想服务器发送数据并接受来自服务器的数据
    
        int n=0;
        char buf[256];
        while(1)
        &#123;
                //读标准输入数据
                memset(buf,0,sizeof(buf));
                n=read(STDIN_FILENO,buf,sizeof(buf));
                //发送数据
                write(cfd,buf,n);
                //读取服务器数据
                memset(buf,0,sizeof(buf));
                n=read(cfd,buf,sizeof(buf));
                if(n&lt;=0)
                &#123;
                        cout&lt;&lt;&quot;read error or connect closed!&quot;&lt;&lt;endl;
                        break;
                &#125;
    
        &#125;
        close(cfd);
        return 1;

&#125;
</code></pre>
<p><a href="https://imgtu.com/i/LvbWx1"><img src="https://s1.ax1x.com/2022/04/29/LvbWx1.png" alt="LvbWx1.png"></a></p>
<h1 id="三次握手与四次挥手"><a href="#三次握手与四次挥手" class="headerlink" title="三次握手与四次挥手"></a>三次握手与四次挥手</h1><h1 id="高并发服务器"><a href="#高并发服务器" class="headerlink" title="高并发服务器"></a>高并发服务器</h1><p><a href="https://www.cnblogs.com/xuejiale/p/10849095.html">Linux C编程之十八 高并发服务器 - pointerC++ - 博客园 (cnblogs.com)</a></p>
<h2 id="多进程并发服务器"><a href="#多进程并发服务器" class="headerlink" title="多进程并发服务器"></a>多进程并发服务器</h2><p><a href="https://imgtu.com/i/LxSVV1"><img src="https://s1.ax1x.com/2022/04/29/LxSVV1.png" alt="LxSVV1.png"></a></p>
<pre><code class="c++">#include&lt;stdlib.h&gt;
#include&lt;sys/types.h&gt;
#include&lt;sys/socket.h&gt;
#include&lt;unistd.h&gt;
#include&lt;arpa/inet.h&gt;
#include&lt;netinet/in.h&gt;
#include&lt;netdb.h&gt;
#include&lt;ctype.h&gt;
#include&lt;stdio.h&gt;
#include&lt;string.h&gt;
int main()
&#123;
        //创建socket
        int lfd=socket(AF_INET,SOCK_STREAM,0);

        //将端口号与if绑定到socket
        struct sockaddr_in serv;
        memset(&amp;serv,0,sizeof(serv));
        serv.sin_family=AF_INET;
        serv.sin_port=htons(8888);
        serv.sin_addr.s_addr=htonl(INADDR_ANY);
    
        bind(lfd,(struct sockaddr*)&amp;serv,sizeof(serv));
    
        //监听
        listen(lfd,128);
    
        //收发数据
    
        pid_t pid;
        int cfd;
        int socklen=sizeof(struct sockaddr_in);
        struct sockaddr_in clientaddr;
        while(1)
        &#123;
            //从等待队列中取出一个客户端
            cfd=accept(lfd,(struct sockaddr*)&amp;clientaddr,(socklen_t*)&amp;socklen);
            printf(&quot;客户端%s已经链接\n&quot;,inet_ntoa(clientaddr.sin_addr));
            // 创建一个新的进程，该进程从父进程复制而来，只有少量数据与父进程不同
            // fork（）函数会返回两个返回值pid，
            // 若pid==0，表示当前运行进程为子进程，若pid&gt;0,表示当前运行进程为父进程，且pid为子进程的进程号
            pid=fork();
            if(pid&lt;0)//子进程创建失败
            &#123;
                perror(&quot;fork error&quot;);
                exit(-1);
            &#125;
            else if(pid&gt;0)//父进程
            &#123;
                close(cfd);//让父进程只进行监听
            &#125;
            else if(pid==0)//子进程
            &#123;
                close(lfd);//关闭监听描述符，让子进程只收发数据
                int n;
                char buff[1024];
                while(1)
                &#123;
                    n=read(cfd,buff,sizeof(buff));
                    printf(&quot;端口号为[%d]&quot;,ntohs(clientaddr.sin_port));
                    if(n&lt;=0)
                    &#123;
                        printf(&quot;read error or client closed,n==%d\n&quot;,n);
                        break;
                    &#125;
                    printf(&quot;n=%d,buff=%s\n&quot;,n,buff);
                    write(cfd,&quot;OK\n&quot;,n);
                &#125;
                close(cfd);//对应子进程收发完数据后关闭文件描述符
                exit(0);//防止子进程继续创建子进程
            &#125;
        &#125;
        close(lfd);//父进程关闭监听描述符
        return 1;

&#125;
</code></pre>
<p><a href="https://imgtu.com/i/LxkI10"><img src="https://s1.ax1x.com/2022/04/29/LxkI10.png" alt="LxkI10.png"></a></p>
<h2 id="多线程并发服务器"><a href="#多线程并发服务器" class="headerlink" title="多线程并发服务器"></a>多线程并发服务器</h2><p><a href="https://imgtu.com/i/LxSAbR"><img src="https://s1.ax1x.com/2022/04/29/LxSAbR.png" alt="LxSAbR.png"></a></p>
<pre><code class="c++">#include&lt;iostream&gt;
#include&lt;string.h&gt;
#include &lt;strings.h&gt;
#include &lt;sys/socket.h&gt;
#include&lt;sys/types.h&gt;
#include&lt;unistd.h&gt;
#include&lt;arpa/inet.h&gt;
#include&lt;netinet/in.h&gt;
#include&lt;pthread.h&gt;
using namespace std;
void* mythread(void* arg)//子线程执行函数
&#123;
        int cfd=*(int *)arg;
        int n;
        char buff[1024];
        while(1)
        &#123;
                memset(buff,0,sizeof(buff));
                n=read(cfd,buff,sizeof(buff));
                if(n&lt;=0)
                &#123;
                        cout&lt;&lt;&quot;read error or client closed!&quot;&lt;&lt;endl;
                        break;
                &#125;

                cout&lt;&lt;&quot;共接受&quot;&lt;&lt;n&lt;&lt;&quot;个字符：&quot;&lt;&lt;buff&lt;&lt;endl;
                //发送数据
                write(cfd,buff,n);
        &#125;
        close(cfd);
        pthread_exit(NULL);

&#125;
int main()
&#123;
        //创建socket
        int lfd=socket(AF_INET, SOCK_STREAM, 0);

        struct sockaddr_in serv;
        bzero(&amp;serv, sizeof(serv));
        serv.sin_family=AF_INET;
        serv.sin_port=htons(8888);//主机字节序转换为网络字节序
        serv.sin_addr.s_addr=htonl(INADDR_ANY);
        //绑定
        bind(lfd,(struct sockaddr*)&amp;serv,sizeof(serv));
        //监听
        listen(lfd,128);
    
        int cfd;
        pthread_t tid;
        while(1)
        &#123;
                //接受客户端
                cfd=accept(lfd,NULL,NULL);
                //创建子线程
                cfd=pthread_create(&amp;tid,NULL,mythread,&amp;cfd);//mythread为线程执行函数
                //设置线程为分离属性（默认为非分离属性），保证线程执行结束后自行释放所占用资源
                pthread_detach(tid);c++
        &#125;
        close(lfd);
        return 1;

&#125;
</code></pre>
<p>注：该代码可以接受数据但无法运行</p>
<p><strong>由于线程创建时与主线程共享内存空间，因此子线程不能关闭lfd（监听描述符），同样主线程也不能关闭cfd（通信描述符）</strong></p>
<h2 id="多路IO转接服务器"><a href="#多路IO转接服务器" class="headerlink" title="多路IO转接服务器"></a>多路IO转接服务器</h2>]]></content>
      <categories>
        <category>网络编程</category>
      </categories>
      <tags>
        <tag>网络编程</tag>
        <tag>C++</tag>
      </tags>
  </entry>
  <entry>
    <title>Qt</title>
    <url>/2022/04/28/Qt/</url>
    <content><![CDATA[<!-- more -->


<h1 id="信号与槽"><a href="#信号与槽" class="headerlink" title="信号与槽"></a>信号与槽</h1><h2 id="按钮控件与信号"><a href="#按钮控件与信号" class="headerlink" title="按钮控件与信号"></a>按钮控件与信号</h2><pre><code class="c++">//    创建按钮对象及其实例，参数一为按钮名称，参数二为将按钮控件镶嵌到主窗口当中
    QPushButton* btn=new QPushButton(&quot;close&quot;,this);
//   重新设置主窗口大小
    resize(600,400);
//    移动按钮位置，按钮创建时默认为原点
    btn-&gt;move(width()/2,height()/2);
//    设置主窗口名称
    this-&gt;setObjectName(&quot;按钮实验&quot;);
//   信号控件，参数：发送信号者，发送的信号，接受信号者，接受的信号
    connect(btn,&amp;QPushButton::clicked,this,&amp;myWidget::close);
</code></pre>
<h2 id="自定义信号"><a href="#自定义信号" class="headerlink" title="自定义信号"></a>自定义信号</h2><pre><code class="c++">#include &quot;mywidget.h&quot;
#include &quot;ui_mywidget.h&quot;
#include&lt;QPushButton&gt;

myWidget::myWidget(QWidget *parent)
    : QWidget(parent)
    , ui(new Ui::myWidget)
&#123;
    ui-&gt;setupUi(this);

    //创建教师对象
    this-&gt;zt=new Teacher(this);
   //创建学生对象
    this-&gt;st=new Student(this);

    connect(zt,&amp;Teacher::hungry,st,&amp;Student::treat);
    classIsOver();
    classIsOver();

&#125;
void myWidget::classIsOver()
&#123;
    //emit发射信号
    emit zt-&gt;hungry();
&#125;

myWidget::~myWidget()
&#123;
    delete ui;
&#125;

</code></pre>
<pre><code class="c++">#include &quot;student.h&quot;
#include&lt;QDebug&gt;

Student::Student(QObject *parent) : QObject(parent)
&#123;

&#125;
void Student::treat()
&#123;
     qDebug()&lt;&lt;&quot;请老师吃饭&quot;&lt;&lt;endl;
&#125;
</code></pre>
<h2 id="自定义信号与槽发生重载的问题"><a href="#自定义信号与槽发生重载的问题" class="headerlink" title="自定义信号与槽发生重载的问题"></a>自定义信号与槽发生重载的问题</h2><pre><code class="c++">   //创建教师对象
    this-&gt;zt=new Teacher(this);
   //创建学生对象
    this-&gt;st=new Student(this);
    //函数指针-&gt;函数地址
    void(Teacher::*teacherSingal)(QString)=&amp;Teacher::hungry;
    void(Student::*studentSingal)(QString)=&amp;Student::treat;

    connect(zt,teacherSingal,st,studentSingal);
    classIsOver();
</code></pre>
<pre><code class="c++">#include &quot;student.h&quot;
#include&lt;QDebug&gt;

Student::Student(QObject *parent) : QObject(parent)
&#123;

&#125;
void Student::treat()
&#123;
     qDebug()&lt;&lt;&quot;请老师吃饭&quot;&lt;&lt;endl;
&#125;
void Student::treat(QString foodName)
&#123;
    qDebug()&lt;&lt;&quot;请老师吃：&quot;&lt;&lt;foodName&lt;&lt;endl;
&#125;

void myWidget::classIsOver()
&#123;
    //emit发射信号
    emit zt-&gt;hungry(&quot;宫保鸡丁&quot;);
&#125;
</code></pre>
<h2 id="按钮触发信号"><a href="#按钮触发信号" class="headerlink" title="按钮触发信号"></a>按钮触发信号</h2><pre><code class="c++"> QPushButton* btn=new QPushButton(&quot;下课&quot;,this);
    this-&gt;resize(600,400);
    //点击下课按钮触发宫保鸡丁时间
    connect(btn,&amp;QPushButton::clicked,this,&amp;myWidget::classIsOver);
void myWidget::classIsOver()
&#123;
    //emit发射信号
    emit st-&gt;treat(&quot;宫保鸡丁&quot;);
&#125;
</code></pre>
<h2 id="信号链接信号"><a href="#信号链接信号" class="headerlink" title="信号链接信号"></a>信号链接信号</h2><pre><code class="c++">//    无参信号与槽链接
    void(Teacher::*teacherSingal)(void)=&amp;Teacher::hungry;
    void(Student::*studentSingal)(void)=&amp;Student::treat;
    connect(zt,teacherSingal,st,studentSingal);

//    信号链接信号,按钮信号链接教师信号 
    connect(btn,&amp;QPushButton::clicked,zt,teacherSingal);
&#125;
void myWidget::classIsOver()
&#123;
    //emit发射信号
    emit zt-&gt;hungry();
//    emit zt-&gt;hungry(&quot;宫保鸡丁&quot;);
//    emit st-&gt;treat(&quot;宫保鸡丁&quot;);
&#125;
</code></pre>
<h2 id="lamdba表达式"><a href="#lamdba表达式" class="headerlink" title="lamdba表达式"></a>lamdba表达式</h2><pre><code class="c++">//    lamdba表达式
//    [外部变量捕获方式](形参)mutable-&gt;返回类型&#123;函数体&#125;(实参);
//    [外部变量捕获方式]：当&#123;&#125;中使用外部变量时，需要在此声明使用的外部变量，称其为捕获，捕获方式有，
//              值传递[a]：该方式传递外部变量进入lamdba表达式内部使用时，默认为const方式，即不可改变传递进来的外部变量的值，
//                                若需要改变，则需要使用关键字mutable，但注意，此刻改变的并非外部变量本体的值，而是拷贝进lamdba的值
//              引用传递[&amp;]：使用引用方式传递外部变量进入lamdba表达式，将lamdba表达式内使用的外部变量与本体外部变量进行绑定
//              自推导方式[=]


//    （形参）：lamdba表达式函数形参
//      &#123;函数体&#125;：lamdba表达式具体实现
//    （实参）：传递给形参的值，&#123;&#125;中使用return语句时必须使用该实参
    

    QPushButton* btn1=new QPushButton(&quot;btn1&quot;,this);
    QPushButton* btn2=new QPushButton(&quot;btn2&quot;,this);
    QPushButton* btn3=new QPushButton(&quot;btn3&quot;,this);
    resize(600,400);
    btn2-&gt;move(300,200);
    btn3-&gt;move(200,300);
    int m=10;
    qDebug()&lt;&lt;&quot;start:&quot;&lt;&lt;m&lt;&lt;endl;
    connect(btn1,&amp;QPushButton::clicked,this,[=]()&#123;qDebug()&lt;&lt;m+10&lt;&lt;endl;&#125;);
    connect(btn2,&amp;QPushButton::clicked,this,[m]()mutable&#123;qDebug()&lt;&lt;m+20&lt;&lt;endl;&#125;);
    qDebug()&lt;&lt;&quot;next:&quot;&lt;&lt;m&lt;&lt;endl;

#if 1
    int ret;
    ret=[m]()mutable-&gt;int&#123;return m+30;&#125;();//值传递
    qDebug()&lt;&lt;&quot;ret_m:&quot;&lt;&lt;ret&lt;&lt;endl;//40
    ret=[=]()-&gt;int&#123;return m+40;&#125;();//自推导传递
    qDebug()&lt;&lt;&quot;ret_=:&quot;&lt;&lt;ret&lt;&lt;endl;//50
    ret=[](int a,int b)-&gt;int&#123;return a+b;&#125;(2,3);
    qDebug()&lt;&lt;&quot;ret_a+b:&quot;&lt;&lt;ret&lt;&lt;endl;//5
    ret=[](int x)-&gt;int&#123;return [](int y)-&gt;int&#123;return y*2;&#125;(x)+6;&#125;(5);
    qDebug()&lt;&lt;ret&lt;&lt;endl;//16
#endif
</code></pre>
<pre><code class="c++">//利用lamdba表达式进行信号传递
    QPushButton* btn_lam=new QPushButton(&quot;关闭&quot;,this);
    btn_lam-&gt;move(100,0);
    connect(btn_lam,&amp;QPushButton::clicked,this,[=]()&#123;
     this-&gt;close();
//        emit st-&gt;treat(&quot;宫保鸡丁&quot;);
//        btn_lam-&gt;setText(&quot;aaa&quot;);//效果：点击“关闭”按钮后，按钮名称自动改为“aaa”
    &#125;);
</code></pre>
<h1 id="控件"><a href="#控件" class="headerlink" title="控件"></a>控件</h1><h2 id="菜单栏与工具栏"><a href="#菜单栏与工具栏" class="headerlink" title="菜单栏与工具栏"></a>菜单栏与工具栏</h2><pre><code class="c++">
    resize(600,400);

//    创建菜单栏QMenuBar,最多有一个
    QMenuBar* bar=menuBar();
//    将菜单栏放入窗口中
    setMenuBar(bar);
//    创建菜单QMenu
   QMenu* fileMenu=bar-&gt;addMenu(&quot;文件&quot;);
   QMenu* editMenu=bar-&gt;addMenu(&quot;编辑&quot;);
//   创建菜单项QAction
   fileMenu-&gt;addAction(&quot;新建&quot;);
   fileMenu-&gt;addSeparator();//添加菜单栏分割线
   fileMenu-&gt;addAction(&quot;打开&quot;);
   fileMenu-&gt;addSeparator();//添加菜单栏分割线
   fileMenu-&gt;addAction(&quot;保存&quot;);
   fileMenu-&gt;addSeparator();//添加菜单栏分割线
   fileMenu-&gt;addAction(&quot;关闭&quot;);
   fileMenu-&gt;addSeparator();

   //工具栏
     QToolBar* toolBar=new QToolBar(this);
//设置默认停靠区域
     addToolBar(Qt::LeftToolBarArea,toolBar);

// 后期设置，左右停靠
    toolBar-&gt;setAllowedAreas(Qt::LeftToolBarArea|Qt::RightToolBarArea);

// 设置浮动
    toolBar-&gt;setFloatable(false);//工具栏将不能浮动在窗口表面

//设置移动
     toolBar-&gt;setMovable(false);//工具栏将不能移动，此时上边左右停靠的设置将失效

// 工具栏中添加内容
// 创建内容
     QAction* helpAction=fileMenu-&gt;addAction(&quot;帮助&quot;);//将菜单栏中的内容添加到工具栏中
     QAction* processAction=fileMenu-&gt;addAction(&quot;项目&quot;);
// 将内容添加到工具栏中
     toolBar-&gt;addAction(helpAction);
     toolBar-&gt;addSeparator();//添加工具栏分割线
     toolBar-&gt;addAction(processAction);
     toolBar-&gt;addSeparator();//添加工具栏分割线

//在工具栏中添加控件
     QPushButton* btn=new QPushButton(&quot;设置&quot;,this);
     toolBar-&gt;addWidget(btn);



tips：
    //菜单栏，最多有一个
    QMenuBar* bar=menuBar();
    setMenuBar(bar);
    //添加菜单内容
    QMenu* fileMenu=bar-&gt;addMenu(&quot;文件&quot;);
    //添加菜单项
    QAction* newAction=fileMenu-&gt;addAction(&quot;创建&quot;);
    //添加分割线
    fileMenu-&gt;addSeparator(newAction);


    //工具栏，可以有多个
    QToolBar* toolBar=new QToolBar(&quot;this&quot;);
    //设置默认停靠区域
    addToolBar(Qt::leftBarArea,toolBar);
    //后期设置停靠区域，浮动，移动，在工具栏中添加内容，控件
</code></pre>
<h2 id="模态与非模态对话框"><a href="#模态与非模态对话框" class="headerlink" title="模态与非模态对话框"></a>模态与非模态对话框</h2><pre><code class="c++">  //点击创建按钮，弹出一个对话框
    connect(ui-&gt;actionNew,&amp;QAction::triggered,[=]()&#123;
        //模态对话框，不可以对其他窗口进行操作
#if 0
        QDialog dlg(this);
        dlg.resize(200,100);
        dlg.exec();
#endif
#if 0
        //非模态对话框,可以对其他窗口进行操作
        QDialog* dlg2=new QDialog(this);
        dlg2-&gt;resize(200,100);
        dlg2-&gt;show();
#endif
</code></pre>
<h2 id="消息对话框"><a href="#消息对话框" class="headerlink" title="消息对话框"></a>消息对话框</h2><pre><code class="c++">//点击创建按钮，弹出一个对话框
    connect(ui-&gt;actionNew,&amp;QAction::triggered,[=]()&#123;
        //错误对话框,参数：父类，标题，提示内容
//        QMessageBox::critical(this,&quot;critical&quot;,&quot;错误&quot;);
        //信息对话框
//        QMessageBox::information(this,&quot;info&quot;,&quot;信息&quot;);
        //提问对话框,参数：1.父类，2.标题，3.提示内容,4.按键类型，5.默认关联回车键
//        QMessageBox::question(this,&quot;ques&quot;,&quot;提问&quot;,QMessageBox::Save|QMessageBox::Cancel,QMessageBox::Cancel);

//        if(QMessageBox::Save==QMessageBox::question(this,&quot;ques&quot;,&quot;提问&quot;,QMessageBox::Save|QMessageBox::Cancel,QMessageBox::Cancel))
//        &#123;
//            qDebug()&lt;&lt;&quot;选择的按键类型是保存&quot;&lt;&lt;endl;
//        &#125;
//        else
//        &#123;
//            qDebug()&lt;&lt;&quot;选择的按键类型是选择取消&quot;&lt;&lt;endl;
//        &#125;

        //警告对话框
        QMessageBox::warning(this,&quot;warning&quot;,&quot;警告&quot;);
</code></pre>
<h2 id="其他对话框"><a href="#其他对话框" class="headerlink" title="其他对话框"></a>其他对话框</h2><pre><code class="c++">connect(ui-&gt;actionnew,&amp;QAction::triggered,[=]()&#123;
        //颜色对话框
//        QColor color=QColorDialog::getColor(QColor(0,255,0));
//        qDebug()&lt;&lt;&quot;red:&quot;&lt;&lt;color.red()&lt;&lt;&quot;green:&quot;&lt;&lt;color.green()&lt;&lt;&quot;blue:&quot;&lt;&lt;color.blue()&lt;&lt;endl;

        //文件对话框，参数：父亲，标题，默认文件路径，筛选的文件类型
        //返回值为文件路径
//        QString str=QFileDialog::getOpenFileName(this,&quot;openFile&quot;,&quot;C:\Users\Administrator\Desktop&quot;,&quot;*.md&quot;);
//        qDebug()&lt;&lt;str&lt;&lt;endl;

        //字体对话框
        bool flag;
        QFont font=QFontDialog::getFont(&amp;flag,QFont(&quot;仿宋&quot;,36));
        qDebug()&lt;&lt;&quot;字体 &quot;&lt;&lt;font.family()&lt;&lt;&quot;大小 &quot;&lt;&lt;font.pointSize()&lt;&lt;&quot;是否加粗 &quot;&lt;&lt;font.bold()&lt;&lt;&quot;是否倾斜 &quot;&lt;&lt;font.italic()&lt;&lt;endl;
    &#125;);
</code></pre>
<h2 id="UI布局设计"><a href="#UI布局设计" class="headerlink" title="UI布局设计"></a>UI布局设计</h2><p><a href="https://imgtu.com/i/LX5eAI"><img src="https://s1.ax1x.com/2022/04/28/LX5eAI.png" alt="LX5eAI.png"></a></p>
<pre><code class="c++">tips：
    1.选取widget进行布局，水平、垂直、栅格
    2.默认窗口与控件之间存在9的间隙，可以调整layoutLeftMargin
    3.利用弹簧进行布局
</code></pre>
<h2 id="按钮控件组"><a href="#按钮控件组" class="headerlink" title="按钮控件组"></a>按钮控件组</h2><p><a href="https://imgtu.com/i/LX5mNt"><img src="https://s1.ax1x.com/2022/04/28/LX5mNt.png" alt="LX5mNt.png"></a></p>
<pre><code class="c++"> //默认性别单选框为男
    ui-&gt;rBtnMan-&gt;setChecked(true);

    //点击”女“，打印信息
    connect(ui-&gt;rBtnWoman,&amp;QRadioButton::clicked,[=]()&#123;
//        qDebug()&lt;&lt;QString::fromUtf8(&quot;女&quot;)&lt;&lt;endl;
         qDebug()&lt;&lt;&quot;女&quot;&lt;&lt;endl;
    &#125;);

    //点击”操作系统“显示状态码,0表示未选中，2表示选中
    connect(ui-&gt;boxOS,&amp;QCheckBox::stateChanged,[=](int state)&#123;
        qDebug()&lt;&lt;state&lt;&lt;endl;
    &#125;);




tips:
    按钮组：
        QPushButton：常用按钮
        QToolButton：工具按钮，用于显示图片，如想显示文字，修改风格toolButtonStyle，凸起风格：autoRaise
        QRadioButton：单选按钮，ui-&gt;rBtnMan-&gt;setChecked(true);//设置默认选项
        QCheckBox:多选按钮，监听状态QStateChecked,0未选中，1半选，2选中
</code></pre>
<h2 id="QListWidget列表容器"><a href="#QListWidget列表容器" class="headerlink" title="QListWidget列表容器"></a>QListWidget列表容器</h2><pre><code class="c++">//利用listwidget写诗
    QListWidgetItem* item=new QListWidgetItem(&quot;锄禾日当午&quot;);
    ui-&gt;listWidget-&gt;addItem(item);//添加一句话
    item-&gt;setTextAlignment(Qt::AlignHCenter);//设置对其方式为居中

    QStringList list;
    list&lt;&lt;&quot;我见青山多妩媚&quot;&lt;&lt;&quot;料青山见我应如是！&quot;;
    ui-&gt;listWidget-&gt;addItems(list);//添加多句话
    ui-&gt;listWidget-&gt;addItems(QStringList()&lt;&lt;&quot;自其变者而观之，则天地曾不能以一瞬&quot;&lt;&lt;&quot;自其不变者而观之，则物与我皆无尽也&quot;);
</code></pre>
<p><a href="https://imgtu.com/i/LX5n4P"><img src="https://s1.ax1x.com/2022/04/28/LX5n4P.png" alt="LX5n4P.png"></a></p>
<h2 id="treeWidget树控件"><a href="#treeWidget树控件" class="headerlink" title="treeWidget树控件"></a>treeWidget树控件</h2><pre><code class="c++">//设置水平头
    ui-&gt;treeWidget-&gt;setHeaderLabels(QStringList()&lt;&lt;&quot;英雄&quot;&lt;&lt;&quot;英雄自我介绍&quot;);

    //设置顶层节点
    QTreeWidgetItem* strongItem=new QTreeWidgetItem(QStringList()&lt;&lt;&quot;力量&quot;);
    QTreeWidgetItem* speedItem=new QTreeWidgetItem(QStringList()&lt;&lt;&quot;速度&quot;);
    QTreeWidgetItem* inteligenceItem=new QTreeWidgetItem(QStringList()&lt;&lt;&quot;智力&quot;);
    //加载顶层节点
    ui-&gt;treeWidget-&gt;addTopLevelItem(strongItem);
    ui-&gt;treeWidget-&gt;addTopLevelItem(speedItem);
    ui-&gt;treeWidget-&gt;addTopLevelItem(inteligenceItem);

    //追加子节点
    QTreeWidgetItem* l1=new QTreeWidgetItem(QStringList()&lt;&lt;&quot;刚被猪&quot;&lt;&lt;&quot;前排坦克。。。。。。&quot;);
    strongItem-&gt;addChild(l1); 
</code></pre>
<p><a href="https://imgtu.com/i/LX5VHA"><img src="https://s1.ax1x.com/2022/04/28/LX5VHA.png" alt="LX5VHA.png"></a></p>
<h2 id="tableWidget表格控件"><a href="#tableWidget表格控件" class="headerlink" title="tableWidget表格控件"></a>tableWidget表格控件</h2><pre><code class="c++">//设置列数
    ui-&gt;tableWidget-&gt;setColumnCount(3);
    //设置水平表头
    ui-&gt;tableWidget-&gt;setHorizontalHeaderLabels(QStringList()&lt;&lt;&quot;姓名&quot;&lt;&lt;&quot;性别&quot;&lt;&lt;&quot;年龄&quot;);
    //设置行数
    ui-&gt;tableWidget-&gt;setRowCount(5);

    //设置正文
    QStringList nameList;
    nameList&lt;&lt;&quot;张三&quot;&lt;&lt;&quot;李四&quot;&lt;&lt;&quot;王五&quot;&lt;&lt;&quot;赵六&quot;&lt;&lt;&quot;丁丁&quot;;
    QList&lt;QString&gt; sexList;
    sexList&lt;&lt;&quot;男&quot;&lt;&lt;&quot;男&quot;&lt;&lt;&quot;女&quot;&lt;&lt;&quot;女&quot;&lt;&lt;&quot;男&quot;;
    for(int i=0;i&lt;5;i++)
    &#123;
        int col=0;
       ui-&gt;tableWidget-&gt;setItem(i,col++,new QTableWidgetItem(nameList[i]));
       ui-&gt;tableWidget-&gt;setItem(i,col++,new QTableWidgetItem(sexList.at(i)));
        //将数字转换为QString
        ui-&gt;tableWidget-&gt;setItem(i,col++,new QTableWidgetItem(QString::number(i+18)));
    &#125;
</code></pre>
<p><a href="https://imgtu.com/i/LX5EBd"><img src="https://s1.ax1x.com/2022/04/28/LX5EBd.png" alt="LX5EBd.png"></a></p>
<h2 id="其他控件介绍"><a href="#其他控件介绍" class="headerlink" title="其他控件介绍"></a>其他控件介绍</h2><pre><code class="c++">//栈控件使用
    //设置默认定位
    ui-&gt;stackedWidget-&gt;setCurrentIndex(2);
    //点击按钮跳转到TabWidget控件页面
    connect(ui-&gt;btnTab,&amp;QPushButton::clicked,[=]()&#123;
       ui-&gt;stackedWidget-&gt;setCurrentIndex(0);
    &#125;);
    //点击按钮跳转到scrollArea控件页面
    connect(ui-&gt;btnScroll,&amp;QPushButton::clicked,[=]()&#123;
        ui-&gt;stackedWidget-&gt;setCurrentIndex(1);
    &#125;);
    //点击按钮跳转到toolBox控件页面
    connect(ui-&gt;btnTool,&amp;QPushButton::clicked,[=]()&#123;
        ui-&gt;stackedWidget-&gt;setCurrentIndex(2);
    &#125;);

    //下拉框
    ui-&gt;comboBox-&gt;addItems(QStringList()&lt;&lt;&quot;宝马&quot;&lt;&lt;&quot;奔驰&quot;&lt;&lt;&quot;拖拉机&quot;);
    //选中“拖拉机”按钮跳转到下拉框拖拉机
    connect(ui-&gt;btnSelect,&amp;QPushButton::clicked,[=]()&#123;
//        ui-&gt;comboBox-&gt;setCurrentIndex(2);
        ui-&gt;comboBox-&gt;setCurrentText(&quot;拖拉机&quot;);
    &#125;);


tips:
    栈控件：ui-&gt;stackedWidget-&gt;setCurrentIndex();
    下拉框：ui-&gt;comboBox-&gt;addItems(QStringList()&lt;&lt;&quot;奔驰&quot;&lt;&lt;&quot;宝马&quot;);
    Qlabel显示图片：ui-&gt;labelImage-&gt;setPixmap(&quot;图片路径&quot;);
    Qlabel显示动图：ui-&gt;LabenImage-&gt;setMoive();
</code></pre>
<h2 id="自定义控件"><a href="#自定义控件" class="headerlink" title="自定义控件"></a>自定义控件</h2><pre><code class="c++">class smallWidget : public QWidget
&#123;
    Q_OBJECT

public:
    explicit smallWidget(QWidget *parent = nullptr);
    ~smallWidget();

    //设置数值
    void setNum(int num);
    //获取数值
    int getNum();

private:
    Ui::smallWidget *ui;
&#125;;
</code></pre>
<pre><code class="c++">smallWidget::smallWidget(QWidget *parent) :
    QWidget(parent),
    ui(new Ui::smallWidget)
&#123;
    ui-&gt;setupUi(this);

    //QSpinBox翻滚，QSlider跟着移动
    void(QSpinBox::* spSingal)(int)=&amp;QSpinBox::valueChanged;
    connect(ui-&gt;spinBox,spSingal,ui-&gt;horizontalSlider,&amp;QSlider::setValue);

    //QSlider移动，QSpinBox跟着翻滚
    connect(ui-&gt;horizontalSlider,&amp;QSlider::valueChanged,ui-&gt;spinBox,&amp;QSpinBox::setValue);
&#125;

void smallWidget::setNum(int num)
&#123;
    ui-&gt;spinBox-&gt;setValue(num);
&#125;
int smallWidget::getNum()
&#123;
    return ui-&gt;spinBox-&gt;value();
&#125;


smallWidget::~smallWidget()
&#123;
    delete ui;
&#125;
</code></pre>
<p><a href="https://imgtu.com/i/LX5K9f"><img src="https://s1.ax1x.com/2022/04/28/LX5K9f.png" alt="LX5K9f.png"></a></p>
<pre><code class="c++">myWidget::myWidget(QWidget *parent)
    : QWidget(parent)
    , ui(new Ui::myWidget)
&#123;
    ui-&gt;setupUi(this);

    //点击按钮获取数值
    connect(ui-&gt;btnGetNum,&amp;QPushButton::clicked,[=]()
    &#123;
       qDebug()&lt;&lt;ui-&gt;widget-&gt;getNum();
    &#125;);

    //点击按钮设置进度条到一半数值
    connect(ui-&gt;btnSetNum,&amp;QPushButton::clicked,[=]()&#123;
        ui-&gt;widget-&gt;setNum(50);
    &#125;);

&#125;
</code></pre>
<p><a href="https://imgtu.com/i/LX5M38"><img src="https://s1.ax1x.com/2022/04/28/LX5M38.png" alt="LX5M38.png"></a></p>
<h1 id="事件"><a href="#事件" class="headerlink" title="事件"></a>事件</h1><h2 id="鼠标事件"><a href="#鼠标事件" class="headerlink" title="鼠标事件"></a>鼠标事件</h2><pre><code class="c++">#include &quot;mylabel.h&quot;
#include&lt;QDebug&gt;
#pragma execution_character_set(&quot;utf-8&quot;)
#include&lt;QMouseEvent&gt;
myLabel::myLabel(QWidget *parent) : QLabel(parent)
&#123;
    //设置鼠标跟踪状态
    setMouseTracking(true);//此时只要是鼠标事件其状态都会被监听
&#125;
void myLabel::enterEvent(QEvent *event)
&#123;
//    qDebug()&lt;&lt;&quot;鼠标进入了!&quot;&lt;&lt;endl;
&#125;
void myLabel::leaveEvent(QEvent *)
&#123;
//    qDebug()&lt;&lt;&quot;鼠标离开了!&quot;&lt;&lt;endl;
&#125;
void myLabel::mousePressEvent(QMouseEvent *ev)
&#123;
    //鼠标左键按下，则显示信息
    if(ev-&gt;button()==Qt::LeftButton)
    &#123;
        //返回鼠标在指定区域内的坐标值以及在整个窗口内的坐标值
        //QString::arg()//用字符串变量参数依次替代字符串中最小数值
        QString str=QString(&quot;鼠标按下了  x=%1  y=%2 gloalX=%3  globalY=%4&quot;).arg(ev-&gt;x()).arg(ev-&gt;y()).arg(ev-&gt;globalX()).arg(ev-&gt;globalY());
        qDebug()&lt;&lt;str;
    &#125;
&#125;

void myLabel::mouseReleaseEvent(QMouseEvent *ev)
&#123;
//    if(ev-&gt;button()==Qt::LeftButton)
    &#123;
        QString str=QString(&quot;鼠标释放了！x=%1 y=%2 globalX=%3 globalY=%4&quot;).arg(ev-&gt;x()).arg(ev-&gt;y()).arg(ev-&gt;globalX()).arg(ev-&gt;globalY());
        qDebug()&lt;&lt;str;
    &#125;
&#125;

void myLabel::mouseMoveEvent(QMouseEvent *ev)
&#123;
//    if(ev-&gt;buttons()&amp;Qt::LeftButton)//组合键
    &#123;
        QString str=QString(&quot;鼠标移动了！x=%1 y=%2 globalX=%3 globalY=%4&quot;).arg(ev-&gt;x()).arg(ev-&gt;y()).arg(ev-&gt;globalX()).arg(ev-&gt;globalY());
        qDebug()&lt;&lt;str;
    &#125;
&#125;



tips:
    在UI中添加一个label ,选中label右键-&gt;提升 -&gt;输入类名myLabel.
    这就完成了对label控件的鼠标事件的重写.
</code></pre>
<p><a href="https://imgtu.com/i/LX5QgS"><img src="https://s1.ax1x.com/2022/04/28/LX5QgS.png" alt="LX5QgS.png"></a></p>
<h2 id="定时器"><a href="#定时器" class="headerlink" title="定时器"></a>定时器</h2><pre><code class="c++">class myWidget : public QWidget
&#123;
    Q_OBJECT

public:
    myWidget(QWidget *parent = nullptr);
    ~myWidget();

    int id1;
    int id2;
    //重写定时器事件
    void timerEvent(QTimerEvent* ev );

private:
    Ui::myWidget *ui;

    Teacher* zt;
    Student* st;
    void classIsOver();
&#125;;
</code></pre>
<pre><code class="c++">myWidget::myWidget(QWidget *parent)
    : QWidget(parent)
    , ui(new Ui::myWidget)
&#123;
    ui-&gt;setupUi(this);

    //启动定时器,startTimer(定时器间隔时长),返回定时器的唯一ID号
    id1=startTimer(1000);
    id2=startTimer(2000);


    //定时器的第二种启动方式
   QTimer* timer=new QTimer(this);
   timer-&gt;start(500);
   connect(timer,&amp;QTimer::timeout,[=]()&#123;
       static int num=1;
       ui-&gt;label_3-&gt;setText(QString::number(num++));
   &#125;);

   //点击按钮停止定时器
   connect(ui-&gt;btn,&amp;QPushButton::clicked,[=]()&#123;
       timer-&gt;stop();
   &#125;);
&#125;

//重写定时器事件
void myWidget::timerEvent(QTimerEvent* ev)//当定时器启动后，系统会自动调用该定时器事件函数
&#123;
    if(ev-&gt;timerId()==id1)
    &#123;
        static int num=1;
        //定时器启动后自动调用该函数，若启动的定时器ID号为1号，则在label上每隔1s自动加1并显示，QString::number将数字转换为Qstring类型数据
        ui-&gt;label-&gt;setText(QString::number(num++));
    &#125;
    if(ev-&gt;timerId()==id2)
    &#123;
        static int num=1;
        ui-&gt;label_2-&gt;setText(QString::number(num++));
    &#125;
&#125;
</code></pre>
<h2 id="事件分发器"><a href="#事件分发器" class="headerlink" title="事件分发器"></a>事件分发器</h2><h2 id="事件过滤器"><a href="#事件过滤器" class="headerlink" title="事件过滤器"></a>事件过滤器</h2><h2 id="绘图事件"><a href="#绘图事件" class="headerlink" title="绘图事件"></a>绘图事件</h2><pre><code class="c++">void myWidget::paintEvent(QPaintEvent*)
&#123;
   QPainter*painter=new QPainter(this);//实例化画家对象

   //设置画笔
   QPen pen(QColor(255,0,0));
   //设置画笔宽度
   pen.setWidth(2);
   //设置画笔风格
   pen.setStyle(Qt::DotLine);

   //让画家使用画笔
    painter-&gt;setPen(pen);

    //设置画刷
    QBrush brush(Qt::cyan);
    //设置画刷风格
    brush.setStyle(Qt::Dense7Pattern);
    
    //让画家使用画刷
   painter-&gt;setBrush(brush);

   //画线
    painter-&gt;drawLine(QPoint(0,0),QPoint(100,100));
    //画椭圆
    painter-&gt;drawEllipse(QPoint(100,100),50,50);
    //画矩形
    painter-&gt;drawRect(QRect(150,150,100,50));
    //画文字
    painter-&gt;drawText(QRect(300,400,300,300),&quot;好好学习，天天向上&quot;);
&#125;
</code></pre>
<h2 id="QFile文件读写操作"><a href="#QFile文件读写操作" class="headerlink" title="QFile文件读写操作"></a>QFile文件读写操作</h2><p><a href="https://imgtu.com/i/LX5ljg"><img src="https://s1.ax1x.com/2022/04/28/LX5ljg.png" alt="LX5ljg.png"></a></p>
<pre><code class="c++">//点击按钮，打开文件
    connect(ui-&gt;btn,&amp;QPushButton::clicked,this,[=]()&#123;
        //获取文件路径
       QString path=QFileDialog::getOpenFileName(this,&quot;打开文件&quot;,&quot;C:\Users\Administrator\Desktop&quot;,&quot;*md&quot;);
       //将文件路径放入到ui窗口内的lineEdit控件中
       ui-&gt;lineEdit-&gt;setText(path);
       
       //在textEdit控件中显示内容
       QFile file(path);
       //打开文件，设置打开方式
       file.open(QIODevice::ReadOnly);
       //读取文件内容
       QByteArray array=file.readAll();
       ui-&gt;textEdit-&gt;setText(array);//将内容显示到textEdit控件中    
       file.close();
        
        
       //写文件
       file.open(QIODevice::Append);//以追加方式写入文件
       file.write(&quot;hello world&quot;);
       file.close();
    &#125;);
</code></pre>
<pre><code class="c++">//点击按钮，打开文件
    connect(ui-&gt;btn,&amp;QPushButton::clicked,this,[=]()&#123;
        //获取文件路径
       QString path=QFileDialog::getOpenFileName(this,&quot;打开文件&quot;,&quot;C:\Users\Administrator\Desktop&quot;,&quot;*md&quot;);
       //将文件路径放入到ui窗口内的lineEdit控件中
       ui-&gt;lineEdit-&gt;setText(path);

       //编码格式类
       QTextCodec* codec=QTextCodec::codecForName(&quot;gbk&quot;);//文件编码默认为utf-8，人为设定文件打开编码格式为gbk
       //在textEdit控件中显示内容
       QFile file(path);
       //打开文件，设置打开方式
       file.open(QIODevice::ReadOnly);
       //读取文件内容
       QByteArray array=file.readAll();
       //将内容显示到textEdit控件中
       ui-&gt;textEdit-&gt;setText(codec-&gt;toUnicode(array));
    &#125;);
</code></pre>
<h2 id="QFileIno文件信息读取"><a href="#QFileIno文件信息读取" class="headerlink" title="QFileIno文件信息读取"></a>QFileIno文件信息读取</h2><pre><code class="c++"> //文件信息类
       QFileInfo info(文件路径);
       qDebug()&lt;&lt;&quot;大小：&quot;&lt;&lt;info.size()&lt;&lt;&quot;   文件后缀名称：&quot;&lt;&lt;info.suffix()&lt;&lt;&quot;   文件名称：&quot;&lt;&lt;info.fileName();
       qDebug()&lt;&lt;&quot;创建日期：&quot;&lt;&lt;info.created().toString(&quot;yyyy/MM/dd hh::mm::ss&quot;)&lt;&lt;endl;
       qDebug()&lt;&lt;&quot;最后修改日期&quot;&lt;&lt;info.lastModified().toString(&quot;yyyy/MM/dd hh::mm::ss&quot;)&lt;&lt;endl;
</code></pre>
]]></content>
      <categories>
        <category>C++</category>
      </categories>
      <tags>
        <tag>c++</tag>
        <tag>Qt</tag>
      </tags>
  </entry>
  <entry>
    <title>git工具</title>
    <url>/2022/05/01/git%E5%B7%A5%E5%85%B7/</url>
    <content><![CDATA[<!-- more -->

<h1 id="github提交"><a href="#github提交" class="headerlink" title="github提交"></a>github提交</h1><ul>
<li>github创建项目</li>
<li>复制SSH链接</li>
<li>git clone “链接”（克隆项目到本地）</li>
<li>将项目文件复制到克隆的文件夹中</li>
<li>使用git进入项目文件夹</li>
<li>git add . (将项目文件添加到仓库)</li>
<li>git commit -m “提交提示信息”（提交项目文件到仓库）</li>
<li>git push -u origin main</li>
</ul>
<p>注：git可以一次提交多个项目文件，因此可以在本地添加多个项目文件后一次性提交</p>
<h1 id="github使用技巧"><a href="#github使用技巧" class="headerlink" title="github使用技巧"></a>github使用技巧</h1><ul>
<li>在线查看项目<br>进入项目详情页按下”.”键，GitHub会自行使用在线vscode查看项目文件代码而不需要下载到本地</li>
<li>在线运行项目<br>在项目详情页地址栏”https:&#x2F;&#x2F;“后加入”gitpod.io&#x2F;“即可进入在线vscode运行查看的项目</li>
</ul>
]]></content>
      <categories>
        <category>git</category>
      </categories>
      <tags>
        <tag>git</tag>
      </tags>
  </entry>
  <entry>
    <title>Linux基础命令</title>
    <url>/2022/04/20/Linux%E5%9F%BA%E7%A1%80%E5%91%BD%E4%BB%A4/</url>
    <content><![CDATA[<!-- more -->

<h1 id="linux命令"><a href="#linux命令" class="headerlink" title="linux命令"></a>linux命令</h1><h2 id="常用终端命令"><a href="#常用终端命令" class="headerlink" title="常用终端命令"></a>常用终端命令</h2><table>
<thead>
<tr>
<th>命令</th>
<th>英文</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td>ls</td>
<td>list</td>
<td>查看当前文件夹内容</td>
</tr>
<tr>
<td>pwd</td>
<td>print work directory</td>
<td>查看当前所在文件夹</td>
</tr>
<tr>
<td>cd</td>
<td>change directory</td>
<td>进入文件夹</td>
</tr>
<tr>
<td>cd ..</td>
<td>.代表当前目录     ..代表上一级目录</td>
<td>返回上一级文件夹</td>
</tr>
<tr>
<td>cd ~</td>
<td></td>
<td>默认返回根目录</td>
</tr>
<tr>
<td>mkdir</td>
<td>make director</td>
<td>创建文件夹</td>
</tr>
<tr>
<td>rmdir</td>
<td>remove director</td>
<td>删除文件夹</td>
</tr>
<tr>
<td>touch</td>
<td>touch</td>
<td>创建文件</td>
</tr>
<tr>
<td>rm</td>
<td>remove</td>
<td>删除文件</td>
</tr>
<tr>
<td>clear</td>
<td></td>
<td>清屏</td>
</tr>
<tr>
<td>cp</td>
<td>copy</td>
<td>拷贝文件</td>
</tr>
<tr>
<td>mv</td>
<td>move</td>
<td>移动文件</td>
</tr>
<tr>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td></td>
<td></td>
<td></td>
</tr>
</tbody></table>
<p>tips:</p>
<ul>
<li>ctrl+shift+&#x3D;	放大终端窗口字体显示</li>
<li>ctrl+-   缩小终端窗口字体显示</li>
</ul>
<h2 id="终端命令格式"><a href="#终端命令格式" class="headerlink" title="终端命令格式"></a>终端命令格式</h2><p>command [-options] [parameter]</p>
<p>如：rm -r 111	删除名为111的文件夹</p>
<h2 id="查阅终端命令帮助文档"><a href="#查阅终端命令帮助文档" class="headerlink" title="查阅终端命令帮助文档"></a>查阅终端命令帮助文档</h2><p>command –help</p>
<p>man command	命令使用手册</p>
<h1 id="文件和目录命令"><a href="#文件和目录命令" class="headerlink" title="文件和目录命令"></a>文件和目录命令</h1><h2 id="ls命令说明"><a href="#ls命令说明" class="headerlink" title="ls命令说明"></a>ls命令说明</h2><p>功能：显示当前目录文件，对应于DOS下的dir命令</p>
<p>linux文件特点：<br>以.开头的文件为隐藏文件，需要用-a参数才能显示，如下：</p>
<p><img src="https://s2.loli.net/2022/04/20/VGk9boiSsXlYvKn.png" alt="image-20220322153918108.png"></p>
<h2 id="ls常用选项"><a href="#ls常用选项" class="headerlink" title="ls常用选项"></a><strong>ls</strong>常用选项</h2><table>
<thead>
<tr>
<th>参数</th>
<th>含义</th>
</tr>
</thead>
<tbody><tr>
<td>-a</td>
<td>显示指定目录下所有子目录及其文件，包括隐藏文件</td>
</tr>
<tr>
<td>-l</td>
<td>以列表方式显示文件详细信息</td>
</tr>
<tr>
<td>-h</td>
<td>与-l配合以人性化的方式显示文件大小</td>
</tr>
</tbody></table>
<p>如下：</p>
<p><img src="https://s2.loli.net/2022/04/20/dGz3uwjYTQftVcg.png" alt="image-20220322154755553.png"></p>
<p><img src="https://s2.loli.net/2022/04/20/ODikzv7Rh41eyQP.png" alt="image-20220322154904581.png"></p>
<p><img src="https://s2.loli.net/2022/04/20/NcoAjmasRJPWGC2.png" alt="image-20220322154951860.png"></p>
<h2 id="ls通配符"><a href="#ls通配符" class="headerlink" title="ls通配符"></a>ls通配符</h2><table>
<thead>
<tr>
<th>通配符</th>
<th>含义</th>
</tr>
</thead>
<tbody><tr>
<td>*</td>
<td>匹配任意个数的字符</td>
</tr>
<tr>
<td>？</td>
<td>匹配某一个字符，但不能代表0个字符</td>
</tr>
<tr>
<td>[abc]</td>
<td>匹配abc中任意一个字符</td>
</tr>
<tr>
<td>[a-f]</td>
<td>匹配a-f字母中任意一个字符</td>
</tr>
</tbody></table>
<h2 id="cd切换目录"><a href="#cd切换目录" class="headerlink" title="cd切换目录"></a>cd切换目录</h2><table>
<thead>
<tr>
<th>命令</th>
<th>含义</th>
</tr>
</thead>
<tbody><tr>
<td>cd</td>
<td>切换到目录</td>
</tr>
<tr>
<td>cd ~</td>
<td>切换到当前用户的家目录</td>
</tr>
<tr>
<td>cd ..</td>
<td>退出当前目录到上一级目录</td>
</tr>
<tr>
<td>cd -</td>
<td>在最近两次访问的目录之间切换</td>
</tr>
</tbody></table>
<p><img src="https://s2.loli.net/2022/04/20/Tf2wJXoirjuPHk5.png" alt="image-20220322162200576.png"></p>
<h2 id="touch创建文件"><a href="#touch创建文件" class="headerlink" title="touch创建文件"></a>touch创建文件</h2><p>1.文件不存在时，创建新的空白文件</p>
<p>2.文件存在时，可修改文件最后一次修改时间</p>
<p><img src="https://s2.loli.net/2022/04/20/zWiETkc1HasMLF4.png" alt="image-20220322162957159.png"></p>
<h2 id="mkdir创建文件夹"><a href="#mkdir创建文件夹" class="headerlink" title="mkdir创建文件夹"></a>mkdir创建文件夹</h2><table>
<thead>
<tr>
<th>选项</th>
<th>含义</th>
</tr>
</thead>
<tbody><tr>
<td>-p</td>
<td>递归创建文件夹</td>
</tr>
</tbody></table>
<p><img src="https://s2.loli.net/2022/04/20/IbB5ErRfu4gj7Uh.png" alt="image-20220322163650769.png"></p>
<h2 id="rm-删除文件或文件夹"><a href="#rm-删除文件或文件夹" class="headerlink" title="rm 删除文件或文件夹"></a>rm 删除文件或文件夹</h2><table>
<thead>
<tr>
<th>选项</th>
<th>含义</th>
</tr>
</thead>
<tbody><tr>
<td>-f</td>
<td>强制删除</td>
</tr>
<tr>
<td>-r</td>
<td>递归删除，删除文件夹时必须使用该参数</td>
</tr>
</tbody></table>
<h1 id="文件拷贝与移动命令"><a href="#文件拷贝与移动命令" class="headerlink" title="文件拷贝与移动命令"></a>文件拷贝与移动命令</h1><h2 id="tree显示文件树形结构"><a href="#tree显示文件树形结构" class="headerlink" title="tree显示文件树形结构"></a>tree显示文件树形结构</h2><p><strong>功能：</strong>以树形结构显示当前目录下所有文件</p>
<table>
<thead>
<tr>
<th>选项</th>
<th>含义</th>
</tr>
</thead>
<tbody><tr>
<td>-d</td>
<td>只显示文件夹</td>
</tr>
</tbody></table>
<p><img src="https://s2.loli.net/2022/04/20/wLlgDJXsBpqGdKY.png" alt="image-20220322171941510.png"></p>
<p><img src="https://s2.loli.net/2022/04/20/7JSpxHuhqIzLCsr.png" alt="image-20220322172013314.png"></p>
<p><img src="https://s2.loli.net/2022/04/20/R2hu6SprVnBo3E7.png" alt="image-20220322172031362.png"></p>
<h2 id="cp复制文件"><a href="#cp复制文件" class="headerlink" title="cp复制文件"></a>cp复制文件</h2><p><strong>命令：</strong>cp 源文件 目标文件</p>
<p><strong>作用：</strong>将给出的源文件复制到目标文件或目录中</p>
<p><img src="https://s2.loli.net/2022/04/20/K2zBNUXMV8Sm6ef.png" alt="image-20220322173405369.png"></p>
<table>
<thead>
<tr>
<th>选项</th>
<th>含义</th>
</tr>
</thead>
<tbody><tr>
<td>-i</td>
<td>覆盖文件前提示</td>
</tr>
<tr>
<td>-r</td>
<td>复制文件夹</td>
</tr>
</tbody></table>
<p><img src="https://s2.loli.net/2022/04/20/GxyneHsBhwOdTXv.png" alt="image-20220322174537680.png"></p>
<p><img src="https://s2.loli.net/2022/04/20/54Ke8Vo61l2sEHn.png" alt="image-20220322174617008.png"><img src="https://s2.loli.net/2022/04/20/zhmbqneT4RGgoEu.png" alt="image-20220322174656349.png"></p>
<h2 id="mv移动文件-x2F-目录或重命名文件-x2F-目录"><a href="#mv移动文件-x2F-目录或重命名文件-x2F-目录" class="headerlink" title="mv移动文件&#x2F;目录或重命名文件&#x2F;目录"></a>mv移动文件&#x2F;目录或重命名文件&#x2F;目录</h2><p><strong>命令：</strong>mv 源文件 目标文件</p>
<p>1.<strong>移动文件夹</strong></p>
<p><img src="https://s2.loli.net/2022/04/20/Vy8vRcjHEKLTdDq.png" alt="image-20220322175123337.png"><img src="https://s2.loli.net/2022/04/20/8trNh7OsJyGvXf9.png" alt="image-20220322175305836.png"></p>
<p>2.<strong>移动文件</strong></p>
<p><img src="https://s2.loli.net/2022/04/20/A1EFvnCkOWwhfs6.png" alt="image-20220322175437927.png"></p>
<table>
<thead>
<tr>
<th>选项</th>
<th>含义</th>
</tr>
</thead>
<tbody><tr>
<td>-i</td>
<td>覆盖文件前提示</td>
</tr>
</tbody></table>
<h1 id="文件内容命令"><a href="#文件内容命令" class="headerlink" title="文件内容命令"></a>文件内容命令</h1><h2 id="cat查看文件内容"><a href="#cat查看文件内容" class="headerlink" title="cat查看文件内容"></a>cat查看文件内容</h2><p><strong>功能：</strong>查看文件内容、创建文件、合并文件、追加文件内容等，适合查看较少内容的文件</p>
<table>
<thead>
<tr>
<th>选项</th>
<th>含义</th>
</tr>
</thead>
<tbody><tr>
<td>-b</td>
<td>对非空输出行编号</td>
</tr>
<tr>
<td>-n</td>
<td>对所有输出行编号</td>
</tr>
<tr>
<td></td>
<td></td>
</tr>
</tbody></table>
<p><img src="https://s2.loli.net/2022/04/20/789MKrcO1XNYqlG.png" alt="image-20220323163405091.png"><img src="https://s2.loli.net/2022/04/20/vFX7Cs1oHQJZ6q2.png" alt="image-20220323163428189.png"><img src="https://s2.loli.net/2022/04/20/zlnjg7EtdsifOGX.png" alt="image-20220323163538070.png"><img src="https://s2.loli.net/2022/04/20/DfN4LeWAk53RXFt.png" alt="image-20220323163453550.png"></p>
<h2 id="more查看文件内容"><a href="#more查看文件内容" class="headerlink" title="more查看文件内容"></a>more查看文件内容</h2><p><strong>功能：</strong>分屏显示文件内容。适合查看较多内容的文件</p>
<table>
<thead>
<tr>
<th>操作键</th>
<th>功能</th>
</tr>
</thead>
<tbody><tr>
<td>空格键</td>
<td>显示手册的下一屏</td>
</tr>
<tr>
<td>Enter键</td>
<td>显示下一行</td>
</tr>
<tr>
<td>b</td>
<td>回滚一屏</td>
</tr>
<tr>
<td>f</td>
<td>前滚一屏</td>
</tr>
<tr>
<td>q</td>
<td>退出</td>
</tr>
<tr>
<td>&#x2F;word</td>
<td>搜索word字符串</td>
</tr>
</tbody></table>
<h2 id="grep文本搜索工具"><a href="#grep文本搜索工具" class="headerlink" title="grep文本搜索工具"></a>grep文本搜索工具</h2><p><strong>功能：</strong>查找匹配的文本内容</p>
<table>
<thead>
<tr>
<th>选项</th>
<th>含义</th>
</tr>
</thead>
<tbody><tr>
<td>-n</td>
<td>显示匹配的行与行号</td>
</tr>
<tr>
<td>-b</td>
<td>显示不包含匹配文本的所有行</td>
</tr>
<tr>
<td>-i</td>
<td>不区分大小写</td>
</tr>
</tbody></table>
<p>常用的两种模式查找：</p>
<table>
<thead>
<tr>
<th>参数</th>
<th>含义</th>
</tr>
</thead>
<tbody><tr>
<td>^a</td>
<td>查找以字符a为开头的行</td>
</tr>
<tr>
<td>a$</td>
<td>查找以字符a为结尾的行</td>
</tr>
</tbody></table>
<p><img src="https://s2.loli.net/2022/04/20/AWNorzebOamkpgQ.png" alt="image-20220323165923263.png"><img src="https://s2.loli.net/2022/04/20/a5cJmnhGICvRP9i.png" alt="image-20220323165954584.png"><img src="https://s2.loli.net/2022/04/20/S4GDhUknqBQ1LZj.png" alt="image-20220323170011732.png"></p>
<h1 id="其他命令"><a href="#其他命令" class="headerlink" title="其他命令"></a>其他命令</h1><h2 id="echo终端打印与-gt-、-gt-gt-重定向"><a href="#echo终端打印与-gt-、-gt-gt-重定向" class="headerlink" title="echo终端打印与&gt;、&gt;&gt;重定向"></a>echo终端打印与&gt;、&gt;&gt;重定向</h2><p><strong>echo</strong> hello相当于cout&lt;&lt;”hello”，即将hello字符串在终端打印出来</p>
<p><strong>重定向</strong>：将文本内容写入到文件当中</p>
<table>
<thead>
<tr>
<th></th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td>&gt;</td>
<td>覆盖源文件所有内容</td>
</tr>
<tr>
<td>&gt;&gt;</td>
<td>在源文件中追加内容</td>
</tr>
</tbody></table>
<p><img src="https://s2.loli.net/2022/04/20/uAmnaQB2LlsvrYD.png" alt="image-20220323171443541.png"></p>
<h2 id="cat写文件"><a href="#cat写文件" class="headerlink" title="cat写文件"></a>cat写文件</h2><p>cat &gt;&gt; 文件名称<br>回车写入文件内容<br>Ctrl+D结束写入</p>
<h2 id="管道命令"><a href="#管道命令" class="headerlink" title="管道命令 |"></a>管道命令 |</h2><p>Linux允许通过管道命令|将一个命令的输出作为另一个命令的输入</p>
<p>如：<img src="https://s2.loli.net/2022/04/20/m41fTBpnXtYE5iU.png" alt="image-20220323172709900.png"></p>
<p><img src="https://s2.loli.net/2022/04/20/RnJ2AUuMzDbwjrQ.png" alt="image-20220323172821104.png"></p>
<p><img src="https://s2.loli.net/2022/04/20/qpRTnjfzD6degEx.png" alt="image-20220323173026914.png"></p>
<h1 id="远程管理命令"><a href="#远程管理命令" class="headerlink" title="远程管理命令"></a>远程管理命令</h1><h2 id="关机-重启"><a href="#关机-重启" class="headerlink" title="关机\重启"></a>关机\重启</h2><pre><code class="c++">shutdown //默认1分钟后关机
shutdown 22:22 //在今天的22：22关机
shutdown +10 //10分钟后关机
shutdown now //现在立刻关机
shutdown -c //取消关机命令
shutdown -r now//现在重启操作系统
</code></pre>
<h2 id="查看-x2F-配置网卡信息"><a href="#查看-x2F-配置网卡信息" class="headerlink" title="查看&#x2F;配置网卡信息"></a>查看&#x2F;配置网卡信息</h2><ul>
<li><strong>ifconfig</strong>查看网卡配置信息（<strong>DOS</strong>下为<strong>ipconfig</strong>）</li>
<li>ifconfig | grep inet 查看网卡对应的IP地址</li>
<li>注意：一台电脑可能有一个物理网卡和多个虚拟网卡，Linux中物理网卡以ensXX表示</li>
<li>127.0.0.1为本地环回地址，一般用来测试本地网卡是否正常</li>
</ul>
<p><img src="https://s2.loli.net/2022/04/20/Pw2AiMJfaL4981F.png" alt="image-20220323180126895.png"></p>
<ul>
<li><p><strong>ping</strong> IP地址，测试目标IP地址的主机是否连接通常</p>
</li>
<li><p>ping 127.0.0.1，测试本机是否连接通畅</p>
</li>
<li><p>终止终端程序执行：<strong>CTRL+C</strong></p>
<p><img src="https://s2.loli.net/2022/04/20/Z2HLjadMqm4QIyW.png" alt="image-20220323180615700.png"></p>
</li>
</ul>
<h2 id="ssh基础"><a href="#ssh基础" class="headerlink" title="ssh基础"></a>ssh基础</h2><p><img src="https://s2.loli.net/2022/04/20/AQblGB2y8XtuFv6.png" alt="image-20220324092715654.png"></p>
<ul>
<li><strong>ssh客户端</strong>是一种使用<strong>secure shell协议</strong>连接到远程计算机的软件程序</li>
<li>ssh是目前可靠，<strong>专为远程登录会话和其他网络服务</strong>提供安全性的协议<ul>
<li>通过ssh协议可以有效<strong>防止远程原理过程中的信息泄露</strong></li>
<li>通过ssh协议可以对所有传输的数据进行加密，也能够<strong>防止DNS欺骗和IP欺骗</strong></li>
</ul>
</li>
<li>ssh协议传输的<strong>数据是经过压缩</strong>的，可以<strong>加快传输速度</strong></li>
</ul>
<h3 id="6-3-1-域名与端口号"><a href="#6-3-1-域名与端口号" class="headerlink" title="6.3.1 域名与端口号"></a>6.3.1 域名与端口号</h3><p><strong>域名：</strong>IP地址的别名，如：<a href="http://www.baidu.com/">www.baidu.com</a></p>
<p><img src="https://s2.loli.net/2022/04/20/GzmYxeXNCEU4qr6.png" alt="image-20220324093342913.png"></p>
<p><strong>端口号</strong></p>
<ul>
<li>通过<strong>IP</strong>地址找到<strong>计算机</strong></li>
<li>通过<strong>端口号</strong>找到<strong>计算机上运行的应用程序</strong><ul>
<li>SSH服务器的默认端口号是22，如果是默认端口号，在连接时可以省略</li>
</ul>
</li>
<li>常见服务端口号列表</li>
</ul>
<table>
<thead>
<tr>
<th>序号</th>
<th>服务</th>
<th>端口号</th>
</tr>
</thead>
<tbody><tr>
<td>01</td>
<td>SSH服务器</td>
<td>22</td>
</tr>
<tr>
<td>02</td>
<td>Web服务器</td>
<td>80</td>
</tr>
<tr>
<td>03</td>
<td>HTTPS</td>
<td>443</td>
</tr>
<tr>
<td>04</td>
<td>FTP服务器</td>
<td>21</td>
</tr>
</tbody></table>
<h3 id="6-3-2-SSH客户端的简单使用"><a href="#6-3-2-SSH客户端的简单使用" class="headerlink" title="6.3.2  SSH客户端的简单使用"></a>6.3.2  SSH客户端的简单使用</h3><p><strong>ssh -p sort user@IP&#x2F;域名</strong></p>
<p><strong>sort：</strong>端口号，默认为SSH服务器端口号22</p>
<p><strong>user：</strong>远程机器上的用户名</p>
<p>（1）查询本地虚拟机Linux的IP地址</p>
<p><img src="https://s2.loli.net/2022/04/20/9k5aqwW6xpB8AcH.png" alt="image-20220324101003815.png"></p>
<p>（2）通过DOS窗口连接本地虚拟机，并通过DOS窗口远程对虚拟机进行操作</p>
<p><img src="https://s2.loli.net/2022/04/20/URST34h2vdnNICu.png" alt="image-20220324101712161.png"></p>
<pre><code class="c++">//注：SSH命令只能在Linux系统和UNIX系统下使用c++
//在Windows系统下使用需要安装PUTTY或者Xshell软件
</code></pre>
<h2 id="scp远程拷贝命令"><a href="#scp远程拷贝命令" class="headerlink" title="scp远程拷贝命令"></a>scp远程拷贝命令</h2><ul>
<li><strong>scp-scure copy</strong>,在Linux下的远程拷贝命令</li>
<li>其地址格式与ssh命令相同，但选项**-P为大写字母**</li>
</ul>
<pre><code class="c++">scp -P port 01.py user@IP:Desktop/01.py   //将本地01.py文件拷贝到远程桌面上
scp -P port user@IP:Desktop/01.py 01.py   //将远程文件拷贝到本地桌面上
加上-r选项可以拷贝文件夹
</code></pre>
<pre><code>将虚拟机Desktop文件夹里的123.txt文件拷贝到本地Desktop文件夹下
</code></pre>
<p><img src="https://s2.loli.net/2022/04/20/iKaZdk5y2rftJVT.png" alt="image-20220324151546457.png"></p>
<pre><code>将本地桌面文件夹的123.txt文件拷贝到虚拟机上的桌面文件夹下
</code></pre>
<p><img src="https://s2.loli.net/2022/04/20/Cf79iL1B3tbwVJZ.png" alt="image-20220324151653275.png"></p>
<pre><code>将虚拟机桌面文件夹下的aaa文件夹拷贝到本地桌面文件夹下
</code></pre>
<p><img src="https://s2.loli.net/2022/04/20/k8uaWJGl7VMKqj6.png" alt="image-20220324151829083.png"></p>
<pre><code>将本地桌面文件夹下的aaa文件拷贝到虚拟机的桌面文件夹下
</code></pre>
<p><img src="https://s2.loli.net/2022/04/20/TUIEm9K8SwRux7N.png" alt="image-20220324152035116.png"></p>
<h1 id="SSH高级"><a href="#SSH高级" class="headerlink" title="SSH高级"></a>SSH高级</h1><h1 id="用户权限"><a href="#用户权限" class="headerlink" title="用户权限"></a>用户权限</h1><h2 id="基本概念"><a href="#基本概念" class="headerlink" title="基本概念"></a>基本概念</h2><ul>
<li>对<strong>文件&#x2F;目录</strong>的权限</li>
</ul>
<table>
<thead>
<tr>
<th>权限</th>
<th>英文</th>
<th>缩写</th>
<th>代号</th>
</tr>
</thead>
<tbody><tr>
<td>读</td>
<td>read</td>
<td>r</td>
<td>4</td>
</tr>
<tr>
<td>写</td>
<td>write</td>
<td>w</td>
<td>2</td>
</tr>
<tr>
<td>执行</td>
<td>excute</td>
<td>x</td>
<td>1</td>
</tr>
</tbody></table>
<ul>
<li>组：由几个不同的用户组成，实际应用中，可以对组设置权限，从而避免对每一个用户依次设置权限</li>
</ul>
<h2 id="ls-l详细信息"><a href="#ls-l详细信息" class="headerlink" title="ls -l详细信息"></a>ls -l详细信息</h2><ul>
<li>权限：第一个字符以d为开头表示目录</li>
<li>硬连接数：表示有几种方式可以访问当前目录&#x2F;文件</li>
<li>拥有者：</li>
<li>组：</li>
<li>大小：</li>
<li>时间：</li>
<li>名称：</li>
<li><img src="https://s2.loli.net/2022/04/20/fRPD7c8MrAoBFVC.png" alt="image-20220324160218211.png"></li>
</ul>
<p><img src="https://s2.loli.net/2022/04/20/94hNoiPbIHEUXWm.png" alt="image-20220324160402306.png"></p>
<h2 id="chmod修改权限命令"><a href="#chmod修改权限命令" class="headerlink" title="chmod修改权限命令"></a>chmod修改权限命令</h2><p><strong>作用：</strong>修改<strong>用户&#x2F;组</strong>对<strong>文件&#x2F;目录</strong>的权限</p>
<pre><code class="c++">chmod +r/w/x 文件名  //给文件增加权限
</code></pre>
<pre><code class="c++">chmod -r/w/x 文件名  //给文件取消权限
</code></pre>
<ul>
<li>给<strong>文件</strong>增加<strong>执行</strong>权限</li>
</ul>
<p><img src="https://s2.loli.net/2022/04/20/nyD2WzRxpHfbT7C.png" alt="image-20220324165539972.png"></p>
<ul>
<li>给<strong>文件</strong>取消<strong>执行、读取</strong>权限</li>
</ul>
<p><img src="https://s2.loli.net/2022/04/20/5Qr6RStHWh9Ppaw.png" alt="image-20220324165704094.png"><img src="https://s2.loli.net/2022/04/20/pKUvWwyqb1hTxZ6.png" alt="image-20220324165717075.png"></p>
<ul>
<li>给<strong>文件夹</strong>取消<strong>执行</strong>权限</li>
</ul>
<p><img src="https://s2.loli.net/2022/04/20/oXdABO3TQPMu7ac.png" alt="image-20220324170653231.png"></p>
<h2 id="超级用户介绍"><a href="#超级用户介绍" class="headerlink" title="超级用户介绍"></a>超级用户介绍</h2><ul>
<li><p>Linux系统中存在一个<strong>root</strong>用户用于<strong>对系统的维护和管理</strong>，对操作系统的资源<strong>具有所有访问权限</strong></p>
</li>
<li><p>Linux安装过程中，会自动创建一个用户账号，该账号称为<strong>标准用户</strong></p>
</li>
<li><p><strong>su</strong>为<strong>substitute user</strong>的缩写，表示用另一个用户的身份</p>
</li>
<li><p><strong>sudo</strong>命令意为用其他身份执行命令，预设身份为<strong>root</strong></p>
</li>
</ul>
<h1 id="组管理"><a href="#组管理" class="headerlink" title="组管理"></a>组管理</h1><p>注：<strong>创建组&#x2F;删除组</strong>都需要<strong>sudo</strong>执行</p>
<table>
<thead>
<tr>
<th>命令</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td>groupadd 组名</td>
<td>添加组</td>
</tr>
<tr>
<td>groupdel  组名</td>
<td>删除组</td>
</tr>
<tr>
<td>cat &#x2F;etc&#x2F;group</td>
<td>确认组信息</td>
</tr>
<tr>
<td>chgrp -R 组名 文件&#x2F;目录名</td>
<td>修改文件&#x2F;目录所在组</td>
</tr>
</tbody></table>
<ul>
<li><strong>组信息</strong>都保存在<strong>etc&#x2F;group</strong>中</li>
<li><strong>&#x2F;etc</strong>是专门用来<strong>保存系统配置文件信息</strong>的目录</li>
</ul>
<p><img src="https://s2.loli.net/2022/04/20/9csAJPGTLbxyp5w.png" alt="image-20220325160837125.png"><a href="https://imgtu.com/i/LrPniR"><img src="https://s1.ax1x.com/2022/04/20/LrPniR.png" alt="LrPniR.png"></a></p>
<p><strong>修改文件目录所在组实操：</strong></p>
<ul>
<li>创建pythonStudy文件目录</li>
</ul>
<p><a href="https://imgtu.com/i/LrPKRx"><img src="https://s1.ax1x.com/2022/04/20/LrPKRx.png" alt="LrPKRx.png"></a></p>
<ul>
<li>创建dev组</li>
</ul>
<p><a href="https://imgtu.com/i/LrPeo9"><img src="https://s1.ax1x.com/2022/04/20/LrPeo9.png" alt="LrPeo9.png"></a></p>
<ul>
<li>将pythonStudy目录所在组修改为dev</li>
</ul>
<p><a href="https://imgtu.com/i/LrPZdJ"><img src="https://s1.ax1x.com/2022/04/20/LrPZdJ.png" alt="LrPZdJ.png"></a></p>
<h1 id="用户管理"><a href="#用户管理" class="headerlink" title="用户管理"></a>用户管理</h1><h2 id="终端命令"><a href="#终端命令" class="headerlink" title="终端命令"></a>终端命令</h2><p><strong>注：创建&#x2F;删除用户以及修改用户密码</strong>的终端命令都需要<strong>sudo</strong>执行</p>
<ul>
<li><p>创建用户&#x2F;设置密码&#x2F;删除用户</p>
<table>
<thead>
<tr>
<th>命令</th>
<th>作用</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>useradd -m -g 组 新建用户名</td>
<td>添加新用户</td>
<td><strong>-m</strong>自动建立用户家目录<br /><strong>-g</strong>指定用户所在组，否则会建立一个同名的组</td>
</tr>
<tr>
<td>passwd 用户名</td>
<td>设置用户密码</td>
<td>如果是普通用户，直接用passwd可以直接修改用户密码</td>
</tr>
<tr>
<td>userdel -r 用户名</td>
<td>删除用户</td>
<td><strong>-r</strong>选项会自动删除用户家目录</td>
</tr>
<tr>
<td>cat&#x2F;etc&#x2F;passwd|grep 用户名</td>
<td>确认用户信息</td>
<td>新建用户后，用户信息会保存在etc&#x2F;passwd文件中</td>
</tr>
</tbody></table>
</li>
</ul>
<p><strong>测试</strong></p>
<ul>
<li>创建新用户myuser</li>
</ul>
<p><a href="https://imgtu.com/i/LrP1sO"><img src="https://s1.ax1x.com/2022/04/20/LrP1sO.png" alt="LrP1sO.png"></a></p>
<ul>
<li>修改myuser密码（输入密码时在屏幕上不打印）</li>
</ul>
<p><a href="https://imgtu.com/i/LrPMz6"><img src="https://s1.ax1x.com/2022/04/20/LrPMz6.png" alt="LrPMz6.png"></a></p>
<ul>
<li>在Windows下的putty远程登录软件上登录myuser用户进行测试</li>
</ul>
<p><a href="https://imgtu.com/i/LrPlQK"><img src="https://s1.ax1x.com/2022/04/20/LrPlQK.png" alt="LrPlQK.png"></a></p>
<h2 id="查看用户信息"><a href="#查看用户信息" class="headerlink" title="查看用户信息"></a>查看用户信息</h2><h1 id="查看系统信息"><a href="#查看系统信息" class="headerlink" title="查看系统信息"></a>查看系统信息</h1><ul>
<li>时间日期<ul>
<li>date</li>
<li>cal</li>
</ul>
</li>
<li>磁盘和目录空间<ul>
<li>df</li>
<li>du</li>
</ul>
</li>
<li>进程信息<ul>
<li>ps</li>
<li>top</li>
<li>kill</li>
</ul>
</li>
</ul>
<h2 id="时间日期"><a href="#时间日期" class="headerlink" title="时间日期"></a>时间日期</h2><table>
<thead>
<tr>
<th>命令</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td>date</td>
<td>查看系统时间</td>
</tr>
<tr>
<td>cal</td>
<td>calendar查看日历，-y 选项可以查看一年的日历</td>
</tr>
</tbody></table>
<p><a href="https://imgtu.com/i/LrPqYR"><img src="https://s1.ax1x.com/2022/04/20/LrPqYR.png" alt="LrPqYR.png"></a></p>
<h2 id="磁盘信息"><a href="#磁盘信息" class="headerlink" title="磁盘信息"></a>磁盘信息</h2><table>
<thead>
<tr>
<th>命令</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td>df -h</td>
<td>disk free 显示磁盘剩余空间</td>
</tr>
<tr>
<td>du -h[目录名]</td>
<td>disk usage 显示磁盘目录下的文件大小</td>
</tr>
</tbody></table>
<ul>
<li>-h表示以人性化的方式显示文件大小</li>
</ul>
<p><a href="https://imgtu.com/i/LrPLf1"><img src="https://s1.ax1x.com/2022/04/20/LrPLf1.png" alt="LrPLf1.png"></a></p>
<h2 id="进程信息"><a href="#进程信息" class="headerlink" title="进程信息"></a>进程信息</h2><table>
<thead>
<tr>
<th>命令</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td>ps aux</td>
<td>process status 查看进程的详细状态</td>
</tr>
<tr>
<td>top</td>
<td>动态显示正在运行中的进程</td>
</tr>
<tr>
<td>kill [-9] 进程代号</td>
<td>终止指定代号的进程，-9表示强制终止</td>
</tr>
</tbody></table>
<ul>
<li><strong>ps选项</strong>说明功能</li>
</ul>
<table>
<thead>
<tr>
<th>选项</th>
<th>含义</th>
</tr>
</thead>
<tbody><tr>
<td>a</td>
<td>显示终端所有进程，包括没有控制终端的进程</td>
</tr>
<tr>
<td>u</td>
<td>显示进程的详细状态</td>
</tr>
<tr>
<td>x</td>
<td>显示没有控制终端的进程</td>
</tr>
</tbody></table>
<p>​	</p>
<h1 id="其他命令-1"><a href="#其他命令-1" class="headerlink" title="其他命令"></a>其他命令</h1><ul>
<li>查找文件<ul>
<li>find</li>
</ul>
</li>
<li>软链接<ul>
<li>ln</li>
</ul>
</li>
<li>打包压缩<ul>
<li>tar</li>
</ul>
</li>
<li>软件安装<ul>
<li>apt-get</li>
</ul>
</li>
</ul>
<h2 id="查找文件"><a href="#查找文件" class="headerlink" title="查找文件"></a>查找文件</h2><table>
<thead>
<tr>
<th>命令</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td>find [路径] -name .py</td>
<td>查找指定路径下扩展名为.py的文件，包括子目录</td>
</tr>
</tbody></table>
<ul>
<li><p>省略路径名，表示在当前目录下查找</p>
</li>
<li><p>正则查找</p>
</li>
<li><p>搜索桌面文件下，包含“1”的文件</p>
</li>
</ul>
<p><a href="https://imgtu.com/i/LrPbk9"><img src="https://s1.ax1x.com/2022/04/20/LrPbk9.png" alt="LrPbk9.png"></a></p>
<ul>
<li>搜索桌面文件下，所有以“.txt”为扩展名的文件</li>
</ul>
<p><a href="https://imgtu.com/i/LrP7TJ"><img src="https://s1.ax1x.com/2022/04/20/LrP7TJ.png" alt="LrP7TJ.png"></a></p>
<ul>
<li>搜索桌面文件下，以数字“1”为开头的文件</li>
</ul>
<p><a href="https://imgtu.com/i/LrPTw4"><img src="https://s1.ax1x.com/2022/04/20/LrPTw4.png" alt="LrPTw4.png"></a></p>
<h2 id="软链接"><a href="#软链接" class="headerlink" title="软链接"></a>软链接</h2><p>​	</p>
<table>
<thead>
<tr>
<th>命令</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td>ln -s</td>
<td>建立该文件的软链接，类似与Windows下的快捷方式</td>
</tr>
</tbody></table>
<ul>
<li>若没有-s表示建立的是一个硬链接</li>
<li><strong>源文件</strong>要使用<strong>绝对路径</strong></li>
</ul>
<p><strong>演练</strong></p>
<ul>
<li>在桌面创建01.py文件与demo&#x2F;b&#x2F;c文件夹，并在01.py文件内写入内容</li>
</ul>
<p><a href="https://imgtu.com/i/LrPv6K"><img src="https://s1.ax1x.com/2022/04/20/LrPv6K.png" alt="LrPv6K.png"></a></p>
<ul>
<li>将01.py文件移动到demo&#x2F;b&#x2F;c文件夹下</li>
</ul>
<p><a href="https://imgtu.com/i/LrPxOO"><img src="https://s1.ax1x.com/2022/04/20/LrPxOO.png" alt="LrPxOO.png"></a></p>
<ul>
<li>在桌面分别创建01.py软链接的相对路径与绝对路径，并分别访问这两个软链接</li>
</ul>
<p><a href="https://imgtu.com/i/LriSmD"><img src="https://s1.ax1x.com/2022/04/20/LriSmD.png" alt="LriSmD.png"></a><a href="https://imgtu.com/i/Lrip0e"><img src="https://s1.ax1x.com/2022/04/20/Lrip0e.png" alt="Lrip0e.png"></a></p>
<ul>
<li>将01.py相对路径的软链接与硬链接移动到demo&#x2F;b&#x2F;c文件夹下，并分别访问这两个软链接，比较两者差异</li>
</ul>
<p><a href="https://imgtu.com/i/Lri9TH"><img src="https://s1.ax1x.com/2022/04/20/Lri9TH.png" alt="Lri9TH.png"></a></p>
<p><a href="https://imgtu.com/i/LriPkd"><img src="https://s1.ax1x.com/2022/04/20/LriPkd.png" alt="LriPkd.png"></a></p>
<h2 id="打包压缩"><a href="#打包压缩" class="headerlink" title="打包压缩"></a>打包压缩</h2><ul>
<li><p><strong>windows</strong> 下使用 <strong>rar</strong></p>
</li>
<li><p><strong>mac</strong> 下使用 <strong>zip</strong></p>
<ul>
<li><strong>linux</strong> 下使用 <strong>tar.gz</strong></li>
</ul>
</li>
</ul>
<h1 id="GDB调试"><a href="#GDB调试" class="headerlink" title="GDB调试"></a>GDB调试</h1><table>
<thead>
<tr>
<th>命令</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td>gdb 可执行程序</td>
<td>启动</td>
</tr>
<tr>
<td>quit（q）</td>
<td>退出</td>
</tr>
<tr>
<td>set args***        show args</td>
<td>给主函数设置参数</td>
</tr>
<tr>
<td>list&#x2F;l</td>
<td>显示当前文件内容</td>
</tr>
<tr>
<td>list&#x2F;l 行号</td>
<td>显示某一行前后内容</td>
</tr>
<tr>
<td>list&#x2F;l 函数名</td>
<td>显示某函数前后内容</td>
</tr>
<tr>
<td>list&#x2F;l 文件名:行号</td>
<td>显示其他文件内容</td>
</tr>
<tr>
<td>list&#x2F;l 文件名:函数名</td>
<td>显示其他文件内容</td>
</tr>
<tr>
<td>set listsize 行数</td>
<td>设置显示行数</td>
</tr>
<tr>
<td>b&#x2F;break 行号、函数名、文件名：行号、文件名：函数</td>
<td>设置断点</td>
</tr>
<tr>
<td>i&#x2F;info b&#x2F;break</td>
<td>查看断点</td>
</tr>
<tr>
<td>d&#x2F;del&#x2F;delete 断点编号</td>
<td>删除断点</td>
</tr>
<tr>
<td>dis&#x2F;disable 断点编号</td>
<td>设置断点无效</td>
</tr>
<tr>
<td>ena&#x2F;enable 断点编号</td>
<td>设置断点生效</td>
</tr>
<tr>
<td>b&#x2F;break 10 if i&#x3D;5</td>
<td>设置条件断点，一般再在循环体内设置</td>
</tr>
<tr>
<td>start</td>
<td>启动程序从第一行开始</td>
</tr>
<tr>
<td>run</td>
<td>运行程序直到断点</td>
</tr>
<tr>
<td>c&#x2F;continue</td>
<td>继续运行到下一个断点</td>
</tr>
<tr>
<td>n&#x2F;next</td>
<td>向下执行，不会进入函数体</td>
</tr>
<tr>
<td>p&#x2F;print</td>
<td>打印变量值</td>
</tr>
<tr>
<td>ptype</td>
<td>打印变量类型</td>
</tr>
<tr>
<td>s&#x2F;step</td>
<td>单步调试，可以进入函数体</td>
</tr>
<tr>
<td>finish</td>
<td>跳出函数体</td>
</tr>
<tr>
<td>until</td>
<td>跳出循环</td>
</tr>
<tr>
<td>display num</td>
<td>设置自动变量（自动打印变量值）5</td>
</tr>
<tr>
<td>i&#x2F;info display</td>
<td>显示自动变量信息</td>
</tr>
<tr>
<td>undisplay 编号</td>
<td>移除自动变量</td>
</tr>
</tbody></table>
<h1 id="open函数"><a href="#open函数" class="headerlink" title="open函数"></a>open函数</h1><pre><code class="c++">#include &lt;sys/types.h&gt;
#include &lt;sys/stat.h&gt;
#include &lt;fcntl.h&gt;
#include&lt;stdio.h&gt;
#include&lt;unistd.h&gt;
int main(int args,char*argc[])
&#123;
    //int open(const char* pathname, int flags);
    //文件打开模式参数flags
    //	必选参数：O_RDONLY, O_WRONLY, or O_RDWR
    //	可选参数：O_APPEND（追加）、O_CREAT（创建新文件）
    
    int fd = open(&quot;a.txt&quot;, O_WRONLY);
    if (fd = -1)//fd=-1表示会打开失败
        perror(&quot;open&quot;);//打印错误信息
    close(fd);//关闭文件描述符

    
//    int open(const char *pathname, int flags, mode_t mode);

    int fd = open(&quot;create.txt&quot;, O_WRONLY | O_CREAT, 0777);
    if (fd = -1)
        perror(&quot;open&quot;);
    close(fd);	

    return 1;
&#125;
</code></pre>
<h1 id="read-x2F-write函数"><a href="#read-x2F-write函数" class="headerlink" title="read&#x2F;write函数"></a>read&#x2F;write函数</h1><pre><code class="c++">//#include &lt;unistd.h&gt;
//ssize_t read(int fd, void* buf, size_t count);
// read() attempts to read up to count bytes 
// from file descriptor fd into the buffer starting at buf.
//参数：
//	-fd：open打开的文件描述符
//	-buf：将文件内容读取到buf中
//	-count：设置buf字节数
//返回值：
//	&gt;0 已经读取的字节数
//	-1 读取失败
//	0  读取结束

//#include &lt;unistd.h&gt;
//ssize_t write(int fd, const void* buf, size_t count);
//write() writes up to count bytes from the buffer 
//starting at buf to the file referred to by the file descriptor fd.

#include &lt;sys/types.h&gt;
#include &lt;sys/stat.h&gt;
#include &lt;fcntl.h&gt;
#include&lt;stdio.h&gt;
#include&lt;unistd.h&gt;
#include&lt;string.h&gt;
#include&lt;iostream&gt;
using namespace std;
int main(int args,char*argc[])
&#123;
    //打开文件create.txt
    int srcfd = open(&quot;create.txt&quot;,O_RDONLY);
    if (srcfd == -1)//如果文件不存在或文件打开失败则返回错误信息
        perror(&quot;open&quot;);

    char buf[1024];
    memset(&amp;buf, 0, sizeof(buf));//初始化buf
    int len = read(srcfd, buf, sizeof(buf));//将create.txt文件中的信息读取到buf中
    if (len &lt; 0)
        perror(&quot;read&quot;);//显示读取错误信息
    cout &lt;&lt; &quot;接受&quot;;
    cout &lt;&lt; len &lt;&lt; &quot;个字符&quot; &lt;&lt; endl;
    cout&lt;&lt;buf&lt;&lt;endl;
    //新建一个文件cp.txt
    int desfd = open(&quot;cp.txt&quot;, O_WRONLY | O_CREAT, 0777);
    if (desfd == -1)
        perror(&quot;open&quot;);//显示新建错误信息
    write(desfd, buf, sizeof(buf));//将buf中的信息写入cp.txt中
    close(srcfd);
    close(desfd);
    return 1;
&#125;
</code></pre>
]]></content>
      <categories>
        <category>Linux</category>
      </categories>
      <tags>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode</title>
    <url>/2022/04/30/leetCode/</url>
    <content><![CDATA[<!-- more -->
<h1 id="双指针"><a href="#双指针" class="headerlink" title="双指针"></a>双指针</h1><h2 id="有序数组的两数之和"><a href="#有序数组的两数之和" class="headerlink" title="有序数组的两数之和"></a>有序数组的两数之和</h2><p><strong>题号：</strong>167</p>
<p><strong>链接：</strong><a href="https://leetcode-cn.com/problems/two-sum-ii-input-array-is-sorted/description/">167. 两数之和 II - 输入有序数组 - 力扣（LeetCode） (leetcode-cn.com)</a></p>
<p><strong>题目描述：</strong>给你一个有序数组，在这个数组中找出两个数字，使其和等于target</p>
<p><strong>题解：</strong>使用双指针从数组两端进行遍历，若sum&gt;target,则大数取小，即j–，反之则小数取大，即i++，如图</p>
<p><img src="https://cs-notes-1256109796.cos.ap-guangzhou.myqcloud.com/437cb54c-5970-4ba9-b2ef-2541f7d6c81e.gif" alt="img"></p>
<pre><code class="c++">class Solution &#123;
public:
    vector&lt;int&gt; twoSum(vector&lt;int&gt;&amp; numbers, int target) &#123;
        int start=0;
        int end=numbers.size()-1;
        int sum=0;
        // vector&lt;int&gt; ans;
        while(start&lt;end)
        &#123;
            sum=numbers[start]+numbers[end];
            if(sum==target)
            &#123;
                // ans.push_back(start+1);
                // ans.push_back(end+1);
                return &#123;start+1,end+1&#125;;
            &#125;
            else if(sum&lt;target)
            &#123;
                start++;
            &#125;
            else&#123;
                end--;
            &#125;
        &#125;
        return &#123;-1,-1&#125;;
    &#125;
&#125;;
</code></pre>
<h2 id="两数平方和"><a href="#两数平方和" class="headerlink" title="两数平方和"></a>两数平方和</h2><p><strong>题号：</strong>633</p>
<p><strong>链接：</strong><a href="https://leetcode-cn.com/problems/sum-of-square-numbers/description/">633. 平方数之和 - 力扣（LeetCode） (leetcode-cn.com)</a></p>
<p><strong>题目描述：</strong>给定一个非负整数 <code>c</code> ，你要判断是否存在两个整数 <code>a</code> 和 <code>b</code>，使得 <code>a^2 + b^2 = c</code> 。</p>
<p><strong>题解：</strong>双指针从两端向中间遍历，其中尾指针设置为sqrt(c)+1,判断平方和是否等于c，若不等，则移动双指针，考虑到整数溢出问题，数据类型采用long</p>
<pre><code class="c++">class Solution &#123;
public:
    bool judgeSquareSum(int c) &#123;
        long i=0;
        long j=sqrt(c)+1;
        // int j=c;
        if(c&lt;0) return false;
        while(i&lt;=j)
        &#123;
            long mul=i*i+j*j;
            if(mul==c)
            &#123;
                return true;
            &#125;
            else if(mul&lt;c)
                i++;
            else
                j--;
        &#125;
        return false;
    &#125;
&#125;;
</code></pre>
<h2 id="反转字符串元音字母"><a href="#反转字符串元音字母" class="headerlink" title="反转字符串元音字母"></a>反转字符串元音字母</h2><p><strong>题号：</strong>345</p>
<p><strong>链接：</strong><a href="https://leetcode-cn.com/problems/reverse-vowels-of-a-string/description/">345. 反转字符串中的元音字母 - 力扣（LeetCode） (leetcode-cn.com)</a></p>
<p><strong>题目描述：</strong>给你一个字符串 s ，仅反转字符串中的所有元音字母，并返回结果字符串。</p>
<p>元音字母包括 ‘a’、’e’、’i’、’o’、’u’，且可能以大小写两种形式出现。</p>
<p><strong>题解：</strong>双指针从两端遍历字符串，当查到元音字母时进行交换</p>
<pre><code class="c++">class Solution &#123;
public:
    string reverseVowels(string s) &#123;
        string voewl=&quot;aeiouAEIOU&quot;;
        int i=0,j=s.length()-1;
        char ch;
        while(i&lt;j)
        &#123;
            while(voewl.find(s[i])==-1 &amp;&amp; i&lt;j) i++;
            while(voewl.find(s[j])==-1 &amp;&amp; i&lt;j) j--;
            if(i&lt;j)
            &#123;
                ch=s[i];
                s[i]=s[j];
                s[j]=ch;
            &#125;
            i++;
            j--;
        &#125;
        return s;
    &#125;
&#125;;
</code></pre>
<h2 id="回文字符串"><a href="#回文字符串" class="headerlink" title="回文字符串"></a>回文字符串</h2><p><strong>题号：</strong>680</p>
<p><strong>链接：</strong><a href="https://leetcode-cn.com/problems/valid-palindrome-ii/description/">680. 验证回文字符串 Ⅱ - 力扣（LeetCode） (leetcode-cn.com)</a></p>
<p><strong>题目描述：</strong>给定一个非空字符串 <code>s</code>，<strong>最多</strong>删除一个字符。判断是否能成为回文字符串。</p>
<p><strong>题解：</strong>使用双指针从两端遍历字符串，当遇到字符不等时分别判定左指针+1时和右指针-1时剩下的字符串是否为回文串</p>
<pre><code class="c++">class Solution &#123;
public:
    bool isPalindrome(string s,int i,int j)//判断是否为回文字符串
    &#123;
        while(i&lt;j)
        &#123;
            if(s[i]==s[j])
            &#123;
                i++;
                j--;
            &#125;
            else
            &#123;
                return false;
            &#125;
        &#125;
        return true;
    &#125; 
    bool validPalindrome(string s) &#123;
        int i=0,j=s.length()-1;
        if(j&lt;0) return false;
        while(i&lt;j)
        &#123;
            if(s[i]!=s[j])//遇到不等的字符
                return isPalindrome(s,i+1,j) || isPalindrome(s,i,j-1);//判断(s,i+1,j)与(s,i,j-1)是否为回文串
            i++;
            j--;
        &#125;
        return true;
    &#125;
&#125;;
</code></pre>
<h2 id="归并两个有序数组"><a href="#归并两个有序数组" class="headerlink" title="归并两个有序数组"></a>归并两个有序数组</h2><p><strong>题号：</strong>88</p>
<p><strong>链接：</strong><a href="https://leetcode-cn.com/problems/merge-sorted-array/description/">88. 合并两个有序数组 - 力扣（LeetCode） (leetcode-cn.com)</a></p>
<p><strong>题目描述：</strong>有两个有序数组nums1（长度为m+n，元素数目为m）与nums2（长度为n，元素数目为n），现需将两个有序数组归并为一个有序数组，并将归并后的结果放入nums1中</p>
<p><strong>题解：</strong>由于题目要求将归并后的数组放入nums1中，且nums1长度为m+n，为避免归并时的元素覆盖数组nums1原值，因此需要从两个数组的末端开始比较</p>
<p>1.设置指针i指向nums1[m-1]</p>
<p>指针j指向nums2[n-1]</p>
<p>指针t指向nums1[m+n-1]</p>
<p>2.比较两指针i与j的元素值，将大值放入nums1[t]中</p>
<pre><code class="c++">class Solution &#123;
public:
    void merge(vector&lt;int&gt;&amp; nums1, int m, vector&lt;int&gt;&amp; nums2, int n) &#123;
        int i=m-1,j=n-1,t=m+n-1;
        while(i&gt;=0 &amp;&amp; j&gt;=0)
        &#123;
            if(nums1[i]&gt;nums2[j]) nums1[t--]=nums1[i--];
            else nums1[t--]=nums2[j--];
        &#125;
        if(i&gt;=0) return;
        while(j&gt;=0) nums1[t--]=nums2[j--];
    &#125;
&#125;;
</code></pre>
<h2 id="判断链表中是否存在环路"><a href="#判断链表中是否存在环路" class="headerlink" title="判断链表中是否存在环路"></a>判断链表中是否存在环路</h2><p><strong>题号：</strong>141</p>
<p><strong>链接：</strong><a href="https://leetcode-cn.com/problems/linked-list-cycle/description/">141. 环形链表 - 力扣（LeetCode） (leetcode-cn.com)</a></p>
<p><strong>题目描述：</strong>给你一个链表的头节点 head ，判断链表中是否有环。</p>
<p>如果链表中存在环 ，则返回 true 。 否则，返回 false 。</p>
<p><img src="https://assets.leetcode-cn.com/aliyun-lc-upload/uploads/2018/12/07/circularlinkedlist.png" alt="img"></p>
<p><strong>题解：</strong>使用快慢指针法，其中快指针一次移动2步，慢指针一次移动1步，若链表中存在环路，则两指针必定会相遇，否则两指针将永远不会相遇</p>
<pre><code class="c++">/**
 * Definition for singly-linked list.
 * struct ListNode &#123;
 *     int val;
 *     ListNode *next;
 *     ListNode(int x) : val(x), next(NULL) &#123;&#125;
 * &#125;;
 */
class Solution &#123;
public:
    bool hasCycle(ListNode *head) &#123;
        ListNode* f=head;
        ListNode* l=head;
        while(f!=NULL &amp;&amp; f-&gt;next!=NULL)//必须先判断f是否为空，再判断f-&gt;next是否为空
        &#123;
            f=f-&gt;next-&gt;next;
            l=l-&gt;next;
            if(l==f)
                return true;
        &#125;
        return false;
    &#125;
&#125;;
</code></pre>
<p><strong>题号：</strong>142</p>
<p><strong>链接：</strong><a href="https://leetcode-cn.com/problems/linked-list-cycle-ii/">142. 环形链表 II - 力扣（LeetCode） (leetcode-cn.com)</a></p>
<p><strong>题目描述：</strong>给你一个链表的头节点 head ，判断链表中是否有环，若有环，则返回环路入口节点。</p>
<p><strong>题解：</strong></p>
<pre><code class="http">&quot;&quot;&quot;
    原理：首先初始化快指针 fast = head.next.next 和 slow = head.next，
    此时快指针走的路长为2, m慢指针走的路长为1，之后令快指针每次走两步，
    慢指针每次走一步，这样快指针走的路长始终是慢指针走的路长的两倍，
    若不存在环，直接返回None，
    若存在环，则 fast 与 slow 肯定会在若干步之后相遇；
    
    性质1：
        设从head需要走 a 步到达环的入口，如果环存在的话，
        再走 b 步可以再次到达该入口（即环的长度为b），
        如果存在环的话，上述描述的 快指针 fast 和 
        慢指针slow 必然会相遇，且此时slow走的路长
        小于 a + b(可以自行证明)，设其为 a + x，
        当快慢指针相遇时，快指针已经至少走完一圈环了，
        不妨设相遇时走了完整的m圈(m &gt;= 1)，有：
        
        快指针走的路长为 a + mb + x
        慢指针走的路长为 a + x
        
        由于快指针fast 走的路长始终是慢指针的 2倍，所以：
        
        a + mb + x = 2(a + x)
        
        化简可得：
        
        a = mb - x  -------------  (*)
    
    当快指针与慢指针相遇时，由于 &lt;性质1&gt; 的存在，
    可以在链表的开头初始化一个新的指针，
    称其为 detection, 此时 detection 距离环的入口的距离为 a，
    
    此时令 detection 和 fast 每次走一步，
    会发现当各自走了 a 步之后，两个指针同时到达了环的入口，理由分别如下：
    
    detection不用说了，走了a步肯定到刚好到入口
    fast已经走过的距离为 a + mb + x，当再走 a 步之后，
    fast走过的总距离为 2a + mb + x，带入性质1的(*)式可得：
    2a + mb + x = a + 2mb，会发现，fast此时刚好走完了
    整整 2m 圈环，正好处于入口的位置。
    
    基于此，我们可以进行循环，直到 detection 和 
    fast 指向同一个对象，此时指向的对象恰好为环的入口。
        
    &quot;&quot;&quot;
</code></pre>
<pre><code class="c++">/**
 * Definition for singly-linked list.
 * struct ListNode &#123;
 *     int val;
 *     ListNode *next;
 *     ListNode(int x) : val(x), next(NULL) &#123;&#125;
 * &#125;;
 */
class Solution &#123;
public:
    ListNode *detectCycle(ListNode *head) &#123;
        ListNode* fast=head;
        ListNode* slow=head;//设置快慢指针
        int count=0;//记录指针相遇次数
        while(fast!=NULL &amp;&amp; fast-&gt;next!=NULL)
        &#123;
            //快指针走两步，慢指针走一步，如果链表中存在环路，则两指针一定会相遇
            fast=fast-&gt;next-&gt;next;
            slow=slow-&gt;next;
            if(fast==slow)//若指针相遇，则说明链表中存在环路
            &#123;
                count++;
                if(count==1)//两指针在第一次相遇时，在表头设置检测指针，此后两指针与检测指针同时移动一步，则当检测指针与fast指针相遇的位置就是链表环路入口位置
                &#123;
                    ListNode* dection=head;
                    while(dection!=fast)
                    &#123;
                        dection=dection-&gt;next;
                        fast=fast-&gt;next;
                    &#125;
                    return dection;             
                &#125;
            &#125;
        &#125;
        return NULL;
    &#125;
&#125;;
</code></pre>
<h2 id="最长子序列"><a href="#最长子序列" class="headerlink" title="最长子序列"></a>最长子序列</h2><p><strong>题号：</strong>524</p>
<p><strong>链接：</strong><a href="https://leetcode-cn.com/problems/longest-word-in-dictionary-through-deleting/">524. 通过删除字母匹配到字典里最长单词 - 力扣（LeetCode） (leetcode-cn.com)</a></p>
<p><strong>题目描述：</strong>给你一个元素为字符串的字典dictionary以及一个字符串s，字符串s可通过删除某些字符后与字典中的字符串元素匹配，试找出长度最长的子序列，若长度相等，则返回字母序最小的子序列</p>
<pre><code class="c++">示例 1：

输入：s = &quot;abpcplea&quot;, dictionary = [&quot;ale&quot;,&quot;apple&quot;,&quot;monkey&quot;,&quot;plea&quot;]
输出：&quot;apple&quot;
示例 2：

输入：s = &quot;abpcplea&quot;, dictionary = [&quot;a&quot;,&quot;b&quot;,&quot;c&quot;]
输出：&quot;a&quot;
</code></pre>
<p><strong>题解：</strong>第一步：将字典中的每一个字符串元素作为匹配模板，并将给出的字符串s视为待匹配字符串，比较两个字符串的每一个字符，若相等则两指针同时后移。若出现不等字符，则将s的指针后移继续与模板进行匹配。若模板指针指向模板字符串的尾部，则说明s中存在一个子序列与字典中的元素匹配，否则说明不存在这样的子序列</p>
<p>第二步：将每次得到的子序列与下一次循环遍历匹配的字符串元素进行比较，若该子序列长度较大或者该子序列的字母序较小，说明当前子序列暂时是最长子序列</p>
<pre><code class="c++">class Solution &#123;
public:
    bool isSubStr(string s,string target)
    &#123;
        //字典中的每一个元素视为匹配模板，字符串s为待匹配者
        //比较每一个字符，若s[i]==target[j]，则两指针同时后移匹配下一个字符
        //若s[i]!=target[j]，则待匹配字符串s指针i后移，模板指针j不动
        //若模板指针j到达尾部，则匹配成功，若待匹配指针i已经到达尾部，但模板指针尚未到达尾部，则字符串s与模板字符串无法匹配
        int j=0;
        for(int i=0;i&lt;s.length() &amp;&amp; j&lt;target.length();i++)
        &#123;
            if(s[i]==target[j])
                j++;
        &#125;
        return j==target.length();
    &#125;


    string findLongestWord(string s, vector&lt;string&gt;&amp; dictionary) &#123;
        string longWord=&quot;&quot;;
        for(string target:dictionary)//使用增强for循环遍历字典每一个元素
        &#123;
            int l1=longWord.length();
            int l2=target.length();
            //若当前得到的子序列长度大于当前字典中的正在遍历的元素长度，则说明当前得到的子序列已经局部最大，跳过本轮循环
            //若当前得到的子序列与target不等或者其串中字母序较小，也可以跳过本轮循环比较
            if(l1&gt;l2 || (l1==l2 &amp;&amp; longWord.compare(target)&lt;0))
                continue;
            if(isSubStr(s,target))
                longWord=target;
        &#125;
        return longWord;
    &#125;
&#125;;
</code></pre>
<h1 id="排序"><a href="#排序" class="headerlink" title="排序"></a>排序</h1><h2 id="数组中第k大元素"><a href="#数组中第k大元素" class="headerlink" title="数组中第k大元素"></a>数组中第k大元素</h2><p><strong>题号：</strong>215</p>
<p><strong>链接：</strong><a href="https://leetcode-cn.com/problems/kth-largest-element-in-an-array/">215. 数组中的第K个最大元素 - 力扣（LeetCode） (leetcode-cn.com)</a></p>
<p><strong>题目描述：</strong>给定整数数组 <code>nums</code> 和整数 <code>k</code>，请返回数组中第 <code>**k**</code> 个最大的元素。</p>
<p>请注意，你需要找的是数组排序后的第 <code>k</code> 个最大的元素，而不是第 <code>k</code> 个不同的元素。</p>
<pre><code class="c++">示例 1:

输入: [3,2,1,5,6,4] 和 k = 2
输出: 5
示例 2:

输入: [3,2,3,1,2,4,5,5,6] 和 k = 4
输出: 4
</code></pre>
<p><strong>题解：</strong>利用大根堆的构建及排序特点，每交换一次大根时就找到一次极大值，在交换第k次时进行返回即可</p>
<pre><code class="c++">class Solution &#123;
public:
  void swap(int&amp; a,int &amp;b)
  &#123;
     int temp=a;
     a=b;
     b=temp;
  &#125;

  void heapAdjust(vector&lt;int&gt;&amp; nums,int p,int n)//大根堆调整
  &#123;
     int temp=nums[p];
     for(int i=2*p+1;i&lt;n;i=i*2+1)
     &#123;
       if(i+1&lt;n &amp;&amp; nums[i+1]&gt;nums[i]) i++;
       if(temp&gt;nums[i]) break;
       nums[p]=nums[i];
       p=i;
     &#125;
     nums[p]=temp;
  &#125;

  int findKthLargest(vector&lt;int&gt;&amp; nums, int k) &#123;
     int n=nums.size(),j;
     for(int i=n/2-1;i&gt;=0;i--)
       heapAdjust(nums,i,n);//构建初始大根堆
     for(j=n-1;j&gt;=n-k;j--)//每次交换就找到一个极大值，故只需要交换k次即可跳出循环输出结果
     &#123;
       swap(nums[0],nums[j]);//交换
       heapAdjust(nums,0,j);//重新调整大根堆
     &#125;
     return nums[j+1];
  &#125;
&#125;;
</code></pre>
<h1 id="贪心算法"><a href="#贪心算法" class="headerlink" title="贪心算法"></a>贪心算法</h1><h2 id="分配饼干"><a href="#分配饼干" class="headerlink" title="分配饼干"></a>分配饼干</h2><p><strong>题号：</strong>455</p>
<p><strong>链接：</strong><a href="https://leetcode-cn.com/problems/assign-cookies/">455. 分发饼干 - 力扣（LeetCode） (leetcode-cn.com)</a></p>
<p><strong>题目描述：</strong>假设你是一位很棒的家长，想要给你的孩子们一些小饼干。但是，每个孩子最多只能给一块饼干。</p>
<p>对每个孩子 i，都有一个胃口值 g[i]，这是能让孩子们满足胃口的饼干的最小尺寸；并且每块饼干 j，都有一个尺寸 s[j] 。如果 s[j] &gt;&#x3D; g[i]，我们可以将这个饼干 j 分配给孩子 i ，这个孩子会得到满足。你的目标是尽可能满足越多数量的孩子，并输出这个最大数值。</p>
<pre><code class="c++">示例 1:

输入: g = [1,2,3], s = [1,1]
输出: 1
解释: 
你有三个孩子和两块小饼干，3个孩子的胃口值分别是：1,2,3。
虽然你有两块小饼干，由于他们的尺寸都是1，你只能让胃口值是1的孩子满足。
所以你应该输出1。
示例 2:

输入: g = [1,2], s = [1,2,3]
输出: 2
解释: 
你有两个孩子和三块小饼干，2个孩子的胃口值分别是1,2。
你拥有的饼干数量和尺寸都足以让所有孩子满足。
所以你应该输出2.
</code></pre>
<p><strong>题解：</strong>首先给孩子的胃口值g[i]与饼干大小s[j]进行升序排序，然后给每一个孩子进行试分配，如果满足s[j]&gt;&#x3D;g[i],则饼干分配完毕，可以得到满足的孩子也加一，否则就让下一个饼干再试分配给该孩子，直到饼干分配完毕或者所有孩子均已得到满足</p>
<pre><code class="c++">class Solution &#123;
public:
    int findContentChildren(vector&lt;int&gt;&amp; g, vector&lt;int&gt;&amp; s) &#123;
        sort(g.begin(),g.end());
        sort(s.begin(),s.end());
        int n=g.size();
        int m=s.size();
        int i=0,j=0;
      while(i&lt;n &amp;&amp; j&lt;m)
      &#123;
          if(s[j]&gt;=g[i])
          &#123;
              i++;
          &#125;
          j++;
      &#125;
      return i;
    &#125;
&#125;;
</code></pre>
<h2 id="无重叠区间"><a href="#无重叠区间" class="headerlink" title="无重叠区间"></a>无重叠区间</h2><p><strong>题号：</strong>435</p>
<p><strong>链接：</strong><a href="https://leetcode-cn.com/problems/non-overlapping-intervals/">435. 无重叠区间 - 力扣（LeetCode） (leetcode-cn.com)</a></p>
<p><strong>题目描述：</strong>给定一个区间的集合 intervals ，其中 intervals[i] &#x3D; [starti, endi] 。返回 需要移除区间的最小数量，使剩余区间互不重叠 。</p>
<pre><code class="c++">示例 1:

输入: intervals = [[1,2],[2,3],[3,4],[1,3]]
输出: 1
解释: 移除 [1,3] 后，剩下的区间没有重叠。
示例 2:

输入: intervals = [ [1,2], [1,2], [1,2] ]
输出: 2
解释: 你需要移除两个 [1,2] 来使剩下的区间没有重叠。
示例 3:

输入: intervals = [ [1,2], [2,3] ]
输出: 0
解释: 你不需要移除任何区间，因为它们已经是无重叠的了。
</code></pre>
<p><strong>题解</strong>：将区间按照左端进行排序，排序后遍历区间判别是否有重叠，但每次选取的end端需选取最小的作为保留区间</p>
<pre><code class="c++">class Solution &#123;
public:
  int eraseOverlapIntervals(vector&lt;vector&lt;int&gt;&gt;&amp; intervals) &#123;
     if(intervals.size()==0) return 0;
     // 将区间按照start进行排序
     sort(intervals.begin(),intervals.end(),[](vector&lt;int&gt; &amp;v1,vector&lt;int&gt; &amp;v2)&#123;return v1[0]&lt;v2[0];&#125;);
     int num=0;
     // 选取第一个区间边界，用于判别与其临近的区间是否有重叠
     int end=intervals[0][1];
     for(int i=1;i&lt;intervals.size();i++)
     &#123;
       // 当有区间发生重叠时，就将重叠的区间加1
       if(intervals[i][0]&lt;end)
       &#123;
         num++;
         // 选取end端小的边界作为不重复区间的边界
         if(intervals[i][1]&lt;end)
           end=intervals[i][1];
       &#125;
       else
         end=intervals[i][1];
     &#125;
     return num;
  &#125;
&#125;;
</code></pre>
<h2 id="投飞镖刺破气球"><a href="#投飞镖刺破气球" class="headerlink" title="投飞镖刺破气球"></a>投飞镖刺破气球</h2><p><strong>题号：</strong>452</p>
<p><strong>链接：</strong><a href="https://leetcode-cn.com/problems/minimum-number-of-arrows-to-burst-balloons/">452. 用最少数量的箭引爆气球 - 力扣（LeetCode） (leetcode-cn.com)</a></p>
<p><strong>题目描述：</strong>有一些球形气球贴在一堵用 XY 平面表示的墙面上。墙面上的气球记录在整数数组 points ，其中points[i] &#x3D; [xstart, xend] 表示水平直径在 xstart 和 xend之间的气球。你不知道气球的确切 y 坐标。</p>
<p>一支弓箭可以沿着 x 轴从不同点 完全垂直 地射出。在坐标 x 处射出一支箭，若有一个气球的直径的开始和结束坐标为 xstart，xend， 且满足  xstart ≤ x ≤ xend，则该气球会被 引爆 。可以射出的弓箭的数量 没有限制 。 弓箭一旦被射出之后，可以无限地前进。</p>
<p>给你一个数组 points ，返回引爆所有气球所必须射出的 最小 弓箭数 。</p>
<pre><code class="c++">示例 1：

输入：points = [[10,16],[2,8],[1,6],[7,12]]
输出：2
解释：气球可以用2支箭来爆破:
-在x = 6处射出箭，击破气球[2,8]和[1,6]。
-在x = 11处发射箭，击破气球[10,16]和[7,12]。
示例 2：

输入：points = [[1,2],[3,4],[5,6],[7,8]]
输出：4
解释：每个气球需要射出一支箭，总共需要4支箭。
示例 3：

输入：points = [[1,2],[2,3],[3,4],[4,5]]
输出：2
解释：气球可以用2支箭来爆破:

- 在x = 2处发射箭，击破气球[1,2]和[2,3]。
- 在x = 4处射出箭，击破气球[3,4]和[4,5]。
</code></pre>
<pre><code class="c++">class Solution &#123;
public:
  int findMinArrowShots(vector&lt;vector&lt;int&gt;&gt;&amp; points) &#123;
     int n=points.size();
     if(n==0) return 0;
     sort(points.begin(),points.end(),[](vector&lt;int&gt;&amp; v1,vector&lt;int&gt;&amp; v2)&#123;return v1[0]&lt;v2[0];&#125;);    
     int count=1;//若气球个数不为0，则至少需要一根箭
     for(int i=1;i&lt;n;i++)
     &#123;
       if(points[i][0]&gt;points[i-1][1])//区间没有重叠时，需要一支箭
       &#123;
         count++;
       &#125;
       else//当区间存在重叠时
       &#123;
         points[i][1]=min(points[i-1][1],points[i][1]);
       &#125;
     &#125;
     return count;
  &#125;
&#125;;
</code></pre>
<h2 id="买卖股票最佳时机"><a href="#买卖股票最佳时机" class="headerlink" title="买卖股票最佳时机"></a>买卖股票最佳时机</h2><p><strong>题号：</strong>121</p>
<p><strong>链接：</strong><a href="https://leetcode-cn.com/problems/best-time-to-buy-and-sell-stock/description/">121. 买卖股票的最佳时机 - 力扣（LeetCode） (leetcode-cn.com)</a></p>
<p><strong>题目描述：</strong>给定一个数组 prices ，它的第 i 个元素 prices[i] 表示一支给定股票第 i 天的价格。</p>
<p>你只能选择 某一天 买入这只股票，并选择在 未来的某一个不同的日子 卖出该股票。设计一个算法来计算你所能获取的最大利润。</p>
<p>返回你可以从这笔交易中获取的最大利润。如果你不能获取任何利润，返回 0 。</p>
<pre><code class="c++">示例 1：

输入：[7,1,5,3,6,4]
输出：5
解释：在第 2 天（股票价格 = 1）的时候买入，在第 5 天（股票价格 = 6）的时候卖出，最大利润 = 6-1 = 5 。
     注意利润不能是 7-1 = 6, 因为卖出价格需要大于买入价格；同时，你不能在买入前卖出股票。
示例 2：

输入：prices = [7,6,4,3,1]
输出：0
解释：在这种情况下, 没有交易完成, 所以最大利润为 0
</code></pre>
<p><strong>题解：</strong>该题可用暴力搜索进行求解，但时间复杂度为o(n^2^),不能满足题解要求，考虑使用贪心算法，每次遍历股票值，当选择到当前的最小值时就开始买入，此后继续进行遍历求最大收益，遍历结束时获取到的最大收益就是最终结果，耗时为O（n）</p>
<pre><code class="c++">class Solution &#123;
public:
  int maxProfit(vector&lt;int&gt;&amp; prices) &#123;
     int n=prices.size();
     int min=prices[0],maxProfit=0;
     for(int i=1;i&lt;n;i++)
     &#123;
       if(prices[i]&lt;min)
         min=prices[i];
       else&#123;
         maxProfit=((prices[i]-min)&gt;maxProfit)?(prices[i]-min):maxProfit;
       &#125;
     &#125;
     return maxProfit;
  &#125;
&#125;;
</code></pre>
<h2 id="买卖股票最佳时机Ⅱ"><a href="#买卖股票最佳时机Ⅱ" class="headerlink" title="买卖股票最佳时机Ⅱ"></a>买卖股票最佳时机Ⅱ</h2><p><strong>题号：</strong>122</p>
<p><strong>链接：</strong><a href="https://leetcode-cn.com/problems/best-time-to-buy-and-sell-stock-ii/">122. 买卖股票的最佳时机 II - 力扣（LeetCode） (leetcode-cn.com)</a></p>
<p><strong>题目描述：</strong>给定一个数组 prices ，其中 prices[i] 表示股票第 i 天的价格。</p>
<p>在每一天，你可能会决定购买和&#x2F;或出售股票。你在任何时候 最多 只能持有 一股 股票。你也可以购买它，然后在 同一天 出售。<br>返回 你能获得的 最大 利润 。</p>
<pre><code class="c++">示例 1:
输入: prices = [7,1,5,3,6,4]
输出: 7
解释: 在第 2 天（股票价格 = 1）的时候买入，在第 3 天（股票价格 = 5）的时候卖出, 这笔交易所能获得利润 = 5-1 = 4 。
     随后，在第 4 天（股票价格 = 3）的时候买入，在第 5 天（股票价格 = 6）的时候卖出, 这笔交易所能获得利润 = 6-3 = 3 。
示例 2:

输入: prices = [1,2,3,4,5]
输出: 4
解释: 在第 1 天（股票价格 = 1）的时候买入，在第 5 天 （股票价格 = 5）的时候卖出, 这笔交易所能获得利润 = 5-1 = 4 。
     注意你不能在第 1 天和第 2 天接连购买股票，之后再将它们卖出。因为这样属于同时参与了多笔交易，你必须在再次购买前出售掉之前的股票。
示例 3:

输入: prices = [7,6,4,3,1]
输出: 0
解释: 在这种情况下, 没有交易完成, 所以最大利润为 0。
</code></pre>
<p><strong>题解</strong>：只要今天的股票比明天的股票小，就将今天的股票买入，然后在明天卖出</p>
<pre><code class="c++">class Solution &#123;
public:
    int maxProfit(vector&lt;int&gt;&amp; prices) &#123;
        int n=prices.size();
        int maxProfit=0;
        for(int i=1;i&lt;n;i++)
        &#123;
          //只要今天价格小于明天价格，就在今天买入，明天卖出
          if(prices[i]&gt;prices[i-1])
              maxProfit+=prices[i]-prices[i-1];
        &#125;
        return maxProfit;
    &#125;
&#125;;
</code></pre>
<h2 id="种植花朵"><a href="#种植花朵" class="headerlink" title="种植花朵"></a>种植花朵</h2><p><strong>题号：</strong>605</p>
<p><strong>链接：</strong><a href="https://leetcode-cn.com/problems/can-place-flowers/">605. 种花问题 - 力扣（LeetCode） (leetcode-cn.com)</a></p>
<p><strong>题目描述：</strong>假设有一个很长的花坛，一部分地块种植了花，另一部分却没有。可是，花不能种植在相邻的地块上，它们会争夺水源，两者都会死去。</p>
<p>给你一个整数数组  flowerbed 表示花坛，由若干 0 和 1 组成，其中 0 表示没种植花，1 表示种植了花。另有一个数 n ，能否在不打破种植规则的情况下种入 n 朵花？能则返回 true ，不能则返回 false。</p>
<pre><code class="c++">示例 1：

输入：flowerbed = [1,0,0,0,1], n = 1
输出：true
示例 2：

输入：flowerbed = [1,0,0,0,1], n = 2
输出：false
</code></pre>
<p><strong>题解：</strong>贪心思想，只要满足连续的3个0就种植花朵，在数组的头尾两端各插入一个0以解决边界问题（防御式编程思想）</p>
<pre><code class="c++">class Solution &#123;
public:
    bool canPlaceFlowers(vector&lt;int&gt;&amp; flowerbed, int n) &#123;
        //防御式编程思想，在数组的两端各插入一个0，以解决边界问题
        flowerbed.insert(flowerbed.begin(),0);
        flowerbed.insert(flowerbed.end(),0);
        int num=flowerbed.size();
        int sum=0;
        for(int i=0;i&lt;num &amp;&amp; sum&lt;n;)//时间优化，只要满足种植花的数目就跳出循环
        &#123;
            //贪心思想，当出现连续的3个0时就种植一朵花
            if(i+2&lt;num &amp;&amp; flowerbed[i]==0 &amp;&amp; flowerbed[i+1]==0 &amp;&amp; flowerbed[i+2]==0)
            &#123;
                flowerbed[++i]=1;
                sum++;
                // if(sum&gt;=n) return true;
            &#125;
            else i++;
        &#125;
        // if(sum&gt;=n) return true;
        // return false;
        return (sum&gt;=n);

     &#125;

&#125;;
</code></pre>
<h2 id="判断是否为子序列"><a href="#判断是否为子序列" class="headerlink" title="判断是否为子序列"></a>判断是否为子序列</h2><p><strong>题号：</strong>392</p>
<p><strong>链接：</strong><a href="https://leetcode-cn.com/problems/is-subsequence/">392. 判断子序列 - 力扣（LeetCode） (leetcode-cn.com)</a></p>
<p><strong>题目描述：</strong>给定字符串 s 和 t ，判断 s 是否为 t 的子序列。</p>
<p>字符串的一个子序列是原始字符串删除一些（也可以不删除）字符而不改变剩余字符相对位置形成的新字符串。（例如，”ace”是”abcde”的一个子序列，而”aec”不是）。</p>
<pre><code>示例 1：

输入：s = &quot;abc&quot;, t = &quot;ahbgdc&quot;
输出：true
示例 2：

输入：s = &quot;axc&quot;, t = &quot;ahbgdc&quot;
输出：false
</code></pre>
<pre><code class="c++">class Solution &#123;
public:
    bool isSubsequence(string s, string t) &#123;
        int m=s.size(),n=t.size();
        int i=0,j=0;
        while(i&lt;m &amp;&amp; j&lt;n)//双指针指向待匹配序列与模板序列，若指针指向字符相等就后移，否则模板序列指针后移继续匹配
        &#123;
            if(s[i]==t[j])
            &#123;
                i++;
                j++;
            &#125;
            else
            &#123;
                j++;
            &#125;
        &#125;
        if(i==m) return true;
        else return false;
    &#125;
&#125;;
</code></pre>
<h2 id="非递减数列"><a href="#非递减数列" class="headerlink" title="非递减数列"></a>非递减数列</h2><p><strong>题号：</strong>665</p>
<p><strong>链接：</strong><a href="https://leetcode-cn.com/problems/non-decreasing-array/">665. 非递减数列 - 力扣（LeetCode） (leetcode-cn.com)</a></p>
<p><strong>题目描述：</strong>给你一个长度为 n 的整数数组 nums ，请你判断在 最多 改变 1 个元素的情况下，该数组能否变成一个非递减数列。</p>
<p>我们是这样定义一个非递减数列的： 对于数组中任意的 i (0 &lt;&#x3D; i &lt;&#x3D; n-2)，总满足 nums[i] &lt;&#x3D; nums[i + 1]。</p>
<pre><code class="c++">示例 1:

输入: nums = [4,2,3]
输出: true
解释: 你可以通过把第一个 4 变成 1 来使得它成为一个非递减数列。
示例 2:

输入: nums = [4,2,1]
输出: false
解释: 你不能在只改变一个元素的情况下将其变为非递减数列。
</code></pre>
<p><strong>题解：</strong>如下图所示</p>
<p><img src="https://s3.bmp.ovh/imgs/2022/04/19/08e34ed552a60a59.png"></p>
<pre><code class="c++">class Solution &#123;
public:
    bool checkPossibility(vector&lt;int&gt;&amp; nums) &#123;
        int n=nums.size();
        int count =0;
        for(int i=1;i+1&lt;n;)
        &#123;
            if(nums[i-1]&lt;=nums[i] &amp;&amp; nums[i]&lt;=nums[i+1])
                i++;
            else
            &#123;
                if(nums[i]&lt;nums[i-1] &amp;&amp; nums[i]&gt;nums[i+1])
                    return false;
                if(nums[i-1]&lt;=nums[i] &amp;&amp; nums[i-1]&gt;nums[i+1])
                &#123;
                    nums[i+1]=nums[i];
                    count++;
                    i++;
                    continue;
                &#125;
                if(nums[i-1]&lt;=nums[i] &amp;&amp; nums[i-1]&lt;=nums[i+1])
                &#123;
                    nums[i]=nums[i+1];
                    count++;
                    i++;
                    continue;
                &#125;
                if(nums[i-1]&gt;nums[i+1] &amp;&amp; nums[i]&lt;=nums[i+1])
                &#123;
                    nums[i-1]=nums[i];
                    count++;
                    i++;
                    continue;
                &#125;
                if(nums[i-1]&lt;=nums[i+1] &amp;&amp; nums[i]&lt;=nums[i+1])
                &#123;
                    nums[i]=nums[i-1];
                    count++;
                    i++;
                    continue;
                &#125;
            &#125;
        &#125;
        return count&lt;=1;
    &#125;
&#125;;
</code></pre>
<pre><code class="c++">class Solution &#123;
public:
    bool checkPossibility(vector&lt;int&gt;&amp; nums) &#123;
        int n=nums.size();
        int count =0;
        for(int i=0;i+1&lt;n &amp;&amp; count&lt;2;i++)
        &#123;
            if(nums[i]&lt;=nums[i+1])
                continue;
            if(i-1&gt;=0 &amp;&amp; nums[i-1]&gt;nums[i+1])//等号不能省略，否则会由于&amp;&amp;运算的bug导致程序出错
                nums[i+1]=nums[i];
            else
                nums[i]=nums[i+1];
            count++;
        &#125;
        return count&lt;=1;
    &#125;
&#125;;
</code></pre>
<h2 id="最大子数组的和"><a href="#最大子数组的和" class="headerlink" title="最大子数组的和"></a>最大子数组的和</h2><p><strong>题号：</strong>53</p>
<p><strong>链接：</strong><a href="https://leetcode-cn.com/problems/maximum-subarray/">53. 最大子数组和 - 力扣（LeetCode） (leetcode-cn.com)</a></p>
<p><strong>题目描述：</strong>给你一个整数数组 nums ，请你找出一个具有最大和的连续子数组（子数组最少包含一个元素），返回其最大和。</p>
<p>子数组 是数组中的一个连续部分。</p>
<pre><code>示例 1：

输入：nums = [-2,1,-3,4,-1,2,1,-5,4]
输出：6
解释：连续子数组 [4,-1,2,1] 的和最大，为 6 。
示例 2：

输入：nums = [1]
输出：1
示例 3：

输入：nums = [5,4,-1,7,8]
输出：23
</code></pre>
<p><strong>题解：f(n) &#x3D; max(f(n-1) + A[n], A[n])</strong></p>
<p>sum&#x3D;f(n-1)+a(n)表示当前局部最优值，如果sum&gt;&#x3D;a(n)，说明sum&gt;&#x3D;0，则令sum&#x3D;f(n-1)+a(n);<br>如果sum&lt;&#x3D;a(n)说明sum&lt;&#x3D;0，即此时f(n-1)拖累了a(n)，则令sum&#x3D;a(n)<br>判断局部最优sum与全局最优max的最大值，求取全局最优max</p>
<pre><code class="c++">class Solution &#123;
public:
    int maxSubArray(vector&lt;int&gt;&amp; nums) &#123;
        int n=nums.size();
        int max=nums[0],sum=nums[0];
        for(int i=1;i&lt;n;i++)
        &#123;
            if(sum+nums[i]&gt;=nums[i])
                sum+=nums[i];
            else    sum=nums[i];
            max=(sum&gt;max)?sum:max;
        &#125;
       return max;
    &#125;
&#125;;
</code></pre>
<h1 id="二分法"><a href="#二分法" class="headerlink" title="二分法"></a>二分法</h1><h2 id="搜索插入位置"><a href="#搜索插入位置" class="headerlink" title="搜索插入位置"></a>搜索插入位置</h2><p><strong>题号：</strong>35</p>
<p><strong>链接：</strong><a href="https://leetcode-cn.com/problems/search-insert-position/">35. 搜索插入位置 - 力扣（LeetCode） (leetcode-cn.com)</a></p>
<p><strong>题目描述：</strong>给定一个排序数组和一个目标值，在数组中找到目标值，并返回其索引。如果目标值不存在于数组中，返回它将会被按顺序插入的位置。</p>
<p>请必须使用时间复杂度为 O(log n) 的算法。</p>
<pre><code class="c++">示例 1:

输入: nums = [1,3,5,6], target = 5
输出: 2
示例 2:

输入: nums = [1,3,5,6], target = 2
输出: 1
示例 3:

输入: nums = [1,3,5,6], target = 7
输出: 4
</code></pre>
<p><strong>二分法的两种情况</strong></p>
<ul>
<li>定义区间为左闭右闭，即[left,right]，则<strong>n&#x3D;nums.size()</strong></li>
</ul>
<p>此时<strong>low&lt;high</strong>且<strong>high&#x3D;mid</strong></p>
<ul>
<li>定义区间为左闭右开，即[left,right)，则<strong>n&#x3D;nums.size()</strong>-1</li>
</ul>
<p>此时<strong>low&lt;&#x3D;high</strong>且<strong>high&#x3D;mid+1</strong></p>
<p><strong>左闭右开</strong></p>
<pre><code class="c++">class Solution &#123;
public:
    int searchInsert(vector&lt;int&gt;&amp; nums, int target) &#123;
        int low=0,high=nums.size()-1;//定义搜索区间为左闭右开
        while(low&lt;=high)
        &#123;
            int mid=low+(high-low)/2;
            if(nums[mid]==target) return mid;
            if(nums[mid]&lt;target) low=mid+1;
            else high=mid-1;
        &#125;
        return high+1;
    &#125;
&#125;;
</code></pre>
<p>左闭右闭</p>
<pre><code class="c++">class Solution &#123;
public:
    int searchInsert(vector&lt;int&gt;&amp; nums, int target) &#123;
        int low=0,high=nums.size();//定义搜索区间为左闭右闭
        while(low&lt;high)
        &#123;
            int mid=low+(high-low)/2;
            if(nums[mid]==target) return mid;
            if(nums[mid]&lt;target) low=mid+1;
            else high=mid;
        &#125;
        return high;
    &#125;
&#125;;
</code></pre>
<h2 id="x的平方根"><a href="#x的平方根" class="headerlink" title="x的平方根"></a>x的平方根</h2><p><strong>题号：</strong>69</p>
<p><strong>链接：</strong><a href="https://leetcode-cn.com/problems/sqrtx/">69. x 的平方根 - 力扣（LeetCode） (leetcode-cn.com)</a></p>
<p><strong>题目描述：</strong>给你一个非负整数 x ，计算并返回 x 的 算术平方根 。</p>
<p>由于返回类型是整数，结果只保留 整数部分 ，小数部分将被 舍去 。</p>
<p>注意：不允许使用任何内置指数函数和算符，例如 pow(x, 0.5) 或者 x ** 0.5 。</p>
<pre><code>示例 1：

输入：x = 4
输出：2
示例 2：

输入：x = 8
输出：2
解释：8 的算术平方根是 2.82842..., 由于返回类型是整数，小数部分将被舍去。
</code></pre>
<pre><code class="c++">class Solution &#123;
public:
    int mySqrt(int x) &#123;
        if(x&lt;=1) return x;
        long l=1,h=x;
        while(l&lt;=h)
        &#123;
            long mid=(l+h)/2;
            long q=mid*mid;
            if(q==x) return mid;
            if(q&lt;x) l=mid+1;
            else h=mid-1;
        &#125;
        return h;//退出循环时h=l-1，故应取小数
    &#125;
&#125;;
</code></pre>
<h2 id="有序数组中大于目标字母的最小字母"><a href="#有序数组中大于目标字母的最小字母" class="headerlink" title="有序数组中大于目标字母的最小字母"></a>有序数组中大于目标字母的最小字母</h2><p><strong>题号：</strong>744</p>
<p><strong>链接：</strong><a href="https://leetcode-cn.com/problems/sqrtx/"><a href="https://leetcode-cn.com/problems/find-smallest-letter-greater-than-target/">744. 寻找比目标字母大的最小字母 - 力扣（LeetCode） (leetcode-cn.com)</a></a></p>
<p><strong>题目描述：</strong>给你一个排序后的字符列表 letters ，列表中只包含小写英文字母。另给出一个目标字母 target，请你寻找在这一有序列表里比目标字母大的最小字母。</p>
<p>在比较时，字母是依序循环出现的。举个例子：</p>
<p>如果目标字母 target &#x3D; ‘z’ 并且字符列表为 letters &#x3D; [‘a’, ‘b’]，则答案返回 ‘a’</p>
<pre><code>示例 1：

输入: letters = [&quot;c&quot;, &quot;f&quot;, &quot;j&quot;]，target = &quot;a&quot;
输出: &quot;c&quot;
示例 2:

输入: letters = [&quot;c&quot;,&quot;f&quot;,&quot;j&quot;], target = &quot;c&quot;
输出: &quot;f&quot;
示例 3:

输入: letters = [&quot;c&quot;,&quot;f&quot;,&quot;j&quot;], target = &quot;d&quot;
输出: &quot;f&quot;
</code></pre>
<pre><code class="c++">class Solution &#123;
public:
    char nextGreatestLetter(vector&lt;char&gt;&amp; letters, char target) &#123;
        int n=letters.size();
        // for(int i=0;i&lt;n;i++)
        // &#123;
        //     if(target&lt;letters[i])
        //         return letters[i];
        // &#125;
        // return letters[0];

        int l=0,h=n-1;
        while(l&lt;=h)
        &#123;
            int mid=l+(h-l)/2;//防止int数据溢出
           if(target&lt;letters[mid]) h=mid-1;//边界问题处理
           else l=mid+1;
        &#125;
        return letters[l%n];
    &#125;
&#125;;
</code></pre>
<h2 id="有序数组中的单一元素"><a href="#有序数组中的单一元素" class="headerlink" title="有序数组中的单一元素"></a>有序数组中的单一元素</h2><p><strong>题号：</strong>540</p>
<p><strong>链接：</strong><a href="https://leetcode-cn.com/problems/single-element-in-a-sorted-array/">540. 有序数组中的单一元素 - 力扣（LeetCode） (leetcode-cn.com)</a></p>
<p><strong>题目描述：</strong>给你一个仅由整数组成的有序数组，其中每个元素都会出现两次，唯有一个数只会出现一次。</p>
<p>请你找出并返回只出现一次的那个数。</p>
<p>你设计的解决方案必须满足 O(log n) 时间复杂度和 O(1) 空间复杂度。</p>
<pre><code>示例 1:

输入: nums = [1,1,2,3,3,4,4,8,8]
输出: 2
示例 2:

输入: nums =  [3,3,7,7,10,11,11]
输出: 10

</code></pre>
<pre><code class="c++">class Solution &#123;
public:
    int singleNonDuplicate(vector&lt;int&gt;&amp; nums) &#123;
        int n=nums.size();
        if(n%2==0) return -1;
        if(n==1) return nums[0];
        int low=0,high=n-1;
        while(low&lt;high)
        &#123;
            int mid=low+(high-low)/2;
            // int mid=(low+high)/2;
            if(nums[mid]==nums[mid-1])//中间值与左边是否相等 
            &#123;
                if((high-mid)%2==0) high=mid-2;//左奇右偶
                else low=mid+1;
            &#125;
            else
            &#123;
                if(nums[mid]==nums[mid+1])//中间值与右边是否相等
                &#123;
                    if((high-mid+1)%2==0) high=mid-1;//左奇右偶
                    else low=mid+2;
                &#125;
                else return nums[mid];//中间值与两边都不相等，故结果就是中间值
            &#125;
        &#125;
        return nums[low];
    &#125;
&#125;;
</code></pre>
<h2 id="第一个错误的版本"><a href="#第一个错误的版本" class="headerlink" title="第一个错误的版本"></a>第一个错误的版本</h2><p><strong>题号：</strong>540</p>
<p><strong>链接：</strong><a href="https://leetcode-cn.com/problems/first-bad-version/">278. 第一个错误的版本 - 力扣（LeetCode） (leetcode-cn.com)</a></p>
<p><strong>题目描述：</strong>你是产品经理，目前正在带领一个团队开发新的产品。不幸的是，你的产品的最新版本没有通过质量检测。由于每个版本都是基于之前的版本开发的，所以错误的版本之后的所有版本都是错的。</p>
<p>假设你有 n 个版本 [1, 2, …, n]，你想找出导致之后所有版本出错的第一个错误的版本。</p>
<p>你可以通过调用 bool isBadVersion(version) 接口来判断版本号 version 是否在单元测试中出错。实现一个函数来查找第一个错误的版本。你应该尽量减少对调用 API 的次数。</p>
<pre><code>示例 1：

输入：n = 5, bad = 4
输出：4
解释：
调用 isBadVersion(3) -&gt; false 
调用 isBadVersion(5) -&gt; true 
调用 isBadVersion(4) -&gt; true
所以，4 是第一个错误的版本。
示例 2：

输入：n = 1, bad = 1
输出：1
</code></pre>
<pre><code class="c++">// The API isBadVersion is defined for you.
// bool isBadVersion(int version);

class Solution &#123;
public:
    int firstBadVersion(int n) &#123;
        if(n==1) return 1;
        int l=0,h=n-1;
        while(l&lt;=h)
        &#123;
            int mid=l+(h-l)/2;
            if(!isBadVersion(mid)) l=mid+1;
            else h=mid-1;
        &#125;
        return l;
    &#125;
&#125;;
</code></pre>
<h2 id="寻找旋转排序数组中的最小值"><a href="#寻找旋转排序数组中的最小值" class="headerlink" title="寻找旋转排序数组中的最小值"></a>寻找旋转排序数组中的最小值</h2><p><strong>题号：</strong>153</p>
<p><strong>链接：</strong><a href="https://leetcode-cn.com/problems/find-minimum-in-rotated-sorted-array/">153. 寻找旋转排序数组中的最小值 - 力扣（LeetCode） (leetcode-cn.com)</a></p>
<p><strong>题目描述：</strong>已知一个长度为 n 的数组，预先按照升序排列，经由 1 到 n 次 旋转 后，得到输入数组。例如，原数组 nums &#x3D; [0,1,2,4,5,6,7] 在变化后可能得到：<br>若旋转 4 次，则可以得到 [4,5,6,7,0,1,2]<br>若旋转 7 次，则可以得到 [0,1,2,4,5,6,7]<br>注意，数组 [a[0], a[1], a[2], …, a[n-1]] 旋转一次 的结果为数组 [a[n-1], a[0], a[1], a[2], …, a[n-2]] 。</p>
<p>给你一个元素值 互不相同 的数组 nums ，它原来是一个升序排列的数组，并按上述情形进行了多次旋转。请你找出并返回数组中的 最小元素 。</p>
<p>你必须设计一个时间复杂度为 O(log n) 的算法解决此问题。</p>
<pre><code class="c++">示例 1：

输入：nums = [3,4,5,1,2]
输出：1
解释：原数组为 [1,2,3,4,5] ，旋转 3 次得到输入数组。
示例 2：

输入：nums = [4,5,6,7,0,1,2]
输出：0
解释：原数组为 [0,1,2,4,5,6,7] ，旋转 4 次得到输入数组。
示例 3：

输入：nums = [11,13,15,17]
输出：11
解释：原数组为 [11,13,15,17] ，旋转 4 次得到输入数组。
</code></pre>
<p><strong>题解：</strong>由于数组原本有序，旋转后产生了间断点，断点两侧均为有序数组，本题可以视为寻找间断点。</p>
<p>当nums[mid]&lt;nums[h]时，说明断点在左侧区间，反之断点在右侧区间</p>
<pre><code class="c++">class Solution &#123;
public:
    int findMin(vector&lt;int&gt;&amp; nums) &#123;
        int l=0,h=nums.size()-1;
        while(l&lt;h)
        &#123;
            int mid=l+(h-l)/2;
            if(nums[mid]&lt;nums[h])   h=mid;
            else    l=mid+1;
        &#125;
        return nums[l];
    &#125;
&#125;;
</code></pre>
<h1 id="字符串"><a href="#字符串" class="headerlink" title="字符串"></a>字符串</h1><h2 id="串中单词个数"><a href="#串中单词个数" class="headerlink" title="串中单词个数"></a>串中单词个数</h2><p><strong>题号：</strong>434</p>
<p><strong>链接：</strong><a href="https://leetcode-cn.com/problems/number-of-segments-in-a-string/">434. 字符串中的单词数 - 力扣（LeetCode） (leetcode-cn.com)</a></p>
<p><strong>题目描述：</strong>统计字符串中的单词个数，这里的单词指的是连续的不是空格的字符。</p>
<p>请注意，你可以假定字符串里不包括任何不可打印的字符。</p>
<pre><code class="c++">示例:

输入: &quot;Hello, my name is John&quot;
输出: 5
解释: 这里的单词是指连续的不是空格的字符，所以 &quot;Hello,&quot; 算作 1 个单词。
</code></pre>
<p>题解：遍历字符串，当第一个字符不是空格，第二个字符是空格时就判定为一个单词分界标志，判定第一个字符是否是空格可排除空串等情况</p>
<pre><code class="c++">class Solution &#123;
public:
  int countSegments(string s) &#123;
     int num=0;
     int n=s.size();
     if(n==0) return num;
     for(int i=0;i&lt;n;i++)
     &#123;
       if(i==0 &amp;&amp; s[i]!=&#39; &#39;) num++;
       if(i+1&lt;n &amp;&amp; s[i]==&#39; &#39; &amp;&amp; s[i+1]!=&#39; &#39;) num++;
     &#125;
     return num;
  &#125;
&#125;;
</code></pre>
]]></content>
      <categories>
        <category>LeetCode</category>
      </categories>
      <tags>
        <tag>LeetCode</tag>
      </tags>
  </entry>
  <entry>
    <title>MySQL基础</title>
    <url>/2023/01/31/mysql%E5%9F%BA%E7%A1%80/</url>
    <content><![CDATA[<!-- more -->

<h1 id="SQL分类"><a href="#SQL分类" class="headerlink" title="SQL分类"></a>SQL分类</h1><p>DDL（数据定义语言）：create、alter、drop、rename、truncate</p>
<p>DML（数据操作语言）:insert、delete、update、select</p>
<p>DCL（数据控制语言）:commit、rollback、savepoint、grant、revoke</p>
<h1 id="基本语句"><a href="#基本语句" class="headerlink" title="基本语句"></a>基本语句</h1><h2 id="select语句"><a href="#select语句" class="headerlink" title="select语句"></a>select语句</h2><h3 id="查询"><a href="#查询" class="headerlink" title="查询"></a>查询</h3><h4 id="全查询"><a href="#全查询" class="headerlink" title="全查询"></a>全查询</h4><pre><code class="sql">SELECT * FROM employees;
</code></pre>
<h4 id="子列查询"><a href="#子列查询" class="headerlink" title="子列查询"></a>子列查询</h4><pre><code class="sql">SELECT employee_id,last_name,department_id
FROM employees;
</code></pre>
<h3 id="取别名"><a href="#取别名" class="headerlink" title="取别名"></a>取别名</h3><p><strong>as关键字</strong></p>
<pre><code class="sql">SELECT employee_id AS eid
FROM employees;
</code></pre>
<p><strong>双引号取别名</strong></p>
<pre><code class="sql">SELECT department_id &quot;did&quot;
FROM employees;

SELECT salary*12 &quot;annual sal&quot;
FROM employees;
</code></pre>
<h3 id="去除重复行"><a href="#去除重复行" class="headerlink" title="去除重复行"></a>去除重复行</h3><p><strong>distinct</strong></p>
<pre><code class="sql">SELECT DISTINCT department_id
FROM employees;
</code></pre>
<p><strong>注</strong>：distinct作用于其后的所有列</p>
<h4 id="空值NULL参与运算"><a href="#空值NULL参与运算" class="headerlink" title="空值NULL参与运算"></a>空值NULL参与运算</h4><pre><code class="sql">SELECT employee_id,salary &quot;月工资&quot;,salary*(1+IFNULL(commission_pct,0))*12 &quot;年工资&quot;
FROM employees;
</code></pre>
<p><strong>注</strong>：空值不等于0，对其参与运算时需要用函数将其转换为0</p>
<h2 id="运算符"><a href="#运算符" class="headerlink" title="运算符"></a>运算符</h2><h3 id="算术运算符"><a href="#算术运算符" class="headerlink" title="算术运算符"></a>算术运算符</h3><p><strong>加（+）、减（-）、乘（*）、除（&#x2F;）和取模（%）</strong></p>
<p><strong>练习</strong>：</p>
<pre><code class="sql">SELECT 12 MOD 3,12 MOD 5,-12 MOD 5,-12 MOD-5,-12 % -3
FROM DUAL; 
</code></pre>
<p><strong>结果</strong>：0 2 -2 -2 0</p>
<p><strong>注</strong>：mod结果正负号取决于mod前数字正负号</p>
<h3 id="比较运算符"><a href="#比较运算符" class="headerlink" title="比较运算符"></a>比较运算符</h3><h4 id="等号运算符"><a href="#等号运算符" class="headerlink" title="等号运算符"></a>等号运算符</h4><pre><code class="sql">SELECT 1=1,1=2,1=&#39;a&#39;,0=&#39;a&#39;,&#39;a&#39;=&#39;a&#39;,&#39;a&#39;=&#39;b&#39;
FROM DUAL;
</code></pre>
<p><strong>结果</strong>：1 0 0 1 1 0</p>
<p><strong>注</strong>：数字与字符进行比较时，字符转换为数字0，字符与字符进行比较时，字符转换为asacii值</p>
<h4 id="安全等于运算符-lt-x3D-gt"><a href="#安全等于运算符-lt-x3D-gt" class="headerlink" title="安全等于运算符&lt;&#x3D;&gt;"></a>安全等于运算符&lt;&#x3D;&gt;</h4><p><strong>专为null的比较而生</strong></p>
<pre><code class="sql">SELECT 1=NULL,NULL=NULL
FROM DUAL;
</code></pre>
<p><strong>结果</strong>：null null</p>
<p><strong>注</strong>：当null参与运算时，其结果永远为null</p>
<pre><code class="sql">SELECT 1&lt;=&gt;NULL,NULL&lt;=&gt;NULL
FROM DUAL;
</code></pre>
<p><strong>结果</strong>：0 1</p>
<p><strong>注</strong>：使用&lt;&#x3D;&gt;可使得null运算符进行比较并返回正确结果</p>
<p><strong>练习</strong>：查询绩效为null的员工信息</p>
<p><strong>解1</strong>：</p>
<pre><code>SELECT employee_id,last_name,commission_pct
FROM employees
WHERE NOT commission_pct &lt;=&gt;NULL;
</code></pre>
<p><strong>解2</strong>：</p>
<pre><code class="sql">SELECT employee_id,last_name,commission_pct
FROM employees
WHERE commission_pct IS NULL;
</code></pre>
<p><strong>解3</strong>：</p>
<pre><code class="sql">SELECT employee_id,last_name,commission_pct
FROM employees
WHERE ISNULL(commission_pct);
</code></pre>
<p><strong>练习</strong> ：查询绩效不为null的员工信息</p>
<pre><code>SELECT employee_id,last_name,commission_pct
FROM employees
WHERE commission_pct IS NOT NULL;
</code></pre>
<h4 id="最小运算符LEAST与最大运算符GREATEST"><a href="#最小运算符LEAST与最大运算符GREATEST" class="headerlink" title="最小运算符LEAST与最大运算符GREATEST"></a>最小运算符LEAST与最大运算符GREATEST</h4><pre><code class="sql">SELECT LEAST(&#39;a&#39;,&#39;b&#39;,&#39;d&#39;),GREATEST(1,23,100)
FROM DUAL;
</code></pre>
<pre><code class="sql">SELECT LEAST(first_name,last_name),GREATEST(LENGTH(first_name),LENGTH(last_name))
FROM employees;
</code></pre>
<h3 id="逻辑运算符"><a href="#逻辑运算符" class="headerlink" title="逻辑运算符"></a>逻辑运算符</h3><h4 id="between-and"><a href="#between-and" class="headerlink" title="between and"></a>between and</h4><pre><code class="sql">SELECT employee_id,last_name,salary
FROM employees
WHERE salary BETWEEN 6000 AND 8000;
</code></pre>
<pre><code class="sql">SELECT employee_id,last_name,salary
FROM employees
WHERE salary &gt;=6000 &amp;&amp; salary&lt;= 8000;
</code></pre>
<p><strong>注</strong>：between and运算符范围边界为双闭区间，边界值都可以取到</p>
<pre><code class="sql">SELECT employee_id,last_name,salary
FROM employees
WHERE salary NOT BETWEEN 6000 AND 8000;
</code></pre>
<h4 id="IN"><a href="#IN" class="headerlink" title="IN"></a>IN</h4><pre><code class="sql">SELECT employee_id,last_name,salary
FROM employees
WHERE salary IN(6000,7000,8000);
</code></pre>
<pre><code class="sql">SELECT employee_id,last_name,salary
FROM employees
WHERE salary=6000 OR salary=7000 OR salary=8000;
</code></pre>
<pre><code class="sql">SELECT employee_id,last_name,salary
FROM employees
WHERE salary NOT IN(6000,7000,8000);
</code></pre>
<h3 id="模糊查询"><a href="#模糊查询" class="headerlink" title="模糊查询"></a>模糊查询</h3><p><strong>like</strong></p>
<h4 id="多字符匹配"><a href="#多字符匹配" class="headerlink" title="多字符匹配%"></a>多字符匹配%</h4><p><strong>练习</strong>：查找姓名以字母a开头的员工信息</p>
<p>SELECT employee_id,last_name,salary<br>FROM employees<br>WHERE last_name LIKE ‘a%’;</p>
<p><strong>练习</strong>：查找姓名包含字母a的员工信息</p>
<pre><code>SELECT employee_id,last_name,salary
FROM employees
WHERE last_name LIKE &#39;%a%&#39;;
</code></pre>
<p><strong>练习</strong>：查找姓名包含字母a和字母e的员工信息</p>
<pre><code class="sql">SELECT employee_id,last_name,salary
FROM employees
WHERE last_name LIKE &#39;%a%&#39; AND last_name LIKE&#39;%e%&#39;;
</code></pre>
<h4 id="单字符匹配"><a href="#单字符匹配" class="headerlink" title="单字符匹配_"></a>单字符匹配_</h4><p><strong>练习</strong>：查找姓名第二个字母是a的员工信息</p>
<pre><code class="sql">SELECT employee_id,last_name,salary
FROM employees
WHERE last_name LIKE &#39;_a%&#39;;
</code></pre>
<p><strong>练习</strong>：查找姓名第二个字母是_的并且第三个字母是a员工信息</p>
<pre><code class="sql">SELECT employee_id,last_name,salary
FROM employees
WHERE last_name LIKE &#39;_\_a%&#39;;
</code></pre>
<h3 id="正则匹配"><a href="#正则匹配" class="headerlink" title="正则匹配"></a>正则匹配</h3><p><strong>被匹配字符串 RLIKR\REGEXP 正则表达式</strong></p>
<table>
<thead>
<tr>
<th>选项</th>
<th>说明</th>
<th>举例</th>
<th>备注</th>
</tr>
</thead>
<tbody><tr>
<td>^</td>
<td>匹配以字符开头的串</td>
<td>‘^b’</td>
<td>匹配以b开头的字符串</td>
</tr>
<tr>
<td>$</td>
<td>匹配以字符结尾的串</td>
<td>‘st$’</td>
<td>匹配以st为结尾的字符串</td>
</tr>
<tr>
<td>.</td>
<td>匹配单个字符</td>
<td>‘b.t’</td>
<td>匹配b和t之间有一个未知字符的字符串</td>
</tr>
<tr>
<td>*</td>
<td>匹配任意各字符</td>
<td>‘b*t’</td>
<td>匹配b和t之间有任意个未知字符的字符串</td>
</tr>
<tr>
<td>+</td>
<td>匹配前面的字符一个或多个</td>
<td>‘ba+’</td>
<td>匹配b开头且有一个或多个a的字符</td>
</tr>
<tr>
<td>[字符串]</td>
<td>匹配包含字符串的文本</td>
<td>‘[abc]’</td>
<td>匹配包含a或b或c的字符</td>
</tr>
</tbody></table>
<pre><code class="sql">SELECT last_name,salary
FROM employees
WHERE last_name RLIKE &#39;___a&#39;;
</code></pre>
<pre><code class="sql">SELECT last_name,salary
FROM employees
WHERE last_name RLIKE &#39;%a%&#39; OR last_name RLIKE &#39;%k%&#39;;
</code></pre>
<pre><code class="sql">SELECT last_name,salary
FROM employees
WHERE first_name RLIKE &#39;e$&#39;;
</code></pre>
<pre><code class="sql">SELECT last_name,salary
FROM employees
WHERE employee_id i;
</code></pre>
<pre><code class="sql">SELECT last_name,salary
FROM employees
WHERE last_name RLIKE &#39;%a%&#39; OR last_name RLIKE &#39;%k%&#39;;
</code></pre>
<h3 id="位运算符"><a href="#位运算符" class="headerlink" title="位运算符"></a>位运算符</h3><table>
<thead>
<tr>
<th>运算符</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td>&amp;</td>
<td>按位与</td>
</tr>
<tr>
<td>|</td>
<td>按位或</td>
</tr>
<tr>
<td>^</td>
<td>按位异或（XOR）</td>
</tr>
<tr>
<td>~</td>
<td>按位取反</td>
</tr>
<tr>
<td>&gt;&gt;</td>
<td>按位右移</td>
</tr>
<tr>
<td>&lt;&lt;</td>
<td>按位左移</td>
</tr>
</tbody></table>
<p>SELECT 8&gt;&gt;1,8&lt;&lt;1<br>FROM DUAL;</p>
<p><strong>结果</strong>：4 16</p>
<p><strong>注</strong>：右移一位除以2，左移一位乘以2</p>
<h2 id="排序与分页"><a href="#排序与分页" class="headerlink" title="排序与分页"></a>排序与分页</h2><h3 id="排序"><a href="#排序" class="headerlink" title="排序"></a>排序</h3><p><strong>order by</strong></p>
<p><strong>升序：asc（默认）	降序：desc</strong></p>
<p><strong>练习</strong>：按照员工工资升序排列显示员工信息</p>
<pre><code class="sql">SELECT employee_id,salary,salary*12 &#39;annual_salary&#39;
FROM employees
ORDER BY salary;
</code></pre>
<p><strong>练习</strong>：按照年工资降序排列显示员工信息</p>
<pre><code class="sql">SELECT employee_id,salary,salary*12 &#39;annual_salary&#39;
FROM employees
WHERE department_id IN(50,60,70)
ORDER BY annual_salary DESC;
</code></pre>
<p><strong>注</strong>：sql的编译顺序是	from表	-&gt;	where过滤-&gt;	select查询信息	-&gt;order by</p>
<p>故，一般将order by语句写在最后，where需要声明在from后，order by之前，并且对别名的使用不能在where语句中出现</p>
<p><strong>练习</strong>：二级排序，按照employee_id降序，若employee_id相同按照annual_salary升序</p>
<pre><code class="sql">SELECT employee_id,salary,salary*12 &#39;annual_salary&#39;
FROM employees
ORDER BY employee_id DESC ,annual_salary;
</code></pre>
<h3 id="分页（切片）"><a href="#分页（切片）" class="headerlink" title="分页（切片）"></a>分页（切片）</h3><p><strong>limit 开始偏移量，显示的记录条数</strong></p>
<p><strong>练习</strong>：查询从第40条记录开始的20条记录的信息</p>
<pre><code class="sql">SELECT *
FROM employees
LIMIT 40,20;
</code></pre>
<pre><code class="sql">SELECT *
FROM employees
LIMIT 20 OFFSET 40;
</code></pre>
<p><strong>注</strong>：limit语句写在整个select语句之后！</p>
<h1 id="多表查询"><a href="#多表查询" class="headerlink" title="多表查询"></a>多表查询</h1><h3 id="基本多表查询"><a href="#基本多表查询" class="headerlink" title="基本多表查询"></a>基本多表查询</h3><p><img src="C:\Users\ASUS\AppData\Roaming\Typora\typora-user-images\image-20230106131303283.png" alt="image-20230106131303283"></p>
<p><strong>练习</strong>：查询员工号及部门名称</p>
<pre><code class="sql">SELECT employee_id,department_name
FROM employees,departments
--连接条件
WHERE employees.department_id=departments.department_id;
</code></pre>
<p><strong>注</strong>：多表查询需要指明连接条件，因此多表查询的前提是需要查询的多个表之间要有连接键</p>
<p><strong>练习</strong>：查询员工号、部门名称、部门号</p>
<pre><code class="sql">SELECT employee_id,departments.department_id,department_name
FROM employees,departments
WHERE employees.department_id=departments.department_id;
</code></pre>
<p><strong>注</strong>：当查询的字段在多个表中均存在时，要注明查询的是哪个表中的字段</p>
<p><strong>练习</strong>：查询员工号，部门号，部门名称（可对表进行别名使用）</p>
<pre><code class="sql">SELECT em.employee_id,de.department_id,de.department_name
FROM employees em,departments de
WHERE em.department_id=de.department_id;
</code></pre>
<p><strong>注</strong>：由于sql语句的执行顺序是from	-》	where	-》	select，因此对表取别名后，在sql语句中表的原名均被别名覆盖，所以where语句以及select语句均需要使用的表的别名而不能使用表的原名</p>
<p>如下示例是错误的做法：</p>
<pre><code class="sql">-- 错误写法
SELECT employees.employee_id,de.department_id,de.department_name
FROM employees em,departments de
WHERE employees.department_id=de.department_id;
</code></pre>
<p><strong>练习</strong>：查询员工号，员工姓名，部门名称，员工所在城市（三表查询）</p>
<pre><code class="sql">SELECT e.employee_id,e.last_name,d.department_name,l.city
FROM employees e,departments d,locations l
WHERE e.department_id=d.department_id
AND d.location_id=l.location_id;
</code></pre>
<h3 id="多表查询的分类"><a href="#多表查询的分类" class="headerlink" title="多表查询的分类"></a>多表查询的分类</h3><h4 id="等值连接与非等值连接"><a href="#等值连接与非等值连接" class="headerlink" title="等值连接与非等值连接"></a>等值连接与非等值连接</h4><p><strong>非等值连接</strong>如下</p>
<p><img src="C:\Users\ASUS\AppData\Roaming\Typora\typora-user-images\image-20230107092704928.png" alt="image-20230107092704928"></p>
<p><strong>练习</strong>：求每个员工的姓名，工资，以及对应工资等级</p>
<pre><code class="sql">SELECT last_name,salary,grade_level
FROM employees e,job_grades j
WHERE e.salary BETWEEN j.lowest_sal AND j.highest_sal;
</code></pre>
<p><strong>分析</strong>：员工姓名及工资在employees表中，工资等级在job_grades表中，但这两张表并无连接键相连，此时可用非等值连接来连接两张表并避免笛卡尔积错误</p>
<h4 id="自连接与非自连接"><a href="#自连接与非自连接" class="headerlink" title="自连接与非自连接"></a>自连接与非自连接</h4><p><strong>自连接</strong>如下</p>
<p>练习：求员工id，姓名以及他的管理者 的id和姓名</p>
<pre><code class="sql">SELECT emp.employee_id,emp.last_name,mgr.employee_id,mgr.last_name
FROM employees emp,employees mgr
WHERE emp.manager_id=mgr.employee_id;
</code></pre>
<p><strong>分析</strong>：由于员工的id和姓名以及他的管理者的id和姓名均在employees表中，此时需要使用自连接</p>
<p><strong>注</strong>：自连接即同一张表自己连接，连接时要将该表视为两张表并用不同的别名代替进行等值连接</p>
<h4 id="内连接与外连接"><a href="#内连接与外连接" class="headerlink" title="内连接与外连接"></a>内连接与外连接</h4><p><img src="C:\Users\ASUS\AppData\Roaming\Typora\typora-user-images\image-20230107102342947.png" alt="image-20230107102342947"></p>
<p><strong>内连接</strong>：满足两表连接条件的连接，如下当两表满足employees.department_id&#x3D;departments.department_id条件时查询出的结果（中图，A∩B）</p>
<pre><code class="sql">SELECT last_name,department_name
FROM employees,departments
WHERE employees.department_id=departments.department_id;
</code></pre>
<p><strong>内连接的另一种写法</strong></p>
<pre><code class="sql">SELECT last_name,department_name
FROM employees e JOIN departments d
ON e.department_id=d.department_id;
</code></pre>
<p><strong>三表内连接</strong></p>
<pre><code class="sql">SELECT last_name,department_name,city
FROM employees e JOIN departments d
ON e.department_id=d.department_id
JOIN locations l
ON d.location_id=l.location_id;
</code></pre>
<p><strong>外连接</strong>：在上述连接条件中<strong>employees.department_id&#x3D;departments.department_id</strong>中，可能存在<strong>employees.department_id为null</strong>但<strong>departments.department_id不为空</strong>的记录，反之亦然，然而此时由于不满足连接条件因此无法查到这些记录。外连接即为了查询到这些记录</p>
<p><strong>左外连接</strong>：查询左表有值但右表为空的记录（左上图）</p>
<p><strong>右外连接</strong>：查询左表为空但右表有值的记录（右上图）</p>
<p><strong>左连接练习</strong>：查询<strong>所有员工</strong>的员工姓名及其对应所在部门（左上图）</p>
<pre><code class="sql">SELECT last_name,department_name
FROM employees e LEFT OUTER JOIN departments d
ON e.department_id=d.department_id;
</code></pre>
<p><strong>右连接练习</strong>：查询<strong>所有部门名称</strong>及各部门所在员工姓名（右上图）</p>
<pre><code class="sql">SELECT last_name,department_name
FROM employees e RIGHT OUTER JOIN departments d
ON e.department_id=d.department_id;
</code></pre>
<p><strong>练习</strong>：左中图</p>
<pre><code class="sql">SELECT last_name,department_name
FROM employees e LEFT OUTER JOIN departments d
ON e.department_id=d.department_id
WHERE d.department_id IS NULL;
</code></pre>
<p><strong>练习</strong>：右中图</p>
<pre><code class="sql">SELECT last_name,department_name
FROM employees e RIGHT OUTER JOIN departments d
ON e.department_id=d.department_id
WHERE e.department_id IS NULL;
</code></pre>
<p><strong>满连接</strong>：mysql无法实现，需要借助union all实现</p>
<p><strong>union</strong>：A∪B且去重，由于有去重操作因此效率较低</p>
<p><strong>union all</strong>：A∪B，不去重</p>
<pre><code class="sql">SELECT last_name,department_name
FROM employees e FULL OUTER JOIN departmnets d
ON e.department_id=d.department_id;
</code></pre>
<p><strong>左上图union alll右中图&#x3D;满连接</strong></p>
<pre><code class="sql">SELECT last_name,department_name
FROM employees e LEFT OUTER JOIN departments d
ON e.department_id=d.department_id
WHERE d.department_id IS NULL
UNION ALL
SELECT last_name,department_name
FROM employees e RIGHT OUTER JOIN departments d
ON e.department_id=d.department_id;
</code></pre>
<h3 id="多表连接练习"><a href="#多表连接练习" class="headerlink" title="多表连接练习"></a>多表连接练习</h3><p><strong>练习1</strong>：显示所有员工的姓名，部门号和部门名称</p>
<pre><code class="sql">SELECT last_name,d.department_id,department_name
FROM employees e LEFT OUTER JOIN departments d
ON e.department_id=d.department_id;
</code></pre>
<p><strong>练习2</strong>：查询90号部门的员工job_id和location_id</p>
<pre><code class="sql">SELECT e.job_id,d.location_id
FROM employees e JOIN departments d
ON e.department_id=d.department_id
WHERE d.department_id=90;
</code></pre>
<p><strong>练习3</strong>:查询所有有奖金的员工的e.last_name,d.department_name,l.location_id,l.city</p>
<pre><code class="sql">SELECT e.last_name,d.department_name,l.location_id,l.city
FROM employees e LEFT OUTER JOIN departments d
ON e.department_id=d.department_id
LEFT JOIN locations l
ON d.location_id=l.location_id
WHERE e.commission_pct IS NOT NULL;
</code></pre>
<p><strong>练习4</strong>：选择城市在Toronto工作的员工的e.last_name,j.job_id,d.department_id,d.department_name,city</p>
<pre><code class="sql">SELECT e.last_name,j.job_id,d.department_id,d.department_name,city
FROM employees e JOIN departments d
ON e.department_id=d.department_id
JOIN jobs j
ON e.job_id=j.job_id
JOIN locations l
ON d.location_id=l.location_id
WHERE city=&#39;Toronto&#39;;
</code></pre>
<p><strong>练习5</strong>:查询员工所在的部门名称、部门地址、姓名、工作、工资，其中员工所在部门的部门名称为Executive</p>
<pre><code class="sql">SELECT d.department_name,l.street_address,e.last_name,e.job_id,e.salary
FROM departments d LEFT JOIN employees e 
ON e.department_id=d.department_id
LEFT JOIN locations l
ON d.location_id=l.location_id
WHERE d.department_name=&#39;Executive&#39;;
</code></pre>
<p><strong>练习6</strong>：选择指定员工的姓名、员工号，及其管理者的姓名、员工号</p>
<pre><code class="sql">SELECT emp.last_name emp_name,emp.employee_id emp_id,mgr.last_name mgr_name,mgr.employee_id mgr_id
FROM employees emp LEFT JOIN employees mgr
ON emp.manager_id=mgr.employee_id;
</code></pre>
<p><strong>练习7</strong>：查询哪些部门没有员工</p>
<pre><code class="sql">SELECT last_name,department_name
FROM employees e RIGHT OUTER JOIN departments d
ON e.department_id=d.department_id
WHERE e.department_id IS  NULL;
</code></pre>
<p><strong>练习8</strong>：查询哪个城市没有部门</p>
<pre><code class="sql">SELECT city,department_name
FROM locations l LEFT OUTER JOIN departments d
ON l.location_id=d.location_id
WHERE d.location_id IS NULL;
</code></pre>
<p><strong>练习9</strong>：查询部门名为Sales和IT的员工信息</p>
<pre><code class="sql">SELECT e.*,department_name
FROM employees e JOIN departments d
ON e.department_id=d.department_id
WHERE department_name IN(&#39;Sales&#39;,&#39;IT&#39;);
</code></pre>
<h1 id="单行函数"><a href="#单行函数" class="headerlink" title="单行函数"></a>单行函数</h1><h2 id="数值函数"><a href="#数值函数" class="headerlink" title="数值函数"></a>数值函数</h2><p><strong>普通函数</strong></p>
<table>
<thead>
<tr>
<th>函数</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td>ABS(X)</td>
<td>绝对值</td>
</tr>
<tr>
<td>SIGN（X）</td>
<td>符号函数</td>
</tr>
<tr>
<td>PI（）</td>
<td>Π值</td>
</tr>
<tr>
<td>CEIL（x）</td>
<td>向下取整</td>
</tr>
<tr>
<td>FLOOR(X)</td>
<td>向上取整</td>
</tr>
<tr>
<td>LEAST(X1,X2,X3…)</td>
<td>最小值</td>
</tr>
<tr>
<td>GREATEST（X1,X2,X3…）</td>
<td>最大值</td>
</tr>
<tr>
<td>RAND（）</td>
<td>0~1之间的随机数</td>
</tr>
<tr>
<td>RAND(X)</td>
<td>0~1之间随机数，x表示随机数种子</td>
</tr>
<tr>
<td>ROUND（x,y）</td>
<td>四舍五入，保留x的y位小数</td>
</tr>
<tr>
<td>TRUNCATE(x,y)</td>
<td>截断x的y位小数</td>
</tr>
<tr>
<td>SQRT(x)</td>
<td>开根号</td>
</tr>
</tbody></table>
<pre><code class="sql">SELECT ABS(-5),PI(),CEIL(42.5),FLOOR(42.3),RAND(),RAND(10),ROUND(255.6),ROUND(123.456,2),TRUNCATE(123.589,1)
FROM DUAL;
</code></pre>
<p><strong>三角函数</strong></p>
<p><strong>进制转换</strong></p>
<h2 id="字符串函数"><a href="#字符串函数" class="headerlink" title="字符串函数"></a>字符串函数</h2><table>
<thead>
<tr>
<th>函数</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td>ASCII</td>
<td>返回第一个字符的ascii值</td>
</tr>
<tr>
<td>length</td>
<td>返回字符串占用字节数</td>
</tr>
<tr>
<td>char_length</td>
<td>返回汉字个数</td>
</tr>
<tr>
<td>concat(s1,s2，s3,…,sn)</td>
<td>拼接字符串</td>
</tr>
<tr>
<td>concat_ws(x,s1,s2,s3,…,sn)</td>
<td>拼接字符串，并在每个字符串之间添加一个连接符x</td>
</tr>
<tr>
<td>insert(str,idx,len,repstr)</td>
<td>将str从idx位置开始的len个长度的字符替换为repstr</td>
</tr>
<tr>
<td>replace（str,a,b）</td>
<td>将str中的a替换为b</td>
</tr>
<tr>
<td>upper（str）</td>
<td>将str转换为大写</td>
</tr>
<tr>
<td>lower（str）</td>
<td>将str转换为小写</td>
</tr>
<tr>
<td>left（str，n）</td>
<td>取出str最左边的n个字符</td>
</tr>
<tr>
<td>right（str，n）</td>
<td>取出str最右边的n个字符</td>
</tr>
<tr>
<td>trim（str）</td>
<td>去除str两边空格</td>
</tr>
<tr>
<td>ltrim（str）</td>
<td>去除str左边空格</td>
</tr>
<tr>
<td>rtrim</td>
<td>去除str右边空格</td>
</tr>
<tr>
<td>lpad（str，len,x）</td>
<td>用x对str进行左填充，直到所有字符个数满足len为止，常用于右对齐</td>
</tr>
<tr>
<td>rpad（str，len，x）</td>
<td>用x对str进行右填充，直到所有字符个数满足len为止，常用于左对齐</td>
</tr>
<tr>
<td>strcmp（str1，str2）</td>
<td>字符串比较</td>
</tr>
<tr>
<td>substr（str，idx，len）</td>
<td>取str从idx开始的len个字符</td>
</tr>
<tr>
<td>locate（substr，str）</td>
<td>返回substr在str中第一次出现的位置</td>
</tr>
<tr>
<td>elt（m,s1,s2,s3）</td>
<td>返回第m个字符串</td>
</tr>
</tbody></table>
<p><strong>练习</strong></p>
<pre><code class="sql">SELECT ASCII(&#39;abc&#39;),LENGTH(&#39;ab1&#39;),LENGTH(&#39;中国&#39;),CHAR_LENGTH(&#39;中国&#39;)
FROM DUAL;

SELECT CONCAT(&#39;hello&#39;,&#39;-&#39;,&#39;world&#39;),CONCAT_WS(&#39;-&#39;,&#39;a&#39;,&#39;b&#39;,&#39;c&#39;)

SELECT INSERT(&#39;hello&#39;,2,3,&#39;ax&#39;),REPLACE(&#39;helloworld&#39;,&#39;wor&#39;,&#39;abcsx&#39;)

SELECT UPPER(&#39;abcx&#39;),LOWER(&#39;ANXS&#39;)

SELECT LEFT(&#39;abc&#39;,2),RIGHT(&#39;asdfg&#39;,3)

SELECT LPAD(last_name,12,&#39;*&#39;),RPAD(salary,15,&#39;*&#39;)
FROM employees

SELECT LTRIM(&#39;  lo jh h g   &#39;),RTRIM(&#39;  lo jh h g   &#39;),TRIM(&#39;  lo jh h g   &#39;)

SELECT REPEAT(&#39;i love you  &#39;,3),STRCMP(&#39;abc&#39;,&#39;abx&#39;)

SELECT SUBSTR(&#39;helloworld&#39;,2,5),LOCATE(&#39;s&#39;,&#39;aabsbsbs&#39;),ELT(2,&#39;sl&#39;,&#39;s2&#39;,&#39;s3&#39;)
</code></pre>
<h2 id="日期时间函数"><a href="#日期时间函数" class="headerlink" title="日期时间函数"></a>日期时间函数</h2><h2 id="流程控制函数"><a href="#流程控制函数" class="headerlink" title="流程控制函数"></a>流程控制函数</h2><p><strong>if函数</strong></p>
<pre><code class="sql">SELECT last_name,salary,IF(salary&gt;=8000,&#39;高工资&#39;,&#39;低工资&#39;) &#39;details&#39;
FROM employees;
</code></pre>
<p><strong>ifnull（value1，value2）如果value1为null，则返回value2，否则返回原值</strong></p>
<pre><code class="sql">SELECT last_name,commission_pct,IFNULL(commission_pct,0) &#39;details&#39;
FROM employees;
</code></pre>
<p><strong>if else函数</strong></p>
<pre><code class="sql">SELECT last_name,salary,CASE WHEN salary&gt;6000 THEN &#39;小屌丝&#39;
                 WHEN salary &gt;8000 THEN &#39;高薪&#39;
                 WHEN salary&gt;12000 THEN &#39;白骨精&#39;
                 END &#39;detail&#39;,department_id
FROM employees;
</code></pre>
<p><strong>switch case 函数</strong></p>
<pre><code class="sql">SELECT last_name,department_id,CASE department_id WHEN 10 THEN salary*2
                          WHEN 20 THEN salary*3
                          WHEN 30 THEN salary*4 
                          END &#39;detail&#39;
FROM employees;
</code></pre>
<h2 id="加密解密函数"><a href="#加密解密函数" class="headerlink" title="加密解密函数"></a>加密解密函数</h2><h2 id="MySQL信息函数"><a href="#MySQL信息函数" class="headerlink" title="MySQL信息函数"></a>MySQL信息函数</h2><h1 id="聚合函数"><a href="#聚合函数" class="headerlink" title="聚合函数"></a>聚合函数</h1><h2 id="常见的聚合函数"><a href="#常见的聚合函数" class="headerlink" title="常见的聚合函数"></a>常见的聚合函数</h2><p><strong>avg\sum\count三者均不计算null值</strong></p>
<pre><code class="sql">SELECT AVG(salary) avg_salary,SUM(salary)
FROM employees;
</code></pre>
<p><strong>注</strong>：avg（）与sum（）函数仅使用于<strong>数值型数据</strong></p>
<pre><code class="sql">SELECT MAX(hire_date),MIN(hire_date)
FROM employees;
</code></pre>
<p><strong>注</strong>：max（）、min（）、count（）函数可用于<strong>任意型数据</strong></p>
<p><strong>avg（）&#x3D;sum（）&#x2F;count（）</strong></p>
<pre><code class="sql">SELECT COUNT(*),COUNT(last_name)
FROM employees

SELECT COUNT(IFNULL(commission_pct,0))
FROM employees;

SELECT AVG(commission_pct),SUM(commission_pct)/COUNT(IFNULL(commission_pct,0)),SUM(commission_pct)/107
FROM employees;
结果：0.222   0.072   0.072
</code></pre>
<p><strong>注</strong>：由于count（）函数不计算null值，且avg（）函数被除数使用count（）计算，因此计算含有null值的平均数的字段时，需要重新定义均值函数</p>
<h2 id="group-by使用"><a href="#group-by使用" class="headerlink" title="group by使用"></a>group by使用</h2><p><strong>声明在from、where之后，order by、limit之前</strong></p>
<p><strong>练习</strong>：计算各部门的平均工资</p>
<pre><code class="sql">SELECT department_id,AVG(salary) avg_sal
FROM employees
GROUP BY department_id;
</code></pre>
<p><strong>练习</strong>：计算各部门内各工种的平均工资</p>
<pre><code class="sql">SELECT department_id,job_id,AVG(salary) avg_sal
FROM employees
GROUP BY department_id,job_id
ORDER BY department_id;
</code></pre>
<p><strong>练习</strong>：计算各部门平均工资以及所有部门平均工资</p>
<pre><code class="sql">SELECT department_id,AVG(salary) avg_sal
FROM employees
GROUP BY department_id WITH ROLLUP;
</code></pre>
<p><strong>注</strong>：with rollup可将分组后的平均数再次计算所有平均数</p>
<h2 id="having使用"><a href="#having使用" class="headerlink" title="having使用"></a>having使用</h2><p><strong>聚合函数的过滤</strong></p>
<p><strong>练习</strong>：显示各部门的平均工资中大于4000的部门</p>
<pre><code class="sql">SELECT department_id,AVG(salary) avg_sal
FROM employees
GROUP BY department_id
HAVING avg_sal&gt;4000;
</code></pre>
<p><strong>注</strong>：having必须和group by配合使用</p>
<p><strong>练习</strong>：显示部门号为10，20，30的平均工资中大于4000的部门</p>
<pre><code class="sql">SELECT department_id,AVG(salary) avg_sal
FROM employees
WHERE department_id IN(10,20,30)
GROUP BY department_id
HAVING avg_sal&gt;4000;
</code></pre>
<p><strong>注</strong>：</p>
<ol>
<li>不能在where中使用聚合函数过滤，因为聚合函数在where之后执行</li>
<li>having可过滤非聚合字段，但一般不使用，因为having是先连接后过滤，而where是先过滤后连接，因此从效率上讲，应当把非聚合函数放在where后过滤，将聚合函数放在having后过滤</li>
</ol>
<h2 id="sql底层原理"><a href="#sql底层原理" class="headerlink" title="sql底层原理"></a>sql底层原理</h2><h3 id="sql查询结构"><a href="#sql查询结构" class="headerlink" title="sql查询结构"></a>sql查询结构</h3><p><strong>方式1</strong></p>
<pre><code class="sql">select 查询字段
from 表
where 多表连接条件 and 非聚合过滤 
group by 分组字段
having 聚合函数过滤
order by （asc\desc）排序字段
limit 
</code></pre>
<p><strong>方式2</strong></p>
<pre><code class="sql">SELECT ...,....,...
FROM ... JOIN ...
ON 多表的连接条件
JOIN ...
ON ...
WHERE 不包含组函数的过滤条件
AND/OR 不包含组函数的过滤条件
GROUP BY ...,...
HAVING 包含组函数的过滤条件
ORDER BY ... ASC/DESC
LIMIT ...,...
</code></pre>
<h3 id="sql语句执行顺序"><a href="#sql语句执行顺序" class="headerlink" title="sql语句执行顺序"></a>sql语句执行顺序</h3><p><strong>from 	-&gt;	where 	-&gt;	group by	-&gt;	having	-&gt;	select	-&gt;	distinct	-&gt;	order by	-&gt;	limit</strong></p>
<p><strong>练习</strong>：查询各个管理者手下员工的最低工资，其中最低工资不能低于6000。 没有管理者的员工不计算在内</p>
<pre><code class="sql">SELECT manager_id,MIN(salary) m_w_minsal
FROM employees
WHERE manager_id IS NOT NULL
GROUP BY manager_id
HAVING m_w_minsal&gt;=6000;	
</code></pre>
<p><strong>练习</strong>：.查询所有部门的名字，location. _id，员工数量和平均工资。并按平均工资降序</p>
<pre><code class="sql">SELECT department_name,location_id,COUNT(employee_id),AVG(salary) avg_sal
FROM employees e LEFT JOIN departments d
ON e.department_id=d.department_id
GROUP BY department_name,location_id
ORDER BY avg_sal DESC;
</code></pre>
<h1 id="子查询"><a href="#子查询" class="headerlink" title="子查询"></a>子查询</h1><p><strong>引例</strong>：谁的工资比Abel多？</p>
<p><strong>实现方式1</strong>：（自连接）</p>
<pre><code class="sql">SELECT e1.last_name,e1.salary 
FROM employees e1 JOIN employees e2
ON e1.salary&gt;e2.salary
WHERE e2.last_name=&#39;Abel&#39;
</code></pre>
<p><strong>实现方式2</strong>：（子查询）</p>
<pre><code class="sql">SELECT last_name,salary
FROM employees
WHERE salary&gt;(
    SELECT salary
    FROM employees
    WHERE last_name=&#39;Abel&#39;
    );
</code></pre>
<h2 id="单行子查询"><a href="#单行子查询" class="headerlink" title="单行子查询"></a>单行子查询</h2><p><strong>练习</strong>：查询工资大于149号员工工资的员工信息</p>
<pre><code class="sql">SELECT * 
FROM employees
WHERE salary&gt;(
        SELECT salary
        FROM employees
        WHERE employee_id=149
        );
</code></pre>
<p><strong>练习</strong>：查询job_id和141号员工job_id相同，且工资大于143号员工工资的员工last_name,job_id,salary</p>
<pre><code class="sql">SELECT last_name,job_id,salary
FROM employees
WHERE job_id=(
        SELECT job_id
        FROM employees
        WHERE employee_id=141
        )
AND salary&gt;(
        SELECT salary
        FROM employees
        WHERE employee_id=143
        );		
</code></pre>
<p><strong>练习</strong>：返回工资最少的员工last_name,job_id,salary</p>
<pre><code class="sql">SELECT last_name,job_id,salary
FROM employees
WHERE salary=(
        SELECT MIN(salary)
        FROM employees
        );
</code></pre>
<p><strong>练习</strong>：查询与141号或174号员工的manager_id和department_id相同的其他员工的employee_id，<br>manager_id，department_id</p>
<pre><code class="sql">SELECT employee_id,manager_id,department_id
FROM employees
WHERE manager_id=(
            SELECT manager_id
            FROM employees
            WHERE employee_id =141
        )
AND department_id=(
            SELECT department_id
            FROM employees
            WHERE employee_id =141
            )
AND emploee_id&lt;&gt;141;
</code></pre>
<p><strong>having子查询练习</strong>：查询最低工资大于50号部门最低工资的部门id和其最低工资</p>
<pre><code class="sql">SELECT department_id,MIN(salary) min_sal
FROM employees
WHERE department_id IS NOT NULL
GROUP BY department_id
HAVING min_sal&gt;(
        SELECT MIN(salary)
        FROM employees
        WHERE department_id=110
        );
</code></pre>
<p><strong>流程控制子查询练习</strong>：查询员工的employee_id,last_name，location</p>
<p>其中，如果员工的department_id与location_id为1800的department_id相同，则location为Canana，否则其余为USA</p>
<pre><code class="sql">SELECT employee_id,last_name, IF(department_id=(SELECT department_id 
                        FROM departments
                        WHERE location_id=1800 ),
                &#39;Canana&#39;,&#39;USA&#39;) &#39;location&#39;
FROM employees
</code></pre>
<pre><code class="sql">SELECT employee_id,last_name,CASE department_id WHEN (SELECT department_id 
                              FROM departments 
                              WHERE location_id=1800)
                            THEN &#39;Canana&#39;
                            ELSE &#39;USA&#39;
                 END &#39;location&#39;
FROM employees
</code></pre>
<h2 id="多行子查询"><a href="#多行子查询" class="headerlink" title="多行子查询"></a>多行子查询</h2><p><strong>子查询结果是由多条记录组成的一张表</strong></p>
<p><strong>IN   ANY   ALL</strong></p>
<p><strong>IN</strong>:符合子查询结果中的<strong>其中一个</strong></p>
<p><strong>练习</strong>：查询员工工资符合各部门最小工资的员工信息</p>
<pre><code class="sql">SELECT employee_id,last_name
FROM employees
WHERE salary IN(
        SELECT MIN(salary)
        FROM employees
        GROUP BY department_id
        )
</code></pre>
<p><strong>ANY</strong>:符合子查询结果中<strong>任意一个</strong></p>
<p><strong>练习</strong>：返回其他job_id中比job_id为‘IT_PROG’部门任一工资低的员工的员工号</p>
<pre><code class="sql">SELECT employee_id,job_id,salary
FROM employees
WHERE job_id&lt;&gt;&#39;IT_PROG&#39;
AND salary &lt; ANY (
          SELECT salary
          FROM employees
          WHERE job_id=&#39;IT_PROG&#39;
          )
</code></pre>
<p><strong>ALL</strong>：符合子查询结果中<strong>所有值</strong></p>
<p><strong>练习</strong>：查询平均工资最低的部门id</p>
<p><strong>解1：</strong></p>
<pre><code class="sql">SELECT department_id,AVG(salary) avg_sal
FROM employees
GROUP BY department_id
HAVING avg_sal &lt;= ALL(
            SELECT AVG(salary)
            FROM employees
            GROUP BY department_id
            )
</code></pre>
<p><strong>解2</strong>：</p>
<pre><code class="sql">SELECT department_id
FROM employees
GROUP BY department_id
HAVING AVG(salary)=(
    -- 查询各部门最小的平均工资
            SELECT MIN(avg_sal)
            FROM (
                -- 查询各部门平均工资，返回一张表，若该表作为子查询的表，需将其进行别名命名
              SELECT AVG(salary) avg_sal
              FROM employees
              GROUP BY department_id
            ) t_avg_sal
            )
</code></pre>
<p><strong>注</strong>：聚合函数不能嵌套使用</p>
<p><strong>多行子查询的空值问题</strong></p>
<pre><code class="sql">SELECT last_name
FROM employees
WHERE employee_id NOT IN(
    -- 若子查询结果中存在空值，则整个查询结果不显示结果
            SELECT manager_id
            FROM employees
            WHERE manager_id IS NOT NULL
            )
</code></pre>
<h2 id="相关子查询"><a href="#相关子查询" class="headerlink" title="相关子查询"></a>相关子查询</h2><p><strong>在子查询中使用外部表</strong></p>
<p><strong>练习</strong>：查询员工工资大于公司平均工资的员工信息</p>
<pre><code class="sql">SELECT last_name,salary,department_id
FROM employees 
WHERE salary &gt; (
        SELECT AVG(salary)
        FROM employees
        )
</code></pre>
<p><strong>练习</strong>：查询员工工资大于本部门平均工资的员工信息</p>
<p><strong>解1</strong>：使用相关子查询</p>
<pre><code class="sql">SELECT last_name,salary,department_id
FROM employees e1
WHERE salary &gt; (
        SELECT AVG(salary)
        FROM employees
        WHERE department_id=e1.department_id-- 子查询中使用外部表
        )
</code></pre>
<p><strong>解2</strong>：在from中声明子查询</p>
<pre><code class="sql">SELECT e1.last_name,e1.salary,e1.department_id
FROM employees e1,(
    -- 在子查询中查出各部门的平均工资作为新的查询表，并通过department_id与员工表进行连接
           SELECT department_id,AVG(salary) avg_sal
           FROM employees
           GROUP BY department_id
           ) e2
WHERE e1.department_id=e2.department_id
AND e1.salary&gt;e2.avg_sal
</code></pre>
<p><strong>注</strong>：除了group by 与 limit之外，其他位置均可使用子查询</p>
<p><strong>练习</strong>：查询员工号和工资，并按照部门名降序排列</p>
<pre><code class="sql">FROM employees e
ORDER BY(
    SELECT department_name
    FROM departments d
    WHERE e.department_id=d.department_id
    )DESC
</code></pre>
<p><strong>exists关键字</strong>：满足子查询的结果，就返回true并保留记录，否则返回false不保留记录</p>
<p><strong>练习</strong>：查询管理者的employee_id,last_name,department_id</p>
<p><strong>解1</strong>：使用多表连接</p>
<pre><code class="sql">SELECT DISTINCT mgr.employee_id,mgr.last_name,mgr.department_id
FROM employees emp JOIN employees mgr
ON emp.manager_id=mgr.employee_id
</code></pre>
<p><strong>解2</strong>：使用子查询</p>
<pre><code class="sql">SELECT employee_id,last_name,department_id
FROM employees
WHERE employee_id IN(
            SELECT DISTINCT manager_id
            FROM employees
            )
</code></pre>
<p><strong>解3</strong>：使用exists关键字</p>
<pre><code class="sql">SELECT employee_id,last_name,department_id
FROM employees e1
WHERE EXISTS(
        SELECT * -- 使用exists关键字时，子查询中的select字段无关
        FROM employees e2
        WHERE e1.employee_id=e2.manager_id
        )
</code></pre>
<p><strong>not exists关键字</strong></p>
<p><strong>练习</strong>：查询departments表中，不存在于employees表中的部门的department_id与department_name</p>
<p><strong>解1</strong>：多表连接</p>
<pre><code class="sql">SELECT e.department_id,d.department_id,d.department_name
FROM employees e RIGHT JOIN departments d
ON e.department_id=d.department_id
WHERE e.department_id IS NULL
</code></pre>
<p><strong>解2</strong>：子查询</p>
<pre><code class="sql">SELECT department_id,department_name
FROM departments d
WHERE NOT EXISTS(
        SELECT* 
        FROM employees e
        WHERE e.department_id=d.department_id
        )
</code></pre>
<h2 id="子查询习题"><a href="#子查询习题" class="headerlink" title="子查询习题"></a>子查询习题</h2><p>#<strong>1</strong>.查询和Zlotkey相同部门的员工姓名和工资</p>
<pre><code class="sql">SELECT last_name,salary
FROM employees
WHERE department_id =(
            SELECT department_id 
            FROM employees
            WHERE last_name=&#39;Zlotkey&#39;
            )
</code></pre>
<p>#<strong>2</strong>.查询工资比公司平均工资高的员工的员工号，姓名和工资。</p>
<pre><code class="sql">SELECT job_id,last_name,salary
FROM employees
WHERE salary&gt; (
        SELECT AVG(salary)
        FROM employees
        )
</code></pre>
<p>#<strong>3</strong>.选择工资大于所有JOB_ID&#x3D;‘SA_ MAN’的员工的工资的员工的last_name, job_id, salary</p>
<pre><code class="sql">SELECT last_name,job_id,salary
FROM employees
WHERE salary &gt; ALL(
        SELECT salary
        FROM employees
        WHERE job_id=&#39;SA_MAN&#39;
        )
</code></pre>
<p>#<strong>4</strong>.查询和姓名中包含字母u的员工在相同部门的员工的员工号和姓名</p>
<pre><code class="sql">SELECT job_id,last_name
FROM employees
WHERE department_id IN(
            SELECT department_id
            FROM employees
            WHERE last_name LIKE &#39;%u%&#39;
            )
</code></pre>
<p>#<strong>5</strong>.查询在部门的location_id为1700的部门工作的员工的员工。</p>
<pre><code class="sql">SELECT employee_id,last_name
FROM employees
WHERE department_id IN (
        SELECT department_id
        FROM departments
        WHERE location_id=1700
        )
</code></pre>
<p>#<strong>6</strong>.查询管理者是King的员工姓名和工资</p>
<pre><code class="sql">SELECT emp.last_name,emp.salary
FROM employees emp JOIN employees mgr
ON emp.manager_id=mgr.employee_id
WHERE mgr.last_name=&#39;King&#39;

SELECT last_name,salary
FROM employees
WHERE manager_id IN (
            SELECT employee_id
            FROM employees
            WHERE last_name=&#39;King&#39;
            )
</code></pre>
<p>#<strong>7</strong>.查询工资最低的员工信息: last. name, salary</p>
<pre><code class="sql">SELECT last_name,salary
FROM employees
WHERE salary = (
        SELECT MIN(salary)
        FROM employees
        )
</code></pre>
<p>#<strong>8</strong>.查询平均工资最低的部门信息</p>
<pre><code class="sql">-- 方式1
SELECT * 
FROM departments
WHERE department_id=(
        -- 获取平均工资最小的部门id
        SELECT department_id
        FROM employees
        GROUP BY department_id
        HAVING AVG(salary)  = (	-- 查询各部门平均工资最小值
                    SELECT MIN(avg_sal)
                    FROM (-- 查询各部门平均工资
                        SELECT AVG(salary) avg_sal
                        FROM employees
                        GROUP BY department_id
                        ) t_dp_avg_sal					
                    )

​		)

-- 方式2
SELECT * 
FROM departments
WHERE department_id=(
        -- 获取平均工资最小的部门id
        SELECT department_id
        FROM employees
        GROUP BY department_id
        HAVING AVG(salary) &lt;= ALL(	-- 查询各部门平均工资最小值
                    SELECT AVG(salary) avg_sal
                    FROM employees
                    GROUP BY department_id
                    )					
        )
        
-- 方式3
SELECT * 
FROM departments
WHERE department_id=(
        -- 获取平均工资最小的部门id
        SELECT department_id
        FROM employees
        GROUP BY department_id
        HAVING AVG(salary) = (	-- 查询各部门平均工资最小值
                    SELECT AVG(salary) avg_sal
                    FROM employees
                    GROUP BY department_id
                    ORDER BY avg_sal
                    LIMIT 0,1
                    )					
        )

-- 方式4	
SELECT d.*
FROM departments d,(
        SELECT department_id,AVG(salary) avg_sal
        FROM employees
        GROUP BY department_id
        ORDER BY avg_sal
        LIMIT 0,1
        )t_avg_sal
WHERE d.department_id=t_avg_sal.department_id
</code></pre>
<p>#<strong>9</strong>.查询平均工资最低的部门信息和该部门的平均工资(相关子查询)</p>
<pre><code class="sql">SELECT d.*,(SELECT AVG(salary) FROM employees WHERE department_id=d.department_id) avg_sal
FROM departments d,(
        SELECT department_id,AVG(salary) avg_sal
        FROM employees
        GROUP BY department_id
        ORDER BY avg_sal
        LIMIT 0,1
        )t_avg_sal
WHERE d.department_id=t_avg_sal.department_id
</code></pre>
<p>#<strong>10</strong>.查询平均工资最高的job信息</p>
<pre><code class="sql">-- 方式1
SELECT *
FROM jobs
WHERE job_id=(
        SELECT job_id
        FROM employees
        GROUP BY job_id
        HAVING AVG(salary) &gt;=ALL(
                    SELECT AVG(salary)
                    FROM employees
                    GROUP BY job_id
                    )
)
-- 方式2
SELECT *
FROM jobs
WHERE job_id =(
        SELECT job_id
        FROM employees
        GROUP BY job_id
        HAVING AVG(salary)=(
                SELECT AVG(salary) avg_sal
                FROM employees
                GROUP BY job_id
                ORDER BY avg_sal DESC
                LIMIT 0,1
                )
        )
</code></pre>
<p>#<strong>11</strong>.查询平均工资高于公司平均工资的部门的哪些?</p>
<pre><code class="sql">SELECT department_id,AVG(salary) avg_sal
FROM employees
WHERE department_id IS NOT NULL
GROUP BY department_id
HAVING avg_sal&gt;(
        SELECT AVG(salary)
        FROM employees
        )
</code></pre>
<p>#<strong>12</strong>.查询出公司中所有manager 的详细信息</p>
<pre><code class="sql">SELECT DISTINCT mgr.employee_id,mgr.salary
FROM employees emp JOIN employees mgr
ON emp.manager_id=mgr.employee_id

SELECT DISTINCT employee_id,salary
FROM employees
WHERE employee_id IN (
            SELECT manager_id
            FROM employees
            )
</code></pre>
<p>#<strong>13</strong>.各个部门中最高工资中最低的那个部门的最低工资是多少？</p>
<pre><code class="sql">-- 方式1
SELECT department_id,salary
FROM employees
HAVING salary = (
        SELECT MIN(max_sal)
        FROM (
            SELECT MAX(salary) max_sal
            FROM employees
            GROUP BY department_id
            )t_max_sal
        )
-- 方式2
SELECT e.department_id,e.salary
FROM employees e,(
        SELECT department_id,MAX(salary) max_sal
        FROM employees
        GROUP BY department_id
        ORDER BY max_sal
        LIMIT 0,1
        )t_max_sal
WHERE e.department_id=t_max_sal.department_id
</code></pre>
<p>#<strong>14</strong>.查询平均工资最高的部门的manager的详细: last_name, department_id, email, salary<br>mgr.last_name, mgr.department_id, mgr.email, mgr.salary</p>
<pre><code class="sql">-- 方式1
SELECT last_name,department_id,email,salary
FROM employees
WHERE employee_id=(
        -- 获取在平均工资最高的部门中工作的经理的manager_id
        SELECT DISTINCT manager_id
        FROM employees
        WHERE department_id=(
                -- 获取平均工资最高的部门id
                SELECT department_id
                FROM employees
                GROUP BY department_id
                HAVING AVG(salary)=(-- 查询各部门最高的平均工资
                        SELECT MAX(avg_sal)
                        FROM (		
                            SELECT department_id,AVG(salary) avg_sal
                            FROM employees
                            WHERE department_id IS NOT NULL
                            GROUP BY department_id
                            ) t_avg_sal
                )
            AND manager_id IS NOT NULL
        )	
)
-- 方式2
SELECT last_name,department_id,email,salary
FROM employees
WHERE employee_id IN (
        -- 获取在平均工资最高的部门中工作的经理的manager_id
        SELECT DISTINCT manager_id
        FROM employees
        WHERE department_id=(
                -- 获取平均工资最高的部门id
                SELECT department_id
                FROM employees
                GROUP BY department_id
                HAVING AVG(salary)=(-- 查询各部门最高的平均工资
                        SELECT AVG(salary) avg_sal
                        FROM employees
                        WHERE department_id IS NOT NULL
                        GROUP BY department_id
                        ORDER BY avg_sal DESC
                        LIMIT 0,1
                            ) 
                )
)	

SELECT last_name,department_id,email,salary
FROM employees
WHERE employee_id IN (
        -- 获取在平均工资最高的部门中工作的经理的manager_id
        SELECT DISTINCT manager_id
        FROM employees e,(
                -- 查询各部门最高的平均工资
                SELECT department_id,AVG(salary) avg_sal
                FROM employees
                WHERE department_id IS NOT NULL
                GROUP BY department_id
                ORDER BY avg_sal DESC
                LIMIT 0,1					 
                )t_max_sal
        WHERE e.department_id=t_max_sal.department_id
)

</code></pre>
<p><strong>15</strong>.查询部门的部门号，其中不包括job_id是‘ST_CLERK’的部门号</p>
<pre><code class="sql">SELECT department_id
FROM employees
WHERE  department_id!=(
        SELECT DISTINCT department_id
        FROM employees
        WHERE job_id=&#39;ST_CLERK&#39;
        )

SELECT department_id
FROM employees e
WHERE NOT EXISTS(
        SELECT *
        FROM departments d
        WHERE e.department_id=d.department_id
        AND job_id=&#39;ST_CLERK&#39;
        )

</code></pre>
<p>#<strong>16</strong>.选择所有没有管理者的员工的last_ name</p>
<pre><code class="sql">SELECT *
FROM employees
WHERE last_name=&#39;King&#39;

SELECT last_name
FROM employees
WHERE manager_id IS NULL
#17.查询员工号、姓名、雇用时间、工资，其中员工的管理者为&quot;De Haan &quot;

SELECT employee_id,last_name,hire_date,salary
FROM employees
WHERE manager_id IN(
        SELECT employee_id
        FROM employees
        WHERE last_name=&#39;De Haan&#39;
        )
</code></pre>
<p>#<strong>18</strong>.查询各部门中工资比本部门平均工资高的员工的员工号，姓名和工资(相关子查询)</p>
<pre><code class="sql">SELECT employee_id,last_name,salary
FROM employees e
WHERE salary &gt;(
        SELECT AVG(salary)
        FROM employees
        WHERE department_id=e.department_id
        )
</code></pre>
<p>#<strong>19</strong>.查询每个部门下的部门人数大于5的部门名称(相关子查询)</p>
<pre><code class="sql">SELECT department_name
FROM departments d
WHERE 5 &lt; (
    SELECT COUNT(*)
    FROM employees
    WHERE department_id=d.department_id
    )
</code></pre>
<p>#<strong>20</strong>.查询每个国家下的部门个数大于2的国家编号(相关子查询)</p>
<h1 id="数据库的创建与删除"><a href="#数据库的创建与删除" class="headerlink" title="数据库的创建与删除"></a>数据库的创建与删除</h1><h2 id="创建数据库"><a href="#创建数据库" class="headerlink" title="创建数据库"></a>创建数据库</h2><pre><code class="sql">CREATE DATABASE test1

CREATE DATABASE IF NOT EXISTS test2;

CREATE DATABASE IF NOT EXISTS test3 CHARACTER SET &#39;UTF8&#39;-- 创建数据库并设定字符集
</code></pre>
<h2 id="使用数据库"><a href="#使用数据库" class="headerlink" title="使用数据库"></a>使用数据库</h2><pre><code class="sql">SHOW DATABASES

SHOW TABLES
</code></pre>
<h2 id="删除数据库"><a href="#删除数据库" class="headerlink" title="删除数据库"></a>删除数据库</h2><pre><code class="sql">DROP DATABASE test1

DROP DATABASE IF EXISTS test2
</code></pre>
<h1 id="创建和管理表"><a href="#创建和管理表" class="headerlink" title="创建和管理表"></a>创建和管理表</h1><h2 id="创建表"><a href="#创建表" class="headerlink" title="创建表"></a>创建表</h2><p><strong>方式1</strong></p>
<pre><code class="sql">CREATE TABLE IF NOT EXISTS mytemp1
(
sid INT,
sname VARCHAR(20),
hireDate DATE
);
</code></pre>
<p>#查看创建表的语句结构</p>
<pre><code class="sql">SHOW CREATE TABLE mytemp1
</code></pre>
<p><strong>方式2</strong>：基于现有的表，同时导入数据</p>
<pre><code class="sql">CREATE TABLE mytemp2
AS 
SELECT employee_id,last_name,salary
FROM employees
</code></pre>
<p><strong>练习</strong>：创建与员工表相同的字段，且不导入数据</p>
<pre><code class="sql">CREATE TABLE mytemp3
AS 
SELECT *
FROM employees
WHERE 1=2
</code></pre>
<h2 id="修改表"><a href="#修改表" class="headerlink" title="修改表"></a>修改表</h2><h3 id="添加字段"><a href="#添加字段" class="headerlink" title="添加字段"></a>添加字段</h3><pre><code class="sql">-- 默认添加在最后
ALTER TABLE mytemp1
ADD salary DOUBLE(10,2)-- 添加一个工资字段，数据类型为double，共十位数，其中2位小数
-- 添加在第一个字段位置
ALTER TABLE mytemp1
ADD phone_number VARCHAR(20) FIRST
-- 添加在sname字段后
ALTER TABLE mytemp1
ADD email VARCHAR(45) AFTER sname
</code></pre>
<h3 id="修改字段"><a href="#修改字段" class="headerlink" title="修改字段"></a>修改字段</h3><pre><code class="sql">ALTER TABLE mytemp1
MODIFY sname VARCHAR(25) DEFAULT &#39;张三&#39;
</code></pre>
<h3 id="重命名字段"><a href="#重命名字段" class="headerlink" title="重命名字段"></a>重命名字段</h3><pre><code class="sql">ALTER TABLE mytemp1
CHANGE salary mouth_salary DOUBLE(10,3)
</code></pre>
<h3 id="删除字段"><a href="#删除字段" class="headerlink" title="删除字段"></a>删除字段</h3><pre><code class="sql">ALTER TABLE mytemp1
DROP COLUMN email
</code></pre>
<h2 id="重命名表"><a href="#重命名表" class="headerlink" title="重命名表"></a>重命名表</h2><p><strong>方式1</strong>：</p>
<pre><code class="sql">RENAME TABLE mytemp1
TO mytemp2
</code></pre>
<p><strong>方式2</strong>：</p>
<pre><code class="sql">ALTER TABLE mytemp2
RENAME TO mytemp3 
</code></pre>
<h2 id="删除表"><a href="#删除表" class="headerlink" title="删除表"></a>删除表</h2><pre><code class="sql">DROP TABLE IF EXISTS mytemp3
</code></pre>
<h2 id="清空表"><a href="#清空表" class="headerlink" title="清空表"></a>清空表</h2><pre><code class="sql">TRUNCATE TABLE mytemp2
</code></pre>
<h1 id="commit与rollback"><a href="#commit与rollback" class="headerlink" title="commit与rollback"></a>commit与rollback</h1><p><strong>truncate与delete异同点</strong></p>
<p><strong>相同点</strong>：均可清空表中数据而保留表的字段</p>
<p><strong>不同点</strong>：truncate删除全部数据，且不可回滚</p>
<p>​			  delete可通过where过滤从而只删除部分数据，且可回滚</p>
<p><strong>注</strong>：DDL自动提交，且不可回滚</p>
<p>​      DML自动提交，但若在执行DML之前，执行	SET <strong>autocommit&#x3D;FALSE</strong>，便可实现回滚操作</p>
<p><strong>练习</strong>：</p>
<pre><code class="sql">CREATE TABLE mytemp3
AS 
SELECT *
FROM employees;

SET autocommit=FALSE;

DELETE FROM mytemp3;

SELECT * FROM mytemp3;

ROLLBACK;
SELECT * FROM mytemp3;
</code></pre>
<h1 id="数据处理之增删改"><a href="#数据处理之增删改" class="headerlink" title="数据处理之增删改"></a>数据处理之增删改</h1><h2 id="添加数据"><a href="#添加数据" class="headerlink" title="添加数据"></a>添加数据</h2><p><strong>方式1</strong>：添加全部数据</p>
<pre><code class="sql">INSERT INTO test VALUES(1,&#39;张三&#39;,3500,&#39;1998-2-6&#39;)
</code></pre>
<p><strong>方式2</strong>：添加指定字段数据</p>
<pre><code class="sql">INSERT INTO test(id,sname,salary)
VALUES(2,&#39;李四&#39;,2000)
</code></pre>
<p><strong>方式3</strong>：根据查询结果添加数据</p>
<pre><code class="sql">INSERT INTO test(id,sname,salary)
SELECT employee_id,last_name,salary
FROM employees
LIMIT 0,2
</code></pre>
<h2 id="更新数据"><a href="#更新数据" class="headerlink" title="更新数据"></a>更新数据</h2><pre><code class="sql">UPDATE test
SET salary=salary+salary*0.2
WHERE id=2
</code></pre>
<h2 id="删除数据"><a href="#删除数据" class="headerlink" title="删除数据"></a>删除数据</h2><pre><code class="sql">DELETE FROM test 
WHERE id=2
</code></pre>
<h1 id="约束"><a href="#约束" class="headerlink" title="约束"></a>约束</h1><p><strong>添加与删除约束</strong></p>
<p>create table添加约束</p>
<p>alter table添加删除约束</p>
<p><strong>查看约束</strong></p>
<pre><code class="sql">SELECT * FROM information_schema.table_constraints
WHERE TABLE_NAME=&#39;employees&#39;
</code></pre>
<h2 id="not-null（非空约束）"><a href="#not-null（非空约束）" class="headerlink" title="not null（非空约束）"></a>not null（非空约束）</h2><pre><code class="sql">
-- 添加非空约束
ALTER TABLE emp1
MODIFY email VARCHAR(15) NOT NULL

INSERT INTO emp1(employee_id,email) VALUES(&#39;1001&#39;,&#39;846@qq.com&#39;)

DESC emp1

-- 删除非空约束
ALTER TABLE emp1
MODIFY salary DOUBLE(8,2)
</code></pre>
<h2 id="unique（唯一约束）"><a href="#unique（唯一约束）" class="headerlink" title="unique（唯一约束）"></a>unique（唯一约束）</h2><pre><code class="sql">
-- 添加唯一约束
ALTER TABLE emp1
MODIFY first_name VARCHAR(20) UNIQUE

ALTER TABLE emp1
ADD CONSTRAINT UNIQUE(last_name)
-- 创建混合唯一约束
CREATE TABLE stu1
(
 id INT,
 sname VARCHAR(20),
 passw VARCHAR(20),
 -- 表级约束
 CONSTRAINT uk_name_pas UNIQUE(sname,passw)
)

CREATE TABLE stu1
(
 id INT,
 sname VARCHAR(20),
 passw VARCHAR(20),
 UNIQUE KEY(sname,passw)
)


-- 删除唯一约束
-- 创建唯一约束时会自动创建唯一索引
-- 删除唯一索引时需要根据索引名进行删除
-- 故删除唯一约束需要根据唯一索引名进行删除

ALTER TABLE stu
DROP INDEX sname

ALTER TABLE emp1
DROP INDEX last_name_2
</code></pre>
<h2 id="primary-key（主键）"><a href="#primary-key（主键）" class="headerlink" title="primary key（主键）"></a>primary key（主键）</h2><pre><code class="sql">
-- 添加主键约束
ALTER TABLE stu
ADD PRIMARY KEY (id)
</code></pre>
<h2 id="自增列auto-increment"><a href="#自增列auto-increment" class="headerlink" title="自增列auto_increment"></a>自增列auto_increment</h2><h2 id="foreign-key（外键）"><a href="#foreign-key（外键）" class="headerlink" title="foreign key（外键）"></a>foreign key（外键）</h2><p><strong>创建表时添加外键</strong></p>
<pre><code class="sql">
-- 先创建主表
CREATE TABLE dept1
(
dept_id INT,
dept_name VARCHAR(15)
);
ALTER TABLE dept1
ADD CONSTRAINT PRIMARY KEY(dept_id);-- 定义主表主键
DESC dept1
-- 创建从表
CREATE TABLE emp1
(
emp_id INT PRIMARY KEY AUTO_INCREMENT,
emp_name VARCHAR(15),
department_id INT,
-- 表级约束
-- 定义从表外键
CONSTRAINT fd_emp1_dept_id FOREIGN KEY(department_id) REFERENCES dept1(dept_id)
);
</code></pre>
<p><strong>创建表后添加外键</strong></p>
<pre><code class="sql">
-- 先创建主表
CREATE TABLE dept2
(
dept_id INT,
dept_name VARCHAR(15)
);
-- 创建从表
CREATE TABLE emp2
(
emp_id INT PRIMARY KEY AUTO_INCREMENT,
emp_name VARCHAR(15),
department_id INT
);


ALTER TABLE dept2
ADD CONSTRAINT PRIMARY KEY(dept_id);-- 定义主表主键
ALTER TABLE emp2
ADD CONSTRAINT fd_emp2_dept_id FOREIGN KEY(department_id) REFERENCES dept2(dept_id);
</code></pre>
<p><strong>删除外键约束</strong></p>
<pre><code class="sql">alter table 从表名
drop foreign key 外键约束名
</code></pre>
<h2 id="check（检查约束）"><a href="#check（检查约束）" class="headerlink" title="check（检查约束）"></a>check（检查约束）</h2><h2 id="default（默认值）"><a href="#default（默认值）" class="headerlink" title="default（默认值）"></a>default（默认值）</h2><p>​	</p>
<h1 id="视图"><a href="#视图" class="headerlink" title="视图"></a>视图</h1><h2 id="视图的创建"><a href="#视图的创建" class="headerlink" title="视图的创建"></a>视图的创建</h2><p>1.创建新的数据库与表</p>
<pre><code class="sql">-- 创建数据库testdb
CREATE DATABASE IF NOT EXISTS testdb;
-- 切换数据库
USE testdb;
-- 在当前数据库创建表emp与表哦dept
CREATE TABLE emp
AS
SELECT *
FROM atguigudb.employees;

CREATE TABLE dept
AS
SELECT *
FROM atguigudb.departments;
</code></pre>
<p>2.创建视图</p>
<p><strong>单表创建视图</strong></p>
<pre><code class="sql">CREATE VIEW vu_emp1
AS
SELECT employee_id,last_name,salary
FROM emp

SELECT * FROM vu_emp1
</code></pre>
<pre><code class="sql">-- 创建视图并使用别名作为视图字段名
CREATE VIEW vu_emp2
AS
SELECT employee_id eid,last_name lname,salary msalary
FROM emp

SELECT * FROM vu_emp2



CREATE VIEW vu_emp3(eid,lname,msalary)
AS
SELECT employee_id,last_name,salary
FROM emp
WHERE salary&gt;8000

SELECT * FROM vu_emp3
</code></pre>
<pre><code class="sql">-- 使用聚合函数创建不存在的字段组成视图
CREATE VIEW vu_emp_sal
AS
SELECT department_id did,AVG(salary) avg_sal
FROM emp
GROUP BY department_id

SELECT * FROM vu_emp_sal
</code></pre>
<p><strong>多表创建视图</strong></p>
<pre><code class="sql">
CREATE VIEW vu_emp_dept
AS 
SELECT e.employee_id,d.department_id,d.department_name
FROM emp e JOIN dept d
ON e.employee_id=d.department_id

SELECT * FROM vu_emp_dept



CREATE VIEW vu_emp_dept1
AS
SELECT CONCAT(e.last_name,&#39;-&#39;,d.department_name) emp_info
FROM emp e JOIN dept d
ON e.employee_id=d.department_id

SELECT * FROM vu_emp_dept1
</code></pre>
<p><strong>查看视图</strong></p>
<pre><code class="sql">-- 查看数据库中的表与视图
SHOW TABLES
-- 查看视图结构
DESC vu_emp1
-- 查看视图属性信息
SHOW TABLE STATUS LIKE &#39;vu_emp1&#39;
</code></pre>
<h2 id="视图的修改"><a href="#视图的修改" class="headerlink" title="视图的修改"></a>视图的修改</h2><h3 id="视图的更新"><a href="#视图的更新" class="headerlink" title="视图的更新"></a>视图的更新</h3><pre><code class="sql">ALTER VIEW vu_emp1
AS
SELECT last_name,salary
FROM emp
</code></pre>
<h3 id="视图的删除"><a href="#视图的删除" class="headerlink" title="视图的删除"></a>视图的删除</h3><pre><code class="sql">DROP VIEW vu_emp1
</code></pre>
<p><strong>注</strong>：若视图中的字段在原表中存在，则对原表的修改会影响到视图，同样对视图的修改也会影响到原表，但若视图的字段在原表中不存在，则无法进行修改</p>
<h1 id="存储过程与函数"><a href="#存储过程与函数" class="headerlink" title="存储过程与函数"></a>存储过程与函数</h1><h2 id="存储过程的创建与使用"><a href="#存储过程的创建与使用" class="headerlink" title="存储过程的创建与使用"></a>存储过程的创建与使用</h2><p><strong>有参存储过程</strong></p>
<pre><code class="sql">DELIMITER //-- 设置分隔符（结束标志默认为;）为//，避免存储过程的创建过程中遇见;时停止编译
CREATE PROCEDURE show_avg_salary()
BEGIN
    SELECT AVG(salary) avg_sal
    FROM emp;
END//
DELIMITER;-- 将分隔符重新换回;

CALL show_avg_salary()-- 调用存储过程
</code></pre>
<p><strong>有参存储过程</strong></p>
<pre><code class="sql">-- 创建存储过程
DELIMITER//
CREATE PROCEDURE show_min_salary(OUT ms DOUBLE)-- 使用输出参数ms
BEGIN
    SELECT MIN(salary) INTO ms
    FROM emp;
END//
DELIMITER;
-- 调用存储过程
CALL show_min_salary(@ms)
-- 查看参数值
SELECT @ms
-- 创建存储过程，根据姓名查询该人的工资
DELIMITER //
CREATE PROCEDURE show_someone_salary(IN empname VARCHAR(20))-- 使用输入参数
BEGIN
    SELECT salary
    FROM emp
    WHERE last_name=empname;
END//
DELIMITER ;
-- 调用方式1，直接传入参数
CALL show_someone_salary(&#39;Abel&#39;)
-- 调用方式2，定义变量赋值，再传入参数
SET @empname :=&#39;Abel&#39;;-- :=为标准赋值符号
CALL show_someone_salary(@empname)
</code></pre>
<p><strong>输入 输出双参数</strong></p>
<pre><code class="sql">-- 根据姓名查询工资
DELIMITER //
CREATE PROCEDURE showsalary(IN empname VARCHAR(20),OUT empsalary DOUBLE)
BEGIN
    SELECT salary INTO empsalary
    FROM emp
    WHERE last_name=empname;
END//
DELIMITER ;

SET @empname=&#39;Abel&#39;;
CALL showsalary(@empname,@empsalary);
SELECT @empsalary
</code></pre>
<p><strong>练习</strong>：查询员工名称为ename的领导姓名</p>
<pre><code class="sql">DELIMITER //
CREATE PROCEDURE show_mgr_name(INOUT ename VARCHAR(20))
BEGIN
    SELECT last_name INTO ename
    FROM emp
    WHERE employee_id = (
                SELECT manager_id
                FROM emp
                WHERE last_name=ename
                );
END//
DELIMITER ;

SET @ename=&#39;Abel&#39;;
CALL show_mgr_name(@ename);
SELECT @ename
</code></pre>
<h2 id="存储函数的使用"><a href="#存储函数的使用" class="headerlink" title="存储函数的使用"></a>存储函数的使用</h2><pre><code class="sql">-- 创建函数前执行此语句可防止函数创建失败
SET GLOBAL log_bin_trust_function_creators=1;
-- 根据姓名查询某员工的email
DELIMITER //
CREATE FUNCTION show_email_by_name(empname VARCHAR(20))
RETURNS VARCHAR(20)
BEGIN
    RETURN(SELECT email FROM emp WHERE last_name=empname);
END//
DELIMITER ;

SET @empname=&#39;Abel&#39;;
SELECT show_email_by_name(@empname)

-- 查询某部门的员工数
DELIMITER //
CREATE FUNCTION show_number(dept_id INT)
RETURNS INT
BEGIN
    RETURN (
        SELECT COUNT(*)
        FROM emp
        WHERE department_id=dept_id
        );
END//
DELIMITER ;

SET @dept_id=30;
SELECT show_number(@dept_id)
</code></pre>
<h1 id="变量、流程控制与游标"><a href="#变量、流程控制与游标" class="headerlink" title="变量、流程控制与游标"></a>变量、流程控制与游标</h1><h2 id="系统变量"><a href="#系统变量" class="headerlink" title="系统变量"></a>系统变量</h2><h2 id="会话变量"><a href="#会话变量" class="headerlink" title="会话变量"></a>会话变量</h2>]]></content>
      <categories>
        <category>数据库</category>
      </categories>
      <tags>
        <tag>数据库</tag>
        <tag>MySQL</tag>
      </tags>
  </entry>
  <entry>
    <title>图像处理基础</title>
    <url>/2022/05/01/%E5%9B%BE%E5%83%8F%E5%A4%84%E7%90%86%E5%9F%BA%E7%A1%80/</url>
    <content><![CDATA[<!-- more -->

<h1 id="图像处理基础"><a href="#图像处理基础" class="headerlink" title="图像处理基础"></a>图像处理基础</h1><pre><code class="python">import os
import cv2 as cv
import numpy as np
import matplotlib.pyplot as plt

获取上一级文件路径

abs_path=os.path.abspath(&quot;..&quot;)
path = abs_path + r&quot;\data\2-7data&quot;

print(path)
</code></pre>
<h2 id="图像显示"><a href="#图像显示" class="headerlink" title="图像显示"></a>图像显示</h2><pre><code class="python">def cv_show(name, img):
    cv.imshow(name, img)
    cv.waitKey(0)
    cv.destroyAllWindows()
</code></pre>
<h2 id="图像读取"><a href="#图像读取" class="headerlink" title="图像读取"></a>图像读取</h2><pre><code class="python">def img_test():

abs_path=r&quot;D:\pythonProject1\opencv_code\2-7data\data&quot;

​    img = cv.imread(path+r&quot;\lena.jpg&quot;)
​    cv_show(&quot;lena&quot;, img)
​    #灰度图
​    cat=cv.imread(path+&quot;\cat.jpg&quot;,cv.IMREAD_GRAYSCALE)
​    cv_show(&quot;cat&quot;,cat)
img_test()

## 
</code></pre>
<h2 id="视频读取"><a href="#视频读取" class="headerlink" title="视频读取"></a>视频读取</h2><pre><code class="python">def video_show():
    video=cv.VideoCapture(path+r&quot;\test.mp4&quot;)
    while open:
        ret,frame=video.read()#ret返回是否能够读取每一帧图像，frame返回视频中的每一帧图像
        gray=cv.cvtColor(frame,cv.COLOR_BGR2GRAY)#将BGR图像转为灰度图
        if frame is None:
            break
        if ret==True:
            cv.imshow(&quot;video&quot;,gray)
        c=cv.waitKey(50)#每帧图像延迟100ms
        if c==27:
            break
    cv.destroyAllWindows()
</code></pre>
<h2 id="图像切片与通道提取分离"><a href="#图像切片与通道提取分离" class="headerlink" title="图像切片与通道提取分离"></a>图像切片与通道提取分离</h2><h3 id="图像切片"><a href="#图像切片" class="headerlink" title="图像切片"></a>图像切片</h3><pre><code class="python">def cut_img():
    cat=cv.imread(path+&quot;\cat.jpg&quot;)
    #图像切片
    img=cat[0:100,0:200]#分别截取高度，宽度

print(img.shape)#顺序：高、宽
</code></pre>
<h3 id="通道提取"><a href="#通道提取" class="headerlink" title="通道提取"></a>通道提取</h3><p>​    </p>
<pre><code class="python">#保留R通道
cat[:,:,0]=0#关闭b通道
cat[:,:,1]=0#关闭g通道

b,g,r=cv.split(cat)
    print(r)
    cv.imshow(&quot;cut_img&quot;,img)
    cv.imshow(&quot;cat&quot;,cat)
    cv.imshow(&quot;b_cat&quot;,b)
    cv.waitKey(0)
    cv.destroyAllWindows()

cut_img()
</code></pre>
<h2 id="边界填充"><a href="#边界填充" class="headerlink" title="边界填充"></a>边界填充</h2><pre><code class="python">def border_caplicate():
    cat=cv.imread(path+&quot;\cat.jpg&quot;,cv.IMREAD_GRAYSCALE)
    top_size,bottom_size,left_size,right_size=(100,100,100,100)#设置边界范围（上下左右）
</code></pre>
<h3 id="复制法，直接借助原图边缘区域作为填充区域"><a href="#复制法，直接借助原图边缘区域作为填充区域" class="headerlink" title="复制法，直接借助原图边缘区域作为填充区域"></a>复制法，直接借助原图边缘区域作为填充区域</h3><pre><code class="python"> replicate=cv.copyMakeBorder(cat,top_size,bottom_size,left_size,right_size,borderType=cv.BORDER_REPLICATE)
</code></pre>
<h3 id="反射法，gfdecba-abcdefg-gfdecba"><a href="#反射法，gfdecba-abcdefg-gfdecba" class="headerlink" title="反射法，gfdecba|abcdefg|gfdecba"></a>反射法，gfdecba|abcdefg|gfdecba</h3><pre><code class="python">    reflect=cv.copyMakeBorder(cat,top_size,bottom_size,left_size,right_size,borderType=cv.BORDER_REFLECT)
</code></pre>
<h3 id="反射法2，gfdecb-abcdefg-fdecba"><a href="#反射法2，gfdecb-abcdefg-fdecba" class="headerlink" title="#反射法2，gfdecb|abcdefg|fdecba"></a>#反射法2，gfdecb|abcdefg|fdecba</h3><pre><code class="python">    reflect101=cv.copyMakeBorder(cat,top_size,bottom_size,left_size,right_size,borderType=cv.BORDER_REFLECT101)
</code></pre>
<pre><code>### 外包装法,abcdefg|abcdefg|abcdefg
</code></pre>
<pre><code class="python">  wrap=cv.copyMakeBorder(cat,top_size,bottom_size,left_size,right_size,borderType=cv.BORDER_WRAP)
</code></pre>
<pre><code>### 常量法，直接设置填充色值
</code></pre>
<pre><code class="python">const=cv.copyMakeBorder(cat,top_size,bottom_size,left_size,right_size,borderType=cv.BORDER_CONSTANT,value=25)
cv_show(&#39;cat&#39;,cat)
plt.subplot(231),plt.imshow(cat,&#39;gray&#39;),plt.title(&#39;gray&#39;)
plt.subplot(232),plt.imshow(replicate,&#39;gray&#39;),plt.title(&#39;replicate&#39;)
plt.subplot(233),plt.imshow(reflect,&#39;gray&#39;),plt.title(&#39;reflect&#39;)
plt.subplot(234),plt.imshow(reflect101,&#39;gray&#39;),plt.title(&#39;reflect101&#39;)
plt.subplot(235),plt.imshow(wrap,&#39;gray&#39;),plt.title(&#39;wrap&#39;)
plt.subplot(236),plt.imshow(const,&#39;gray&#39;),plt.title(&#39;const&#39;)
plt.show()

## 
</code></pre>
<h2 id="数值计算"><a href="#数值计算" class="headerlink" title="数值计算"></a>数值计算</h2><pre><code class="python">def img_numpy_calculate():
    cat=cv.imread(path+&quot;\cat.jpg&quot;)
    add_cat=cat+50#将图像三个通道的色值均加50，越界时（255）自动对256取余
    super_cat=cat+add_cat
    print(cat[:5,:,0])
    print(add_cat[:5,:,0])
    print(super_cat[:5,:,0])
    cv_show(&#39;cat&#39;,cat)
    cv_show(&#39;add_cat&#39;,add_cat)
    cv_show(&#39;super_cat&#39;,super_cat)
</code></pre>
<h2 id="图像融合"><a href="#图像融合" class="headerlink" title="图像融合"></a>图像融合</h2><pre><code class="python">def img_add():
    cat=cv.imread(path+&quot;\cat.jpg&quot;)
    print(cat.shape)

cv_show(&quot;cat&quot;,cat)

res=cv.resize(cat,(400,224))#重新指定图像大小

print(res.shape)

cv_show(&quot;cat&quot;,res)

res1=cv.resize(cat,(0,0),fx=3,fy=2)#横轴扩大3倍，纵轴扩大2倍

cv_show(&#39;res1&#39;,res1)
</code></pre>
<h3 id="直接相加"><a href="#直接相加" class="headerlink" title="直接相加"></a>直接相加</h3><pre><code class="python">dog=cv.imread(path+&quot;\dog.jpg&quot;)
print(dog.shape)
dog=cv.resize(dog,(cat.shape[1],cat.shape[0]))
print(dog.shape)
cat_add_dog=cat+dog#越界取余
cv_show(&quot;cat_add_dog&quot;,cat_add_dog)
add=cv.add(cat,dog)#越界直接赋值为255
cv_show(&quot;add&quot;,add)
</code></pre>
<pre><code>### 设置权重相加
</code></pre>
<pre><code class="python">res=0.6*cat+0.4*dog+0
res=cv.addWeighted(cat,0.6,dog,0.4,0)
cv_show(&quot;res&quot;,res)
</code></pre>
<h1 id="二值化与平滑处理"><a href="#二值化与平滑处理" class="headerlink" title="二值化与平滑处理"></a>二值化与平滑处理</h1><pre><code class="python">import os
import cv2 as cv
import numpy as np
import matplotlib.pyplot as plt

# 获取上一级文件路径

abs_path=os.path.abspath(&quot;..&quot;)
path = abs_path + r&quot;\data\2-7data&quot;
def cv_show(name,img):
    cv.imshow(name,img)
    cv.waitKey(0)
    cv.destroyAllWindows()

cat=cv.imread(path+&quot;\cat.jpg&quot;,cv.IMREAD_GRAYSCALE)
</code></pre>
<h2 id="图像二值化"><a href="#图像二值化" class="headerlink" title="图像二值化"></a>图像二值化</h2><pre><code class="python">def src_thresh():

    # 参数

    # src:图像

    # thresh:阈值

    # marval:超出阈值所赋的值

    # type： 二值化操作类型

    # 返回值

    # ret：阈值

    # thresh：阈值化处理后的图像

    #图像二值化：大于阈值的数取最大值255，否则为0
    ret1,thresh1=cv.threshold(cat,127,255,cv.THRESH_BINARY)
    #图像二值化：大于阈值设置为0，否则为255
    ret2,thresh2=cv.threshold(cat,127,255,cv.THRESH_BINARY_INV)
    #大于阈值的部分赋值为该阈值，否则不变
    ret3,thresh3=cv.threshold(cat,127,255,cv.THRESH_TRUNC)
    #小于阈值的部分设置为0，否则不变
    ret4,thresh4=cv.threshold(cat,127,255,cv.THRESH_TOZERO)
    #大于阈值的部分设置为0，否则不变
    ret5,thresh5=cv.threshold(cat,127,255,cv.THRESH_TOZERO_INV)

    title=[&quot;cat&quot;,&quot;THRESH_BINARY&quot;,&quot;THRESH_BINARY_INV&quot;,&quot;THRESH_TRUNC&quot;,&quot;THRESH_TOZERO&quot;,&quot;THRESH_TOZERO_INV&quot;]
    img=[cat,thresh1,thresh2,thresh3,thresh4,thresh5]
    for i in range(6):
        plt.subplot(2,3,i+1),plt.imshow(img[i],&quot;gray&quot;)
        plt.title(title[i])
    plt.show()
</code></pre>
<h2 id="图像平滑处理"><a href="#图像平滑处理" class="headerlink" title="图像平滑处理"></a>图像平滑处理</h2><pre><code class="python">def blur():
    img=cv.imread(path+&quot;\lenaNoise.png&quot;)
    # cv_show(&quot;img&quot;,img)
    # 均值滤波，取3*3矩阵的均值作为矩阵中心像素点的色值
    blur=cv.blur(img,(3,3))
    # cv_show(&quot;blur&quot;,blur)

    # 方框滤波
    # 归一化为true，即将处理后的色值归一到0-255之间，与均值滤波效果等同，
    box1=cv.boxFilter(img,-1,(3,3),normalize=True)
    # cv_show(&quot;box1&quot;,box1)
    # 若normalize为false，则直接对卷积核求和，越界直接截断为255
    box2=cv.boxFilter(img,-1,(3,3),normalize=False)
    # cv_show(&quot;box2&quot;,box2)

    # 高斯滤波，利用正态分布，距离正态分布均值近的数值权重大，距离均值远的数值权重小
    # 参数：src，卷积核大小，标准差
    gaussian=cv.GaussianBlur(img,(3,3),1)
    # cv_show(&quot;gaussian_blur&quot;,gaussian)

    # 中值滤波，取矩阵的中值作为矩阵中心像素点的色值，对于椒盐噪声而言，中值滤波的效果最好
    median=cv.medianBlur(img,3)
    # cv_show(&quot;median&quot;,median)

    res=np.hstack((blur,gaussian,median))#横向拼接矩阵
    cv.imshow(&quot;blur,gaussian,median&quot;,res)
    cv.waitKey(0)
    cv.destroyAllWindows()
blur()
</code></pre>
<h1 id="图像形态学处理"><a href="#图像形态学处理" class="headerlink" title="图像形态学处理"></a>图像形态学处理</h1><pre><code class="python">import cv2 as cv
import numpy as np
import matplotlib.pyplot as plt
import os
abs_path=os.path.abspath(&quot;..&quot;)
path=abs_path+r&quot;\data\2-7data&quot;
def cv_show(name,img):
    cv.imshow(name,img)
    cv.waitKey(0)
    cv.destroyAllWindows()

img=cv.imread(path+&quot;\dige.png&quot;)
</code></pre>
<h2 id="腐蚀与膨胀"><a href="#腐蚀与膨胀" class="headerlink" title="腐蚀与膨胀"></a>腐蚀与膨胀</h2><pre><code class="python">def erosion_dilate():

    # 形态学-腐蚀操作,去除多余噪点

    kernel=np.ones((5,5))
    erosion=cv.erode(img,kernel,iterations=1)

    # res=np.hstack((img,erosion))

    # cv_show(&quot;res&quot;,res)

    # pie=cv.imread(path+&quot;\pie.png&quot;)

    # kernel=np.ones((10,10))

    # erosion=cv.erode(pie,kernel,iterations=1)

    # res=np.hstack((pie,erosion))

    # cv_show(&quot;ee&quot;,res)

    # 形态学-膨胀操作，去除噪点过度处理的还原

    dig_dilate=cv.dilate(erosion,np.ones((5,5)),iterations=1)
    res=np.hstack((img,erosion,dig_dilate))
    cv_show(&quot;erosion_dilate&quot;,res)
</code></pre>
<h2 id="开运算与闭运算"><a href="#开运算与闭运算" class="headerlink" title="开运算与闭运算"></a>开运算与闭运算</h2><pre><code class="python">def morphology_openAndClose():
    # 开运算：先腐蚀再膨胀
    res_open=cv.morphologyEx(img,cv.MORPH_OPEN,kernel=np.ones((5,5)))
    cv_show(&quot;morphology_open&quot;,res_open)
    # 闭运算：先膨胀再腐蚀
    res_close=cv.morphologyEx(img,cv.MORPH_CLOSE,kernel=np.ones((5,5)))
    cv_show(&quot;morphology_close&quot;,res_close)
</code></pre>
<h2 id="梯度运算-膨胀-腐蚀，得到边缘结果"><a href="#梯度运算-膨胀-腐蚀，得到边缘结果" class="headerlink" title="梯度运算:膨胀-腐蚀，得到边缘结果"></a>梯度运算:膨胀-腐蚀，得到边缘结果</h2><pre><code class="python">def morphology_gradient():
    # pie=cv.imread(path+&quot;\pie.png&quot;)
    res=cv.morphologyEx(img,cv.MORPH_GRADIENT,kernel=np.ones((5,5)))
    cv_show(&quot;morphology_gradient&quot;,res)
</code></pre>
<h2 id="礼帽与黑帽"><a href="#礼帽与黑帽" class="headerlink" title="礼帽与黑帽"></a>礼帽与黑帽</h2><pre><code class="python">
# 礼帽：原始输入-开运算，得到噪点,可突出更加明亮区域
# 黑帽：闭运算-原始输入，得到噪点轮廓
def tophatAndblackhat():
    tophat=cv.morphologyEx(img,cv.MORPH_TOPHAT,np.ones((5,5)))
    cv_show(&quot;tophat&quot;,tophat)
    blackhat=cv.morphologyEx(img,cv.MORPH_BLACKHAT,np.ones((5,5)))
    cv_show(&quot;blackhat&quot;,blackhat)
</code></pre>
<h1 id="梯度算子"><a href="#梯度算子" class="headerlink" title="梯度算子"></a>梯度算子</h1><pre><code class="python">import cv2 as cv
import matplotlib.pyplot as plt
import numpy as np
import os
abs_path=os.path.abspath(&quot;..&quot;)
path=abs_path+r&quot;\data\2-7data&quot;
img=cv.imread(path+&quot;\lena.jpg&quot;,cv.IMREAD_GRAYSCALE)
def cv_show(name,img):
    cv.imshow(name,img)
    cv.waitKey(0)
    cv.destroyAllWindows()

# cv_show(&quot;img&quot;,img)
</code></pre>
<h2 id="sobel算子"><a href="#sobel算子" class="headerlink" title="sobel算子"></a>sobel算子</h2><p><a href="https://imgtu.com/i/OC3yPs"><img src="https://s1.ax1x.com/2022/05/01/OC3yPs.png" alt="OC3yPs.png"></a></p>
<p><a href="https://imgtu.com/i/OC3IIJ"><img src="https://s1.ax1x.com/2022/05/01/OC3IIJ.png" alt="OC3IIJ.png"></a></p>
<pre><code class="python">
#     [-1 0 1     [-1 -2 -1
# Gx=  -2 0 2  Gy=  0  0  0
#      -1 0 1]      1  2  1]

def sobel_grad_test(img=cv.imread(path+&quot;\pie.png&quot;)):
    # 参数
    # src
    # ddepth：图像深度，cv.CV_64F可使色值保留负数而不使其截断
    # dx：横轴变换，1表示在横轴变换，0表示不变换
    # dy：纵轴变换
    # ksize表示卷积核大小
    sobelx=cv.Sobel(img,cv.CV_64F,1,0,ksize=3)
    sobelx=cv.convertScaleAbs(sobelx)#将计算到的矩阵结果取绝对值
    sobely=cv.Sobel(img,cv.CV_64F,0,1,ksize=3)
    sobely=cv.convertScaleAbs(sobely)
    sobel=cv.addWeighted(sobelx,0.5,sobely,0.5,0)
    res=np.hstack((img,sobel))
    cv_show(&quot;res&quot;,res)

# sobel_grad_test(img)

# Scharr算子

#     [ -3  0  3     [-3 -10  -3

# Gx=  -10  0 10  Gy=  0   0   0

#       -3  0  3]      3  10   3]

# laplacian算子

#     [ 0   1  0

# Gx=   1  -4  1

#       0   1  0]
</code></pre>
<h2 id="Scharr算子"><a href="#Scharr算子" class="headerlink" title="Scharr算子"></a>Scharr算子</h2><p><a href="https://imgtu.com/i/OC3HR1"><img src="https://s1.ax1x.com/2022/05/01/OC3HR1.png" alt="OC3HR1.png"></a></p>
<pre><code class="python">sobelx=cv.Sobel(img,cv.CV_64F,1,0,ksize=3)
sobely=cv.Sobel(img,cv.CV_64F,0,1,ksize=3)
sobelx=cv.convertScaleAbs(sobelx)
sobely=cv.convertScaleAbs(sobely)
sobel=cv.addWeighted(sobelx,0.5,sobely,0.5,0)

# scharr算子

scharrx=cv.Scharr(img,cv.CV_64F,1,0)
scharry=cv.Scharr(img,cv.CV_64F,0,1)
scharrx=cv.convertScaleAbs(scharrx)
scharry=cv.convertScaleAbs(scharry)
scharr=cv.addWeighted(scharrx,0.5,scharry,0.5,0)
</code></pre>
<h2 id="laplacian算子"><a href="#laplacian算子" class="headerlink" title="laplacian算子"></a>laplacian算子</h2><p><a href="https://imgtu.com/i/OC3bxx"><img src="https://s1.ax1x.com/2022/05/01/OC3bxx.png" alt="OC3bxx.png"></a></p>
<pre><code class="python">
laplacian=cv.Laplacian(img,cv.CV_64F)
laplacian=cv.convertScaleAbs(laplacian)

res=np.hstack((img,sobel,scharr,laplacian))
cv_show(&quot;img-sobel-scharr-laplacian&quot;,res)
</code></pre>
<h1 id="canny边缘检测"><a href="#canny边缘检测" class="headerlink" title="canny边缘检测"></a>canny边缘检测</h1><p><a href="https://imgtu.com/i/OC8uJs"><img src="https://s1.ax1x.com/2022/05/01/OC8uJs.png" alt="OC8uJs.png"></a><br><a href="https://imgtu.com/i/OC8KWn"><img src="https://s1.ax1x.com/2022/05/01/OC8KWn.png" alt="OC8KWn.png"></a><br><a href="https://imgtu.com/i/OC8nij"><img src="https://s1.ax1x.com/2022/05/01/OC8nij.png" alt="OC8nij.png"></a><br><a href="https://imgtu.com/i/OC8eoQ"><img src="https://s1.ax1x.com/2022/05/01/OC8eoQ.png" alt="OC8eoQ.png"></a><br><a href="https://imgtu.com/i/OC8Zdg"><img src="https://s1.ax1x.com/2022/05/01/OC8Zdg.png" alt="OC8Zdg.png"></a><br><a href="https://imgtu.com/i/OC8Mzq"><img src="https://s1.ax1x.com/2022/05/01/OC8Mzq.png" alt="OC8Mzq.png"></a></p>
<pre><code class="python">import os
import cv2 as cv
import matplotlib.pyplot as plt
import numpy as np

abs_path=os.path.abspath(&quot;..&quot;)
path=abs_path+r&quot;\data\2-7data&quot;
img=cv.imread(path+&quot;\lena.jpg&quot;,cv.IMREAD_GRAYSCALE)

def cv_show(name,img):
    cv.imshow(name,img)
    cv.waitKey(0)
    cv.destroyAllWindows()


# 1.图像平滑处理：高斯滤波去除影响检测的噪点
# 2.利用sobel算子计算各像素点的梯度及其方向
# 3.非极大值抑制：找出在某一领域内梯度极大的像素点并保留，其余的像素点去除
# 4.双阈值检测，检测确定真实的和潜在的边缘
# 5.通过抑制孤立的弱边缘最终完成边缘检测

# 阈值范围较大会导致边缘检测效果较差
# 阈值范围较小会检测出部分噪点
cv1=cv.Canny(img,80,150)#参数：src，下阈值，上阈值
cv2=cv.Canny(img,50,100)
res1=np.hstack((cv1,cv2))
cv_show(&quot;lena&quot;,res1)

car=cv.imread(path+&quot;\car.png&quot;,cv.IMREAD_GRAYSCALE)
car1=cv.Canny(car,120,250)
car2=cv.Canny(car,50,100)
res2=np.hstack((car1,car2))
cv_show(&quot;car&quot;,res2)
</code></pre>
]]></content>
      <categories>
        <category>计算机视觉</category>
      </categories>
      <tags>
        <tag>opencv</tag>
        <tag>python</tag>
        <tag>计算机视觉</tag>
      </tags>
  </entry>
  <entry>
    <title>内部排序</title>
    <url>/2022/04/05/%E5%86%85%E9%83%A8%E6%8E%92%E5%BA%8F/</url>
    <content><![CDATA[<!-- more -->
<h1 id="直接插入排序"><a href="#直接插入排序" class="headerlink" title="直接插入排序"></a>直接插入排序</h1><p>算法思想：</p>
<pre><code class="c++">vector&lt;int&gt; insertSort(vector&lt;int&gt; num)
&#123;
    int i, j, temp;
    for (i = 1; i &lt; num.size(); i++)
    &#123;
        temp = num[i];
        for (j = i - 1; j &gt;= 0 &amp;&amp; temp&lt;num[j]; j--)//易错点，&amp;&amp;前后不可拆分为if语句，这是一个连续的循环
        &#123;
            num[j + 1] = num[j];
        &#125;
        num[j + 1] = temp;
    &#125;

    return num;
&#125;
</code></pre>
<h1 id="希尔排序"><a href="#希尔排序" class="headerlink" title="希尔排序"></a>希尔排序</h1><p>算法思想：</p>
<pre><code class="c++">vector&lt;int&gt; shellSort(vector&lt;int&gt; num)
&#123;
    int i, j, temp;
    int n = num.size();
    for (int gap = n / 2; gap &gt; 0; gap = gap / 2)//增量设置的边界条件不能等于0
    &#123;
        for (i = gap; i &lt; n; i++)
        &#123;
            temp = num[i];
            for (j = i - gap; j &gt;= 0 &amp;&amp; temp&lt;num[j]; j -= gap)
                num[j + gap] = num[j];
            num[j + gap] = temp;
        &#125;
    &#125;
    return num;

&#125;
</code></pre>
<h1 id="冒泡排序"><a href="#冒泡排序" class="headerlink" title="冒泡排序"></a>冒泡排序</h1><p>算法思想：</p>
<pre><code class="c++">vector&lt;int&gt; bubbleSort(vector&lt;int&gt; num)
&#123;
    int n = num.size();
    for (int i = 0; i &lt; n; i++)
    &#123;
        bool flag = false;
        for (int j = 0; j &lt; n - i - 1; j++)
        &#123;
            if (num[j + 1] &lt; num[j])
            &#123;
                swap(num[j], num[j + 1]);
                flag = true;
            &#125;
        &#125;
        if (!flag)
            break;
    &#125;
    return num;
&#125;
</code></pre>
<h1 id="快速排序"><a href="#快速排序" class="headerlink" title="快速排序"></a>快速排序</h1><p>算法思想：</p>
<pre><code class="c++">int paratition(vector&lt;int&gt;&amp; num, int low, int high)
&#123;
    int base = num[low];//选择基准元素
    while (low &lt; high)
    &#123;
        //num[high] &gt;= base中的=判断不能省略
        while (num[high] &gt;= base  &amp;&amp; low &lt; high   ) high--;//该循环跳出语句中&amp;&amp;前后衔接可互换，但由于&amp;&amp;的短路特性，更建议先判断low&lt;high
        num[low] = num[high];
        while (low &lt; high &amp;&amp; num[low] &lt;= base ) low++;
        num[high] = num[low];
    &#125;
    num[low] = base;
    return low;
&#125;

void quickSort(vector&lt;int&gt;&amp; num,int low,int high)
&#123;

    if (low &lt; high)//该语句不可少，只有在low&lt;high时的交换才起作用，否则在递归时会出现错误
    &#123;
        int j = paratition(num, low, high);
        quickSort(num, low, j - 1);
        quickSort(num, j + 1, high);
    &#125;

&#125;
</code></pre>
<h1 id="选择排序"><a href="#选择排序" class="headerlink" title="选择排序"></a>选择排序</h1><p>算法思想：</p>
<pre><code class="c++">vector&lt;int&gt; selectSort(vector&lt;int&gt; num)
&#123;
    int n = num.size();
    for (int i = 0; i &lt; n; i++)
    &#123;
        int min = i;
        for (int j = i + 1; j &lt; n; j++)
        &#123;
            if (num[j] &lt; num[min])
            &#123;
                min = j;
            &#125;
        &#125;
        if (min != i)
        &#123;
            swap(num[min], num[i]);
        &#125;
    &#125;
    return num;
&#125;
</code></pre>
<h1 id="堆排序"><a href="#堆排序" class="headerlink" title="堆排序"></a>堆排序</h1><p>算法思想：</p>
<pre><code class="c++">void heapAdjust(vector&lt;int&gt;&amp; num, int p, int n)
&#123;
    int temp = num[p];//取出根节点，待找到该根节点的子树中最大的节点之后与该根节点进行交换
    for (int i = 2 * p + 1; i &lt; n; i = 2*i+1)//遍历子树，寻找最大节点
    &#123;
        if (i + 1 &lt; n &amp;&amp; num[i + 1] &gt; num[i]) i++;//首先比较父节点的两个孩子的最大值
        if (temp &gt;= num[i]) break;//如果父节点比两个孩子节点都大，则不需要交换，说明当前子树已经是大根堆
        num[p] = num[i];//将最大孩子进行上移为父节点
        p = i;//继续遍历子树，保证其子树也是大根堆
    &#125;
    num[p] = temp;
&#125;
void heapSort(vector&lt;int&gt;&amp; num)
&#123;
    //构建初始大根堆
    int n = num.size();
    for (int i = n / 2 - 1; i &gt;= 0; i--)//从最后一个非叶子节点向上构造大根堆
        heapAdjust(num, i, n);

    for (int j = n - 1; j &gt;= 0; j--)
    &#123;
        //大根堆的第一个数字是最大的,将大根与最后一个数字进行交换，则该数组的最后一个数一定是最大的
        swap(num[0], num[j]);
        //将数组长度减去1后，调整堆，重新进行交换
        heapAdjust(num, 0, j);
    &#125;

&#125;
</code></pre>
<h1 id="归并排序"><a href="#归并排序" class="headerlink" title="归并排序"></a>归并排序</h1><p>算法思想：</p>
<pre><code class="c++">void merge(vector&lt;int&gt;&amp; num, int low, int mid, int high)
&#123;
    vector&lt;int&gt; help;//定义辅助数组
    help.resize(high - low + 1);
    int i = low, j = mid + 1, k = 0;//i为第一序列的扫描指针，j为第二序列的扫描指针，k为辅助数组的扫描指针
    while (i &lt;= mid &amp;&amp; j &lt;= high)// = 不能省略
    &#123;
        if (num[i] &lt; num[j]) help[k++] = num[i++];
        else help[k++] = num[j++];
    &#125;
    while (i &lt;= mid) help[k++] = num[i++];
    while (j &lt;= high) help[k++] = num[j++];
    for (int i = low, k = 0; i &lt;= high; i++, k++)
    &#123;
        num[i] = help[k];
    &#125;
&#125;

void mergeSort(vector&lt;int&gt;&amp; num, int low, int high)
&#123;
    if (low == high)//递归跳出条件，即当子序列长度为1时终止递归
        return;
    int mid = (low + high) / 2;
    mergeSort(num, low, mid);//递归划分左区间，直到区间序列个数为1时终止递归
    mergeSort(num, mid + 1, high);//递归划分右区间，知道区间序列个数为1时终止递归
    merge(num, low, mid, high);//合并
&#125;
</code></pre>
<h1 id="桶排序"><a href="#桶排序" class="headerlink" title="桶排序"></a>桶排序</h1><p>算法思想：</p>
<pre><code>定义一个编号为[0,max]的桶，其中max为待排序数组当中的最大值
遍历数组，将数组的中的元素值放到该元素对应编号的桶中，桶数组的值代表放入桶中的个数，初始时桶中个数为0
按照桶的编号顺序依次取出桶中的元素
</code></pre>
<pre><code class="c++">//桶排序
void bucketSort(vector&lt;int&gt;&amp; nums) 
&#123;
    //思想：

    int n = nums.size();
    int max=0;
    for (int i = 0; i &lt; n; i++)
    &#123;
        if (nums[i] &gt; max)
            max = nums[i];
    &#125;
    vector&lt;int&gt; bucket(max+1, 0);//初始化桶
    for (int i = 0; i &lt; n; i++)//将数组中的数放到对应编号的桶中
        bucket[nums[i]]++;
    for (int i = 0,j=0; i &lt;= max; i++)
    &#123;
        while (bucket[i]--&gt; 0)
            nums[j++] = i;
    &#125;
&#125;
</code></pre>
]]></content>
      <categories>
        <category>数据结构与算法</category>
      </categories>
      <tags>
        <tag>排序</tag>
        <tag>数据结构与算法</tag>
      </tags>
  </entry>
  <entry>
    <title>正则表达式</title>
    <url>/2022/04/30/%E6%AD%A3%E5%88%99%E8%A1%A8%E8%BE%BE%E5%BC%8F/</url>
    <content><![CDATA[<!-- more -->

<pre><code class="python"># -*- codeing=utf-8 -*-
# @Author:姜磊
# 人间烟火气，最抚凡人心
# -*- coding: UTF-8 -*-
import re
string = &#39;small smell sm.ll smll smaall sm3ll smAll smaaaall sm\nll sm ll small smbll smbbll sm..ll sm**ll smaaaaaall&#39;
#print(re.search(&quot;small&quot;,string))


#匹配单个字符
&#39;&#39;&#39;
print(re.findall(&quot;small&quot;,string))#查找所有匹配的字符
print(re.findall(&quot;small|smell&quot;,string))#管道符，查找small或者smell
print(re.findall(&quot;sm.ll&quot;,string))#.符号可以匹配任意一个字符
print(re.findall(&quot;sm[abcd]ll&quot;,string))#[]符号限定只匹配abcd中的字符
print(re.findall(&quot;sm[a-z]ll&quot;,string))#限定匹配所有的小写字母
print(re.findall(&quot;sm[a-zA-Z0-9]ll&quot;,string))#匹配小写字母，大写字母和数字
print(re.findall(&quot;sm\.ll&quot;,string))#转义
print(re.findall(&quot;sm[^a]ll&quot;,string))#除了a以外的所有字符
&#39;&#39;&#39;

#量化符
&#39;&#39;&#39;
print(re.findall(&quot;sm.&#123;2&#125;ll&quot;,string))#任意一个字符匹配2两次
print(re.findall(&quot;sm[a-z]&#123;2,4&#125;ll&quot;,string))#匹配所有的小写字母出现2-4次
print(re.findall(&quot;sm[a-z]&#123;2,&#125;ll&quot;,string))#匹配所有的小写字母出现2次以上
print(re.findall(&quot;sm[a-z]?ll&quot;,string))#&#123;0,1&#125;
print(re.findall(&quot;sm[a-z]+ll&quot;,string))#&#123;1，&#125;
print(re.findall(&quot;sm[a-z]*ll&quot;,string))#&#123;0，&#125;
&#39;&#39;&#39;


eg=&quot;My name is jianglei.&quot;
name0=&quot;My name is .+\.&quot;
name=&quot;My name is (.+)\.&quot;#()表示提取符合正则表达式的字符串中（）中的内容
print(re.findall(name0,eg))
print(re.findall(name,eg)[0])

&#39;&#39;&#39;
rawdata = &#39;
555-1239
Moe Szyslak
(636) 555-0113
Burns, C.Montgomery
555-6542
Rev. Timothy Lovejoy
555 8904
Ned Flanders
636-555-3226
Simpson,Homer
5553642
Dr. Julius Hibbert&#39;
&#39;&#39;&#39;


rawdata = &#39;555-1239Moe Szyslak(636) 555-0113Burns, C.Montgomery555-6542Rev. Timothy Lovejoy555 8904Ned Flanders636-555-3226Simpson,Homer5553642Dr. Julius Hibbert&#39;

tel_pattern=&#39;\(?[0-9]&#123;0,3&#125;\)?[ ]?[0-9]&#123;3&#125;[- ]?[0-9]&#123;4&#125;&#39;
tel=re.findall(tel_pattern,rawdata)
#print(tel)

name_pattern=&#39;[a-zA-Z]&#123;0,5&#125;[,\.]?[ ]?[a-zA-Z]&#123;1,7&#125;[\. ,][a-zA-Z]&#123;1,10&#125;&#39;
name=re.findall(name_pattern,rawdata)
#print(name)

import pandas as pd
ans=pd.DataFrame(&#123;&#39;name&#39;:name,&#39;tel&#39;:tel&#125;)
print(ans)
</code></pre>
]]></content>
      <categories>
        <category>python</category>
      </categories>
      <tags>
        <tag>正则表达式、python</tag>
      </tags>
  </entry>
  <entry>
    <title>网易云新歌榜VIP下载</title>
    <url>/2022/04/30/%E7%BD%91%E6%98%93%E4%BA%91%E6%96%B0%E6%AD%8C%E6%A6%9CVIP%E4%B8%8B%E8%BD%BD/</url>
    <content><![CDATA[<!-- more -->


<pre><code class="python">import requests
from bs4 import BeautifulSoup as bs

# # url=r&#39;https://music.163.com/#/discover/toplist?id=3779629&#39;

# # url = r&#39;https://music.163.com/discover/toplist?id=3779629&#39;

# # url=r&quot;https://music.163.com/discover/toplist?id=3778678&quot;

url=r&quot;https://music.163.com/artist?id=88145&quot;

# headers=&#123;&#39;user-agent&#39;: &#39;Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/80.0.3987.163 Safari/537.36 Edg/80.0.361.111&#39;&#125;

# 头部信息

headers=&#123;&#39;User-Agent&#39;: &#39;Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/99.0.4844.74 Safari/537.36 Edg/99.0.1150.52&#39;&#125;
#发送请求
res=requests.get(url,headers=headers)

# 获取返回html数据

html=res.text

# 解析HTML文档

soup=bs(html,&quot;html.parser&quot;)

# print(soup)

# 获取歌曲下载列表

inf = soup.find(&#39;ul&#39;, class_=&#39;f-hide&#39;)

# print(inf)

# 获取所有歌曲的链接

infor = inf.find_all(&#39;a&#39;)

# print(infor)

idlist=[]#歌曲id号
namelist=[]#歌曲名称
num=0
for i in infor:
    num=num+1
    name = i.text#获取a标签内容，即歌曲名称
    namelist.append(name)
    # print(i)
    href=i.get(&quot;href&quot;)#获取a标签下的href属性值
    # print(href)
    id=href[9:]#获取歌曲id号
    idlist.append(id)
    url=&#39;https://music.163.com/&#39;+href#获取每首音乐的详情链接
    print(str(num)+&#39; &#39;+name+&#39; &#39;+url)

def download():
    while True:
        a = input(&quot;请输入你要下载歌曲的序号：&quot;)
        b = int(a) - 1
        aa = idlist[b]    # 取音乐id
        fn = namelist[b]    # 取音乐名
        # 定义请求路由 &gt; 下载播放链接
        url = &#39;http://music.163.com/song/media/outer/url?id=&#123;&#125;.mp3&#39;.format(aa)  # 破解
        music = requests.get(url, headers=headers).content

        # 文件下载
        with open(&#39;D:\pythonProject1\graduate\music\&#123;&#125;.mp3&#39;.format(fn), &#39;wb&#39;) as f:
            f.write(music)
            print(fn + &#39;   下载完毕&#39;)

download()
</code></pre>
]]></content>
      <categories>
        <category>项目</category>
      </categories>
      <tags>
        <tag>爬虫 - python - 项目</tag>
      </tags>
  </entry>
  <entry>
    <title>机房预约系统</title>
    <url>/2022/04/28/%E6%9C%BA%E6%88%BF%E9%A2%84%E7%BA%A6%E7%B3%BB%E7%BB%9F/</url>
    <content><![CDATA[<p>机房预约系统项目简介</p>
<h1 id="机房预约系统"><a href="#机房预约系统" class="headerlink" title="机房预约系统"></a>机房预约系统</h1><p><a href="https://imgtu.com/i/LX4BTI"><img src="https://s1.ax1x.com/2022/04/28/LX4BTI.png" alt="LX4BTI.png"></a></p>
<h2 id="机房预约系统需求"><a href="#机房预约系统需求" class="headerlink" title="机房预约系统需求"></a>机房预约系统需求</h2><h3 id="身份简介"><a href="#身份简介" class="headerlink" title="身份简介"></a>身份简介</h3><ul>
<li>学生：申请预约机房</li>
<li>教师：审核学生预约</li>
<li>管理员：创建、注销学生、教师账号</li>
</ul>
<h3 id="机房简介"><a href="#机房简介" class="headerlink" title="机房简介"></a>机房简介</h3><ul>
<li>1号机房：最大容量20人</li>
<li>2号机房：最大容量5人</li>
<li>3号机房：最大容量100人</li>
</ul>
<h3 id="申请简介"><a href="#申请简介" class="headerlink" title="申请简介"></a>申请简介</h3><ul>
<li>管理员每周清理预约</li>
<li>学生申请一周内的预约，申请时间为周一至周五，时段为上午或下午</li>
<li>教师审核学生预约是否通过</li>
</ul>
<h3 id="系统具体需求"><a href="#系统具体需求" class="headerlink" title="系统具体需求"></a>系统具体需求</h3><ul>
<li>进入主菜单选择身份登录<ul>
<li>学生：学号、姓名、密码</li>
<li>教师：工号、姓名、密码</li>
<li>管理员：姓名、密码</li>
<li>退出</li>
</ul>
</li>
<li>进入各身份子菜单<ul>
<li>学生子菜单<ul>
<li>申请预约：申请时间（周一至周五）、申请时段（上午、下午）、申请机房号（1-3号）、预约状态（审核中、通过、失败、取消）</li>
<li>查看自身预约：查看自己预约状态</li>
<li>查看所有预约：查看所有预约状态</li>
<li>取消预约：取消预约记录</li>
<li>退出登录</li>
</ul>
</li>
<li>教师子菜单<ul>
<li>查看所有预约：查看学生所有预约</li>
<li>审核预约：审核学生预约是否通过</li>
<li>退出登录</li>
</ul>
</li>
<li>管理员子菜单<ul>
<li>添加账号：添加学生或者教师账号</li>
<li>查看账号：查看所有账号</li>
<li>撤销账号：注销账号，并删除所有注销账号有关的订单信息</li>
<li>查看机房信息：查看机房信息</li>
<li>清空预约：清空所有预约</li>
<li>退出登录</li>
</ul>
</li>
</ul>
</li>
</ul>
<span id="more"></span>

<h2 id="创建主菜单"><a href="#创建主菜单" class="headerlink" title="创建主菜单"></a>创建主菜单</h2><pre><code class="c++">class menu
&#123;
public:
    void main_menu();//主菜单

    void manager_menu(identity* Manager);//管理员子菜单

    void student_menu(identity* Student);//学生子菜单

    void teacher_menu(identity* Teacher);//教师子菜单
&#125;;

void menu::main_menu()//主菜单
&#123;
    cout &lt;&lt; &quot;\t\t==============欢迎来到机房预约系统====================&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t -----------------------------------------------------&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                   1.学生代表                        |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                   2.教师代表                        |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                   3. 管理员                         |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                   0.  退出                          |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot; \t\t----------------------------------------------------- &quot; &lt;&lt; endl;
    cout &lt;&lt; endl &lt;&lt; &quot;请输入您的身份:&quot;;
&#125;
int main()
&#123;
    int select=0;
    while (1)
    &#123;
        menu Menu;
        Menu.main_menu();
        cin &gt;&gt; select;
        switch (select) &#123;
        case 1://学生子菜单
            break;
        case 2://教师子菜单
            break;
        case 3://管理员子菜单
            break;
        case 0://退出系统
            return 0;
            break;
        default:
            cout &lt;&lt; &quot;输入有误!请重新输入！&quot; &lt;&lt; endl;
            system(&quot;pause&quot;);
            system(&quot;cls&quot;);
        &#125;
    &#125;
    return 1;
&#125;
</code></pre>
<h2 id="创建身份类"><a href="#创建身份类" class="headerlink" title="创建身份类"></a>创建身份类</h2><h4 id="身份类基类"><a href="#身份类基类" class="headerlink" title="身份类基类"></a>身份类基类</h4><pre><code class="c++">class identity
&#123;
public:
    string name;//身份姓名
    string password;//身份密码

    virtual void order_menu() = 0;//定义子菜单纯虚函数，子身份继承父类身份类时重写子菜单
&#125;;
</code></pre>
<h4 id="学生身份子类"><a href="#学生身份子类" class="headerlink" title="学生身份子类"></a>学生身份子类</h4><h5 id="student-h"><a href="#student-h" class="headerlink" title="student.h"></a>student.h</h5><pre><code class="c++">class student:public identity
&#123;
public:
    string id;//学号
    student();
    student(string id, string s_name, string s_password);
    
    void order_menu();//学生子菜单
    void apply_order();//申请预约
    void show_order();//查看预约
    void show_all_order();//查看所有预约
    void cancel_order();//取消预约
&#125;;
</code></pre>
<h5 id="student-cpp"><a href="#student-cpp" class="headerlink" title="student.cpp"></a>student.cpp</h5><pre><code class="c++">student::student() &#123;&#125;
student::student(string id, string s_name, string s_password) &#123;&#125;

void student::order_menu()&#123;&#125;

void student::apply_order()&#123;&#125;

void student::show_order()&#123;&#125;

void student::show_all_order()&#123;&#125;

void student::cancel_order()&#123;&#125;
</code></pre>
<h4 id="教师身份子类"><a href="#教师身份子类" class="headerlink" title="教师身份子类"></a>教师身份子类</h4><h5 id="teacher-h"><a href="#teacher-h" class="headerlink" title="teacher.h"></a>teacher.h</h5><pre><code class="c++">class teacher:public identity
&#123;
public:
    string id;//工号

    teacher();
    teacher(string id, string t_name, string t_password);
    
    void order_menu();//显示教师子菜单
    void show_allt_order();//查看所有预约
    void valid_order();//审核预约
&#125;;
</code></pre>
<h5 id="teacher-cpp"><a href="#teacher-cpp" class="headerlink" title="teacher.cpp"></a>teacher.cpp</h5><pre><code class="c++">teacher::teacher() &#123;&#125;;
teacher::teacher(string id, string t_name, string t_password) &#123;&#125;

void teacher::order_menu()&#123;&#125;

void teacher::show_allt_order()&#123;&#125;

void teacher::valid_order()&#123;&#125;
</code></pre>
<h4 id="管理员身份子类"><a href="#管理员身份子类" class="headerlink" title="管理员身份子类"></a>管理员身份子类</h4><h5 id="manager-h"><a href="#manager-h" class="headerlink" title="manager.h"></a>manager.h</h5><pre><code class="c++">class manager :public identity
&#123;
public:
    manager();
    manager( string m_name, string m_password);
    
    void order_menu();//显示管理员菜单
    void add_person();//添加账号
    void show_person();//查看账号
    void show_computer();//查看机房信息
    void clean_file();//清空账号	
&#125;;
</code></pre>
<h5 id="manager-cpp"><a href="#manager-cpp" class="headerlink" title="manager.cpp"></a>manager.cpp</h5><pre><code class="c++">manager::manager() &#123;&#125;;
manager::manager(string m_name,string m_password) &#123;&#125;

void manager::order_menu() &#123;&#125;

void manager::add_person() &#123;&#125;

void manager::show_person()&#123;&#125;

void manager::show_computer()&#123;&#125;

void manager::clean_file()&#123;&#125;
</code></pre>
<h2 id="添加后台数据文件"><a href="#添加后台数据文件" class="headerlink" title="添加后台数据文件"></a>添加后台数据文件</h2><p>定义全局文件：</p>
<h5 id="global-file-h"><a href="#global-file-h" class="headerlink" title="global_file.h"></a>global_file.h</h5><pre><code class="c++">#define ADMINFILE			&quot;admin.txt&quot;
#define STUDENTFILE			&quot;student.txt&quot;
#define TEACHERFILE			&quot;teacher.txt&quot;
#define COMPUTERROOMFILRE    &quot;computer_room.txt&quot;
#define ORDERFILE            &quot;order.txt&quot;
</code></pre>
<h2 id="登录模块"><a href="#登录模块" class="headerlink" title="登录模块"></a>登录模块</h2><p>在menu.h文件中给menu类中添加成员函数：</p>
<pre><code class="c++">void login_in(string file_name, int type);//登录模块
</code></pre>
<p>在menu.cpp文件中实现登录模块功能：</p>
<pre><code class="c++">void menu::login_in(string file_name, int type)
&#123;
    ifstream ifs;
    ifs.open(file_name, ios::in);
    if (!ifs.is_open())
    &#123;
        cout &lt;&lt; &quot;文件打开失败！&quot; &lt;&lt; endl;
        ifs.close();
        return;
    &#125;
    cout &lt;&lt; endl;
    string id;
    if (type == 1)
    &#123;
        cout &lt;&lt; &quot;请输入学号：&quot;;
        cin &gt;&gt; id;
    &#125;
    else
    &#123;
        if (type == 2)
        &#123;
            cout &lt;&lt; &quot;请输入工号:&quot;;
            cin &gt;&gt; id;
        &#125;
    &#125;
    string name, password;
    cout &lt;&lt; &quot;请输入姓名：&quot;;
    cin &gt;&gt; name;
    cout &lt;&lt; &quot;请输入密码：&quot;;
    cin &gt;&gt; password;

string fid, fname, fpassword;
identity* person = NULL;//定义多态父类身份指针
if (type == 1)//学生登录验证
&#123;
    while (ifs &gt;&gt; fid &amp;&amp; ifs &gt;&gt; fname &amp;&amp; ifs &gt;&gt; fpassword)//c++编码方式为ANSI（gbk）
    &#123;

​		if (fid == id &amp;&amp; fname == name &amp;&amp; fpassword == password)
​		&#123;

​			cout &lt;&lt; &quot;登陆成功！&quot; &lt;&lt; endl;
​			ifs.close();
​			system(&quot;pause&quot;);
​			system(&quot;cls&quot;);

​			person = new student(id, name, password);
​			//进入学生子菜单
​			student_menu(person);
​			return;
​		&#125;
​	&#125;
&#125;
else &#123;
​	if (type == 2)//教师登录验证
​	&#123;
​		while (ifs &gt;&gt; fid &amp;&amp; ifs &gt;&gt; fname &amp;&amp; ifs &gt;&gt; fpassword)
​		&#123;
​			if (fid == id &amp;&amp; fname == name &amp;&amp; fpassword == password)
​			&#123;
​				cout &lt;&lt; &quot;登陆成功！&quot; &lt;&lt; endl;
​				system(&quot;pause&quot;);
​				system(&quot;cls&quot;);

​				person = new teacher(id, name, password);
​				//进入教师子菜单
​				teacher_menu(person);
​				return;
​			&#125;
​		&#125;
​	&#125;
​	else
​		if (type == 3)//管理员登录验证
​		&#123;
​			while (ifs &gt;&gt; fname &amp;&amp; ifs &gt;&gt; fpassword)
​			&#123;
​				if (fname == name &amp;&amp; fpassword == password)
​				&#123;
​					cout &lt;&lt; &quot;登录成功！&quot; &lt;&lt; endl;
​					system(&quot;pause&quot;);
​					system(&quot;cls&quot;);

​					person = new manager(name, password);
​					//进入管理员子菜单
​					manager_menu(person);
​					return;
​				&#125;
​			&#125;
​		&#125;
&#125;
cout &lt;&lt; &quot;登录验证失败！&quot; &lt;&lt; endl;
system(&quot;pause&quot;);
system(&quot;cls&quot;);
return;
&#125;
</code></pre>
<h2 id="管理员模块"><a href="#管理员模块" class="headerlink" title="管理员模块"></a>管理员模块</h2><h3 id="管理员菜单搭建"><a href="#管理员菜单搭建" class="headerlink" title="管理员菜单搭建"></a>管理员菜单搭建</h3><pre><code class="c++">void manager::order_menu() &#123;
    cout &lt;&lt; &quot;欢迎管理员：&quot;&lt;&lt;this-&gt;name&lt;&lt;&quot;登录！&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t -----------------------------------------------------&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                   1.添加账号                        |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                   2.查看账号                        |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                   3.查看机房                        |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                   4.清空预约                        |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                   0.注销登录                        |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot; \t\t----------------------------------------------------- &quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;请选择您的操作：&quot; ;
&#125;
</code></pre>
<h3 id="添加账号"><a href="#添加账号" class="headerlink" title="添加账号"></a>添加账号</h3><h4 id="输入账号信息并写入后台文件student-txt文件中"><a href="#输入账号信息并写入后台文件student-txt文件中" class="headerlink" title="输入账号信息并写入后台文件student.txt文件中"></a>输入账号信息并写入后台文件student.txt文件中</h4><pre><code class="c++">void manager::add_person() &#123;
    int type = 0;
    string p_id, p_name, p_password;
    ofstream ofs;
    string p_filename;
    while (1)
    &#123;
        cout &lt;&lt; endl &lt;&lt; &quot;1.学生&quot; &lt;&lt; endl;
        cout &lt;&lt; &quot;2.教师&quot; &lt;&lt; endl &lt;&lt; endl;
        cout &lt;&lt; &quot;请输入添加的账户类型：&quot;;
        cin &gt;&gt; type;
        if (type == 1)
        &#123;
            p_filename = STUDENTFILE;
            cout &lt;&lt; &quot;请输入学号：&quot;;
            break;
        &#125;
        else if (type == 2)
        &#123;
            p_filename = TEACHERFILE;
            cout &lt;&lt; &quot;请输入工号：&quot;;
            break;
        &#125;
        else
        &#123;
            cout &lt;&lt; &quot;输入错误！请重新输入！&quot; &lt;&lt; endl;
        &#125;
    &#125;
cin &gt;&gt; p_id;
cout &lt;&lt; &quot;请输入姓名：&quot;;
cin &gt;&gt; p_name;
cout &lt;&lt; &quot;请输入密码：&quot;;
cin &gt;&gt; p_password;

ofs.open(p_filename, ios::out | ios::app);//以追加方式写入账户账户信息
if (!ofs.is_open())
&#123;
    cout &lt;&lt; &quot;文件打开失败！&quot; &lt;&lt; endl;
&#125;
else
&#123;
    ofs &lt;&lt; p_id &lt;&lt; &quot; &quot; &lt;&lt; p_name &lt;&lt; &quot; &quot; &lt;&lt; p_password &lt;&lt; endl;
&#125;
ofs.close();
cout &lt;&lt; &quot;添加成功！&quot; &lt;&lt; endl;

system(&quot;pause&quot;);
system(&quot;cls&quot;);
return;

&#125;
</code></pre>
<h4 id="处理检测账号是否重复问题"><a href="#处理检测账号是否重复问题" class="headerlink" title="处理检测账号是否重复问题"></a>处理检测账号是否重复问题</h4><h5 id="获取账号信息"><a href="#获取账号信息" class="headerlink" title="获取账号信息"></a>获取账号信息</h5><p>在manager.h文件中添加成员变量：</p>
<pre><code class="c++">vector&lt;student&gt; vstu;//获取学生账户信息
vector&lt;teacher&gt; vtea;//获取教师账户信息
</code></pre>
<p>在manager.h文件中读取后台账号数据信息与检测账号是否重复的成员函数：</p>
<pre><code class="c++">void init_vector();//初始化容器，从后台数据库中获取当前账号信息
bool check_repeat(string id,int type);//检测是否有重复账户
</code></pre>
<p>在manager.cpp文件中实现账号信息读取功能并将该函数写入构造函数中：</p>
<pre><code class="c++">void manager::init_vector()
&#123;
    ifstream ifs;
    string f_id, f_name, f_passord;
    vstu.clear();//容器初始化
    vtea.clear();

ifs.open(STUDENTFILE, ios::in);//读取学生信息
if (!ifs.is_open())
&#123;
    cout &lt;&lt; &quot;文件打开失败！&quot;&lt;&lt;endl;
    ifs.close();
&#125;
else
&#123;
    while (ifs &gt;&gt; f_id &amp;&amp; ifs &gt;&gt; f_name &amp;&amp; ifs &gt;&gt; f_passord)
    &#123;
        vstu.push_back(student(f_id, f_name, f_passord));
    &#125;
    cout &lt;&lt; &quot;当前学生数量为：&quot; &lt;&lt; vstu.size() &lt;&lt; endl;
    ifs.close();
&#125;

ifs.open(TEACHERFILE, ios::in);
if (!ifs.is_open())
&#123;
    cout &lt;&lt; &quot;文件打开失败！&quot;&lt;&lt;endl;
    ifs.close();
&#125;
else
&#123;
    while (ifs &gt;&gt; f_id &amp;&amp; ifs &gt;&gt; f_name &amp;&amp; ifs &gt;&gt; f_passord)
    &#123;
        //cout &lt;&lt; f_id &lt;&lt; &quot; &quot; &lt;&lt; f_name &lt;&lt; &quot; &quot; &lt;&lt; f_passord &lt;&lt; endl;
        vtea.push_back(teacher(f_id, f_name, f_passord));
    &#125;
    cout &lt;&lt; &quot;当前教师数量为：&quot; &lt;&lt; vtea.size() &lt;&lt; endl;
    ifs.close();
&#125;
&#125;
</code></pre>
<h5 id="添加检测账号重复函数"><a href="#添加检测账号重复函数" class="headerlink" title="添加检测账号重复函数"></a>添加检测账号重复函数</h5><p>在manager.cpp文件中实现账号信息读取功能账号（id）重复检测功能：</p>
<pre><code class="c++">bool manager::check_repeat(string id, int type)
&#123;
    if (type == 1)
    &#123;
        for (vector&lt;student&gt;::iterator it = vstu.begin(); it != vstu.end(); it++)
        &#123;
            if (it-&gt;id == id)
            &#123;
                return true;
            &#125;
        &#125;
    &#125;
    else if (type == 2)
    &#123;
        for (vector&lt;teacher&gt;::iterator it = vtea.begin(); it != vtea.end(); it++)
        &#123;
            if (it-&gt;id == id)
            &#123;
                return true;
            &#125;
        &#125;
    &#125;
    

return false;

&#125;
</code></pre>
<p>在add_person()函数中添加错误提示信息变量：</p>
<pre><code class="c++">string err;//错误提示信息
</code></pre>
<p>追加以下代码检测输入id是否有重复：</p>
<pre><code class="c++">while (1)
    &#123;
        cin &gt;&gt; p_id;
        if (this-&gt;check_repeat(p_id, type))
        &#123;
            cout &lt;&lt; err &lt;&lt; endl;
        &#125;
        else
            break;
    &#125;
</code></pre>
<h4 id="添加账号功能完善"><a href="#添加账号功能完善" class="headerlink" title="添加账号功能完善"></a>添加账号功能完善</h4><pre><code class="c++">void manager::add_person() &#123;
    int type = 0;
    string p_id, p_name, p_password;
    ofstream ofs;
    string p_filename;
    string err;//错误提示信息
    while (1)
    &#123;
        cout &lt;&lt; endl &lt;&lt; &quot;1.学生&quot; &lt;&lt; endl;
        cout &lt;&lt; &quot;2.教师&quot; &lt;&lt; endl &lt;&lt; endl;
        cout &lt;&lt; &quot;请输入添加的账户类型：&quot;;
        cin &gt;&gt; type;
        if (type == 1)
        &#123;
            p_filename = STUDENTFILE;
            err = &quot;学号已存在！请重新输入！&quot;;
            cout &lt;&lt; &quot;请输入学号：&quot;;
            break;
        &#125;
        else if (type == 2)
        &#123;
            p_filename = TEACHERFILE;
            err = &quot;工号已经存在！请重新输入！&quot;;
            cout &lt;&lt; &quot;请输入工号：&quot;;
            break;
        &#125;
        else
        &#123;
            cout &lt;&lt; &quot;输入错误！请重新输入！&quot; &lt;&lt; endl;
        &#125;
    &#125;

while (1)
&#123;
    cin &gt;&gt; p_id;
    if (this-&gt;check_repeat(p_id, type))
    &#123;
        cout &lt;&lt; err &lt;&lt; endl;
    &#125;
    else
        break;
&#125;

cout &lt;&lt; &quot;请输入姓名：&quot;;
cin &gt;&gt; p_name;
cout &lt;&lt; &quot;请输入密码：&quot;;
cin &gt;&gt; p_password;

ofs.open(p_filename, ios::out | ios::app);//以追加方式写入账户账户信息
if (!ofs.is_open())
&#123;
    cout &lt;&lt; &quot;文件打开失败！&quot; &lt;&lt; endl;
&#125;
else
&#123;
    ofs &lt;&lt; p_id &lt;&lt; &quot; &quot; &lt;&lt; p_name &lt;&lt; &quot; &quot; &lt;&lt; p_password &lt;&lt; endl;
&#125;
ofs.close();
cout &lt;&lt; &quot;添加成功！&quot; &lt;&lt; endl;

this-&gt;init_vector();//每次添加成功后就更新获取后台数据的容器中的数据，以保证检测账户重复机制的完整性

system(&quot;pause&quot;);
system(&quot;cls&quot;);
return;

&#125;
</code></pre>
<h3 id="查看账号"><a href="#查看账号" class="headerlink" title="查看账号"></a>查看账号</h3><pre><code class="c++">void manager::show_person()
&#123;
    int select = 0;
    cout &lt;&lt; endl &lt;&lt; &quot;1.学生&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;2.教师&quot; &lt;&lt; endl &lt;&lt; endl;
    cout &lt;&lt; &quot;请输入查看的账户类型：&quot;;
    while (1)
    &#123;
        cin &gt;&gt; select;
        if (select == 1)
        &#123;
            for (vector&lt;student&gt;::iterator it = vstu.begin(); it != vstu.end(); it++)
            &#123;
                cout &lt;&lt; &quot;工号：&quot; &lt;&lt; (*it).id &lt;&lt; &quot;\t&quot;
                    &lt;&lt; &quot;姓名：&quot; &lt;&lt;(*it).name &lt;&lt; &quot;\t&quot;
                    &lt;&lt; &quot;密码&quot;  &lt;&lt;(*it).password&lt;&lt; endl;
            &#125;
            system(&quot;pause&quot;);
            system(&quot;cls&quot;);
            break;
        &#125;
        else if (select == 2)
        &#123;
            for (vector&lt;teacher&gt;::iterator t_it = vtea.begin(); t_it != vtea.end(); t_it++)
            &#123;
                cout&lt;&lt;&quot;工号：&quot; &lt;&lt; (*t_it).id &lt;&lt; &quot;\t&quot; 
                    &lt;&lt;&quot;姓名：&quot;&lt;&lt; (*t_it).name &lt;&lt; &quot;\t&quot;
                    &lt;&lt;&quot;密码&quot;&lt;&lt; (*t_it).name &lt;&lt; endl;
            &#125;
            system(&quot;pause&quot;);
            system(&quot;cls&quot;);
            break;
        &#125;
        else
        &#123;
            cout &lt;&lt; &quot;输入错误！请重新输入！&quot; &lt;&lt; endl;
        &#125;
    &#125;
&#125;
</code></pre>
<h3 id="查看机房信息"><a href="#查看机房信息" class="headerlink" title="查看机房信息"></a>查看机房信息</h3><h4 id="添加机房信息文件"><a href="#添加机房信息文件" class="headerlink" title="添加机房信息文件"></a>添加机房信息文件</h4><p>computer_file.h</p>
<pre><code class="c++">class computer
&#123;
public:
    string c_id;
    int capcity;
    vector&lt;computer&gt; vc;//获取机房信息

    computer();
    computer(string id, int capcity);
&#125;;
</code></pre>
<h4 id="实现加载机房信息功能"><a href="#实现加载机房信息功能" class="headerlink" title="实现加载机房信息功能"></a>实现加载机房信息功能</h4><p>computer_file.cpp</p>
<pre><code class="c++">computer::computer()
&#123;
    ifstream ifs;
    ifs.open(COMPUTERROOMFILRE, ios::in);
    while (!ifs.is_open())
    &#123;
        cout &lt;&lt; &quot;文件打开失败！&quot; &lt;&lt; endl;
        ifs.close();
    &#125;
    string c_id;
    int capcity;
    while (ifs &gt;&gt; c_id &amp;&amp; ifs &gt;&gt; capcity)
    &#123;
        vc.push_back(computer(c_id, capcity));
    &#125;
&#125;
computer::computer(string id, int capcity)
&#123;
    this-&gt;c_id = id;
    this-&gt;capcity = capcity;
&#125;
</code></pre>
<h4 id="查看机房信息功能实现"><a href="#查看机房信息功能实现" class="headerlink" title="查看机房信息功能实现"></a>查看机房信息功能实现</h4><p>manager.cpp</p>
<pre><code class="c++">void manager::show_computer()
&#123;
    computer com;
    for (vector&lt;computer&gt;::iterator it = com.vc.begin(); it!= com.vc.end(); it++)
    &#123;
        cout &lt;&lt; &quot;机房号：&quot; &lt;&lt; it-&gt;c_id &lt;&lt; &quot;\t机房数量：&quot; &lt;&lt; it-&gt;capcity &lt;&lt; endl;
    &#125;
    system(&quot;pause&quot;);
    system(&quot;cls&quot;);
    return;
&#125;
</code></pre>
<h3 id="清空账号"><a href="#清空账号" class="headerlink" title="清空账号"></a>清空账号</h3><pre><code class="c++">void manager::clean_file()
&#123;
    ofstream ofs;
    ofs.open(ORDERFILE, ios::trunc);
    ofs.close();
    cout &lt;&lt; &quot;清空成功！&quot; &lt;&lt; endl;
    system(&quot;pause&quot;);
    system(&quot;cls&quot;);
&#125;
</code></pre>
<h2 id="学生模块"><a href="#学生模块" class="headerlink" title="学生模块"></a>学生模块</h2><h3 id="学生子菜单搭建"><a href="#学生子菜单搭建" class="headerlink" title="学生子菜单搭建"></a>学生子菜单搭建</h3><pre><code class="c++">void student::order_menu()
&#123;
    cout &lt;&lt; &quot;欢迎：&quot; &lt;&lt; this-&gt;name &lt;&lt; &quot;登录！&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t -----------------------------------------------------&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                   1.申请预约                        |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                   2.查看预约                        |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                   3.查看所有预约                    |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                   4.取消预约                        |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                   0.注销登录                        |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot; \t\t----------------------------------------------------- &quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;请选择您的操作：&quot;;
&#125;
</code></pre>
<h3 id="申请预约"><a href="#申请预约" class="headerlink" title="申请预约"></a>申请预约</h3><pre><code class="c++">void student::apply_order()
&#123;
    cout &lt;&lt; endl;
    cout &lt;&lt; &quot;1.周一&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;2.周二&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;3.周三&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;4.周四&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;5.周五&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;请选择申请日期：&quot;;
    int date=0;
    while (1)
    &#123;
        cin &gt;&gt; date;
        if (date &lt; 1 || date&gt;5)
            cout &lt;&lt; &quot;输入错误！请重新输入！&quot; &lt;&lt; endl;
        else
            break;
    &#125;
    cout &lt;&lt; endl;
    cout &lt;&lt; &quot;1.上午&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;2.下午&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;请选择时间段：&quot;;
    int interval=0;
    while (1)
    &#123;
        cin &gt;&gt; interval;
        if (interval&gt;=1 &amp;&amp; interval&lt;=2)
            break;
        else
            cout &lt;&lt; &quot;输入错误！请重新输入！&quot; &lt;&lt; endl;

    &#125;
    
    computer c;//加载机房信息
    cout &lt;&lt; endl;
    cout &lt;&lt;&quot;1号机房：&quot;&lt;&lt; c.vc[0].capcity &lt;&lt; endl;
    cout &lt;&lt;&quot;2号机房：&quot;&lt;&lt; c.vc[1].capcity &lt;&lt; endl;
    cout &lt;&lt;&quot;3号机房：&quot;&lt;&lt; c.vc[2].capcity &lt;&lt; endl;
    cout &lt;&lt; &quot;请选择机房号：&quot;;
    int num = 0;
    while (1)
    &#123;
        cin &gt;&gt; num;
        if (num &lt; 1 || num&gt;3)
            cout &lt;&lt; &quot;输入错误！请重新输入！&quot; &lt;&lt; endl;
        else
            break;
    &#125;
    
    ofstream ofs;
    ofs.open(ORDERFILE, ios::out | ios::app);
    ofs &lt;&lt; &quot;date:&quot; &lt;&lt; date &lt;&lt; &quot; &quot;;
    ofs &lt;&lt; &quot;interval:&quot; &lt;&lt; interval &lt;&lt; &quot; &quot;;
    ofs &lt;&lt; &quot;number:&quot; &lt;&lt; num &lt;&lt; &quot; &quot;;
    ofs &lt;&lt; &quot;stu_id:&quot; &lt;&lt; this-&gt;id &lt;&lt; &quot; &quot;;
    ofs &lt;&lt; &quot;stu_name:&quot; &lt;&lt; this-&gt;name &lt;&lt; &quot; &quot;;
    ofs &lt;&lt; &quot;status:&quot; &lt;&lt; 1 &lt;&lt; endl;//审核中
    ofs.close();
    cout &lt;&lt; &quot;预约成功！审核中！&quot; &lt;&lt; endl;
    
    system(&quot;pause&quot;);
    system(&quot;cls&quot;);
    return;

&#125;
</code></pre>
<h3 id="查看自身预约"><a href="#查看自身预约" class="headerlink" title="查看自身预约"></a>查看自身预约</h3><p>order_file.h</p>
<pre><code class="c++">class order_file
&#123;
public:
    

    map&lt;int, map&lt;string, string&gt;&gt; m;//存放所有订单记录
    int size;//订单记录数
    
    void sub_order(map&lt;string, string&gt;&amp; message,string order_message);//获取每条订单记录信息并将其存放在this-&gt;message中
    order_file();

&#125;;
</code></pre>
<p>order_file.cpp</p>
<h4 id="获取订单信息"><a href="#获取订单信息" class="headerlink" title="获取订单信息"></a>获取订单信息</h4><pre><code class="c++">void order_file::sub_order(map&lt;string, string&gt;&amp; message,string order_message)
&#123;
    int pos = order_message.find(&quot;:&quot;);
    if (pos != -1)
    &#123;
        string key = order_message.substr(0, pos);//截取关键字
        string value = order_message.substr(pos + 1, order_message.size() - pos-1);//截取值
        message.insert(make_pair(key, value));
    &#125;
&#125;

order_file::order_file()
&#123;
    this-&gt;size = 0;

    string date;//预约日期
    string interval;//预约时间段
    string com_number;//预约机房号
    string stu_id;//预约学生学号
    string stu_name;//预约学生姓名
    string status;//预约状态
    
    ifstream ifs;
    ifs.open(ORDERFILE, ios::in);
    while (ifs &gt;&gt; date &amp;&amp; ifs &gt;&gt; interval &amp;&amp; ifs &gt;&gt; com_number
        &amp;&amp; ifs &gt;&gt; stu_id &amp;&amp; ifs &gt;&gt; stu_name &amp;&amp; ifs &gt;&gt; status)
    &#123;
        map&lt;string, string&gt; message;
        sub_order(message,date);//截取日期放入小容器中
        sub_order(message,interval);//截取时间段放入小容器中
        sub_order(message,com_number);//截取机房号放入小容器中
        sub_order(message,stu_id);//截取学号放入小容器中
        sub_order(message,stu_name);//截取学生姓名放入小容器中
        sub_order(message,status);//截取日期审核状态小容器中
    
        this-&gt;m.insert(make_pair(this-&gt;size++, message));
    &#125;
    ifs.close();

&#125;
</code></pre>
<h4 id="查看预约功能实现（不显示取消订单记录）"><a href="#查看预约功能实现（不显示取消订单记录）" class="headerlink" title="查看预约功能实现（不显示取消订单记录）"></a>查看预约功能实现（不显示取消订单记录）</h4><pre><code class="c++">void student::show_order()
&#123;
    order_file order;
    if (order.size == 0)
    &#123;
        cout &lt;&lt; &quot;无预约记录！&quot; &lt;&lt; endl;
        system(&quot;pause&quot;);
        system(&quot;cls&quot;);
        return;
    &#125;

    for (int i = 0; i &lt; order.size; i++)
    &#123;
        if (order.m[i][&quot;stu_id&quot;] == this-&gt;id  &amp;&amp; order.m[i][&quot;status&quot;]!=&quot;0&quot;)
        &#123;
            cout &lt;&lt; &quot;日期：周&quot; &lt;&lt; order.m[i][&quot;date&quot;] &lt;&lt; &quot;\t&quot;;
            cout &lt;&lt; &quot;时间段：&quot; &lt;&lt; (order.m[i][&quot;interval&quot;]==&quot;1&quot;?&quot;上午&quot;:&quot;下午&quot;)&lt;&lt; &quot;\t&quot;;
            cout &lt;&lt; &quot;机房号：&quot; &lt;&lt; order.m[i][&quot;number&quot;] &lt;&lt; &quot;号&quot; &lt;&lt; &quot;\t&quot;;
            string status=&quot;预约状态：&quot;;
            if (order.m[i][&quot;status&quot;] == &quot;1&quot;)
                status += &quot;审核中&quot;;
            else if (order.m[i][&quot;status&quot;] == &quot;2&quot;)
                status += &quot;预约成功！&quot;;
            else if (order.m[i][&quot;status&quot;] == &quot;-1&quot;)
                status += &quot;预约失败！&quot;;
            cout &lt;&lt; status &lt;&lt; endl;
        &#125;
    &#125;
    system(&quot;pause&quot;);
    system(&quot;cls&quot;);
    return;

&#125;
</code></pre>
<h3 id="查看所有预约（不显示取消订单记录）"><a href="#查看所有预约（不显示取消订单记录）" class="headerlink" title="查看所有预约（不显示取消订单记录）"></a>查看所有预约（不显示取消订单记录）</h3><pre><code class="c++">void student::show_all_order()
&#123;
    order_file order;
    if (order.size == 0)
    &#123;
        cout &lt;&lt; &quot;无预约记录！&quot; &lt;&lt; endl;
        system(&quot;pause&quot;);
        system(&quot;cls&quot;);
        return;
    &#125;

    for (int i = 0; i &lt; order.size; i++)
    &#123;
        if (order.m[i][&quot;status&quot;] != &quot;0&quot;)
        &#123;
            cout &lt;&lt; &quot;学号：&quot; &lt;&lt; order.m[i][&quot;stu_id&quot;] &lt;&lt; &quot;\t&quot;;
            cout &lt;&lt; &quot;姓名：&quot; &lt;&lt; order.m[i][&quot;stu_name&quot;] &lt;&lt; &quot;\t&quot;;
            cout &lt;&lt; &quot;日期：周&quot; &lt;&lt; order.m[i][&quot;date&quot;] &lt;&lt; &quot;\t&quot;;
            cout &lt;&lt; &quot;时间段：&quot; &lt;&lt; (order.m[i][&quot;interval&quot;] == &quot;1&quot; ? &quot;上午&quot; : &quot;下午&quot;) &lt;&lt; &quot;\t&quot;;
            cout &lt;&lt; &quot;机房号：&quot; &lt;&lt; order.m[i][&quot;number&quot;] &lt;&lt; &quot;号&quot; &lt;&lt; &quot;\t&quot;;
            string status = &quot;预约状态：&quot;;
            if (order.m[i][&quot;status&quot;] == &quot;1&quot;)
                status += &quot;审核中&quot;;
            else if (order.m[i][&quot;status&quot;] == &quot;2&quot;)
                status += &quot;预约成功！&quot;;
            else if (order.m[i][&quot;status&quot;] == &quot;-1&quot;)
                status += &quot;预约失败！&quot;;
            cout &lt;&lt; status &lt;&lt; endl;
        &#125;
    &#125;
    system(&quot;pause&quot;);
    system(&quot;cls&quot;);
    return;

&#125;
</code></pre>
<h3 id="取消预约"><a href="#取消预约" class="headerlink" title="取消预约"></a>取消预约</h3><h4 id="添加更新订单记录功能"><a href="#添加更新订单记录功能" class="headerlink" title="添加更新订单记录功能"></a>添加更新订单记录功能</h4><p>在order_file.h文件中给order_file类添加更新订单成员函数</p>
<pre><code class="c++">void update_order();//更新预约记录
</code></pre>
<p>在order_file.cpp文件中实现更新订单记录功能：</p>
<pre><code class="c++">void order_file::update_order()
&#123;
    if (this-&gt;size == 0)
        return;
    ofstream ofs;
    ofs.open(ORDERFILE, ios::out | ios::trunc);
    for (int i = 0; i &lt; this-&gt;size; i++)
    &#123;
        ofs &lt;&lt; &quot;date:&quot; &lt;&lt; this-&gt;m[i][&quot;date&quot;] &lt;&lt; &quot; &quot;;
        ofs &lt;&lt; &quot;interval:&quot; &lt;&lt; this-&gt;m[i][&quot;interval&quot;] &lt;&lt; &quot; &quot;;
        ofs &lt;&lt; &quot;number:&quot; &lt;&lt; this-&gt;m[i][&quot;number&quot;] &lt;&lt; &quot; &quot;;
        ofs &lt;&lt; &quot;stu_id:&quot; &lt;&lt; this-&gt;m[i][&quot;stu_id&quot;] &lt;&lt; &quot; &quot;;
        ofs &lt;&lt; &quot;stu_name:&quot; &lt;&lt; this-&gt;m[i][&quot;stu_name&quot;] &lt;&lt; &quot; &quot;;
        ofs &lt;&lt; &quot;status:&quot; &lt;&lt; this-&gt;m[i][&quot;status&quot;] &lt;&lt; endl;
    &#125;
&#125;
</code></pre>
<h4 id="取消预约功能实现"><a href="#取消预约功能实现" class="headerlink" title="取消预约功能实现"></a>取消预约功能实现</h4><pre><code class="c++">void student::cancel_order()
&#123;
    order_file order;//加载订单信息
    vector&lt;int&gt; v_id;//存放要取消的订单记录号

    if (order.size == 0)
    &#123;
        cout &lt;&lt; &quot;无预约记录！&quot; &lt;&lt; endl;
        system(&quot;pause&quot;);
        system(&quot;cls&quot;);
        return;
    &#125;
    int index = 1;
    for (int i = 0; i &lt; order.size; i++)
    &#123;
        if (order.m[i][&quot;stu_id&quot;] == this-&gt;id)
        &#123;
            if (order.m[i][&quot;status&quot;] == &quot;1&quot; || order.m[i][&quot;status&quot;] == &quot;2&quot;)
            &#123;
                v_id.push_back(i);
                cout &lt;&lt; &quot;(&quot; &lt;&lt; index++ &lt;&lt; &quot;)  &quot;;
                cout &lt;&lt; &quot;学号：&quot; &lt;&lt; order.m[i][&quot;stu_id&quot;] &lt;&lt; &quot;\t&quot;;
                cout &lt;&lt; &quot;姓名：&quot; &lt;&lt; order.m[i][&quot;stu_name&quot;] &lt;&lt; &quot;\t&quot;;
                cout &lt;&lt; &quot;预约时间：周&quot; &lt;&lt; order.m[i][&quot;date&quot;] &lt;&lt; &quot;\t&quot;;
                cout &lt;&lt; &quot;预约时段：&quot; &lt;&lt; (order.m[i][&quot;interval&quot;] == &quot;1&quot; ? &quot;上午&quot; : &quot;下午&quot;) &lt;&lt; &quot;\t&quot;;
                cout &lt;&lt; &quot;机房号：&quot; &lt;&lt; order.m[i][&quot;number&quot;] &lt;&lt; &quot;\t&quot;;
                string status = &quot;预约状态：&quot;;
                cout &lt;&lt; (order.m[i][&quot;status&quot;] == &quot;1&quot; ? (status += &quot;审核中！&quot;) : (status += &quot;预约成功！&quot;)) &lt;&lt; endl;
            &#125;
        &#125;
    &#125;
    
    int select=0;
    while (1)
    &#123;
        cout &lt;&lt; &quot;请选择取消预约记录条数,0代表返回：&quot;;
        cin &gt;&gt; select;
        if (select &gt;= 0 &amp;&amp; select &lt;= order.size)
        &#123;
            if (select == 0)
            &#123;
                system(&quot;cls&quot;);
                return;
            &#125;
            order.m[v_id[select - 1]][&quot;status&quot;] = &quot;0&quot;;
            order.update_order();
            cout &lt;&lt; &quot;取消预约成功！&quot; &lt;&lt; endl;
            system(&quot;pause&quot;);
            system(&quot;cls&quot;);
            return;
        &#125;
        else
        &#123;
            cout &lt;&lt; &quot;输入有误！请重新输入！&quot; &lt;&lt; endl;
        &#125;
    &#125;

&#125;
</code></pre>
<h2 id="教师模块"><a href="#教师模块" class="headerlink" title="教师模块"></a>教师模块</h2><h3 id="显示教师子菜单"><a href="#显示教师子菜单" class="headerlink" title="显示教师子菜单"></a>显示教师子菜单</h3><pre><code class="c++">void teacher::order_menu()
&#123;
    cout &lt;&lt; &quot;欢迎教师：&quot; &lt;&lt; this-&gt;name &lt;&lt; &quot;登录！&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t -----------------------------------------------------&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                   1.查看所有预约                    |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                   2.审核预约                        |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                   0.注销登录                        |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot; \t\t----------------------------------------------------- &quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;请选择您的操作：&quot;;
&#125;
</code></pre>
<h3 id="查看所有预约"><a href="#查看所有预约" class="headerlink" title="查看所有预约"></a>查看所有预约</h3><pre><code class="c++">void teacher::show_allt_order()
&#123;
    order_file order;
    if (order.size == 0)
    &#123;
        cout &lt;&lt; &quot;无预约记录！&quot; &lt;&lt; endl;
        system(&quot;pause&quot;);
        system(&quot;cls&quot;);
        return;
    &#125;

    for (int i = 0; i &lt; order.size; i++)
    &#123;
        cout &lt;&lt; &quot;(&quot; &lt;&lt; i + 1 &lt;&lt; &quot;)  &quot;;
        cout &lt;&lt; &quot;学号：&quot; &lt;&lt; order.m[i][&quot;stu_id&quot;] &lt;&lt; &quot;\t&quot;;
        cout &lt;&lt; &quot;姓名：&quot; &lt;&lt; order.m[i][&quot;stu_name&quot;] &lt;&lt; &quot;\t&quot;;
        cout &lt;&lt; &quot;日期：周&quot; &lt;&lt; order.m[i][&quot;date&quot;] &lt;&lt; &quot;\t&quot;;
        cout &lt;&lt; &quot;时间段：&quot; &lt;&lt; (order.m[i][&quot;interval&quot;] == &quot;1&quot; ? &quot;上午&quot; : &quot;下午&quot;) &lt;&lt; &quot;\t&quot;;
        cout &lt;&lt; &quot;机房号：&quot; &lt;&lt; order.m[i][&quot;number&quot;] &lt;&lt; &quot;号&quot; &lt;&lt; &quot;\t&quot;;
        string status = &quot;预约状态：&quot;;
        if (order.m[i][&quot;status&quot;] == &quot;1&quot;)
            status += &quot;审核中&quot;;
        else if (order.m[i][&quot;status&quot;] == &quot;2&quot;)
            status += &quot;预约成功！&quot;;
        else if (order.m[i][&quot;status&quot;] == &quot;-1&quot;)
            status += &quot;预约失败！&quot;;
        else if (order.m[i][&quot;status&quot;] == &quot;0&quot;)
            status += &quot;预约已取消！&quot;;
        cout &lt;&lt; status &lt;&lt; endl;
    &#125;
    system(&quot;pause&quot;);
    system(&quot;cls&quot;);
    return;
&#125;
</code></pre>
<h3 id="审核预约"><a href="#审核预约" class="headerlink" title="审核预约"></a>审核预约</h3><pre><code class="c++">void teacher::valid_order()
&#123;
    order_file order;
    int index = 1;
    vector&lt;int&gt; v_id;

    if (order.size == 0)
    &#123;
        cout &lt;&lt; &quot;无预约记录！&quot; &lt;&lt; endl;
        system(&quot;pause&quot;);
        system(&quot;cls&quot;);
        return;
    &#125;
    for (int i = 0; i &lt; order.size; i++)
    &#123;
        if (order.m[i][&quot;status&quot;] == &quot;1&quot;)
        &#123;
            v_id.push_back(i);
            cout &lt;&lt; &quot;(&quot; &lt;&lt; index++ &lt;&lt; &quot;)  &quot;;
            cout &lt;&lt; &quot;学号：&quot; &lt;&lt; order.m[i][&quot;stu_id&quot;] &lt;&lt; &quot;\t&quot;;
            cout &lt;&lt; &quot;姓名：&quot; &lt;&lt; order.m[i][&quot;stu_name&quot;] &lt;&lt; &quot;\t&quot;;
            cout &lt;&lt; &quot;时间：周&quot; &lt;&lt; order.m[i][&quot;date&quot;] &lt;&lt; &quot;\t&quot;;
            cout &lt;&lt; &quot;时间段：&quot; &lt;&lt; ((order.m[i][&quot;interval&quot;] == &quot;1&quot;) ? (&quot;上午&quot;) : (&quot;下午&quot;)) &lt;&lt; &quot;\t&quot;;
            cout &lt;&lt; &quot;机房号：&quot; &lt;&lt; order.m[i][&quot;number&quot;] &lt;&lt; &quot;\t&quot;;
            if (order.m[i][&quot;status&quot;] == &quot;1&quot;)
            &#123;
                cout &lt;&lt; &quot;预约状态：审核中！&quot; &lt;&lt; endl;
            &#125;
        &#125;
    &#125;
    cout &lt;&lt; endl;
    int select = 0;
    string res;
    cout &lt;&lt; &quot;请选择审核记录,0代表返回：&quot;;
    cin &gt;&gt; select;
    while (1)
    &#123;
        if (select &gt;= 0 &amp;&amp; select &lt;= order.size)
        &#123;
            if (select == 0)
            &#123;
                system(&quot;cls&quot;);
                return;
            &#125;
            while (1)
            &#123;
                cout &lt;&lt; &quot;\n1.通过\n2.不通过&quot; &lt;&lt; endl;
                cout &lt;&lt; &quot;请选择审核结果：&quot;;
                cin &gt;&gt; res;
                if (res == &quot;1&quot;)
                &#123;
                    order.m[v_id[select - 1]][&quot;status&quot;] = &quot;2&quot;;
                    break;
                &#125;
                else if (res == &quot;2&quot;)
                &#123;
                    order.m[v_id[select - 1]][&quot;status&quot;] = &quot;-1&quot;;
                    break;
                &#125;
                else
                &#123;
                    cout &lt;&lt; &quot;输入错误！请重新输入！&quot; &lt;&lt; endl;
                &#125;
            &#125;
            order.update_order();
            cout &lt;&lt; &quot;审核成功！&quot; &lt;&lt; endl;
            system(&quot;pause&quot;);
            system(&quot;cls&quot;);
            return;
        &#125;
        else
        &#123;
            cout &lt;&lt; &quot;输入错误！请重新输入！&quot; &lt;&lt; endl;
        &#125;
    &#125;
&#125;
</code></pre>
<h2 id="功能完善"><a href="#功能完善" class="headerlink" title="功能完善"></a>功能完善</h2><h3 id="添加管理员撤销账号功能"><a href="#添加管理员撤销账号功能" class="headerlink" title="添加管理员撤销账号功能"></a>添加管理员撤销账号功能</h3><p>在manager.h文件中给manager类添加成员函数：</p>
<pre><code class="c++">void erase_person();//注销账号
void update_person(int type);//更新账号信息
</code></pre>
<p>在manager.cpp文件中实现更新账号功能：</p>
<pre><code class="c++">void manager::update_person(int type)
&#123;
    ofstream ofs;
    if (type == 1)
    &#123;
        ofs.open(STUDENTFILE, ios::out | ios::trunc);
        for (vector&lt;student&gt;::iterator it=vstu.begin();it!=vstu.end();it++)
        &#123;
            ofs &lt;&lt; it-&gt;id &lt;&lt; &quot; &quot; &lt;&lt; it-&gt;name &lt;&lt; &quot; &quot; &lt;&lt; it-&gt;password &lt;&lt; endl;
        &#125;
    &#125;
    else if (type == 2)
    &#123;
        ofs.open(TEACHERFILE, ios::out | ios::trunc);
        for (vector&lt;teacher&gt;::iterator it = vtea.begin(); it != vtea.end(); it++)
        &#123;
            ofs &lt;&lt; it-&gt;id &lt;&lt; &quot; &quot; &lt;&lt; it-&gt;name &lt;&lt; &quot; &quot; &lt;&lt; it-&gt;password &lt;&lt; endl;
        &#125;
    &#125;
    ofs.close();
&#125;
</code></pre>
<p>在manager.cpp文件中实现注销账号功能：</p>
<pre><code class="c++">void manager::erase_person()
&#123;
    cout &lt;&lt; &quot;\n1.学生\n2.教师&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;请输入注销账号类型：&quot;;
    int type;
    string era_sid;
    while (1)
    &#123;
        cin &gt;&gt; type;
        if (type == 1)
        &#123;
            if (vstu.size() == 0)
            &#123;
                cout &lt;&lt; &quot;学生账户为空！&quot; &lt;&lt; endl;
                system(&quot;pause&quot;);
                system(&quot;cls&quot;);
                return;
            &#125;

​		for (vector&lt;student&gt;::iterator it = vstu.begin(); it != vstu.end(); it++)
​		&#123;
​			cout&lt;&lt; &quot; 学号：&quot; &lt;&lt; it-&gt;id &lt;&lt; &quot;\t姓名：&quot; &lt;&lt; it-&gt;name &lt;&lt; endl;
​		&#125;
​		cout &lt;&lt; &quot;请输入注销学号：&quot;;
​		cin &gt;&gt; era_sid;
​		for (vector&lt;student&gt;::iterator it = vstu.begin(); it != vstu.end(); )
​		&#123;
​			if (it-&gt;id == era_sid)
​			&#123;
​				vstu.erase(it);
​				it = vstu.begin();//erase每次删除一个元素后vector迭代器就会失效，故需重置迭代器
​			&#125;
​			else
​				it++;
​		&#125;
​		break;
​	&#125;
​	else if(type == 2)
​	&#123;
​		if (vtea.size() == 0)
​		&#123;
​			cout &lt;&lt; &quot;教师账户为空！&quot; &lt;&lt; endl;
​			system(&quot;pause&quot;);
​			system(&quot;cls&quot;);
​			return;
​		&#125;
​		for (vector&lt;teacher&gt;::iterator it = vtea.begin(); it != vtea.end(); it++)
​		&#123;
​			cout &lt;&lt; &quot; 工号：&quot; &lt;&lt; it-&gt;id &lt;&lt; &quot;\t姓名：&quot; &lt;&lt; it-&gt;name &lt;&lt; endl;
​		&#125;
​		cout &lt;&lt; &quot;请输入注销工号：&quot;;
​		cin &gt;&gt; era_sid;
​		for (vector&lt;teacher&gt;::iterator it = vtea.begin(); it != vtea.end();)
​		&#123;
​			if (it-&gt;id == era_sid)
​			&#123;
​				vtea.erase(it);
​				it = vtea.begin();//erase每次删除一个元素后vector迭代器就会失效，故需重置迭代器
​			&#125;
​			else
​				it++;
​		&#125;
​		break;
​	&#125;
​	else
​	&#123;
​		cout &lt;&lt; &quot;输入错误！请重新输入！&quot; &lt;&lt; endl;
​		system(&quot;pause&quot;);
​		system(&quot;cls&quot;);
​		return;
​	&#125;
&#125;

this-&gt;update_person(type);//更新后台数据库

//更新订单信息
order_file order;
if (type == 1)
&#123;
    for (map&lt;int, map&lt;string, string&gt;&gt;::iterator it = order.m.begin(); it != order.m.end();)
    &#123;
        if (it-&gt;second[&quot;stu_id&quot;] == era_sid)
        &#123;
            order.m.erase(it);
            it = order.m.begin();
            order.size--;
        &#125;
        else
            it++;
    &#125;
&#125;
order.update_order();
cout &lt;&lt; &quot;注销成功！&quot; &lt;&lt; endl;
system(&quot;pause&quot;);
system(&quot;cls&quot;);
return;

&#125;
</code></pre>
<h3 id="添加学生申请预约检测功能"><a href="#添加学生申请预约检测功能" class="headerlink" title="添加学生申请预约检测功能"></a>添加学生申请预约检测功能</h3><p>在student.cpp文件中给apply_order()函数中，在学生输入预约信息后追加以下代码：</p>
<pre><code class="c++">order_file order;//加载订单信息
    for (int i = 0; i &lt; order.size; i++)
    &#123;
        if (order.m[i][&quot;stu_id&quot;] == this-&gt;id &amp;&amp; order.m[i][&quot;stu_name&quot;] == this-&gt;name
            &amp;&amp;order.m[i][&quot;date&quot;] ==to_string(date)
            &amp;&amp; order.m[i][&quot;interval&quot;] == to_string(interval)
            &amp;&amp; order.m[i][&quot;number&quot;] == to_string(num))
        &#123;
            if (order.m[i][&quot;status&quot;] == &quot;1&quot;)
            &#123;
                cout &lt;&lt; &quot;您已预约该时段，教师正在审核中，请耐心等待！&quot; &lt;&lt; endl;
                system(&quot;pause&quot;);
                system(&quot;cls&quot;);
                return;
            &#125;
            else if (order.m[i][&quot;status&quot;] == &quot;2&quot;)
            &#123;
                cout &lt;&lt; &quot;您已成功预约该时段，不能重复预约！&quot; &lt;&lt; endl;
                system(&quot;pause&quot;);
                system(&quot;cls&quot;);
                return;
            &#125;
        &#125;
    &#125;
</code></pre>
<h2 id="完整代码"><a href="#完整代码" class="headerlink" title="完整代码"></a>完整代码</h2><h3 id="global-file-h-1"><a href="#global-file-h-1" class="headerlink" title="global_file.h"></a>global_file.h</h3><pre><code class="c++">#pragma once
#include&lt;iostream&gt;
using namespace std;
#define ADMINFILE			&quot;admin.txt&quot;//&quot;D:\VSwork\VS_work\机房预约系统（再版）\机房预约系统（再版）\admin.txt&quot;
#define STUDENTFILE			&quot;student.txt&quot;//&quot;D:\VSwork\VS_work\机房预约系统（再版）\机房预约系统（再版）\student.txt&quot;
#define TEACHERFILE			&quot;teacher.txt&quot;//&quot;D:\VSwork\VS_work\机房预约系统（再版）\机房预约系统（再版）\teacher.txt&quot;
#define COMPUTERROOMFILRE   &quot;computer_room.txt&quot;//&quot;D:\VSwork\VS_work\机房预约系统（再版）\机房预约系统（再版）\computer_room.txt&quot;
#define ORDERFILE           &quot;order.txt&quot;//&quot;D:\VSwork\VS_work\机房预约系统（再版）\机房预约系统（再版）\order.txt&quot;
</code></pre>
<h3 id="computer-file-h"><a href="#computer-file-h" class="headerlink" title="computer_file.h"></a>computer_file.h</h3><pre><code class="c++">#pragma once
#include&lt;iostream&gt;
#include&lt;string&gt;
#include&lt;vector&gt;
#include&quot;global_file.h&quot;
#include&lt;fstream&gt;
using namespace std;

class computer
&#123;
public:
    string c_id;
    int capcity;
    vector&lt;computer&gt; vc;//获取机房信息

    computer();
    computer(string id, int capcity);

&#125;;
</code></pre>
<h3 id="computer-file-cpp"><a href="#computer-file-cpp" class="headerlink" title="computer_file.cpp"></a>computer_file.cpp</h3><pre><code class="c++">#include&quot;computer_room.h&quot;
computer::computer()
&#123;
    ifstream ifs;
    ifs.open(COMPUTERROOMFILRE, ios::in);
    while (!ifs.is_open())
    &#123;
        cout &lt;&lt; &quot;文件打开失败！&quot; &lt;&lt; endl;
        ifs.close();
    &#125;
    string c_id;
    int capcity;
    while (ifs &gt;&gt; c_id &amp;&amp; ifs &gt;&gt; capcity)
    &#123;
        vc.push_back(computer(c_id, capcity));
    &#125;
&#125;

computer::computer(string id, int capcity)
&#123;
    this-&gt;c_id = id;
    this-&gt;capcity = capcity;
&#125;
</code></pre>
<h3 id="order-file-h"><a href="#order-file-h" class="headerlink" title="order_file.h"></a>order_file.h</h3><pre><code class="c++">#pragma once
#include&lt;iostream&gt;
#include&lt;map&gt;
#include&lt;string&gt;
#include&lt;fstream&gt;
#include&quot;global_file.h&quot;
using namespace std;
class order_file
&#123;
public:
    

    map&lt;int, map&lt;string, string&gt;&gt; m;//存放所有订单记录
    int size;//订单记录数
    
    void sub_order(map&lt;string, string&gt;&amp; message,string order_message);//获取每条订单记录信息并将其存放在this-&gt;message中
    order_file();
    void update_order();//更新预约记录

&#125;;
</code></pre>
<h3 id="order-file-cpp"><a href="#order-file-cpp" class="headerlink" title="order_file.cpp"></a>order_file.cpp</h3><pre><code class="c++">#include&quot;order_file.h&quot;

void order_file::sub_order(map&lt;string, string&gt;&amp; message,string order_message)
&#123;
    int pos = order_message.find(&quot;:&quot;);
    if (pos != -1)
    &#123;
        string key = order_message.substr(0, pos);//截取关键字
        string value = order_message.substr(pos + 1, order_message.size() - pos-1);//截取值
        message.insert(make_pair(key, value));
    &#125;
&#125;

order_file::order_file()
&#123;
    this-&gt;size = 0;

    string date;//预约日期
    string interval;//预约时间段
    string com_number;//预约机房号
    string stu_id;//预约学生学号
    string stu_name;//预约学生姓名
    string status;//预约状态
    
    ifstream ifs;
    ifs.open(ORDERFILE, ios::in);
    while (ifs &gt;&gt; date &amp;&amp; ifs &gt;&gt; interval &amp;&amp; ifs &gt;&gt; com_number
        &amp;&amp; ifs &gt;&gt; stu_id &amp;&amp; ifs &gt;&gt; stu_name &amp;&amp; ifs &gt;&gt; status)
    &#123;
        map&lt;string, string&gt; message;
        sub_order(message,date);//截取日期放入小容器中
        sub_order(message,interval);//截取时间段放入小容器中
        sub_order(message,com_number);//截取机房号放入小容器中
        sub_order(message,stu_id);//截取学号放入小容器中
        sub_order(message,stu_name);//截取学生姓名放入小容器中
        sub_order(message,status);//截取日期审核状态小容器中
    
        this-&gt;m.insert(make_pair(this-&gt;size++, message));
    &#125;
    ifs.close();

&#125;

void order_file::update_order()
&#123;
    if (this-&gt;size == 0)
        return;
    ofstream ofs;
    ofs.open(ORDERFILE, ios::out | ios::trunc);
    for (int i = 0; i &lt; this-&gt;size; i++)
    &#123;
        ofs &lt;&lt; &quot;date:&quot; &lt;&lt; this-&gt;m[i][&quot;date&quot;] &lt;&lt; &quot; &quot;;
        ofs &lt;&lt; &quot;interval:&quot; &lt;&lt; this-&gt;m[i][&quot;interval&quot;] &lt;&lt; &quot; &quot;;
        ofs &lt;&lt; &quot;number:&quot; &lt;&lt; this-&gt;m[i][&quot;number&quot;] &lt;&lt; &quot; &quot;;
        ofs &lt;&lt; &quot;stu_id:&quot; &lt;&lt; this-&gt;m[i][&quot;stu_id&quot;] &lt;&lt; &quot; &quot;;
        ofs &lt;&lt; &quot;stu_name:&quot; &lt;&lt; this-&gt;m[i][&quot;stu_name&quot;] &lt;&lt; &quot; &quot;;
        ofs &lt;&lt; &quot;status:&quot; &lt;&lt; this-&gt;m[i][&quot;status&quot;] &lt;&lt; endl;
    &#125;
&#125;
</code></pre>
<h3 id="identity-h"><a href="#identity-h" class="headerlink" title="identity.h"></a>identity.h</h3><pre><code class="c++">#pragma once
#include&lt;iostream&gt;
#include&lt;string&gt;
using namespace std;
class identity
&#123;
public:
    string name;
    string password;

    virtual void order_menu() = 0;

&#125;;
</code></pre>
<h3 id="manager-h-1"><a href="#manager-h-1" class="headerlink" title="manager.h"></a>manager.h</h3><pre><code class="c++">#pragma once
#include&quot;identity.h&quot;
#include&lt;fstream&gt;
#include&lt;vector&gt;
#include&quot;student.h&quot;
#include&quot;teacher.h&quot;
#include&quot;global_file.h&quot;
#include&quot;computer_room.h&quot;
#include&quot;order_file.h&quot;

using namespace std;

class manager :public identity
&#123;
public:
    vector&lt;student&gt; vstu;//获取学生账户信息
    vector&lt;teacher&gt; vtea;//获取教师账户信息

    manager();
    manager( string m_name, string m_password);
    
    void order_menu();//显示管理员菜单
    void add_person();//添加账号
    void show_person();//查看账号
    void erase_person();//注销账号
    void show_computer();//查看机房信息
    void clean_file();//清空账号
    void update_person(int type);
    
    void init_vector();//初始化容器，从后台数据库中获取当前信息
    bool check_repeat(string id,int type);//检测是否有重复账户

&#125;;
</code></pre>
<h3 id="manager-cpp-1"><a href="#manager-cpp-1" class="headerlink" title="manager.cpp"></a>manager.cpp</h3><pre><code class="c++">#pragma once
#include&quot;manager.h&quot;
#include&lt;algorithm&gt;
using namespace std;

class computer;

manager::manager() &#123;&#125;;
manager::manager(string m_name,string m_password) &#123;
    this-&gt;name = m_name;
    this-&gt;password = m_password;

    init_vector();//初始化容器，获取当前数据库信息
    computer com;
    cout &lt;&lt; &quot;当前机房数量为：&quot; &lt;&lt; com.vc.size() &lt;&lt; endl;

&#125;

void manager::order_menu() &#123;
    cout &lt;&lt; &quot;欢迎管理员：&quot;&lt;&lt;this-&gt;name&lt;&lt;&quot;登录！&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t -----------------------------------------------------&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                   1.添加账号                        |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                   2.查看账号                        |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                   3.注销账号                        |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                   4.查看机房                        |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                   5.清空预约                        |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                   0.注销登录                        |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot; \t\t----------------------------------------------------- &quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;请选择您的操作：&quot; ;
&#125;

void manager::init_vector()
&#123;
    ifstream ifs;
    string f_id, f_name, f_passord;
    vstu.clear();//容器初始化
    vtea.clear();

    ifs.open(STUDENTFILE, ios::in);//读取学生信息
    if (!ifs.is_open())
    &#123;
        cout &lt;&lt; &quot;文件打开失败！&quot;&lt;&lt;endl;
        ifs.close();
    &#125;
    else
    &#123;
        while (ifs &gt;&gt; f_id &amp;&amp; ifs &gt;&gt; f_name &amp;&amp; ifs &gt;&gt; f_passord)
        &#123;
            vstu.push_back(student(f_id, f_name, f_passord));
        &#125;
        cout &lt;&lt; &quot;当前学生数量为：&quot; &lt;&lt; vstu.size() &lt;&lt; endl;
        ifs.close();
    &#125;
    
    ifs.open(TEACHERFILE, ios::in);
    if (!ifs.is_open())
    &#123;
        cout &lt;&lt; &quot;文件打开失败！&quot;&lt;&lt;endl;
        ifs.close();
    &#125;
    else
    &#123;
        while (ifs &gt;&gt; f_id &amp;&amp; ifs &gt;&gt; f_name &amp;&amp; ifs &gt;&gt; f_passord)
        &#123;
            //cout &lt;&lt; f_id &lt;&lt; &quot; &quot; &lt;&lt; f_name &lt;&lt; &quot; &quot; &lt;&lt; f_passord &lt;&lt; endl;
            vtea.push_back(teacher(f_id, f_name, f_passord));
        &#125;
        cout &lt;&lt; &quot;当前教师数量为：&quot; &lt;&lt; vtea.size() &lt;&lt; endl;
        ifs.close();
    &#125;

&#125;

bool manager::check_repeat(string id, int type)
&#123;
    if (type == 1)
    &#123;
        for (vector&lt;student&gt;::iterator it = vstu.begin(); it != vstu.end(); it++)
        &#123;
            if (it-&gt;id == id)
            &#123;
                return true;
            &#125;
        &#125;
    &#125;
    else if (type == 2)
    &#123;
        for (vector&lt;teacher&gt;::iterator it = vtea.begin(); it != vtea.end(); it++)
        &#123;
            if (it-&gt;id == id)
            &#123;
                return true;
            &#125;
        &#125;
    &#125;
    

    return false;

&#125;

void manager::add_person() &#123;
    int type = 0;
    string p_id, p_name, p_password;
    ofstream ofs;
    string p_filename;
    string err;//错误提示信息
    while (1)
    &#123;
        cout &lt;&lt; endl &lt;&lt; &quot;1.学生&quot; &lt;&lt; endl;
        cout &lt;&lt; &quot;2.教师&quot; &lt;&lt; endl &lt;&lt; endl;
        cout &lt;&lt; &quot;请输入添加的账户类型：&quot;;
        cin &gt;&gt; type;
        if (type == 1)
        &#123;
            p_filename = STUDENTFILE;
            err = &quot;学号已存在！请重新输入！&quot;;
            cout &lt;&lt; &quot;请输入学号：&quot;;
            break;
        &#125;
        else if (type == 2)
        &#123;
            p_filename = TEACHERFILE;
            err = &quot;工号已经存在！请重新输入！&quot;;
            cout &lt;&lt; &quot;请输入工号：&quot;;
            break;
        &#125;
        else
        &#123;
            cout &lt;&lt; &quot;输入错误！请重新输入！&quot; &lt;&lt; endl;
        &#125;
    &#125;

    while (1)
    &#123;
        cin &gt;&gt; p_id;
        if (this-&gt;check_repeat(p_id, type))
        &#123;
            cout &lt;&lt; err &lt;&lt; endl;
        &#125;
        else
            break;
    &#125;


    cout &lt;&lt; &quot;请输入姓名：&quot;;
    cin &gt;&gt; p_name;
    cout &lt;&lt; &quot;请输入密码：&quot;;
    cin &gt;&gt; p_password;
    
    ofs.open(p_filename, ios::out | ios::app);//以追加方式写入账户账户信息
    if (!ofs.is_open())
    &#123;
        cout &lt;&lt; &quot;文件打开失败！&quot; &lt;&lt; endl;
    &#125;
    else
    &#123;
        ofs &lt;&lt; p_id &lt;&lt; &quot; &quot; &lt;&lt; p_name &lt;&lt; &quot; &quot; &lt;&lt; p_password &lt;&lt; endl;
    &#125;
    ofs.close();
    cout &lt;&lt; &quot;添加成功！&quot; &lt;&lt; endl;
    
    this-&gt;init_vector();//每次添加成功后就更新获取后台数据的容器中的数据，以保证检测账户重复机制的完整性
    
    system(&quot;pause&quot;);
    system(&quot;cls&quot;);
    return;

&#125;

void manager::show_person()
&#123;
    int select = 0;
    cout &lt;&lt; endl &lt;&lt; &quot;1.学生&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;2.教师&quot; &lt;&lt; endl &lt;&lt; endl;
    cout &lt;&lt; &quot;请输入查看的账户类型：&quot;;
    while (1)
    &#123;
        cin &gt;&gt; select;
        if (select == 1)
        &#123;
            if (vstu.size() == 0)
            &#123;
                cout &lt;&lt; &quot;学生账户为空！&quot; &lt;&lt; endl;
            &#125;
            for (vector&lt;student&gt;::iterator it = vstu.begin(); it != vstu.end(); it++)
            &#123;
                cout &lt;&lt; &quot;工号：&quot; &lt;&lt; (*it).id &lt;&lt; &quot;\t&quot;
                    &lt;&lt; &quot;姓名：&quot; &lt;&lt;(*it).name &lt;&lt; &quot;\t&quot;
                    &lt;&lt; &quot;密码&quot;  &lt;&lt;(*it).password&lt;&lt; endl;
            &#125;
            system(&quot;pause&quot;);
            system(&quot;cls&quot;);
            break;
        &#125;
        else if (select == 2)
        &#123;
            if (vtea.size() == 0)
            &#123;
                cout &lt;&lt; &quot;教师账户为空！&quot; &lt;&lt; endl;
            &#125;
            for (vector&lt;teacher&gt;::iterator t_it = vtea.begin(); t_it != vtea.end(); t_it++)
            &#123;
                cout&lt;&lt;&quot;工号：&quot; &lt;&lt; (*t_it).id &lt;&lt; &quot;\t&quot; 
                    &lt;&lt;&quot;姓名：&quot;&lt;&lt; (*t_it).name &lt;&lt; &quot;\t&quot;
                    &lt;&lt;&quot;密码&quot;&lt;&lt; (*t_it).name &lt;&lt; endl;
            &#125;
            system(&quot;pause&quot;);
            system(&quot;cls&quot;);
            break;
        &#125;
        else
        &#123;
            cout &lt;&lt; &quot;输入错误！请重新输入！&quot; &lt;&lt; endl;
        &#125;
    &#125;
&#125;

void manager::update_person(int type)
&#123;
    ofstream ofs;
    if (type == 1)
    &#123;
        ofs.open(STUDENTFILE, ios::out | ios::trunc);
        for (vector&lt;student&gt;::iterator it=vstu.begin();it!=vstu.end();it++)
        &#123;
            ofs &lt;&lt; it-&gt;id &lt;&lt; &quot; &quot; &lt;&lt; it-&gt;name &lt;&lt; &quot; &quot; &lt;&lt; it-&gt;password &lt;&lt; endl;
        &#125;
    &#125;
    else if (type == 2)
    &#123;
        ofs.open(TEACHERFILE, ios::out | ios::trunc);
        for (vector&lt;teacher&gt;::iterator it = vtea.begin(); it != vtea.end(); it++)
        &#123;
            ofs &lt;&lt; it-&gt;id &lt;&lt; &quot; &quot; &lt;&lt; it-&gt;name &lt;&lt; &quot; &quot; &lt;&lt; it-&gt;password &lt;&lt; endl;
        &#125;
    &#125;
    ofs.close();
&#125;

void manager::erase_person()
&#123;
    cout &lt;&lt; &quot;\n1.学生\n2.教师&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;请输入注销账号类型：&quot;;
    int type;
    string era_sid;
    while (1)
    &#123;
        cin &gt;&gt; type;
        if (type == 1)
        &#123;
            if (vstu.size() == 0)
            &#123;
                cout &lt;&lt; &quot;学生账户为空！&quot; &lt;&lt; endl;
                system(&quot;pause&quot;);
                system(&quot;cls&quot;);
                return;
            &#125;

            for (vector&lt;student&gt;::iterator it = vstu.begin(); it != vstu.end(); it++)
            &#123;
                cout&lt;&lt; &quot; 学号：&quot; &lt;&lt; it-&gt;id &lt;&lt; &quot;\t姓名：&quot; &lt;&lt; it-&gt;name &lt;&lt; endl;
            &#125;
            cout &lt;&lt; &quot;请输入注销学号：&quot;;
            cin &gt;&gt; era_sid;
            for (vector&lt;student&gt;::iterator it = vstu.begin(); it != vstu.end(); )
            &#123;
                if (it-&gt;id == era_sid)
                &#123;
                    it=vstu.erase(it);
                    //it = vstu.begin();//erase每次删除一个元素后vector迭代器就会失效，故需重置迭代器
                &#125;
                else
                    it++;
            &#125;
            break;
        &#125;
        else if(type == 2)
        &#123;
            if (vtea.size() == 0)
            &#123;
                cout &lt;&lt; &quot;教师账户为空！&quot; &lt;&lt; endl;
                system(&quot;pause&quot;);
                system(&quot;cls&quot;);
                return;
            &#125;
            for (vector&lt;teacher&gt;::iterator it = vtea.begin(); it != vtea.end(); it++)
            &#123;
                cout &lt;&lt; &quot; 工号：&quot; &lt;&lt; it-&gt;id &lt;&lt; &quot;\t姓名：&quot; &lt;&lt; it-&gt;name &lt;&lt; endl;
            &#125;
            cout &lt;&lt; &quot;请输入注销工号：&quot;;
            cin &gt;&gt; era_sid;
            for (vector&lt;teacher&gt;::iterator it = vtea.begin(); it != vtea.end(); it++)
            &#123;
                if (it-&gt;id == era_sid)
                &#123;
                    vtea.erase(it);
                    it = vtea.begin();//erase每次删除一个元素后vector迭代器就会失效，故需重置迭代器
                &#125;
                else
                    it++;
            &#125;
            break;
        &#125;
        else
        &#123;
            cout &lt;&lt; &quot;输入错误！请重新输入！&quot; &lt;&lt; endl;
            system(&quot;pause&quot;);
            system(&quot;cls&quot;);
            return;
        &#125;
    &#125;
    
    this-&gt;update_person(type);//更新后台数据库
    
    //更新订单信息
    order_file order;
    if (type == 1)
    &#123;
        for (map&lt;int, map&lt;string, string&gt;&gt;::iterator it = order.m.begin(); it != order.m.end();)
        &#123;
            if (it-&gt;second[&quot;stu_id&quot;] == era_sid)
            &#123;
                order.m.erase(it);
                it = order.m.begin();
                order.size--;
            &#125;
            else
                it++;
        &#125;
    &#125;
    order.update_order();
    cout &lt;&lt; &quot;注销成功！&quot; &lt;&lt; endl;
    system(&quot;pause&quot;);
    system(&quot;cls&quot;);
    return;

&#125;

void manager::show_computer()
&#123;
    computer com;
    for (vector&lt;computer&gt;::iterator it = com.vc.begin(); it!= com.vc.end(); it++)
    &#123;
        cout &lt;&lt; &quot;机房号：&quot; &lt;&lt; it-&gt;c_id &lt;&lt; &quot;\t机房数量：&quot; &lt;&lt; it-&gt;capcity &lt;&lt; endl;
    &#125;
    system(&quot;pause&quot;);
    system(&quot;cls&quot;);
    return;
&#125;

void manager::clean_file()
&#123;
    ofstream ofs;
    ofs.open(ORDERFILE, ios::trunc);
    ofs.close();
    cout &lt;&lt; &quot;清空成功！&quot; &lt;&lt; endl;
    system(&quot;pause&quot;);
    system(&quot;cls&quot;);
&#125;
</code></pre>
<h3 id="student-h-1"><a href="#student-h-1" class="headerlink" title="student.h"></a>student.h</h3><pre><code class="c++">#pragma once
#include&quot;identity.h&quot;
#include&quot;computer_room.h&quot;
#include&lt;vector&gt;
#include&lt;fstream&gt;
#include&quot;global_file.h&quot;
#include&quot;order_file.h&quot;
using namespace std;
class student:public identity
&#123;
public:
    string id;

    student();
    student(string id, string s_name, string s_password);
    
    void order_menu();//学生子菜单
    void apply_order();//申请预约
    void show_order();//查看预约
    void show_all_order();//查看所有预约
    void cancel_order();//取消预约

&#125;;
</code></pre>
<h3 id="student-cpp-1"><a href="#student-cpp-1" class="headerlink" title="student.cpp"></a>student.cpp</h3><pre><code class="c++">#pragma once
#include&lt;iostream&gt;
#include&quot;student.h&quot;
using namespace std;

student::student() &#123;&#125;
student::student(string id, string s_name, string s_password) &#123;
    this-&gt;id = id;
    this-&gt;name = s_name;
    this-&gt;password = s_password;
&#125;

void student::order_menu()
&#123;
    cout &lt;&lt; &quot;欢迎：&quot; &lt;&lt; this-&gt;name &lt;&lt; &quot;登录！&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t -----------------------------------------------------&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                   1.申请预约                        |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                   2.查看预约                        |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                   3.查看所有预约                    |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                   4.取消预约                        |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                   0.注销登录                        |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot; \t\t----------------------------------------------------- &quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;请选择您的操作：&quot;;
&#125;

void student::apply_order()
&#123;
    cout &lt;&lt; endl;
    cout &lt;&lt; &quot;1.周一&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;2.周二&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;3.周三&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;4.周四&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;5.周五&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;请选择申请日期：&quot;;
    int date=0;
    while (1)
    &#123;
        cin &gt;&gt; date;
        if (date &lt; 1 || date&gt;5)
            cout &lt;&lt; &quot;输入错误！请重新输入！&quot; &lt;&lt; endl;
        else
            break;
    &#125;
    cout &lt;&lt; endl;
    cout &lt;&lt; &quot;1.上午&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;2.下午&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;请选择时间段：&quot;;
    int interval=0;
    while (1)
    &#123;
        cin &gt;&gt; interval;
        if (interval&gt;=1 &amp;&amp; interval&lt;=2)
            break;
        else
            cout &lt;&lt; &quot;输入错误！请重新输入！&quot; &lt;&lt; endl;

    &#125;
    
    computer c;//加载机房信息
    cout &lt;&lt; endl;
    cout &lt;&lt;&quot;1号机房：&quot;&lt;&lt; c.vc[0].capcity &lt;&lt; endl;
    cout &lt;&lt;&quot;2号机房：&quot;&lt;&lt; c.vc[1].capcity &lt;&lt; endl;
    cout &lt;&lt;&quot;3号机房：&quot;&lt;&lt; c.vc[2].capcity &lt;&lt; endl;
    cout &lt;&lt; &quot;请选择机房号：&quot;;
    int num = 0;
    while (1)
    &#123;
        cin &gt;&gt; num;
        if (num &lt; 1 || num&gt;3)
            cout &lt;&lt; &quot;输入错误！请重新输入！&quot; &lt;&lt; endl;
        else
            break;
    &#125;
    
    order_file order;//加载订单信息
    for (int i = 0; i &lt; order.size; i++)
    &#123;
        if (order.m[i][&quot;stu_id&quot;] == this-&gt;id &amp;&amp; order.m[i][&quot;stu_name&quot;] == this-&gt;name
            &amp;&amp;order.m[i][&quot;date&quot;] ==to_string(date)
            &amp;&amp; order.m[i][&quot;interval&quot;] == to_string(interval)
            &amp;&amp; order.m[i][&quot;number&quot;] == to_string(num))
        &#123;
            if (order.m[i][&quot;status&quot;] == &quot;1&quot;)
            &#123;
                cout &lt;&lt; &quot;您已预约该时段，教师正在审核中，请耐心等待！&quot; &lt;&lt; endl;
                system(&quot;pause&quot;);
                system(&quot;cls&quot;);
                return;
            &#125;
            else if (order.m[i][&quot;status&quot;] == &quot;2&quot;)
            &#123;
                cout &lt;&lt; &quot;您已成功预约该时段，不能重复预约！&quot; &lt;&lt; endl;
                system(&quot;pause&quot;);
                system(&quot;cls&quot;);
                return;
            &#125;
        &#125;
    &#125;
    ofstream ofs;
    ofs.open(ORDERFILE, ios::out | ios::app);
    ofs &lt;&lt; &quot;date:&quot; &lt;&lt; date &lt;&lt; &quot; &quot;;
    ofs &lt;&lt; &quot;interval:&quot; &lt;&lt; interval &lt;&lt; &quot; &quot;;
    ofs &lt;&lt; &quot;number:&quot; &lt;&lt; num &lt;&lt; &quot; &quot;;
    ofs &lt;&lt; &quot;stu_id:&quot; &lt;&lt; this-&gt;id &lt;&lt; &quot; &quot;;
    ofs &lt;&lt; &quot;stu_name:&quot; &lt;&lt; this-&gt;name &lt;&lt; &quot; &quot;;
    ofs &lt;&lt; &quot;status:&quot; &lt;&lt; 1 &lt;&lt; endl;//审核中
    ofs.close();
    cout &lt;&lt; &quot;预约成功！审核中！&quot; &lt;&lt; endl;
    
    system(&quot;pause&quot;);
    system(&quot;cls&quot;);
    return;

&#125;

void student::show_order()
&#123;
    order_file order;
    if (order.size == 0)
    &#123;
        cout &lt;&lt; &quot;无预约记录！&quot; &lt;&lt; endl;
        system(&quot;pause&quot;);
        system(&quot;cls&quot;);
        return;
    &#125;
    

    for (int i = 0; i &lt; order.size; i++)
    &#123;
        if (order.m[i][&quot;stu_id&quot;] == this-&gt;id  &amp;&amp; order.m[i][&quot;status&quot;]!=&quot;0&quot;)
        &#123;
            cout &lt;&lt; &quot;日期：周&quot; &lt;&lt; order.m[i][&quot;date&quot;] &lt;&lt; &quot;\t&quot;;
            cout &lt;&lt; &quot;时间段：&quot; &lt;&lt; (order.m[i][&quot;interval&quot;]==&quot;1&quot;?&quot;上午&quot;:&quot;下午&quot;)&lt;&lt; &quot;\t&quot;;
            cout &lt;&lt; &quot;机房号：&quot; &lt;&lt; order.m[i][&quot;number&quot;] &lt;&lt; &quot;号&quot; &lt;&lt; &quot;\t&quot;;
            string status=&quot;预约状态：&quot;;
            if (order.m[i][&quot;status&quot;] == &quot;1&quot;)
                status += &quot;审核中&quot;;
            else if (order.m[i][&quot;status&quot;] == &quot;2&quot;)
                status += &quot;预约成功！&quot;;
            else if (order.m[i][&quot;status&quot;] == &quot;-1&quot;)
                status += &quot;预约失败！&quot;;
            cout &lt;&lt; status &lt;&lt; endl;
        &#125;
    &#125;
    system(&quot;pause&quot;);
    system(&quot;cls&quot;);
    return;

&#125;

void student::show_all_order()
&#123;
    order_file order;
    if (order.size == 0)
    &#123;
        cout &lt;&lt; &quot;无预约记录！&quot; &lt;&lt; endl;
        system(&quot;pause&quot;);
        system(&quot;cls&quot;);
        return;
    &#125;

    for (int i = 0; i &lt; order.size; i++)
    &#123;
        if (order.m[i][&quot;status&quot;] != &quot;0&quot;)
        &#123;
            cout &lt;&lt; &quot;学号：&quot; &lt;&lt; order.m[i][&quot;stu_id&quot;] &lt;&lt; &quot;\t&quot;;
            cout &lt;&lt; &quot;姓名：&quot; &lt;&lt; order.m[i][&quot;stu_name&quot;] &lt;&lt; &quot;\t&quot;;
            cout &lt;&lt; &quot;日期：周&quot; &lt;&lt; order.m[i][&quot;date&quot;] &lt;&lt; &quot;\t&quot;;
            cout &lt;&lt; &quot;时间段：&quot; &lt;&lt; (order.m[i][&quot;interval&quot;] == &quot;1&quot; ? &quot;上午&quot; : &quot;下午&quot;) &lt;&lt; &quot;\t&quot;;
            cout &lt;&lt; &quot;机房号：&quot; &lt;&lt; order.m[i][&quot;number&quot;] &lt;&lt; &quot;号&quot; &lt;&lt; &quot;\t&quot;;
            string status = &quot;预约状态：&quot;;
            if (order.m[i][&quot;status&quot;] == &quot;1&quot;)
                status += &quot;审核中&quot;;
            else if (order.m[i][&quot;status&quot;] == &quot;2&quot;)
                status += &quot;预约成功！&quot;;
            else if (order.m[i][&quot;status&quot;] == &quot;-1&quot;)
                status += &quot;预约失败！&quot;;
            cout &lt;&lt; status &lt;&lt; endl;
        &#125;
    &#125;
    system(&quot;pause&quot;);
    system(&quot;cls&quot;);
    return;

&#125;

void student::cancel_order()
&#123;
    order_file order;//加载订单信息
    vector&lt;int&gt; v_id;//存放要取消的订单记录号

    if (order.size == 0)
    &#123;
        cout &lt;&lt; &quot;无预约记录！&quot; &lt;&lt; endl;
        system(&quot;pause&quot;);
        system(&quot;cls&quot;);
        return;
    &#125;
    int index = 1;
    for (int i = 0; i &lt; order.size; i++)
    &#123;
        if (order.m[i][&quot;stu_id&quot;] == this-&gt;id)
        &#123;
            if (order.m[i][&quot;status&quot;] == &quot;1&quot; || order.m[i][&quot;status&quot;] == &quot;2&quot;)
            &#123;
                v_id.push_back(i);
                cout &lt;&lt; &quot;(&quot; &lt;&lt; index++ &lt;&lt; &quot;)  &quot;;
                cout &lt;&lt; &quot;学号：&quot; &lt;&lt; order.m[i][&quot;stu_id&quot;] &lt;&lt; &quot;\t&quot;;
                cout &lt;&lt; &quot;姓名：&quot; &lt;&lt; order.m[i][&quot;stu_name&quot;] &lt;&lt; &quot;\t&quot;;
                cout &lt;&lt; &quot;预约时间：周&quot; &lt;&lt; order.m[i][&quot;date&quot;] &lt;&lt; &quot;\t&quot;;
                cout &lt;&lt; &quot;预约时段：&quot; &lt;&lt; (order.m[i][&quot;interval&quot;] == &quot;1&quot; ? &quot;上午&quot; : &quot;下午&quot;) &lt;&lt; &quot;\t&quot;;
                cout &lt;&lt; &quot;机房号：&quot; &lt;&lt; order.m[i][&quot;number&quot;] &lt;&lt; &quot;\t&quot;;
                string status = &quot;预约状态：&quot;;
                cout &lt;&lt; (order.m[i][&quot;status&quot;] == &quot;1&quot; ? (status += &quot;审核中！&quot;) : (status += &quot;预约成功！&quot;)) &lt;&lt; endl;
            &#125;
        &#125;
    &#125;
    
    int select=0;
    while (1)
    &#123;
        cout &lt;&lt; &quot;请选择取消预约记录条数,0代表返回：&quot;;
        cin &gt;&gt; select;
        if (select &gt;= 0 &amp;&amp; select &lt;= order.size)
        &#123;
            if (select == 0)
            &#123;
                system(&quot;cls&quot;);
                return;
            &#125;
            order.m[v_id[select - 1]][&quot;status&quot;] = &quot;0&quot;;
            order.update_order();
            cout &lt;&lt; &quot;取消预约成功！&quot; &lt;&lt; endl;
            system(&quot;pause&quot;);
            system(&quot;cls&quot;);
            return;
        &#125;
        else
        &#123;
            cout &lt;&lt; &quot;输入有误！请重新输入！&quot; &lt;&lt; endl;
        &#125;
    &#125;

&#125;
</code></pre>
<h3 id="teacher-h-1"><a href="#teacher-h-1" class="headerlink" title="teacher.h"></a>teacher.h</h3><pre><code class="c++">#pragma once
#include&lt;vector&gt;
#include&quot;identity.h&quot;
#include&quot;order_file.h&quot;
#include&quot;student.h&quot;
using namespace std;
class teacher:public identity
&#123;
public:
    string id;

    teacher();
    teacher(string id, string t_name, string t_password);
    
    void order_menu();//显示教师子菜单
    void show_allt_order();//查看所有预约
    void valid_order();//审核预约

&#125;;
</code></pre>
<h3 id="teacher-cpp-1"><a href="#teacher-cpp-1" class="headerlink" title="teacher.cpp"></a>teacher.cpp</h3><pre><code class="c++">#pragma once
#include&quot;teacher.h&quot;
using namespace std;

teacher::teacher() &#123;&#125;;
teacher::teacher(string id, string t_name, string t_password) &#123;
    this-&gt;id = id;
    this-&gt;name = t_name;
    this-&gt;password = t_password;
&#125;

void teacher::order_menu()
&#123;
    cout &lt;&lt; &quot;欢迎教师：&quot; &lt;&lt; this-&gt;name &lt;&lt; &quot;登录！&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t -----------------------------------------------------&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                   1.查看所有预约                    |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                   2.审核预约                        |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                   0.注销登录                        |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot; \t\t----------------------------------------------------- &quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;请选择您的操作：&quot;;
&#125;

void teacher::show_allt_order()
&#123;
    order_file order;
    if (order.size == 0)
    &#123;
        cout &lt;&lt; &quot;无预约记录！&quot; &lt;&lt; endl;
        system(&quot;pause&quot;);
        system(&quot;cls&quot;);
        return;
    &#125;

    for (int i = 0; i &lt; order.size; i++)
    &#123;
        cout &lt;&lt; &quot;(&quot; &lt;&lt; i + 1 &lt;&lt; &quot;)  &quot;;
        cout &lt;&lt; &quot;学号：&quot; &lt;&lt; order.m[i][&quot;stu_id&quot;] &lt;&lt; &quot;\t&quot;;
        cout &lt;&lt; &quot;姓名：&quot; &lt;&lt; order.m[i][&quot;stu_name&quot;] &lt;&lt; &quot;\t&quot;;
        cout &lt;&lt; &quot;日期：周&quot; &lt;&lt; order.m[i][&quot;date&quot;] &lt;&lt; &quot;\t&quot;;
        cout &lt;&lt; &quot;时间段：&quot; &lt;&lt; (order.m[i][&quot;interval&quot;] == &quot;1&quot; ? &quot;上午&quot; : &quot;下午&quot;) &lt;&lt; &quot;\t&quot;;
        cout &lt;&lt; &quot;机房号：&quot; &lt;&lt; order.m[i][&quot;number&quot;] &lt;&lt; &quot;号&quot; &lt;&lt; &quot;\t&quot;;
        string status = &quot;预约状态：&quot;;
        if (order.m[i][&quot;status&quot;] == &quot;1&quot;)
            status += &quot;审核中&quot;;
        else if (order.m[i][&quot;status&quot;] == &quot;2&quot;)
            status += &quot;预约成功！&quot;;
        else if (order.m[i][&quot;status&quot;] == &quot;-1&quot;)
            status += &quot;预约失败！&quot;;
        else if (order.m[i][&quot;status&quot;] == &quot;0&quot;)
            status += &quot;预约已取消！&quot;;
        cout &lt;&lt; status &lt;&lt; endl;
    &#125;
    system(&quot;pause&quot;);
    system(&quot;cls&quot;);
    return;

&#125;

void teacher::valid_order()
&#123;
    order_file order;
    int index = 1;
    vector&lt;int&gt; v_id;

    if (order.size == 0)
    &#123;
        cout &lt;&lt; &quot;无预约记录！&quot; &lt;&lt; endl;
        system(&quot;pause&quot;);
        system(&quot;cls&quot;);
        return;
    &#125;
    for (int i = 0; i &lt; order.size; i++)
    &#123;
        if (order.m[i][&quot;status&quot;] == &quot;1&quot;)
        &#123;
            v_id.push_back(i);
            cout &lt;&lt; &quot;(&quot; &lt;&lt; index++ &lt;&lt; &quot;)  &quot;;
            cout &lt;&lt; &quot;学号：&quot; &lt;&lt; order.m[i][&quot;stu_id&quot;] &lt;&lt; &quot;\t&quot;;
            cout &lt;&lt; &quot;姓名：&quot; &lt;&lt; order.m[i][&quot;stu_name&quot;] &lt;&lt; &quot;\t&quot;;
            cout &lt;&lt; &quot;时间：周&quot; &lt;&lt; order.m[i][&quot;date&quot;] &lt;&lt; &quot;\t&quot;;
            cout &lt;&lt; &quot;时间段：&quot; &lt;&lt; ((order.m[i][&quot;interval&quot;] == &quot;1&quot;) ? (&quot;上午&quot;) : (&quot;下午&quot;)) &lt;&lt; &quot;\t&quot;;
            cout &lt;&lt; &quot;机房号：&quot; &lt;&lt; order.m[i][&quot;number&quot;] &lt;&lt; &quot;\t&quot;;
            if (order.m[i][&quot;status&quot;] == &quot;1&quot;)
            &#123;
                cout &lt;&lt; &quot;预约状态：审核中！&quot; &lt;&lt; endl;
            &#125;
        &#125;
    &#125;
    cout &lt;&lt; endl;
    int select = 0;
    string res;
    cout &lt;&lt; &quot;请选择审核记录,0代表返回：&quot;;
    cin &gt;&gt; select;
    while (1)
    &#123;
        if (select &gt;= 0 &amp;&amp; select &lt;= order.size)
        &#123;
            if (select == 0)
            &#123;
                system(&quot;cls&quot;);
                return;
            &#125;
            while (1)
            &#123;
                cout &lt;&lt; &quot;\n1.通过\n2.不通过&quot; &lt;&lt; endl;
                cout &lt;&lt; &quot;请选择审核结果：&quot;;
                cin &gt;&gt; res;
                if (res == &quot;1&quot;)
                &#123;
                    order.m[v_id[select - 1]][&quot;status&quot;] = &quot;2&quot;;
                    break;
                &#125;
                else if (res == &quot;2&quot;)
                &#123;
                    order.m[v_id[select - 1]][&quot;status&quot;] = &quot;-1&quot;;
                    break;
                &#125;
                else
                &#123;
                    cout &lt;&lt; &quot;输入错误！请重新输入！&quot; &lt;&lt; endl;
                &#125;
            &#125;
            order.update_order();
            cout &lt;&lt; &quot;审核成功！&quot; &lt;&lt; endl;
            system(&quot;pause&quot;);
            system(&quot;cls&quot;);
            return;
        &#125;
        else
        &#123;
            cout &lt;&lt; &quot;输入错误！请重新输入！&quot; &lt;&lt; endl;
        &#125;
    &#125;
&#125;
</code></pre>
<h3 id="menu-h"><a href="#menu-h" class="headerlink" title="menu.h"></a>menu.h</h3><pre><code class="c++">#include&lt;string&gt;
#include&lt;fstream&gt;
#include&quot;global_file.h&quot;
#include&quot;identity.h&quot;
#include&quot;student.h&quot;
#include&quot;teacher.h&quot;
#include&quot;manager.h&quot;
using namespace std;

class menu
&#123;
public:
    void main_menu();//主菜单

    void manager_menu(identity* Manager);//管理员子菜单
    
    void student_menu(identity* Student);//学生子菜单
    
    void teacher_menu(identity* Teacher);//教师子菜单
    
    void login_in(string file_name, int type);//登录模块

&#125;;
</code></pre>
<h3 id="menu-cpp"><a href="#menu-cpp" class="headerlink" title="menu.cpp"></a>menu.cpp</h3><pre><code class="c++">#pragma once
#include&quot;menu.h&quot;
void menu::main_menu()//主菜单
&#123;
    cout &lt;&lt; &quot;\t\t==============欢迎来到机房预约系统====================&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t -----------------------------------------------------&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                   1.学生代表                        |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                   2.教师代表                        |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                   3. 管理员                         |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                   0.  退出                          |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot;\t\t|                                                     |&quot; &lt;&lt; endl;
    cout &lt;&lt; &quot; \t\t----------------------------------------------------- &quot; &lt;&lt; endl;
    cout &lt;&lt; endl &lt;&lt; &quot;请输入您的身份:&quot;;
&#125;

void menu::manager_menu(identity* Manager)
&#123;
    while (1)
    &#123;
        Manager-&gt;order_menu();//父类指针只能调用公共接口
        //将父类指针强制转为子类指针，以调用子类接口
        manager* man = (manager*)Manager;
        int select = 0;
        cin &gt;&gt; select;
        if (select == 1)
        &#123;
            //cout &lt;&lt; &quot;添加账号&quot; &lt;&lt; endl;
            man-&gt;add_person();
        &#125;
        else if (select == 2)
        &#123;
            //cout &lt;&lt; &quot;查看账号&quot; &lt;&lt; endl;
            man-&gt;show_person();
        &#125;
        else if (select == 3)
        &#123;
            //cout &lt;&lt; &quot;注销账号&quot; &lt;&lt; endl;
            man-&gt;erase_person();
        &#125;
        else if (select == 4)
        &#123;
            //cout &lt;&lt; &quot;查看机房信息&quot; &lt;&lt; endl;
            man-&gt;show_computer();
        &#125;
        else if (select == 5)
        &#123;
            //cout &lt;&lt; &quot;清空预约&quot; &lt;&lt; endl;
            man-&gt;clean_file();
        &#125;
        else if (select == 0)
        &#123;
            delete Manager;
            cout &lt;&lt; &quot;注销成功！&quot; &lt;&lt; endl;
            system(&quot;pause&quot;);
            system(&quot;cls&quot;);
            return;
        &#125;
        else &#123;
            cout &lt;&lt; &quot;输入错误！请重新输入！&quot; &lt;&lt; endl;
            system(&quot;pause&quot;);
            system(&quot;cls&quot;);
        &#125;
    &#125;

&#125;

void menu::student_menu(identity* Student)
&#123;
    while (1)
    &#123;
        Student-&gt;order_menu();
        student* stu = (student*)Student;
        int select = 0;
        cin &gt;&gt; select;
        if (select == 1)
        &#123;
            stu-&gt;apply_order();//申请预约
        &#125;
        else if (select == 2)
        &#123;
            stu-&gt;show_order();//查看预约
        &#125;
        else if (select == 3)
        &#123;
            stu-&gt;show_all_order();//查看所有预约
        &#125;
        else if (select == 4)
        &#123;
            stu-&gt;cancel_order();//取消预约
        &#125;
        else if (select == 0)//退出登录
        &#123;
            delete Student;
            cout &lt;&lt; &quot;注销成功！&quot; &lt;&lt; endl;
            system(&quot;pause&quot;);
            system(&quot;cls&quot;);
            return;
        &#125;
        else &#123;
            cout &lt;&lt; &quot;输入错误！请重新输入！&quot; &lt;&lt; endl;
            system(&quot;pause&quot;);
            system(&quot;cls&quot;);
        &#125;
    &#125;
&#125;

void menu::teacher_menu(identity* Teacher)
&#123;
    teacher* tea = (teacher*)Teacher;
    int select = 0;
    while (1)
    &#123;
        tea-&gt;order_menu();
        cin &gt;&gt; select;
        if (select == 1)
        &#123;
            tea-&gt;show_allt_order();//显示所有预约
        &#125;
        else if (select == 2)
        &#123;
            tea-&gt;valid_order();//审核预约
        &#125;
        else if (select == 0)
        &#123;
            delete Teacher;
            cout &lt;&lt; &quot;注销登录成功！&quot; &lt;&lt; endl;
            system(&quot;pause&quot;);
            system(&quot;cls&quot;);
            return;
        &#125;
        else
        &#123;
            cout &lt;&lt; &quot;输入错误！请重新输入！&quot; &lt;&lt; endl;
            system(&quot;pause&quot;);
            system(&quot;cls&quot;);
        &#125;
    &#125;
&#125;

void menu::login_in(string file_name, int type)
&#123;
    ifstream ifs;
    ifs.open(file_name, ios::in);
    if (!ifs.is_open())
    &#123;
        cout &lt;&lt; &quot;文件打开失败！&quot; &lt;&lt; endl;
        ifs.close();
        return;
    &#125;
    cout &lt;&lt; endl;
    string id;
    if (type == 1)
    &#123;
        cout &lt;&lt; &quot;请输入学号：&quot;;
        cin &gt;&gt; id;
    &#125;
    else
    &#123;
        if (type == 2)
        &#123;
            cout &lt;&lt; &quot;请输入工号:&quot;;
            cin &gt;&gt; id;
        &#125;
    &#125;
    string name, password;
    cout &lt;&lt; &quot;请输入姓名：&quot;;
    cin &gt;&gt; name;
    cout &lt;&lt; &quot;请输入密码：&quot;;
    cin &gt;&gt; password;

    string fid, fname, fpassword;
    identity* person = NULL;
    if (type == 1)//学生登录验证
    &#123;
        while (ifs &gt;&gt; fid &amp;&amp; ifs &gt;&gt; fname &amp;&amp; ifs &gt;&gt; fpassword)//c++编码方式为ANSI（gbk）
        &#123;
            //cout &lt;&lt; fid &lt;&lt; &quot; &quot; &lt;&lt; fname &lt;&lt; &quot; &quot; &lt;&lt; fpassword &lt;&lt; endl;
    
            if (fid == id &amp;&amp; fname == name &amp;&amp; fpassword == password)
            &#123;
    
                cout &lt;&lt; &quot;登陆成功！&quot; &lt;&lt; endl;
                ifs.close();
                system(&quot;pause&quot;);
                system(&quot;cls&quot;);
    
                person = new student(id, name, password);
                //进入学生子菜单
                student_menu(person);
                return;
            &#125;
        &#125;
    &#125;
    else &#123;
        if (type == 2)//教师登录验证
        &#123;
            while (ifs &gt;&gt; fid &amp;&amp; ifs &gt;&gt; fname &amp;&amp; ifs &gt;&gt; fpassword)
            &#123;
                if (fid == id &amp;&amp; fname == name &amp;&amp; fpassword == password)
                &#123;
                    cout &lt;&lt; &quot;登陆成功！&quot; &lt;&lt; endl;
                    system(&quot;pause&quot;);
                    system(&quot;cls&quot;);
    
                    person = new teacher(id, name, password);
                    //进入教师子菜单
                    teacher_menu(person);
                    return;
                &#125;
            &#125;
        &#125;
        else
            if (type == 3)//管理员登录验证
            &#123;
                while (ifs &gt;&gt; fname &amp;&amp; ifs &gt;&gt; fpassword)
                &#123;
                    if (fname == name &amp;&amp; fpassword == password)
                    &#123;
                        cout &lt;&lt; &quot;登录成功！&quot; &lt;&lt; endl;
                        system(&quot;pause&quot;);
                        system(&quot;cls&quot;);
    
                        person = new manager(name, password);
                        //进入管理员子菜单
                        manager_menu(person);
                        return;
                    &#125;
                &#125;
            &#125;
    &#125;
    cout &lt;&lt; &quot;登录验证失败！&quot; &lt;&lt; endl;
    system(&quot;pause&quot;);
    system(&quot;cls&quot;);
    return;

&#125;
</code></pre>
<h3 id="机房预约系统-cpp"><a href="#机房预约系统-cpp" class="headerlink" title="机房预约系统.cpp"></a>机房预约系统.cpp</h3><pre><code class="c++">#include&lt;iostream&gt;
#include&quot;menu.h&quot;
using namespace std;
int main()
&#123;
    int select=0;
    while (1)
    &#123;
        menu Menu;
        Menu.main_menu();
        cin &gt;&gt; select;
        switch (select) &#123;
        case 1://学生子菜单
            Menu.login_in(STUDENTFILE, 1);
            break;
        case 2://教师子菜单
            Menu.login_in(TEACHERFILE, 2);
            break;
        case 3://管理员子菜单
            Menu.login_in(ADMINFILE, 3);
            break;
        case 0://退出系统
            cout &lt;&lt; &quot;欢迎下次使用！&quot; &lt;&lt; endl;
            return 0;
            break;
        default:
            cout &lt;&lt; &quot;输入有误!请重新输入！&quot; &lt;&lt; endl;
            system(&quot;pause&quot;);
            system(&quot;cls&quot;);
        &#125;
    &#125;
    return 1;
&#125;
</code></pre>
]]></content>
      <categories>
        <category>项目</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>项目</tag>
      </tags>
  </entry>
  <entry>
    <title>豆瓣影评词云统计分析</title>
    <url>/2022/04/30/%E8%B1%86%E7%93%A3%E5%BD%B1%E8%AF%84%E8%AF%8D%E4%BA%91%E7%BB%9F%E8%AE%A1%E5%88%86%E6%9E%90/</url>
    <content><![CDATA[<!-- more -->

<pre><code class="python">import requests#请求服务器获取网页数据
from bs4 import BeautifulSoup as bs#解析数据
import re#正则匹配
import jieba.analyse#分词包,提取关键词
import matplotlib.pyplot as plt#画图函数
# import matplotlib
from wordcloud import WordCloud#词云包，显示词频统计

url=&quot;https://movie.douban.com/cinema/nowplaying/zhengzhou/&quot;
headers=&#123;&quot;User-Agent&quot;: &quot;Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/80.0.3987.163 Safari/537.36 Edg/80.0.361.111&quot;&#125;
res=requests.get(url,headers=headers)#封装网页向服务器发送请求
html=res.text#将服务前返回的数据转码为字符串
soup=bs(html,&quot;html.parser&quot;)#解析请求数据
# print(soup)
nowplaying_movie=soup.find(&quot;ul&quot;,class_=&quot;lists&quot;)#获取包含电影id及名称的标签
# print(nowplaying_movie)
nowplaying_movie_list=nowplaying_movie.find_all(&quot;li&quot;,class_=&quot;list-item&quot;)#获取最新电影列表信息
# print(nowplaying_movie_list)

nowplaying_list=[]
for item in nowplaying_movie_list:
    nowplaying_dict=&#123;&#125;#存储每一部电影的id号和名称
    nowplaying_dict[&#39;id&#39;]=item[&#39;data-subject&#39;]
    for img_item in item.find_all(&quot;img&quot;):
        nowplaying_dict[&#39;name&#39;]=img_item[&#39;alt&#39;]
    nowplaying_list.append(nowplaying_dict)

# for i in nowplaying_list:
#     print(i)
    # 27619748

num=1
for item in nowplaying_list:
    print(str(num)+&quot;:&quot;+item[&#39;name&#39;])
    num=num+1

i=int(input(&quot;请输入电影序号:&quot;))

# 影片短评网址
for i in range(10):
    page = (10 - 1) * 20  # 获取10页评论信息
    url1 = r&quot;https://movie.douban.com/subject/&quot; + nowplaying_list[i - 1][&#39;id&#39;] + &quot;/comments?start=&quot; + str(
        page) + &quot;&amp;limit=20&quot;
    # url1=r&quot;https://movie.douban.com/subject/26826330/comments?start=0&amp;limit=20&quot;
    headers = &#123;
        &quot;User-Agent&quot;: &quot;Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/80.0.3987.163 Safari/537.36 Edg/80.0.361.111&quot;&#125;
    # headers=&#123;&quot;user-agent&quot;: &quot;Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/80.0.3987.163 Safari/537.36 Edg/80.0.361.111&quot;&#125;
    req = requests.get(url1, headers=headers)
    html1 = req.text  # 获取评论网页数据
    soup1 = bs(html1, &quot;html.parser&quot;)
    # print(soup1)
    comments = soup1.find(&quot;div&quot;, class_=&quot;mod-bd&quot;)
    # print(comments)
    comment_lists = comments.find_all(&quot;div&quot;, class_=&quot;comment-item&quot;)  # 获取电影存储所有短评的标签
    # print(comment_lists)
    comment_list = []
    for comment_p in comment_lists:
        comment = comment_p.find_all(&#39;p&#39;)[0].text  # 获取所有短评
        if comment is not None:
            # print(comment)
            comment_list.append(comment)

# print(comment_list)
com=&#39;&#39;
for k in range(len(comment_list)):#将所有评论数据转换为字符串
    com=com+comment_list[k]

#清理数据
pattern=re.compile(r&#39;[\u4e00-\u9fa5]+&#39;)#正则匹配,去除数据中的换行符及其他无关符号
filterdata=re.findall(pattern,com)
cleaned_comments=&#39;&#39;.join(filterdata)#清理后得到的最终评论数据
print(cleaned_comments)

#提取关键词
# jieba.analyse.set_stop_words(&quot;D:\pythonProject1\spider_code\jieba_stop_words.txt&quot;)
result=jieba.analyse.textrank(cleaned_comments,topK=50,withWeight=True)#对清理的数据提取出50个关键词，并显示每个关键词的权重比例
# result输出数据为以元组为元素的列表，故将元组转换为字典
keywords=dict()
for item in result:
    keywords[item[0]]=item[1]

stopwords=&#39;&#39;#存储对词频统计无意义不相关的词汇,即添加停用词(可自定义)
f=open(&#39;D:\pythonProject1\spider_code\jieba_stop_words.txt&#39;,encoding=&quot;utf-8&quot;)#jieba_stop_words.txt,停用词字典数据(自定义)
while True:
    word=f.readline()
    if word==&#39;&#39;:
        break
    stopwords+=word
# print(stopwords)

keywords=&#123;x:keywords[x] for x in keywords if x not in stopwords&#125;#将评论数据与停用词作比较,清理无意义的停用词
print(keywords)

# 显示词云图
plt.rcParams[&#39;figure.figsize&#39;]=(10.0,5.0)#设置图像显示大小
# 设置字体类型\颜色\大小
wordcloud=WordCloud(font_path=&#39;simhei.ttf&#39;,background_color=&#39;white&#39;,max_font_size=80,stopwords=stopwords)
myword=wordcloud.fit_words(keywords)#词云统计图
plt.imshow(myword)#显示词云图
plt.axis(&quot;off&quot;)
plt.show()
</code></pre>
]]></content>
      <categories>
        <category>项目</category>
      </categories>
      <tags>
        <tag>python</tag>
        <tag>项目</tag>
        <tag>爬虫</tag>
        <tag>分词</tag>
      </tags>
  </entry>
</search>
